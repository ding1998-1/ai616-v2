# 手机 H5 视觉与易用性优化计划

## 目标

在不改动录音分片、逐片 ACK、断网续传、身份绑定、实时转写和录音合并协议的前提下，重构手机 H5 的信息层级与操作路径，并与本轮桌面会中页、纪要确认页一起验收和部署。

## 设计原则

1. 录音是唯一主任务：首屏只突出会议、身份、录音状态、计时和开始/暂停/结束。
2. 转写是旁路能力：云端或本地 ASR 故障不得阻断录音；状态文案必须展示真实链路。
3. 声纹属于会后校准：不在现场主流程要求注册，不作为开始录音门槛。
4. 异常可理解：断网、补传、等待 ACK、ASR 重连、会议阶段变化使用单一状态区，避免重复提示。
5. 单手可操作：主要按钮位于拇指可达区域，结束录音保留明确防误触确认。

## 页面结构

### 首屏固定区

- 会议名称、当前议题和会议状态
- 当前实名身份与发言人切换
- 录音计时、音量反馈和录音主按钮
- 网络、录音上传、ASR 三个独立但紧凑的状态

### 内容区

- 最新转写置顶，历史转写按时间向下排列
- 手动补录收进轻量入口
- 录音结果、回放、补传状态合并为一个“录音记录”区

### 次要区

- 签字确认仅在纪要已生成时出现
- 声纹校准移动到会后工具区
- 设备技术信息、绑定步骤和调试字段默认折叠

## 功能保护清单

- 不修改 MediaRecorder 3 秒切片逻辑
- 不修改 IndexedDB/本地待传队列
- 不修改 chunk 上传、ACK、status、complete 接口
- 不修改 WebSocket 自动重连及本地 ASR 降级
- 不修改身份和会议权限校验
- 不修改签字接口和正式归档拦截

## 验收尺寸

- iPhone 15 Pro：393 × 852
- 常见安卓：360 × 800
- 大屏手机：430 × 932
- 横屏仅保证可恢复操作，不作为主布局

## 验收场景

1. 登录后刷新仍保持登录态。
2. 扫码进入正确会议并显示实名身份。
3. 开始录音后计时、音量、上传和转写状态正确。
4. ASR 不可用时录音继续，文案不误报。
5. 断网后继续录音，恢复网络后补传并完成合并。
6. 暂停、继续、结束录音操作清晰且无重复提示。
7. 转写修正、手动补录、签字和录音回放保持可用。
8. 桌面会中页能够收到手机端转写和录音状态。

## 执行顺序

1. 重构手机 H5 JSX 信息层级。
2. 重写移动端视觉样式与安全区适配。
3. 清理现场声纹注册入口和重复状态提示。
4. 运行前端生产构建与后端关键测试。
5. 三种手机尺寸视觉实测，并与 1920 × 1080 桌面端联动检查。
6. 形成部署包，备份 44 当前版本，部署并重启前端/后端所需服务。
7. 验证公网 H5、WebSocket、录音回传和 PC 同步后完成交付。
