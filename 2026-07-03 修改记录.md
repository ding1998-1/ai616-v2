# 2026-07-03 修改记录

## 今日目标

完成 2026-07-02 剩余 6 项待处理修复，解决代码审查发现的 2 项致命隐患，修复手机端录音状态 bug，切换后端为 systemd 管理。

---

## 已完成修复 ✅

### 1. 录音文件损坏根因修复

- **问题**：`MediaRecorder.start(3000)` 每 3 秒分段产生 fragmented MP4，部分片段丢失 moov atom
- **修复**：改为流式上传 — 保留 timeslice 分段，每 3 秒上传一次 chunk 到后端，避免浏览器内存溢出
- **文件**：
  - `frontend/src/pages/MobileMeetingRecorder.jsx` — 流式上传 chunk，停止时通知后端合并
  - `frontend/src/pages/MeetingComplianceWorkflow.jsx` — 同上
  - `backend_full.py` — 新增 `/api/meeting/recorder/audio/chunk` 和 `/api/meeting/recorder/audio/complete` 端点

### 2. DashScope ASR 强制默认

- **问题**：用户 localStorage 存了 `qwen`，无法切回云端 ASR
- **修复**：初始化时强制写入 `dashscope`，"本地识别"按钮永久禁用
- **文件**：`frontend/src/pages/MobileMeetingRecorder.jsx`

### 3. DeepSeek API 重试逻辑

- **问题**：503 直接抛异常，纪要降级到本地规则
- **修复**：`_generate` 方法添加指数退避重试（503/429/502/504），最多 3 次；`_llm_refine_text` 添加异常日志
- **文件**：`backend/llm_client.py`、`backend_full.py`

### 4. Whisper 终审归档自动触发

- **问题**：`_run_whisper_review` 是 fire-and-forget，后端重启或快速归档导致 Whisper 未运行
- **修复**：提取 `_run_whisper_review_for_meeting` 为独立函数；归档时检查 Whisper 是否已完成，未完成则自动触发
- **文件**：`backend_full.py`

### 5. 转写时间戳按 clientTime 排序

- **问题**：多设备同时提交转写，按 server_time 排序导致时间乱序
- **修复**：DB 查询改用 `COALESCE(client_time, server_time)` 排序；前端排序同步修改
- **文件**：`backend/db.py`、`frontend/src/pages/MeetingComplianceWorkflow.jsx`

### 6. 本地 Paraformer ASR 质量问题

- **状态**：通过修复 2 解决 — 强制 DashScope 云端 ASR，本地 Paraformer 不再被使用

---

## 代码审查追加修复 ✅

### 7. Whisper 并发锁（防 GPU OOM）

- **问题**：`asyncio.create_task` 无并发限制，多个 Whisper 任务同时运行会撑爆 4090 显存
- **修复**：添加 `_whisper_semaphore = asyncio.Semaphore(1)`，全局同一时刻只有一个 Whisper 任务
- **文件**：`backend_full.py`

### 8. 流式上传（防浏览器 OOM）

- **问题**：去掉 timeslice 后，长时间录音的所有数据堆积在浏览器内存，导致手机 OOM 崩溃
- **修复**：保留 timeslice 分段，每 3 秒上传一次 chunk 到后端，录音结束后后端合并为完整文件
- **文件**：
  - `frontend/src/pages/MobileMeetingRecorder.jsx` — `uploadAudioChunk`、`completeAudioUpload`
  - `frontend/src/pages/MeetingComplianceWorkflow.jsx` — `uploadMicAudioChunk`、`completeMicAudioUpload`
  - `backend_full.py` — `/api/meeting/recorder/audio/chunk`、`/api/meeting/recorder/audio/complete`

---

## 手机端录音状态 Bug 修复 ✅

### 9. 录音状态不一致

- **问题**：手机点"开始录音"后，开始按钮仍可点击，结束按钮灰色不可用，但 ASR 已在运行
- **原因**：`loadRecorderHistory` 每隔几秒检查会议阶段，如果不是"会中记录"就强制 `setRecording(false)`，但 ASR WebSocket 未关闭
- **修复**：移除强制停止逻辑，改为只弹警告提示；手机端不控制会议阶段，由 PC 端管理
- **文件**：`frontend/src/pages/MobileMeetingRecorder.jsx`

---

## 系统管理优化 ✅

### 10. 后端切换为 systemd 管理

- **问题**：之前用 `nohup` 手动启动，开机不自启、崩溃不重启
- **修复**：移除 service 文件中不存在的 `qwen3-asr` 依赖，启用 `ai-compliance.service`
- **当前状态**：
  - `ai-compliance.service` — enabled，开机自启，崩溃自动重启
  - `paraformer-asr.service` — enabled，开机自启
  - `nginx.service` — enabled，开机自启

---

## 修改文件清单

| 文件 | 改动 |
|------|------|
| `frontend/src/pages/MobileMeetingRecorder.jsx` | 流式上传、强制 DashScope、录音状态修复 |
| `frontend/src/pages/MeetingComplianceWorkflow.jsx` | 流式上传、时间戳排序 |
| `backend/llm_client.py` | DeepSeek 重试逻辑 |
| `backend_full.py` | Whisper 并发锁、流式上传端点、归档自动触发、异常日志 |
| `backend/db.py` | ORDER BY 改用 client_time |
| `/etc/systemd/system/ai-compliance.service` | 移除 qwen3-asr 依赖 |

---

## 当前系统状态

| 组件 | 开机自启 | 状态 | 说明 |
|------|---------|------|------|
| 后端服务 | ✅ | ✅ active | ai-compliance.service，PID 60934 |
| DashScope ASR | — | ✅ 可用 | 云端 API，无需本地服务 |
| Paraformer ASR | ✅ | ✅ active | 备用，当前不使用 |
| Whisper 终审 | — | ✅ 可用 | 有并发锁保护 |
| DeepSeek 纪要 | — | ✅ 已加重试 | 指数退避，最多 3 次 |
| Nginx | ✅ | ✅ active | 反向代理 |
