# 2026-06-30 AI 会议系统修改记录

## 一、本地 ASR 模型替换

### 1.1 停用 Qwen3-ASR-1.7B
- 模型: `/home/ai/桌面/Qwen3-ASR-1.7B` (4.4GB) 已删除
- 服务: `qwen3-asr.service` 已 mask
- Conda 环境: `qwen3-asr` 已删除

### 1.2 尝试 SenseVoiceSmall
- 模型: `iic/SenseVoiceSmall` (897MB)
- 问题: 非自回归模型，流式输出只有"嗯。嗯。"，准确率极低
- 结论: 不适合流式场景，已删除

### 1.3 部署 Paraformer-Large
- 模型: `iic/speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch` (953MB)
- 框架: FunASR 1.3.14
- GPU: GPU 1 (RTX 4090)
- 服务: `paraformer-asr.service` (端口 8091)
- Conda 环境: `funasr-sensevoice`
- 推理模式: 批量识别 v3（非流式，每 1s 全量识别 + 增量提取）

### 1.4 Paraformer 调优参数
| 参数 | 值 | 说明 |
|------|-----|------|
| beam_size | 10 | 束搜索宽度 |
| chunk_size | [0,10,5] | 600ms 上下文窗口 |
| VAD | fsmn-vad | 语音活动检测 |
| Punc | ct-punc | 标点恢复 |
| BATCH_INTERVAL | 1.0s | 批量识别间隔 |

## 二、WebSocket 协议重构（backend_full.py）

### 2.1 回到 `type: "final"` 协议
- 废弃 preview/commit 双轨（前端兼容性问题）
- 恢复 `_fuzzy_lcp` 增量提取 + ≥6字/句尾标点发送
- 1.5s 静默超时强制提交（flush + 重启 ASR session）

### 2.2 SenseVoice 标签清洗
- 添加 `_TOK_RE` 精确正则：只去 `<|标签|>` 和英文控制符，保留所有中文
- 处理链路：FunASR 清洗 → 后端 `_clean()` → `_fuzzy_lcp` → final

### 2.3 修复 800ms 超时位置 bug
- 超时检查从"空文本条件块内部"提到"所有路径末尾"
- 防止非空文本时超时永不触发导致无限吞字

### 2.4 文本清洗放宽
- `_clean_asr_text`：去掉最小字符限制，保留标点，只去连续重复语气词
- 解决"转写内容无效" 400 错误

## 三、前端修改

### 3.1 MobileMeetingRecorder.jsx
| 改动 | 说明 |
|------|------|
| ASR 后端选择 | 云端识别 / 本地识别 切换按钮，localStorage 持久化 |
| WS 自动重连 | 10 次指数退避重连（3s→15s） |
| 音频采集优化 | AudioContext 固定 16kHz，缓冲 8192，避免重采样失真 |
| AGC 自动增益 | RMS 计算，目标 20%，最多 3 倍增益 |
| 录音看门狗 | 10s 无音频回调 → 恢复 AudioContext |
| 页面可见性 | 切后台再回来自动恢复录音 |
| PC 结束会议检测 | 轮询检查会议阶段，PC 结束 → 手机自动停录 |
| 排序修复 | push 替代 unshift，新消息在底部 |

### 3.2 MeetingComplianceWorkflow.jsx
| 改动 | 说明 |
|------|------|
| 说话人卡片 | 彩色头像圆圈 + 气泡，腾讯会议/飞书风格 |
| 三 Tab | 会议纪实 / 会议纪要 / 待办事项 切换 |
| 滚动修复 | 去掉 `.reverse()` 排序错乱，新消息到自动滚顶 |
| SSE 重连 | 指数退避 3s→6s→12s→max60s |
| 创建会议表单精简 | 从 110 行砍到 40 行 |

### 3.3 LoginPage.jsx
- 去掉硬编码默认标题
- Pydantic 数组错误格式化

### 3.4 App.jsx
- mobile-recorder / issue-collect 包裹 ErrorBoundary

## 四、后端 Bug 修复

### 4.1 致命级
| Bug | 修复 |
|-----|------|
| `_db_upsert_meeting` 删除全部 meeting_transcripts | upsert 不删 transcripts 表 |
| `@app.post` 装饰器粘到 `_clean_asr_text` → 422 | 删除多余装饰器 |
| 重复 `MeetingTranscriptChunkRequest` 类定义 | 删除本地版本，统一用 models.py |

### 4.2 高优先级
| Bug | 修复 |
|-----|------|
| 合并长度 200 字绕过 | `len(prev)+len(new)≤200` |
| ASR session 重启异常吞没 | 加 error log + break |
| Jaccard 字符集去重误判 | 改为 difflib.SequenceMatcher 文本比较 |
| 决策提取不区分"同意"/"不同意" | 加否定前缀检测 |
| 待办关键词太泛 | 改为模式匹配 `需要安排/由X负责/下周` |
| 单字"对/好"被过滤 | 只过滤纯语气词单字 |

### 4.3 中优先级
| Bug | 修复 |
|-----|------|
| SSE 重连无退避 | 指数退避 3→6→12→max60s |
| postTranscript 失败留幽灵行 | catch 后 filter 清理 |
| FunASR SESSIONS.pop 无锁 | 加 async with SESSION_LOCK |
| finished/close 无异常保护 | 全部加 try/except |
| WAL 孤儿转写 | 清理 SQL |

## 五、Paraformer 推理模式演进

```
v1: 累积全部音频 → 模型重复识别 → 文本鬼畜叠加 ❌
v2: 逐 token 流式(cache) → "对对对" 幻觉 ❌  
v3: 批量识别 1s/次 + VAD + 标点 → 连贯文本 ✅
```

## 六、服务状态

| 服务 | 端口 | GPU | 环境 | 自启 |
|------|------|-----|------|------|
| ai-compliance | 8002 | GPU 0 | ai_compliance | ✅ |
| paraformer-asr | 8091 | GPU 1 | funasr-sensevoice | ✅ |
| nginx | 80 | - | 系统 | ✅ |

## 七、前端 ASR 切换

手机录音页顶部两个按钮：
- **云端识别** → DashScope Fun-ASR（阿里云，高准确率）
- **本地识别** → Paraformer-Large（GPU 1，数据不出内网）
- 选择存 localStorage，录音中不可切换

## 八、前端创建会议流程优化

### 8.1 创建页面精简
- 新创建时显示居中卡片式表单，不打开复杂三栏工作区
- 创建完成后自动进入完整工作区

### 8.2 手动添加议题
- 底部操作栏添加 `手动输入议题 + 添加按钮`
- 手动议题绿色 Tag 标记，可删除
- 不依赖 AI 议题池，无 AI 议题时可直接手动输入创建

### 8.3 Arco Design 图标替换
- 安装 `@arco-design/web-react` (v2.66) 图标库
- 会议性质按钮图标：`IconFile` / `IconCommon` 替代 emoji
- 全局去除 📋 ⚖️ emoji

### 8.4 手动添加待办
- 待办 Tab 增加手动输入：事项 + 责任人 + 截止日期 + 添加按钮
- 手动待办绿色标记 `手动 1/2/3`，AI 待办黄色标记 `AI 1/2/3`
- 手动项可 × 删除

### 8.5 议题清单统一展示（手动/AI 一视同仁）
- "待上会议题"摘要区原本只列 AI 勾选议题（且截断前 5 条），手动议题不入列表只计数 → 改为 AI + 手动合并展示
- 每条议题带 `AI`（蓝）/ `手动`（绿）标记，手动项可在清单内直接 × 删除
- 删除底部重复的手动议题 Tag 行，避免割裂（同一条议题不再出现两次）
- 空状态/输入框文案改为"无需 AI 也能创建"，明确手动议题可独立走完整创建流程
- 底层 `createMeeting`（`allAgendaItems = 勾选 + 手动`、`agendaDrafts` 合并提交）本就支持纯手动，本次仅补齐 UI 体验

### 8.6 左侧议程规划区合并手动议题
- 议题池底部"已勾选 N 个议题"绿色提示 + 议程明细 + 预计总时长原本只统计 AI 勾选项（`selectedIssueCards`），手动议题不进该区域 → 加手动议题后左侧一片空白（截图红框处）
- 改为 `plannedItems = AI勾选 + 手动`：计数、标题拼接、议程明细列表、总时长全部含手动议题
- 手动议题在左侧可视、可编辑时长、可 × 删除；时长改动写回对应来源（AI→`agendaDrafts`，手动→`manualAgendaItems`）
- 描述补一句"AI 议题需在上方勾选，手动议题可在右侧直接输入"

### 8.7 会议性质图标修复
- "普通 / 三重一大"按钮误用 Arco Design 图标（`IconFile` / `IconCommon`），与全文件 Ant Design 图标体系冲突，渲染变形/超大
- 改为 Ant Design `FileTextOutlined`（普通）/ `SafetyCertificateOutlined`（三重一大），与整页风格统一
- 删除文件顶部未使用的 7 个 Arco 图标 import（纯死代码）；Arco 依赖保留（`OAWorkflow.jsx` 仍在用）

### 8.8 三列底部对齐
- 手动添加议题输入框从右侧创建表单挪到中间列（议题池）底部，填满截图红框空白区
- 中间列改为内部滚动（`overflow:hidden` + 议题列表 `flex:1 overflowY:auto`），流程提示框和输入框 `flexShrink:0` 固定贴底
- 右侧"取消/创建会议"按钮从内联样式改为 `meeting-create-action-bar` 类（`flex:0 0 auto + sticky bottom`）
- 三列底边现在对齐：左列输入框 / 中列添加议题 / 右列创建按钮在同一水平线

## 九、音频采集优化

| 优化 | 旧值 | 新值 |
|------|------|------|
| AudioContext 采样率 | 浏览器默认 (44.1/48kHz) | 固定 16kHz |
| ScriptProcessor 缓冲 | 4096 (85ms) | 8192 (512ms) |
| AGC 目标 RMS | 15% | 20% |
| AGC 最大增益 | 5x | 3x |
| 音频打包批次 | 12000 字节 (~375ms) | 16000 字节 (~500ms) |

## 十、Paraformer 参数调优历程

| 阶段 | 模型 | 模式 | 效果 |
|------|------|------|------|
| v1 | SenseVoiceSmall | 流式 cache | 只输出"嗯。嗯。" ❌ |
| v2 | Paraformer-Large | 流式 cache | "对对对" 幻觉 ❌ |
| v3 | Paraformer-Large | 批量 1s/次 | 连贯文本 ✅ |
| v4 | Paraformer + VAD + Punc | 批量 + beam=10 | 带标点精准文本 ✅ |

## 十一、录音稳定性增强

- AudioContext 挂起自动恢复 (`onstatechange`)
- 10s 无声看门狗检测
- 页面可见性 API 监听（切后台保护）
- PC 结束会议 → 手机 3s 内自动停录

## 十二、ASR 协议优化

- 回到 `type: "final"` + `newText` 协议（前端兼容）
- `_fuzzy_lcp` 容错 2 字同音修正
- `_TOK_RE` 精准正则：只去尖括号标签，保留中文
- 1.5s 静默 → finish + 重启 session

## 十三、关键命令

```bash
# 重启 ASR
sudo systemctl restart paraformer-asr

# 重启后端
sudo systemctl restart ai-compliance

# 查看 ASR 日志
journalctl -u paraformer-asr -f

# GPU 状态
nvidia-smi
```
