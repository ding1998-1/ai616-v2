# 2026-08-25 生产问题修复计划

## 已确认事实

- 生产服务使用 `/home/ai/miniconda3/envs/ai_compliance`，由 systemd 开机自启。
- 新版运行目录为 `/home/ai/文档/ai616`；旧版只保留在带时间戳的备份目录，无旧进程和旧自启项。
- 手机扫码 403 来自浏览器复用了其他账号或其他会议的登录态，未完成本场参会人绑定。
- 红头 DOCX 404 来自前端直接下载尚未生成的文档文件。
- 模块化后端缺少旧单体中的 Whisper 后台触发任务；历史会议状态不能证明当前任务正在运行。

## 修复顺序

### 录音回传可靠性基线

- 参考 MIT 开源项目 `Realmullens/tributary` 的录音不变量，仅移植状态机，不引入其视频会议业务。
- 录音分片必须先写入 IndexedDB，再发送到服务端。
- 只有收到服务端 2xx/ACK 后才允许删除本地分片。
- 页面刷新、断网或崩溃后，查询服务端已收分片并按差集续传。
- 每次录音建立独立 `recording_session_id`，与会议、参会人和设备共同确定音轨归属。
- 服务端必须校验 `0..final_chunk_count-1` 全部存在后才可合并。

### Tributary 与 meeting-minutes 的统一录音架构

- 唯一原始证据是手机每 3 秒生成的录音分片；不再另外复制一份 30 秒音频。
- Tributary 方式负责端侧可靠性：IndexedDB 先落盘、幂等上传、ACK 后删除、差集续传。
- meeting-minutes 方式负责录音完整性：每 10 个分片形成一个 30 秒检查点状态，记录起止分片、字节数、时间范围和缺片情况。
- 每条音轨持久化 `recording_session_id` / `participant_id` / `audio_client_id` / `recording_start_time` / `start_offset_ms`，供多端对齐。
- 停止录音后先验证分片连续性，再无损封装为完整音轨；合并失败时原始分片和检查点不删除。
- 转写必须保留相对录音时间 `audio_start_time` / `audio_end_time`，可从文本跳转到音频证据。
- 声纹分轨和身份匹配只在完整音轨生成后运行，不阻塞现场录音与实时转写。

1. 外部会议入口必须验证当前 Token 对本场会议的访问权；不匹配则清除旧登录态并要求本场登记。
2. 红头 DOCX 下载前先生成正式文档；兼容下载接口在文件缺失时补生成。
3. 将 Whisper 终审调度、运行状态、失败状态和结果持久化拆入模块化服务，在进入会后终审时触发。
4. 会后页面移除装饰性占位和双重滚动，按“成果校对—责任落实—归档”组织；待办只保留可执行任务。
5. 完成单元测试、生产依赖检查和真实接口测试后再部署。

## 待办的产品定义

待办不是会议笔记，也不是所有讨论内容。只有同时具备“明确动作、责任人、截止时间或可跟踪状态”的事项才进入待办；否则保留在纪实、决议或风险中。待办用于会后提醒、逾期预警、完成确认和责任追溯。
