# 2026-06-26 修改记录

## 一、致命修复

### 1. AI 生成会议纪要 500 错误
- **根因**：`_invalidate_meetings_cache()` 被 3 处调用但从未定义 → NameError → 500
- **影响范围**：`GET /api/meetings/{id}/records`、`GET /api/meetings/{id}/archive/docx`
- **修复**：新增 `_invalidate_meetings_cache()` 函数，清除会议缓存

### 2. 去重逻辑 bug
- **根因**：`now <= prev_time` 用了 `<=`，相同秒的时间戳被跳过 → 去重从未生效
- **修复**：改为 `now < prev_time`
- **验证**：快速连续相同转写，第二条正确被拦截

---

## 二、AI 纪要质量优化

### 3. DOCX/Records 转写截断
- **问题**：`_deepseek_generate_meeting_records` 只取 `transcripts[-80:]`（最后 80 条）
- **修复**：改为全部转写。≤100 全送，>100 智能采样（头部 20 + 中部均匀 ~60 + 尾部 30）
- **chronicle 截断**：`_local_generate_meeting_records` 只取 `clean_transcripts[-30:]` → 改为全部

### 4. Prompt 重写
- **问题**：Decisions 照抄 ASR 口语原文（"行，就看看效果。因为本身效后代理代理..."），Summary 混入试音内容
- **修复**：
  - 开启 DeepSeek thinking 模式
  - 明确要求 summary/decisions/todos 用正式书面语提炼，每条 ≤80 字
  - chronicle 保留全部原文
  - 添加 `_is_garbled_asr` 过滤乱码碎片
- **效果**：335 条转写 → 5 条概括 + 3 条决议 + 3 条待办 + 335 条纪实

### 5. DOCX 章节重新排序
- **问题**：会议纪实（原文）在纪要/决议前面，用户先看到原文再看到 AI 整理
- **修复**（两套生成逻辑都改了）：
  - markdown 版：一~八章重新编号
  - `_heading(doc)` 版：一会议议题 → 二会议摘要 → **三会议纪要 → 四决议草案 → 五待办事项** → **六会议纪实** → 七签署确认

---

## 三、前端修复

### 6. Spin 组件未导入
- **问题**：`<Spin size="large" />` 使用但 antd import 缺少 `Spin`
- **修复**：import 行加入 `Spin`

### 7. QRCode errorLevel 重复属性
- **问题**：同一个 `<QRCode>` 标签上写了 `errorLevel="H"` 和 `errorLevel="M"`
- **修复**：删除重复行，保留 `errorLevel="H"`

### 8. 转写列表显示不全
- **问题 1**：`meetingLiveRows.slice(0, 18)` 硬限制 18 条 → 335 条只显示 18
- **问题 2**：轮询 `doPoll` 直接覆盖 `remoteTranscripts`，SSE 增量数据被覆盖
- **问题 3**：API 默认 `limit=200`，335 条只返回 200
- **修复**：
  - `meetingLiveRows` 去掉 `.slice(0, 18)`
  - 轮询改为合并模式（不覆盖 SSE 数据）
  - API 请求加 `?limit=2000`
  - SSE 上限 `-200` → `-500`

### 9. 会中界面精简
- **删除**：AI 会中雷达、会中辅助（快捷标记/材料缺口）两个 panel
- **保留**：左侧实时转写 + 右侧本次会议议题 + 底部控制栏

---

## 四、后端架构重构

### 10. 消除代码双份维护（P0）
- **问题**：`backend_full.py` 和 `backend/db.py` 各有 46 个同名函数，改一处忘一处就出 bug
- **修复**：
  - `backend/db.py` 补齐缺失的 `_invalidate_meetings_cache`、`_public_meeting.generatedRecords`、`generated_records_json` 列
  - `backend_full.py` 删除全部 46 个重复函数（-981 行），统一走 `backend/db.py` 导入
  - 验证：所有函数 `from backend.db`

### 11. 共享依赖抽离
- **新建 `backend/deps.py`**（183 行）：认证、会议构建、网络、部门管理
- `backend_full.py` 删除 14 个重复函数（-208 行）

### 12. 路由模块化
- **新建 `backend/routes/auth.py`**（196 行）：登录/注册/用户/部门 API，独立 APIRouter
- `backend_full.py` 删除 auth 路由函数（-144 行）
- 解决循环导入：路由模块只 import `backend.db`/`backend.config`/`backend.deps`

### 13. 数据持久化
- **问题**：`generatedRecords` 只存内存，服务重启/worker 切换就丢失，每次都要重新调 DeepSeek（30-60s）
- **修复**：
  - DB meetings 表新增 `generated_records_json TEXT` 列
  - `_db_insert_meeting_rows` 写入，`_meeting_from_row` 读取
  - `_public_meeting` 的 `include_detail` 中加入 `generatedRecords`
  - 首次生成后写入 DB，重启不丢

---

## 五、性能与体验

### 14. 会议列表分页
- **问题**：209 个会议全量加载
- **修复**：`GET /api/meetings` 加 `limit`/`offset` 参数，默认 50 条/页，返回 `total`

### 15. 转写虚拟滚动
- **问题**：335 条转写全量 DOM 渲染
- **修复**：引入 `react-window`，用 `VirtualList` 替代 `.map()`，620px 高度视口

### 16. CSS 提取
- **问题**：`MeetingComplianceWorkflow.jsx` 6975 行，CSS 占 2742 行
- **修复**：提取到 `MeetingComplianceWorkflow.css`，JSX 6975→4239 行（-39%）

### 17. Nginx 限流
- `nginx-fixed.conf` 加 API 30r/m + LLM 5r/m 限流
- 已有 HTTPS 注释模板（宝塔服务器已处理 SSL 终结）

### 18. 错误日志
- 7 处 `.catch(() => {})` / `.catch(_ => {})` 改为 `console.warn("...", err)`

### 19. 构建优化
- `vite.config.js`：`chunkSizeWarningLimit: 1000`
- antd-icons 独立 chunk

### 20. 死代码清理
- 删除 `radarPinnedOpen`、`aiOutputRows`、`complianceRows`、`radarExpanded` 等未使用变量

---

## 六、文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend_full.py` | 修改 | **7899→6578 行**（-17%），删 46 DB 函数 + 14 deps 函数 + auth 路由；重新排序 DOCX 章节；去重修复 |
| `backend/db.py` | 修改 | 新增 `generated_records_json` 列 + 迁移 + `_invalidate_meetings_cache` + `_public_meeting.generatedRecords` |
| `backend/deps.py` | 新建 | 共享依赖：认证、会议构建、部门管理（183 行） |
| `backend/routes/auth.py` | 新建 | 认证/用户/部门路由（196 行） |
| `backend/routes/__init__.py` | 修改 | 路由器声明 |
| `frontend/src/pages/MeetingComplianceWorkflow.jsx` | 修改 | **6975→4239 行**（-39%），CSS 提取、Spin 导入、转写显示全量、去 slice 限制、VirtualList、去雷达/辅助、错误日志 |
| `frontend/src/pages/MeetingComplianceWorkflow.css` | 新建 | 独立样式文件（2747 行） |
| `frontend/vite.config.js` | 修改 | chunkSizeWarningLimit: 1000 |
| `nginx-fixed.conf` | 修改 | 限流 30r/m + 5r/m |
| `migrate_to_2080ti.sh` | 新建 | 2080Ti 无损迁移脚本 |
| `setup_https.sh` | 新建 | HTTPS 配置脚本 |
| `2026-06-26 修改记录.md` | 新建 | 本文件 |

---

## 七、当前系统状态

```
后端: 6,578 行 Python  │  ~110 API 端点  │  backend_full.py 瘦身 17%
前端: 4,239 行 JSX     │  CSS 独立 2,747 行  │  react-window 虚拟滚动
数据: SQLite 24MB + ChromaDB 43MB + 文件 ~500MB
部署: systemd + nginx  │  端口 8002 / 80
代码: 统一走 backend/db.py，无重复函数
```

## 八、宝塔配置备忘录

合规问答 SSE 流式需要宝塔 nginx 对 `/api/kb_stream` 和 `/api/meetings/` 加：
```nginx
proxy_buffering off;
proxy_read_timeout 600s;
proxy_http_version 1.1;
proxy_set_header Connection '';
chunked_transfer_encoding on;
```

## 九、下次继续事项

- [ ] `backend/routes/` 其余域名迁移（meetings/documents/compliance）
- [ ] `MeetingComplianceWorkflow.jsx` 拆分为多个组件文件
- [ ] 部署到 2080Ti 新主机（迁移包已打好）
- [ ] 宝塔 nginx 加上 SSE 流式配置
