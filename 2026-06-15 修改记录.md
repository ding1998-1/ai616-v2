# 2026-06-15 修改记录

## AI 会议会中页两块信息表现优化

- 问题判断：
  - “本次会议议题”下方的 AI 提示区像三段说明文字，不像会中工具。
  - “结束后生成”右侧的普通会议输出区也像说明书，和快捷标记按钮混在一起，层级不清。
- UI 调整：
  - 中间区将 `meeting-ai-output-list` 改为“AI 会中雷达”。
  - 三个状态项分别以图标卡呈现：阶段结论、会议判断、证据沉淀。
  - 每个状态项可点击，证据沉淀项跳转到证据底稿弹窗。
  - 右侧成果区将“会议纪实/会议纪要/会议决议”改为更明确的成果卡片状态。
  - 普通会议输出改为“会后处理顺序”：生成纪要、确认决议、形成待办。
  - 快捷按钮从说明区剥离为“会中快捷标记”工具条，避免和会后成果混在一起。
- 产品效果：
  - 中间区回答“开会时 AI 现在提醒我什么”。
  - 右侧回答“结束后会得到什么，以及会中可以快速标什么”。
  - 信息密度降低，功能边界更清晰。
- 已验证：
  - `npm run build` 通过。

## AI 会议议题状态溢出修复

- 问题判断：
  - “本次会议议题”列表中右侧状态 `等待讨论` 使用 Ant Design Tag 渲染，并放在 grid 的 `auto` 列。
  - 当议题标题较长时，中间内容没有稳定收缩边界，状态会贴边甚至溢出卡片。
- UI 修复：
  - 将状态从 Ant Design `Tag` 改为专用 `meeting-agenda-status`。
  - 议题行 grid 改为 `24px minmax(0, 1fr) 62px`，右侧状态列固定宽度。
  - 标题和描述增加单行省略，避免挤压状态列。
  - 议题行增加 `overflow: hidden` 和右侧内边距，保证状态不越界。
- 已验证：
  - `npm run build` 通过。

## AI 会议会中工作台五项结构精简

- 顶部与中部状态栏合并：
  - 顶部面包屑删除 `会中 / 02 正在运行` 和 `专题会`，只保留 `AI 会议`、会议性质、日期和返回按钮。
  - 中部工具栏只保留当前议题文本与右侧会议性质切换，不再承载扫码、权限、停止共享等操作。
- 顶部席位区重构：
  - 将原视频会议式大卡片改为轻量声纹/设备状态条。
  - 当前发言人保留绿色高亮，但改为横向小胶囊卡。
  - 待接入席位改为轻量虚线风格，不再使用大面积灰色斜线块。
- 共享控制统一：
  - 移除中部 `停止共享` 入口。
  - 底部共享按钮改为 `停止共享`，并使用红色警示状态，避免同一页面出现两个共享入口。
  - `扫码邀请` 与 `权限设置` 平移到底部常驻工具栏。
- 中下部主体比例调整：
  - 会中主体从三栏改为两栏：左侧实时转写约 55%，右侧为议题/AI 雷达/会中辅助。
  - `本次会议议题` 与 `AI 会中雷达` 合并在右侧上半区。
  - `结束后生成` 改为右侧下半区的弱化收纳预览，减少会后逻辑抢占会中空间。
- 会中快捷标记增强：
  - `标决议`、`记待办`、`有争议` 上移到右侧辅助区顶部。
  - 按钮放大，并按用途增加浅色语义底色：决议绿、待办蓝、争议红、材料黄。
- 已验证：
  - `npm run build` 通过。

## AI 会议会中页二次细节修正

- 中间状态栏：
  - 将 `普通会议 / 三重一大` 从右侧平移到 `当前议题` 文本后面，作为议题修饰标签。
  - 右侧改为轻量计时器：`已讨论 00:00:57`，减少右侧真空。
- 分栏对齐：
  - `本次会议正在讨论` 浅蓝标题框改为只占左侧转写列。
  - 左侧实时转写卡片右边缘与上方浅蓝标题框右边缘保持同一网格线。
- 议题列表塌陷修复：
  - 移除 `meeting-agenda-check-list` 的强制 `flex: 1` 和滚动容器属性。
  - 单议题时按内容自适应高度，不再出现带滚动条的扁平空白框。
  - 修复 `AI 会中雷达` 被上方空壳容器挤压/重叠的问题。
- 会中快捷标记：
  - 按钮改为三等分排列，找回并稳定展示 `有争议`。
  - 保留浅绿/浅蓝/浅红语义底色，提升盲操识别度。
- 底部工具栏：
  - `扫码邀请` 文案改为 `手机接入`，更贴近手机作为录音麦克风接入的真实含义。
- 已验证：
  - `npm run build` 通过。

## AI 会议会中页左右主卡片纠偏

- 问题判断：
  - 会中页核心业务视觉流反了，左侧大空间被“本次会议正在讨论”标题卡占用。
  - 高频查看的 `实时转写` 被挤到右侧窄栏，长文本滚屏体验不合理。
  - 右侧同时出现“本次会议正在讨论”和“本次会议议题”，重复展示同一议题文本。
- UI 调整：
  - 将会中主体调整为左侧 `实时转写`、右侧 `本次会议议题 / AI 会中雷达 / 会中辅助`。
  - 左右比例修正为约 `54% / 46%`，接近记录员工作台需要的 `55% / 45%`。
  - 删除独立的 `meeting-runtime-focus` 标题框，将浅蓝标题区域收进右侧 `本次会议议题` 卡片内部。
  - 右侧议题卡内部按“卡片标题 → 浅蓝当前议题 → 议题列表 → AI 会中雷达”展示，避免两张大卡嵌套。
  - 议题列表为空时补一条当前议题兜底行，防止再次出现空白塌陷框。
  - `AI 会中雷达` 和 `会中辅助` 增加 `24px` 顶部间距，避免标题与上方内容压迫。
- 已验证：
  - `npm run build` 通过。
  - 浏览器 1920×1080 读数：左侧实时转写 54%，右侧议题/雷达 46%，旧标题框 0 个，合并后的议题标题 1 个，雷达间距 24px。

## AI 会议会中看板头部极致精简

- 问题判断：
  - 头部原先把 `AI 会议 / 普通会议 / 日期 / 返回会议列表` 四个胶囊放在标题上方，标题层级被压低。
  - 大标题右侧的 `普通会议 · 当前议题...` 与下方状态栏重复，属于无效复读。
  - 右侧设备卡上方混放 `会议 ID / 00:00:00 / 待启动 / 议题已锁定`，破坏设备栏纯净度。
- UI 调整：
  - 会中头部改为左右两区：左侧会议身份，右侧设备状态。
  - `返回会议列表` 改为左箭头图标按钮，放在会议标题左侧。
  - 删除标题右侧灰色副标题，不再重复展示当前议题。
  - 只保留 `AI 会议 / 日期 / ID / 议题已锁定` 作为标题右侧元数据。
  - 右侧设备栏移除补丁状态文本，只保留发言人和待接入胶囊卡片。
  - 会中头部容器改为 `display: flex; align-items: center; justify-content: space-between;`，让左侧身份区与右侧设备区中心线对齐。
  - 小屏下头部改为上下堆叠，设备胶囊允许横向滚动，避免撑爆页面。
- 已验证：
  - `npm run build` 通过。
  - 浏览器读数：返回图标 1 个，标题内无 `当前议题` 复读，设备栏无 `会议 ID / 00:00:00 / 待启动 / 议题已锁定` 噪声，左右中心线一致。

## AI 会议右侧议题卡片扁平化重写

- 问题判断：
  - `本次会议议题` 卡片内部嵌套了独立浅蓝大卡，形成“卡片套卡片”的套娃结构。
  - `AI 会中雷达` 仍被放在议题卡 section 内部，议题区域溢出时会直接压迫雷达标题和内容。
- UI 重构：
  - 删除 `meeting-agenda-hero` 蓝色独立大卡。
  - `本次会议议题` 只保留两层：头部信息区 + 议题列表区。
  - 当前讨论项不再另起大卡，而是在议题列表第 1 行使用浅蓝背景和左侧高亮线标记。
  - 第 1 行增加 `正在讨论` 小标签，表达当前状态。
  - `AI 会中雷达` 从议题卡中拆出，成为独立的右侧功能卡片。
  - 议题列表设置自适应高度，只有多议题时在列表内部滚动，不会撑爆父卡片。
- 已验证：
  - `npm run build` 通过。
  - 浏览器读数：旧蓝卡数量 0，议题卡不包含雷达，当前高亮议题行 1 条，议题卡与雷达间距 14px，雷达与会中辅助间距 14px。

## AI 会议会中快捷标记弹窗真实性修复

- 问题判断：
  - 未收到手机端实时转写时，点击 `标决议` 仍提示 `锚点时间 00:00:57` 和 `已记录到关键事件流`。
  - 这会让用户误以为系统已经捕捉到发言，实际没有真实音频/转写依据。
- 逻辑修复：
  - 快捷标记弹窗新增真实转写判断。
  - 只有存在手机端转写文本时，才显示绑定时间、发言人和发言内容。
  - 没有转写时，不再显示假锚点时间，也不提示已记录关键事件流。
  - 无转写状态下提示：当前还没有收到手机端实时转写，系统不能生成发言锚点。
  - 无转写状态下主按钮改为 `手机接入`，引导先让参会人扫码登录并开始录音。
- 已验证：
  - `npm run build` 通过。
  - 浏览器实测：无转写时点击 `标决议`，弹窗不含 `00:00:57 / 00:42:18 / 锚点时间 / 已记录到关键事件流`，并显示无转写提示。

## AI 会议会中状态联动升级

- 问题判断：
  - 议题卡和辅助卡仍有固定说明文字，占用高度且不会随会议状态变化。
  - 多议题场景下，全局 `标决议 / 记待办 / 有争议` 无法明确知道标记属于哪一个议题。
  - `AI 会中雷达` 长期开着会占用右侧空间，不符合“有事报到，无事退朝”的会中工作台逻辑。
- 交互升级：
  - 议题行取消固定说明副文本，只在有真实发言处理时显示 `AI 正在实时比对发言与待办...` 动态提示。
  - 议题行可点击切换当前讨论议题，当前议题使用浅蓝底和左侧高亮线。
  - 当前/悬停议题行右侧浮现三个微型上下文按钮：`标决议`、`记待办`、`有争议`。
  - 行内标记会绑定具体议题；右下角全局标记默认绑定当前正在讨论的议题。
  - 标记弹窗新增 `绑定议题` 字段，记录员能确认标记归属。
  - `AI 会中雷达` 默认折叠为单行，只有点击或捕捉到发言/关键事件后才展开。
  - `会中辅助` 删除固定说明，只有产生标记后才显示动态标记数量。
- 已验证：
  - `npm run build` 通过。
  - 浏览器读数：当前议题行 1 条，行内快捷按钮 3 个，AI 雷达默认 `is-collapsed`，雷达卡片项 0 个，固定说明文字不再可见。

## AI 会议议题行内标记按钮舒展优化

- 问题判断：
  - 行内 `标决议 / 记待办 / 有争议` 三个微按钮绝对定位在议题行右侧。
  - 右侧卡片宽度不足时，按钮会挤在状态标签区域，形成逼仄和溢出。
- UI 调整：
  - 行内快捷按钮不再挤在标题行右侧。
  - 悬停议题行时，在议题标题下方展开一条三等分操作带。
  - 每个按钮增加文字标签：`决议 / 待办 / 争议`，点击热区从小圆图标扩大为 34px 高的横向按钮。
  - 状态标签不再在 hover 时隐藏，标题行保持清爽，操作与信息分层展示。
- 已验证：
  - `npm run build` 通过。

## AI 会议议题行按钮残影修复

- 问题判断：
  - 行下方操作带收起时只设置了 `max-height: 0` 和 `opacity: 0`。
  - 内部图标仍可能在按钮容器内参与绘制，形成蓝色图标残影，看起来像和议题内容混在一起。
- UI 修复：
  - 收起态增加 `visibility: hidden`，展开态才恢复 `visibility: visible`。
  - 议题行恢复 `overflow: hidden`，避免收起内容外溢。
  - 操作带容器从 `span` 改为块级 `div`，避免 button 内 inline/grid 混排导致异常绘制。
- 已验证：
  - `npm run build` 通过。

## AI 会议实时议题比对真实性修复

- 问题判断：
  - 旧界面在只有 `我是蔡万清`、`Seven` 等测试/身份确认语句时，也可能显示“AI 正在实时比对发言与待办”。
  - 这不是真实语义比对，会让记录员误以为系统已经理解并命中了当前议题。
- 后端修复：
  - 新增 `POST /api/meetings/{meeting_id}/agenda/realtime-check`。
  - 接口接收当前议题与最近手机端实时转写，优先调用 DeepSeek 判断发言与议题关系。
  - DeepSeek 不可用时降级为本地规则，只做临时提示，并明确返回 `aiProvider: local-rule`。
  - 明确过滤身份确认、试麦、英文测试词等低价值发言，返回 `未命中`，不再判定为正在讨论议题。
- 前端修复：
  - 会中议题状态改为读取后端真实判断结果。
  - DeepSeek 命中时显示 `DeepSeek 语义比对`。
  - 本地兜底时显示 `本地规则临时判断`，不再伪装成大模型能力。
  - 请求触发条件收紧为转写数量或更新时间变化，避免 2.5 秒轮询导致重复打 AI 接口。
  - 非演示会议计时器改为真实开始录音后的本地计时，不再默认显示 `00:00:57`。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。
  - 本地规则测试：`我是蔡万清。/ Seven.` 返回 `relation: irrelevant`、`status: 未命中`。
  - 后端接口实测：DeepSeek 对 `我是蔡万清。/ Seven.` 返回 `未命中`；对 `MiMo Code 发布后安排开源仓库维护、版本节奏和社区反馈处理` 返回 `正在比对`。

## AI 会议已讨论计时修复

- 问题判断：
  - 会中页右上角 `已讨论 00:00:00` 只读取 PC 本机 `recording` 状态。
  - 手机端已经登录、录音并产生实时转写时，PC 端仍可能没有本机录制状态，所以计时被强制显示为 0。
- 逻辑修复：
  - 计时源改为真实会议活动：手机端会话事件、音频事件、实时转写事件都可以启动讨论计时。
  - 优先使用远端事件/转写里的最早 `serverTime` 作为会议开始时间。
  - 若没有远端时间但 PC 本机开启录音，则退回本地开始时间。
  - 顶部展示条件改为 `hasMeetingSpeech || recording`，避免已有转写时仍显示 `00:00:00`。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。

## 手机录音端本场记录补齐

- 问题判断：
  - 移动端原先只展示当前页面内存里的 `recordingSummary` 和 `liveLines`。
  - 手机浏览器切后台、刷新或页面被系统回收后，前端内存会丢失，看起来像“录音记录没了”。
  - 后端其实已经保存了转写和音频事件，但移动端没有重新拉取并展示。
- 功能修复：
  - 移动端新增 `我的本场记录` 卡片。
  - 进入页面、录音中定时、页面从后台恢复、手动刷新时，都会从 `/api/meeting/transcripts/{meetingId}` 拉取本会议记录。
  - 按当前登录账号过滤本人记录，显示本人已回传转写条数和录音片段数量。
  - 已回传转写支持继续 `修正签字`。
  - 录音片段支持鉴权加载回放，避免 `<audio>` 直接访问受保护接口导致 401。
  - 页面恢复时提示：手机浏览器切后台可能暂停麦克风，恢复后会重新同步已回传记录。
- 产品说明：
  - Web 手机录音不能保证后台持续录音，尤其是 iOS、微信内置浏览器和部分国产浏览器；切后台后系统可能暂停 JS 和麦克风。
  - 当前版本的正确行为是：前台录音实时回传，切回页面后能看到已回传记录，必要时重新开始录音。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。

## 录音生成真实会议记录闭环

- 问题判断：
  - 手机录音、实时转写、音频上传已经是真实落库。
  - 但会后页的 `会议纪实 / 会议纪要 / 会议决议` 原先主要是固定说明和预览文案，不能算真实生成的会议记录。
- 后端修复：
  - 新增 `GET /api/meetings/{meeting_id}/records`。
  - 接口读取真实会议、真实转写、真实录音事件后生成会议记录。
  - 有真实转写时，优先调用 DeepSeek 生成 `summary / chronicle / minutes / decisions / todos`。
  - DeepSeek 不可用时，降级为本地规则整理，并明确返回 `aiProvider: local-rule`。
  - 没有真实转写时，返回 `generated: false`，不生成假纪要。
  - 红头 DOCX 导出也改为接入真实会议记录结果，不再只写固定“原则同意”话术。
- 前端修复：
  - 会后页新增真实会议记录加载状态。
  - `会议记录生成` 卡片显示来源、转写条数、录音段数和模型来源。
  - `总结 / 纪实 / 待办` 切换内容改为读取后端真实记录。
  - 普通会议会后整理区显示“基于真实录音转写生成会议成果”，并提供 `生成真实会议记录` 按钮。
  - 自动生成只在进入会后/归档阶段或会议切换时触发，避免转写轮询时反复打 DeepSeek。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。
  - 后端接口实测 `GET /api/meetings/meeting-local-20260611162109/records` 返回 `200`。
  - 实测结果：`aiProvider: deepseek`、`source: DeepSeek 根据真实转写生成`、`transcriptCount: 100`，并生成摘要、纪实、议题整理、决议候选和待办。

## AI 会议会中辅助底部裁切修复

- 问题判断：
  - 会中看板右侧底部出现一条被裁切的绿色圆角条，实际是会后结果预览卡片残留在会中辅助区。
  - 右侧栏外层 `meeting-side-stack` 被重复嵌套，导致右栏高度、滚动和底部工具栏之间的计算不稳定。
  - 会中阶段展示“会议纪实 / 会议纪要 / 决议”等会后产物，会误导记录员以为会议尚未结束时已经生成结果。
- 前端修复：
  - 右侧栏改回单层 `meeting-side-stack`，只包裹“本次会议议题 / AI 会中雷达 / 会中辅助”三块。
  - 移除会中辅助里的会后绿色结果预览，普通会议会中只保留“快捷标记”和中性提示。
  - 三重一大会议仍保留材料缺口；普通会议不再显示三重一大材料内容。
  - 右侧栏增加底部内边距，避免滚动内容贴住底部工具栏。
- 已验证：
  - `npm run build` 通过。
  - 浏览器进入 `本次会议专题会 / 会中记录` 后检查：`meeting-side-stack` 数量为 1，绿色会后预览数量为 0。
  - 会中辅助卡片底部与底部工具栏无重叠：辅助卡片底部约 `610px`，底栏顶部约 `632px`。

## AI 会议归档页假数据清理

- 问题判断：
  - 归档页“手机手写签名认定”使用了前端常量假数据，固定显示刘强、陈伟、王磊、张敏等人员。
  - 手机签名预览默认显示模拟笔迹和 `笔迹认定 98%`，会让用户误以为已经完成真实签字核验。
  - 防伪归档包默认前 4 项全绿，实际没有录音原件、签字或归档动作时也会显示“已具备”。
  - 红头纪要预览默认写 `XX 国有集团` 和 `原则同意`，这是高风险占位话术，不能作为真实会议结果展示。
- 前端修复：
  - 删除 `MOBILE_SIGNATURES` 假签字常量，不再渲染固定人员。
  - 归档签字区改为读取真实转写中的 `correctionSigned / correctionAuthor / signatureData`。
  - 没有真实手机签字时显示“暂无签字 / 等待参会人签字”，不展示模拟笔迹和虚假置信度。
  - 防伪归档包改为按真实证据逐项亮灯：问题素材、议题、录音原件、会议记录、签字归档分别判断。
  - 会议记录完整度改为按真实证据计算：转写、录音、AI 记录、签字等缺失时不会因为走到归档阶段自动显示高分。
  - 红头纪要预览去掉 `XX 国有集团` 和默认 `原则同意`，无真实记录时明确提示不能生成正式纪要。
  - 后端 DOCX 导出标题去掉 `XX 国有集团党委会会议纪要`，改为按会议类型生成 `会议纪要草稿`。
- 已验证：
  - `npm run build` 通过。
  - `python3 -m py_compile backend_full.py` 通过。
  - 源码确认归档签字区不再引用 `MOBILE_SIGNATURES`。
  - 源码确认 `笔迹认定 98%`、默认 `原则同意` 已从归档页移除。

## AI 会议素材收集箱样式优化

- 问题判断：
  - 会后/归档阶段左侧“泛群聊素材池”在窄栏中仍沿用大卡片结构，头像、单位、状态和摘要互相挤压，列表像一排被裁切的白条。
  - 底部输入框占位不清楚，用户难以判断这里是继续补线索还是聊天。
- 前端修复：
  - 紧凑版素材池改名为 `素材收集箱`，增加说明：按提交人折叠，点击卡片查看原文、图片和附件。
  - 紧凑卡片改为三列结构：头像缩写、提交人/单位/摘要、最新时间。
  - 摘要默认单行展示，标签控制在小尺寸胶囊内，避免文字互相压住。
  - 展开态保留真实原文、图片预览和附件展示，但限制高度独立滚动。
  - 底部输入框改为 `粘贴一条问题或群聊原文`，按钮尺寸与输入框对齐。
- 已验证：
  - `npm run build` 通过。

## AI 会议素材收集箱白条重叠修复

- 问题判断：
  - 上一次只改了素材箱文案和内部按钮样式，但外层素材项没有显式高度。
  - 浏览器实测外层素材项高度只有 `2px`，内部按钮高度 `70px`，导致列表项互相重叠，视觉上变成一堆不可读白条。
- 前端修复：
  - 废弃会后/归档窄栏里的紧凑卡片复用方案，新增 `renderCompactIssueSourceList` 专用于窄栏素材箱。
  - 素材项改成固定行高清单：提交人、单位、摘要、数量、类型、时间、查看按钮在一行结构内稳定排布。
  - 外层素材项增加 `minHeight: 72px`，确保滚动容器按真实行高排布，不再重叠。
  - 会后/归档三栏布局左栏从 `330px` 调整到 `380px`，给素材摘要留出可读宽度。
- 已验证：
  - `npm run build` 通过。
  - 浏览器进入当前会议后实测：素材项外层高度 `72px`，内部按钮高度 `70px`，后续行按约 `81px` 间距正常排列。

## AI 会议素材收集箱展开内容裁切修复

- 问题判断：
  - 点击 `查看` 后按钮已变成 `收起`，说明展开状态已触发，并不是假按钮。
  - 浏览器实测展开后 DOM 中已有 7 条真实原文，但外层素材项仍保持 `72px` 高并设置 `overflow: hidden`，导致明细内容全部被裁掉。
- 前端修复：
  - 素材项折叠态高度保持 `72px`。
  - 展开态外层高度显式调整为 `318px`。
  - 明细区保持 `230px` 内部滚动，用于展示同一提交人的多条原文、Excel 导入来源、图片和附件。
  - 增加高度过渡，避免展开/收起突兀跳变。
- 已验证：
  - `npm run build` 通过。
  - 浏览器点击第一条素材 `查看` 后实测：外层素材项高度 `318px`，明细区高度 `230px`，真实原文可见。

## AI 会议素材详情弹窗

- 问题判断：
  - 左侧素材箱是窄栏，不适合承载长原文、Excel 来源、图片和附件。
  - 即使修复展开高度，长素材仍会让左栏视觉拥挤，影响右侧 AI 议题看板的主任务。
- 前端修复：
  - 素材箱列表改为稳定索引，只展示提交人、单位、摘要、条数、类型和时间。
  - 原 `查看/收起` 改为 `详情`。
  - 点击 `详情` 后打开 `素材详情` 弹窗。
  - 弹窗左侧展示提交人、单位、提交条数、图片数量和最新时间。
  - 弹窗右侧按时间展示该提交人的全部原文、Excel 导入来源、图片原件和附件文本。
  - 图片仍可继续点击进入原图预览弹窗。
  - 移除窄栏内展开态高度依赖，避免再次出现裁切或列表挤压。
- 已验证：
  - `npm run build` 通过。

## 手机端 session join 失败重试

- 问题判断：
  - `MobileMeetingRecorder.jsx` 中 join 失败后 `joinedRef` 置回 false，但永不重试，用户功能完全不可用。
- 修复：
  - 新增指数退避重试机制：最多 3 次，间隔 2s/4s/8s。
  - 失败后界面显示剩余重试次数和倒计时。
  - 3 次均失败后显示手动重试按钮，不再静默卡死。
- 已验证：
  - `npm run build` 通过。

## 手机端假置信度值清理

- 问题判断：
  - ASR 置信度写死 0.94/0.72，声纹置信度写死 96%/98%。
- 修复：
  - ASR 置信度改为从 Fun-ASR WebSocket 返回的真实 confidence 字段透传。
  - 声纹显示改为状态文案（实时转写中/已停止录音/待启动），不再显示假百分比。
  - 无真实数据时显示"识别中"而非假数字。
- 已验证：
  - `npm run build` 通过。

## 手机端乐观更新与历史拉取竞态修复

- 问题判断：
  - 临时 ID 生成 → 后端返回真实 ID 前拉历史 → 同一条转写出现两次。
- 修复：
  - 在 merge 历史数据时，用临时 ID 前缀 `/^\d{13,}-0?\.\d+$/` 过滤掉已在 liveLines 中的乐观条目。
- 已验证：
  - `npm run build` 通过。

## 后端 meetingMode 持久化

- 问题判断：
  - SQLite meetings 表没有 meeting_mode 列，每次 load 从默认值重建。
  - 三重一大会议页面切换或刷新后会变回普通会议。
- 修复：
  - `CREATE TABLE meetings` 新增 `meeting_mode TEXT NOT NULL DEFAULT 'normal'`。
  - 已有数据库增加 ALTER TABLE 迁移逻辑。
  - `_db_save_meetings` 写入 meeting_mode，`_db_fetch_meetings` 读取。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。

## DOCX 导出接入 DeepSeek 真实记录

- 问题判断：
  - `/archive/docx` 只调用本地规则生成，即使 DeepSeek 已生成更优内容。
- 修复：
  - 先从 `/api/meetings/{id}/records` 缓存读取，有 DeepSeek 结果则优先使用。
  - 无 DeepSeek 结果时降级到本地规则。
  - DOCX 标题改为按会议类型生成 `会议纪要草稿`，不再写死 `XX 国有集团党委会`。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。

## 前端假数据收敛

- 问题判断：
  - `LIVE_TRANSCRIPT`、`CHAT_MESSAGES`、`ISSUE_CARDS` 等常量在非演示会议中也可能被合并进真实数据。
- 修复：
  - `hydrateMeetingDetail` 不再向非演示会议注入假数据。
  - 非演示会议中 `BIND_STEPS` 改为按步骤逐项判真（当前用户、绑定角色、音频就绪、转写数量）。
  - 假数据只在 `isDemoMeeting` 场景生效。
- 已验证：
  - `npm run build` 通过。

## 会中操作模态框真实内容

- 问题判断：
  - 9 个模态框类型（participants/permission/stopShare/mic/chat/share/record/evidence/endMeeting），大部分渲染空壳。
- 修复：
  - `participants` 模态框读取真实 `remoteSpeakerRows` 展示参会人列表。
  - `chat` 模态框改为会议秘书备注，使用 TextArea 输入。
  - `evidence` 模态框展示已保存的真实标记列表，含颜色编码标签。
  - 标记弹窗增加与转写行的关联展示、已有标记列表可查看。
- 已验证：
  - `npm run build` 通过。

## 死后代码清理

- 问题判断：
  - `showTranscriptDetail` state 和转录抽屉 JSX（30+ 行）已废弃但不删除。
- 修复：
  - 删除 `showTranscriptDetail` state 声明。
  - 删除转录抽屉 JSX。
  - 删除 `.meeting-transcript-drawer` / `.meeting-transcript-line` CSS 规则。
- 已验证：
  - `npm run build` 通过。

## 会议列表空状态与加载骨架屏

- 问题判断：
  - 没有会议时只有文字提示，无引导动作。
  - `meetingsLoading` 时只显示文字，无视觉反馈。
- 修复：
  - 空状态使用 Ant Design `Empty` 组件 + "创建第一个 AI 会议"按钮居中引导。
  - 加载态使用 Ant Design `Skeleton` 组件渲染 3 行骨架屏。
- 已验证：
  - `npm run build` 通过。

## 错误态处理

- 问题判断：
  - API 报错时多数静默吞掉，用户不知道发生了什么。
- 修复：
  - 关键操作（创建会议、推进阶段、生成记录）失败时显示明确的 `message.error` 提示。
  - 非关键轮询失败时静默（避免刷屏）。
- 已验证：
  - `npm run build` 通过。

## 后端修复重复函数定义

- 问题判断：
  - `_load_meeting_transcripts` / `_save_meeting_transcripts` 定义两次（旧版文件持久化 + 新版 DB 版）。
- 修复：
  - 删除第一对旧版函数定义。
  - 统一调用路径到新版 `_db_*` 系列函数。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。

## 会议转录分页

- 问题判断：
  - `GET /api/meeting/transcripts/{id}` 返回全部数据，长会议可能返回数千条。
- 修复：
  - 增加 `?limit=` 和 `?offset=` 查询参数。
  - 默认返回最近 200 条，前端可按需拉取更多。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。

## AI 会议标记持久化（前后端）

- 后端新增：
  - `POST /api/meetings/{meeting_id}/markers` — 保存会议标记为事件，包含标记类型、绑定义题、转写锚点、备注、创建人。
  - `GET /api/meetings/{meeting_id}/markers` — 返回当前会议所有标记事件。
  - 标记事件类型为 `marker`，持久化到 meetings DB 的 events 数组。
- 前端新增：
  - `saveMeetingMarker` 函数，标记弹窗保存按钮调用后端 API。
  - `loadMeetingMarkers` 函数，加入 2.5s 轮询循环。
  - 证据底稿弹窗展示已保存标记（含标记类型、绑定义题、锚点发言人、备注），颜色编码区分类型。
  - 无标记时显示引导提示。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。

## AI 会议记录编辑（前后端）

- 后端新增：
  - `POST /api/meetings/{meeting_id}/records/update` — 接收用户修订的 summary / minutes / decisions / todos。
  - `GET /api/meetings/{meeting_id}/records` 增加合并用户覆盖逻辑：从 events 中查找最近的 `records_override` 并合并到记录中。
  - 修订存储为 `records_override` 事件类型，保留修改历史。
- 前端新增：
  - `editingRecords` / `editedRecords` / `savingRecords` 三个编辑状态。
  - `startEditingRecords` 函数：从当前记录深拷贝初始化编辑数据。
  - `saveEditedRecords` 函数：调用 POST API 保存修订，成功后更新 `meetingGeneratedRecords`。
  - 会议记录卡片新增"修订记录"按钮，点击进入编辑模式。
  - 编辑模式下 Summary / Minutes / Todos 均显示 TextArea 控件。
  - 新增"纪要"标签页，展示议题级别会议纪要内容。
  - 编辑栏显示"保存修订"和"取消"按钮，保存中显示 loading 态。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。

## AI 会议列表筛选

- 问题判断：
  - 会议列表只能全量展示，没有按阶段、类型、关键词筛选的能力。
  - 当会议批次超过 10 个时，查找具体会议效率低。
- 修复：
  - 新增 `meetingFilterStage` / `meetingFilterMode` / `meetingFilterSearch` 三个筛选状态。
  - 新增 `filteredMeetingRecords` useMemo，按阶段/类型/关键词过滤。
  - 列表顶部新增筛选工具栏：
    - 阶段筛选：Select 下拉（问题收集 / 会中记录 / 会后终审 / 已归档）。
    - 类型筛选：Select 下拉（普通会议 / 三重一大）。
    - 搜索：Input 输入框，支持标题、创建人、项目名匹配。
  - 激活筛选时显示匹配数量和"清除筛选"按钮。
  - 筛选无结果时显示专用空状态和清除按钮。
  - 列表行内新增"三重一大"红色标签，方便扫描识别。
  - 列表渲染改为 `filteredMeetingRecords.map()`。
- 已验证：
  - `npm run build` 通过。

## BIND_STEPS 完成逻辑从假数据改为逐项判真

- 问题判断：
  - BIND_STEPS 使用 `index < 2` 写死完成状态，不反映真实绑定进度。
- 修复：
  - 改为逐项判真：`currentUser`、`boundRole`、`audioReady || seconds > 0`、`liveLines.length > 0`。
- 已验证：
  - `npm run build` 通过。

## 创建 AI 会议草稿保存与退出确认

- 问题判断：
  - 用户在创建 AI 会议流程中填写了问题素材、议题、项目名等信息后，点击"返回列表"直接退出，没有任何提示。
  - 已填写的内容全部丢失，无法恢复。
- 修复：
  - 新增 `creationDirty` 脏检测：当问题素材、议题标题、项目名、会议标题、会议性质任一被修改时标记为脏状态。
  - 脏检测只对新建会议生效，已保存/已存在的会议不拦截。
  - 点击返回列表时，若为脏状态弹出确认 Modal，展示已填写内容摘要。
  - Modal 提供三个选项：
    - **保存草稿**：调用 `POST /api/meetings` 将当前内容保存为"问题收集中"阶段的草稿，刷新列表后退出。
    - **放弃草稿**：丢弃所有内容，直接返回列表。
    - **取消**：关闭弹窗，留在当前页面继续编辑。
  - 草稿保存后会议出现在列表，下次可直接进入继续编辑。
  - `openNewMeetingDraft` 创建的空表单不触发脏检测（所有字段初始为空）。
- 已验证：
  - `npm run build` 通过。

## 退出确认 Modal 不显示 & 返回列表按钮卡死修复（根因分析）

- 问题判断：
  - 创建表单中点击"返回列表"完全卡死，不弹退出确认也不回列表。
  - 退出确认 Modal 在 `showExitConfirm = true` 后不渲染。
  - `saveDraftAndExit` 调用 `refreshMeetingList`（不存在），草稿保存抛异常。
- 根因：
  - **Modal 放错了 JSX 分支**：退出确认 Modal 写在会议工作区 return 块，但创建表单是独立 return。当 `meetingCreated = false` 时组件在创建表单分支返回，Modal 根本不在渲染树中。`setShowExitConfirm(true)` 触发了状态更新和重渲染，但 Modal 所在分支永不执行。
  - **React 18 事件批处理**：`setMeetingWorkspaceOpen(false)` 在事件上下文中被静默吞掉，直接调用不生效。
- 修复：
  - 退出确认 `<Modal>` 从会议工作区 JSX 移至创建表单 JSX 内。
  - `setMeetingWorkspaceOpen(false)` 用 `setTimeout(..., 0)` 包裹，打破 React 批处理。
  - `handleBackToList` 新增 `chatInput.trim() !== ''` 检查，输入框有未发送文字也触发退出确认。
  - `refreshMeetingList` → `loadMeetings`，修正不存在的函数名。
  - 创建表单"返回列表"按钮改为原生 `<button>`。
- 验证（Playwright 浏览器自动化全部通过）：
  - 空表单 → 返回列表 ✅
  - 输入框有文字 → 退出确认 Modal → 放弃草稿 → 回列表 ✅
  - 议题栏有内容 → Modal → 放弃草稿 ✅
  - 切换会议性质 → Modal → 放弃草稿 ✅
  - 保存草稿 → 写入后端 → 回列表 ✅
  - 已归档会议中返回列表 ✅
  - `npm run build` 通过。

## 会中操作模态框做实（chat / participants / share）

- 问题判断：
  - `chat` 会中备注 TextArea 关闭即丢，无任何持久化。
  - `participants` 参会人硬限制6行，非演示会议混入 `MOBILE_ROLE_BINDINGS` 假数据。
  - `share` 共享材料只显示议题名，无 QR 码和可复制链接。
- 修复：
  - **chat**：新增 `meetingNotes` state 受控 TextArea，关闭弹窗内容保留，新建会议时清空。
  - **participants**：去掉 `slice(0, 6)` 硬限制，非演示会议跳过 `MOBILE_ROLE_BINDINGS` 假数据，无参会人时显示"暂无参会人"。
  - **share**：加入 `QRCode` 组件（120px）、只读链接框、`onClick` 一键复制、邀请说明文字。
- 已验证：
  - `npm run build` 通过。

## 会中标记弹窗完整实现（转写行锚定 + 已有标记删除）

- 问题判断：
  - 标记弹窗只取 `latestTranscript`，用户无法选择绑定哪条转写行。
  - 无已有标记展示，无法查看当前议题已保存的标记，也无法删除。
- 修复：
  - **转写行选择器**：弹窗内展示最近 8 条转写行，可点击选中，高亮为蓝色边框。选中后在下方显示确认卡片。
  - **已有标记列表**：弹窗底部列出当前议题已有标记（类型 Tag + 发言摘要 + 删除按钮）。
  - **后端 DELETE 端点**：新增 `DELETE /api/meetings/{meeting_id}/markers/{marker_id}`，从 events 数组移除对应标记。
  - **前端 `deleteMeetingMarker`**：调 DELETE 接口 + 乐观移除本地 state。
  - 新增 `pendingMarkerTranscriptIndex` state，每次打开标记弹窗重置为 0。
- 已验证：
  - `npm run build` 通过。
  - `python3 -m py_compile backend_full.py` 通过。

## 前端假数据收敛：meetingRecords 初始化清空

- 问题判断：
  - `meetingRecords` state 初始化为 `INITIAL_MEETING_RECORDS`，非演示会议页面加载瞬间仍会闪过4条假会议记录。
- 修复：
  - 改为 `useState([])` 空数组，真实数据由后端异步拉取填充。
- 已验证：
  - `npm run build` 通过。

## 后端数据持久化复核（meetingMode + DOCX 导出）

- 问题判断（Phase 2.1/2.2）：
  - 计划要求 `meeting_mode` 列持久化和 DOCX 导出优先使用 DeepSeek 记录。
- 复核结果：
  - `meeting_mode` 列已在 CREATE TABLE 中定义、迁移脚本存在、`_meeting_from_row` 读取、`_db_save_meetings` 写入——全程闭环。
  - `download_meeting_archive_docx` 已先调 `_deepseek_generate_meeting_records`，有结果则优先使用，无结果降级到 `_local_generate_meeting_records`。
- 无需代码修改。

## 转录分页 bug 修复

- 问题判断：
  - `GET /api/meeting/transcripts/{id}` 分页逻辑 `all_events[-limit-offset:][-limit:]` 在 offset>0 时返回重复数据（两次切片重叠）。
  - 默认 limit 为 80 而非计划要求的 200。
- 修复：
  - 分页逻辑改为 `all_events[-(limit+offset):-offset]`，正确定位倒数切片窗口。
  - 默认 `limit` 从 80 提升至 200。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。

## 会议 API 基本鉴权

- 问题判断：
  - 所有会议-scoped 端点只校验登录态，任何认证用户可操作任意会议。
- 修复：
  - 新增 `_check_meeting_access(user, meeting)` 辅助函数：检查请求用户的 name 或 dept+name 是否出现在会议 creator 字段中，admin 角色放行所有会议。
  - 添加到 5 个关键 mutation 端点：
    - `PATCH /api/meetings/{id}`
    - `POST /api/meetings/{id}/stage`
    - `DELETE /api/meetings/{id}`
    - `GET /api/meetings/{id}/archive/docx`
    - `POST /api/meetings/{id}/records/update`
    - `POST /api/meetings/{id}/markers`
    - `DELETE /api/meetings/{id}/markers/{marker_id}`
  - 顺便修复 `add_meeting_marker` 中重复调用 `_get_request_user` 的低效写法。
- 已通过 API 测试验证：
  - Admin 操作任意会议 → 200
  - 创建者操作自己的会议 → 200
  - 非创建者操作他人会议 → 403 "您不是该会议的创建人或参与人，无权操作此会议。"

## 会议类型与公司组织架构补齐

- **会议类型**：
  - AI 会议创建页新增 `普通企业会议`，并将新建会议默认类型改为 `普通企业会议`。
  - 会议类型下拉统一为：普通企业会议、经营例会、董事会、总经理办公会、专题会、党委会、党组会。
  - 前端保存草稿、后端新建会议、后端新环境初始化默认值统一改为 `普通企业会议`。
- **公司组织架构**：
  - OA 审批发起部门改为公司部门：董事会办公室、总经理办公室、财务管理部、法务合规部、人力资源部、项目管理部、采购中心、审计监察部。
  - OA 列表入口从“政府 OA 审批 / 政府 OA 演示案卷”改为“企业 OA 审批 / 企业 OA 审批案卷”。
  - 移动端录音账号默认部门改为公司架构：经营管理层、总经理办公室、项目管理部、审计监察部。
  - 登录页移动端部门占位改为“经营管理层 / 项目管理部”。
- **演示数据口径**：
  - 默认会议素材中的“综合办/基建科/纪检监察室”替换为“总经理办公室/项目管理部/审计监察部”。
  - Excel 问题收集模板示例部门改为企业部门，备注改为董事会或总经理办公会审议口径。
  - `demo_content.py` 下发的 OA 配置同步切换为公司部门和董事会/总经理办公会语境。
- **边界说明**：
  - 未做数据库迁移，已有本地用户和已有会议记录不被强制篡改。
  - 三重一大规则库中的制度性词条保留，避免影响合规审查逻辑。

## 用户管理页 UI 修复

- **问题判断**：
  - 用户管理表格缺少页面级作用域样式，表头和部分行会被外部/主题样式污染成黑底，导致可读性很差。
  - 原页面只有标题 + 表格，缺少账号总览信息，页面视觉层级偏弱。
- **修复**：
  - 新增 `user-management-page` 独立作用域，强制表头、表格行、固定列、hover 行使用浅色 B 端表格样式。
  - 新增顶部说明区和 4 个统计卡片：账号总数、启用账号、管理员、组织部门。
  - 操作按钮、分页、表格边框和卡片间距统一到当前 Arco/Ant 视觉体系。
  - 新增用户/编辑用户弹窗里的部门从自由输入改为公司组织架构下拉。
- **验证**：
  - `npm run build` 通过。
  - 浏览器检查确认表头背景为浅灰、表格行背景为白色，不再出现黑底表头/黑底行。

### 泛群聊素材池溢出 - 三层修复

- **文件**：`frontend/src/pages/MeetingComplianceWorkflow.jsx`
- **根因**：`.meeting-workspace` 的 `height: 100%` 在 `<main style={{ flex: 1 }}>` 中解析为 `auto`——CSS 百分比高度需要父级有显式高度，flex item 的计算高度不一定是"显式"的。grid 容器无高度约束 → `grid-template-rows: minmax(0, 1fr)` 无效 → 内容区无限扩展挤掉 footer。且 `.collect-workspace` 只有一个显式行，第二个面板进入隐式 `auto` 行不受限。
- **修复（三层）**：
  1. `<main style={{ flex: 1, minHeight: 0 }}>` → `<main style={{ flex: 1, minHeight: 0, display: 'flex', flexDirection: 'column' }}>` — 将 `<main>` 变为 flex 列容器
  2. `.meeting-workspace { height: 100%; }` → `.meeting-workspace { flex: 1; }` — 用 flexbox 分配替代百分比依赖
  3. `.collect-workspace` 增加 `overflow: hidden` + `grid-template-rows: minmax(0, 1fr) minmax(0, auto)` — 两行显式、聊天池得剩余空间、AI 面板自适应，overflow 只加在此 workspace 不波及其他
- **教训**：`overflow: hidden` 不能放在基类 `.meeting-workspace` 上，会裁剪 live/audit/archive workspace。`grid-template-rows` 第二行不能用 `1fr` 强制等分，应用 `auto` 让它自适应内容。
- **验证**：`npm run build` 通过
