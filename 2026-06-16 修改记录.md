# 2026-06-16 修改记录

## 后端性能优化（全链路）

### 会议数据层

- 问题判断：
  - `MEETINGS_LOCK`（threading.Lock）串行化 22 个会议 API，并发读写互斥。
  - `_load_meetings()` 每次都 5 次 `SELECT *` 查全量数据，list 接口也只用到摘要。
  - `_db_save_meetings` 用 DELETE ALL + INSERT ALL 做写入，单条修改触发全量重建。
- 改动：
  - 会议内存缓存 2s TTL，读路径移除 `MEETINGS_LOCK`，只读端点不再与写互斥。
  - `_db_fetch_meetings` 已有 `include_details` 参数但从未使用，list 传 `False` 跳过 4 次子表查询。
  - 抽取 `_db_upsert_meeting` / `_db_delete_meeting_rows`，单会议写入 O(5ms) 替代 O(n·50ms)。
  - `_save_meetings` 永远走 upsert，DELETE ALL 路径仅用于一次性 legacy migration。
- 验证：
  - 20 并发列表请求全部 <6ms，无锁冲突。缓存命中率 ~95%。

### SQLite 连接复用

- 问题：每个 API 调用重新 `sqlite3.connect()` + 设 PRAGMAs，21MB 数据库每次 open 有开销。
- 改动：`threading.local()` 线程级连接复用，首次连接后缓存在线程本地。

### LLM 调用优化

- 问题判断：
  - `_agenerate` 用 `run_in_executor(None, ...)` 占用 asyncio 默认线程池，LLM 调用 30-90s 会耗尽。
  - 每次 LLM 调用新建 `httpx.Client/AsyncClient`，重复 TCP+TLS 握手 50-100ms。
  - `llm_semaphore` 限制 2 并发，10 人会议场景 AI 调用排队。
- 改动：
  - 创建独立 `_llm_executor`（8 workers），LLM 不再占用默认线程池。
  - 共享 `_httpx_async_client` / `_httpx_sync_client` 连接池（keepalive 20，max 50）。
  - `llm_semaphore` 从 2 提升至 5。
  - `_astream` 替换 per-call `httpx.AsyncClient()` 为共享客户端。

### FastAPI 启动预热

- 问题：SentenceTransformer + ChromaDB 在首个用户请求时懒加载，首请求阻塞 5-15 秒。
- 改动：lifespan handler 启动时后台线程并行加载 ChromaDB、模型、数据迁移。

### 实时会议链路（10 人 × 40 分钟场景）

- 问题判断：
  - `_db_save_transcripts` 每次 DELETE ALL + INSERT ALL，chunk 写入随会议进行越来越慢。
  - `_db_load_transcripts` 加载所有会议转写再按 meeting_id 过滤，桌面轮询全表扫描。
  - `MEETING_TRANSCRIPTS_LOCK` 串行化所有转写读写，10 人同时写入互相阻塞。
  - `_append_meeting_activity` 每来一个 chunk 都触发全量 meeting load/save。
- 改动：
  - 新增 `_db_upsert_transcript` 单行 INSERT/UPDATE，chunk 写入不碰全量数据。
  - 新增 `_db_load_transcripts_for_meeting` 按 meeting_id WHERE 查询，桌面轮询不再扫全表。
  - 转写缓存 1s TTL + `_invalidate_transcripts_cache`，读路径移除锁。
  - 新增 `_append_meeting_activity_light`，事件直接写 `meeting_events` 表 + 自动裁剪旧事件，不触发 meeting 全量保存。
  - `recorder/session`、`recorder/audio`、`transcripts/chunk` 全部改用增量写入。
  - 音频下载改为直接 SQL 查询 `meeting_events` 表。
- 验证：
  - 50 并发转写写入全部 200 OK，总耗时 59ms（旧代码 2s+）。
  - 10 并发转写轮询 30ms 完成，无锁冲突。

### 致命问题修复

- 裸 `except:` × 2 → `except Exception:`，防止吞 `asyncio.CancelledError` 导致进程挂死。
- JWT 签名密钥 `AUTH_SECRET` 移除硬编码默认值，写进 `.env`，启动时空值拒绝。
- 文件上传无限制 → 通用 100MB / 音频 80MB / Excel 20MB 上限，超限返回 413。
- HTTP 连接池 + LLM 线程池在 lifespan shutdown 中清理，防止 worker 重启 fd 泄漏。
- 列表缓存统一走全量缓存，避免 list 查询每次都穿透到 DB。

### 前端 & Nginx

- Nginx：gzip 压缩（4.9MB → ~1.2MB）、静态资源 1 年缓存、location 从 9 个合并为 5 个、API 30r/m + LLM 5r/m 限流。
- Vite：`@ant-design/icons` 拆分为独立 68KB chunk。
- 前端 build 通过。

### 硬编码密钥清除

- DeepSeek API key 从源码移除，写入 `.env`。
- DashScope API key 已在 `.env` 中，源码只读环境变量。
- JWT 签名密钥从源码移除，写入 `.env`。

### 部署注意

- `.env` 新增 `DEEPSEEK_API_KEY`、`APP_AUTH_SECRET`，重启前需确认已配置。
- Nginx 配置已更新 `nginx-fixed.conf`，需 `sudo nginx -s reload` 生效。
- 后端重启后首次请求不再卡顿（预热已完成）。

## 模块拆分（backend/ 目录）

将 `backend_full.py`（6500 行单文件）拆分为模块化结构：

```
backend/
  __init__.py         # 包标记
  config.py           # 全局配置、环境变量、路径常量、锁、缓存 (167行)
  models.py           # 所有 Pydantic 请求/响应模型 (150行)
  db.py               # SQLite 连接池、Schema、CRUD、缓存层 (1490行, 40函数)
  llm_client.py        # DeepSeekThinkingLLM + HTTP 连接池 (422行)
  main.py              # FastAPI 入口、lifespan、路由注册 (144行)
  routes/
    __init__.py
    *.py               # 路由模块（待迁移）
```

- `config.py`：环境变量、路径、锁、缓存、WAL checkpoint、上传限制、ASR 重连参数、网络工具函数
- `models.py`：20 个 Pydantic 模型，含 ChatResponse/LoginRequest/MeetingUpsertRequest 等
- `db.py`：所有 SQLite 操作（连接复用、schema、CRUD、增量写入）+ 缓存包装层
- `llm_client.py`：DeepSeek LLM 客户端（支持 thinking/stream）+ HTTP 连接池
- `main.py`：FastAPI app + lifespan 预热 + CORS + 路由注册骨架

**当前状态**：核心模块已完成且 `backend_full.py` 已接入导入。`backend_full.py` 仍为运行中的服务入口，底层已从新模块导入。路由迁移为下一步工作。

**验证**：服务重启后 `/health` 全部 green，登录/会议列表/转写查询均正常。

## 补充稳固性措施

### /health 健康检查端点

- `GET /health` 返回 DB、ChromaDB、LLM key、ASR key 状态 + 会议/转写计数 + 运行时长。
- 任一组件异常时 status 降级为 `degraded`，方便监控告警。

### Fun-ASR WebSocket 断线重连

- 问题：40 分钟会议中 Fun-ASR 连接断开，手机端音频数据丢失，无法恢复。
- 改动：
  - DashScope 连接增加 3 次重试（指数退避 1s→2s→4s）。
  - 主循环中 DashScope 断开时自动重连（最多 5 次，退避 1s→16s）。
  - 重连后回放缓冲区最近 40 帧音频（约 2s），最大限度减少丢失。
  - 手机端收到 `reconnecting` / `reconnected` 消息可感知重连状态。

### 缓存上限保护

- 会议缓存 >500 条自动淘汰，下次查询从 DB 重建。
- 转写缓存 >50 会议或 >5000 条自动淘汰。
- 防止长时间运行后内存 OOM。

### WAL checkpoint 管理

- 写入路径每 5 分钟检查 WAL 文件，>10MB 自动 TRUNCATE。
- 防止 SQLite WAL 文件无限增长撑爆磁盘。

### systemd 开机自启动

- 创建 `ai-compliance.service`，配置为 systemd 服务。
- 安装路径：`/etc/systemd/system/ai-compliance.service`
- 启动：`sudo systemctl start ai-compliance`
- 开机自启：`sudo systemctl enable ai-compliance`
- 日志：`journalctl -u ai-compliance -f`

## 今日修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend_full.py` | 修改 | 全链路性能优化 + 模块化导入接入 |
| `.env` | 修改 | 新增 DEEPSEEK_API_KEY、APP_AUTH_SECRET |
| `nginx-fixed.conf` | 重写 | gzip、缓存、限流、location 合并 |
| `frontend/vite.config.js` | 修改 | antd-icons 分包 |
| `backend/config.py` | 新建 | 全局配置模块 |
| `backend/models.py` | 新建 | Pydantic 模型模块 |
| `backend/db.py` | 新建 | SQLite + 缓存层模块（42 函数） |
| `backend/llm_client.py` | 新建 | DeepSeek LLM 客户端模块 |
| `backend/main.py` | 新建 | FastAPI 入口骨架 |
| `ai-compliance.service` | 新建 | systemd 开机自启配置 |
| `backup_db.py` | 新建 | 数据库自动备份脚本 |
| `tests/test_core.py` | 新建 | 核心链路自动化测试（32 用例） |

## 数据库备份

- `backup_db.py`：WAL checkpoint → 安全复制 → 过期清理
- 策略：每小时备份，保留 7×24=168 份；每天 02:00 额外保留日备份，保留 30 天
- 调度：crontab `0 * * * *` 每小时自动执行
- 恢复：`python backup_db.py --restore` 列出备份，`--restore <名称>` 恢复指定版本

## 自动化测试

- `tests/test_core.py`：32 个测试用例，覆盖 6 大模块
- TestHealth：健康检查、组件状态、计数、运行时长
- TestAuth：登录成功/失败、空用户名、/me 端点
- TestMeetings：列表、创建、查询、PATCH、归档、添加问题
- TestTranscripts：写入 chunk、查询、空会议查询
- TestDatabase：14 个 DB 函数单元测试（连接、JSON、ID 清理、权限、归一化等）
- TestConfig：数据库文件存在、密钥已配、上传限制合理
- 运行：`python -m pytest tests/test_core.py -v`（44 passed）

## 知识库参考资料增强

- `/api/kb_stream` 新增 `type: "sources"` SSE 事件，含来源文件名+页码/片段位置+原文预览
- PDF 向量化按页切分，保留 `page`/`total_pages` 元数据 → 显示"第N/M页"
- 前端 AI 回答下方"📚 参考资料"，每条可点击 Modal 查看原文
- 批量重向量化：宁波产权交易中心 39 个 DOCX 重新入库（569 chunks）

## 用户管理真实化

- 部门 CRUD API（`/api/departments`），含删除保护+重命名同步
- `_resolve_meeting_role` 移除硬编码 username→角色映射，全部从用户记录读取
- 用户表单新增 `meetingRole`/`meetingSeat` 字段，前端部门从 API 加载
- 前端新增部门管理弹窗（新建/重命名/删除）

## 合同审查 + 审计端点修复

- 合同审查 `_get_httpx_sync()` 缺少 `trust_env=False` → `ALL_PROXY=socks://` 导致崩溃 → 修复
- OCR 同理修复
- `/api/audit_stream` 别名补充（前端报 404→修复）
- 测试从 32→44 用例，覆盖之前漏掉的 audit/kb/departments/contract 端点

## 日志归档 + 请求审计

- 日志轮转：`RotatingFileHandler`，10MB×10 个文件，同时写 stdout
- 审计中间件：每个请求记录 `method path → status duration user IP`
- 日志示例：`【审计】POST /api/meetings/.../agenda/generate → 200  38421ms  user=admin  ip=192.168.66.44`

## 知识库扫描件 OCR

- 新增 `_chunk_text_with_pages()` 函数，PDF 按页切分保留页码
- 扫描件 PDF 通过百炼 OCR 提取文字后入库（PyMuPDF 渲染页面 → DashScope qwen-vl-ocr）
- 城投合规资料/宁波产权交易中心 43 个文件全部入库（39 DOCX + 4 OCR扫描PDF），共 2928 chunks
- 参考资料显示"第N/M页"（PDF）或"片段N"（DOCX）

## 前端白屏修复

- 新增 `ErrorBoundary` 组件，懒加载 chunk 加载失败时显示"⚠️ 页面加载失败" + 刷新按钮
- `renderContent()` → `useMemo` + `key={currentPage}` 消除页面切换空白帧

## 运维工具

- `check_system.sh`：9 项状态检测（进程→API→端点→Nginx→自启→备份→日志→磁盘→测试），只读安全
- `check_system.sh --fix`：检测 + 自动修复
- `setup_prod.sh`：首次部署/完整重建（nginx + systemd + 启动 + 测试）
- `backup_db.py`：每小时自动备份，保留 7 天，`--restore` 恢复

## 数据清理

- 清理 53 个测试会议（test-* / zhao-* / meeting-perf-*）
- 演示数据保留但标记清晰
