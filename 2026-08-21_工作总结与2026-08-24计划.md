# 2026-08-21 工作总结与 2026-08-24 计划

## 2026-08-24 执行进展（断电恢复后更新）

- 后端拆分已收口：`backend/main.py` 成为唯一模块化入口，不再导入 `backend_full.py`，后者仅保留作验收/回滚对照。
- 路径门禁已建立：旧入口 135 个业务动作全部完成对照；新入口保留全部非 OnlyOffice 路径，只删除 9 个明确废弃的 OnlyOffice 编辑器、插件与回调接口。
- 已拆分并注册会议主流程、会议辅助、材料、计时、ASR WebSocket、成果、签字、权限、规则、知识、审核、普通文档和合同域。
- ASR 恢复缓存清理任务已纳入主入口生命周期，进程关闭时会正确取消和等待。
- 项目隔离环境已对齐锁定的 FastAPI 0.135.1 / Starlette 0.52.1，解决系统包与项目包混用导致的假失败。
- 后端全量离线回归：**66/66 通过**；模块化服务真实启动、关闭、健康检查、OpenAPI 与未登录 401 均通过。
- 前端生产构建通过；浏览器实测登录页正常、无控制台错误，`3000 → 8002` 的 `/api` 代理可用。
- 44 只读取证和 Golden Case 已完成：固定 8 月 19 日悦仲会议 721 条转写，原始业务 payload 只保存在本机忽略目录，不提交敏感内容。
- 真实 Qwen 重放完整覆盖 721/721、3 次 Map 成功。首次 Reduce 超时；增加重试后仍超时；拆分 4000-token 输出预算后 Reduce 一次完成，但返回 `minutes=0` 且 basis 覆盖为 0。已补正式件质量门禁：该结果现在会进入 `needs_review`，禁止作为正式件，不再把“JSON 返回成功”误判为“校对通过”。
- 44 部署尚未执行：本机没有可用 SSH 私钥或主机别名，API 管理员凭据不能替代服务器部署凭据。完成本地复测后需要补充 44 的 SSH 登录方式，才能执行备份、切换和回滚验证。

## 一、今日结论

今天完成了会议系统前端关键体验修复、后端纪要质量方案落地的第一阶段，以及后端模块化拆分的核心能力建设。

当前代码**尚未达到可部署到 `192.168.66.44` 的状态**。原因不是核心生成能力未实现，而是：

- 新纪要管线已经完成，但还需真实 Golden Case 验证；
- 模块化后端入口尚未覆盖 `backend_full.py` 中全部需要保留的生产路由；
- Whisper 终审、文档、知识库、审核、合同等旧接口仍需完成迁移与运行态验证；
- 44 上的线上版本与本地仓库需要做差异取证和部署前备份。

因此今天停在“核心模块已落地并通过专项测试，生产入口尚未切换”的安全位置。

---

## 二、今日前端工作

### 1. 本地开发端口纠正

- 前端开发端口统一为 `3000`。
- 本地访问地址：`http://localhost:3000/`。
- 前端代理后端目标确认可指向：`http://192.168.66.44:8002`。

### 2. 新建会议页面修复

- 修复议题状态标签被压缩、变形问题。
- 移除创建页左侧工作区内部嵌套滚动，避免“下面内容看不见”。
- 保留页面级滚动，减少多层滚动造成的遮挡和操作迷失。
- 未保存会议草稿不再提前请求会议详情、转写、SSE 和标记接口，消除无意义 404。

### 3. 前端验证

- 已进行 `1366px` 桌面宽度检查。
- 前端生产构建通过。
- 当前仍有 Ant Design vendor 包体较大的构建警告，暂不影响运行，后续单独做拆包优化。

---

## 三、后端方案与架构工作

### 1. 完整重读并采用质量方案

执行依据：

- `2026-08-21_纪要生成质量改造方案.md`
- `2026-08-21_会议纪要生成链路改造计划.md`

最终确认的实现原则：

- 保留 v1，不删除旧生成逻辑；
- 新管线使用 `RECORDS_PIPELINE=v1|v2` Feature Flag；
- 按录音文件边界切块，单块超过 4000 字再按时间/segment 边界拆分；
- 75 分钟、8 段录音约执行 8 次并发 Map 和 1 次 Reduce；
- 校对合并在 Map 调用中，不额外增加一次 LLM 调用；
- evidence 必须逐字来自原文；
- 纪要链路默认使用内网 Qwen，不主动发送全文到外部 DeepSeek；
- Map 失败重试一次，仍失败则走 v1 fallback 并标记 `degraded`；
- 不改数据库 Schema，不修改 `.env`。

### 2. 当前后端结构确认

- 生产入口仍是 `backend_full:app`。
- `backend_full.py` 仍约有 135 个旧路由。
- 模块化核心已覆盖会议、议题、录音、转写、成果、权限、签字、材料、计时等主要会议域。
- `backend.main` 当前仍引用若干不存在或尚未迁移完整的路由文件，暂不能直接替代生产入口。
- OnlyOffice 在线编辑相关能力不再迁移；普通文档上传、下载、解析和导出仍需保留。

---

## 四、今日已完成的后端代码

### 1. Records Pipeline v2

新增：

- `backend/schemas/meeting_records.py`
- `backend/services/meeting_record_generation_service.py`
- `tests/test_meeting_record_generation_service.py`

已实现：

- Whisper segment 标准化；
- 按音频文件边界分组；
- 单文件超过 4000 字按 segment 和时间边界拆分；
- Map 并发限制；
- 每个 Map 失败后重试一次；
- Reduce 单次调用；
- `minutes / decisions / risks / disclosures / todos` 结构；
- `basis` 时间区间、原文引句和 segment ID；
- evidence 原文逐字校验；
- 风险与披露事项独立输出；
- 参会人名册责任人校验，无法匹配时输出“待确认”；
- 全量 segment 覆盖率；
- 输入 SHA256、模型、管线版本、切分策略和调用次数快照；
- v1 fallback 与 `degraded` 标记。

专项测试：**4/4 通过**。

### 2. 校对与领域词典

新增：

- `backend/services/meeting_proofread_service.py`
- `data/glossary.json`
- `tests/test_meeting_proofread_service.py`

已实现：

- 领域词典候选；
- 不做全局暴力替换；
- 保留 `rawText / correctedText`；
- 修订原因、来源和置信度；
- 数字、日期、金额、否定关系等关键语义保护；
- 正式文件校对准入检查。

### 3. 正式文件与证据底稿

新增：

- `backend/services/meeting_document_service.py`
- `tests/test_meeting_document_service.py`

已实现：

- 正式会议纪要 DOCX；
- 独立证据底稿 DOCX；
- 正式正文不写入完整 ASR 原文；
- 证据底稿保存全量有效转写；
- 噪音和剔除行进入独立附录；
- 超过 2 分钟断档标记；
- Map 分段边界；
- 正式条目到原始 segment 的反向索引；
- 生成参数快照和文件 SHA256。

校对与文档专项测试：**8/8 通过**。

### 4. 声纹会前预检

新增：

- `backend/services/voiceprint_preflight_service.py`
- `tests/test_voiceprint_preflight_service.py`

已实现：

- 复用现有 `voiceprint_profiles`，不新增数据库表；
- 校验参会人是否有可用声纹；
- 返回缺失人员结构；
- 缺失时提供 HTTP 409 语义；
- 支持“说话人A/B + 人工改派”的后续兜底信息；
- 进入会中、会后终审和归档前执行预检。

### 5. 决议确认闭环

修改：

- `backend/services/agenda_service.py`
- `backend/services/meeting_service.py`

新增：

- `tests/test_agenda_decision_flow.py`

已实现：

```text
draft → confirmed → signing → signed → archived
```

并支持 `rejected`：

- 确认时写入 `confirmed_at`；
- 标题或内容修改后自动 `version + 1`；
- 修改后退回 `draft`；
- 清除旧确认时间；
- 旧版本签字失效；
- 已归档决议禁止修改；
- 兼容旧的 `decision_pending` 等状态。

声纹、决议和原签字专项测试合计：**11项通过**。

### 6. 主线接线

已修改：

- `backend/config.py`
- `backend/routes/meetings.py`
- `backend/routes/outcomes.py`
- `backend/services/outcome_service.py`

已完成：

- 新增 `RECORDS_PIPELINE` 开关，非法值自动回到 v1；
- 声纹缺失映射为 HTTP 409；
- 新增声纹预检查询接口；
- 新增 v2 纪要生成接口；
- GET records 在启用 v2 后支持首次生成与 force 重生成；
- 纪要生成专门绑定 `QwenLocalLLM`，不使用 DeepSeek 优先实例；
- Whisper 缺失时降级实时转写并标明来源；
- 新增正式文件与证据底稿生成接口；
- 新增两类 DOCX 下载接口；
- 下载路径限制在会议文件目录内，避免任意路径读取。

---

## 五、今日测试结果

### 已通过

- 新生成管线专项测试：4项通过；
- 校对与双文档专项测试：8项通过；
- 声纹、决议及签字相关专项测试：11项通过；
- 不依赖运行中服务的后端测试：**42/42 通过**；
- 本轮修改文件 Python 语法编译通过。

### 全量测试基线发现的问题

完整执行仓库测试时发现：

- 根目录 `test_chroma.py` 和 `test_chroma_batch.py` 依赖不完整的 `transformers` 环境；
- 根目录 `test_stream.py` 会在收集阶段直接访问外部运行服务并超时；
- `tests/test_core.py` 依赖本地 `8002` 后端先启动，否则大量连接拒绝；
- `APP_AUTH_SECRET` 未设置时，认证相关测试无法运行；测试时使用了临时进程变量，没有修改 `.env`；
- 部分旧 FastAPI/Starlette 组合存在版本兼容风险，需要在 44 的实际 Python 环境确认。

### 本地测试环境

- 已创建项目隔离 `.venv`；
- 仅安装项目依赖清单中已经声明的 `pytest` 和 `PyJWT`；
- 未修改系统 Python，未安装全局依赖。

---

## 六、今日暂停时的真实状态

### 已完成

- Records Pipeline v2 核心能力；
- 校对和领域词典；
- 正式件与证据底稿服务；
- 声纹预检；
- 决议状态闭环；
- 模块级 API 第一轮接线；
- 42项本地模块测试通过。

### 未完成

- 8月19日悦仲会议 Golden Case 数据尚未从44导出；
- A1～A8 尚未用真实会议完成验收；
- `backend_full.py` 尚未完成所有保留路由的模块化迁移；
- 模块化 `backend.main` 尚未成为可直接上线的完整生产入口；
- Whisper终审、转写校订、设置等会议辅助接口仍在迁移中；
- 规则、知识库、审核、合同、普通文档接口仍在迁移中；
- 签字完成后自动推进决议 `signed` 状态还需端到端确认；
- DOCX 尚未进行真实会议视觉渲染检查；
- 尚未在本地启动模块化后端完成接口测试；
- 尚未部署到44。

### 今天明确不做的动作

- 不修改数据库 Schema；
- 不修改 `.env`；
- 不删除 `backend_full.py`；
- 不迁移 OnlyOffice 在线编辑功能；
- 不在验证不足时部署44。

---

## 七、2026-08-24（下周一）工作计划

## P0：先完成后端拆分

### 1. 收口会议辅助域

- 完成 `meeting_review_service.py` 和 `meeting_review.py`；
- 迁移 Whisper 终审读取；
- 迁移转写文本校订；
- 迁移说话人人工改派；
- 迁移设置接口；
- 迁移实时待办；
- 统一正式件/证据底稿状态与下载接口；
- 保证同一能力只保留一份实现。

### 2. 收口弱化业务工具域

- 迁移规则库接口；
- 迁移知识库和议题级检索；
- 迁移审核流接口；
- 迁移合同接口；
- 迁移普通文档上传、下载、解析和导出；
- 不迁移 OnlyOffice 编辑器、插件和回调链路。

### 3. 完成生产应用装配

- `backend/app_factory.py` 注册全部保留路由；
- 重写 `backend/main.py`，只负责生命周期和应用装配；
- `backend_full.py` 改为兼容入口或旧版回滚入口；
- 明确生产启动命令；
- 输出旧路由到新路由的完整映射表；
- 检查重复路由和缺失路由。

## P0：Golden Case 与纪要质量验收

### 4. 从44导出8月19日悦仲会议

需要导出：

- 8段录音；
- Whisper event；
- 721条转写；
- 当前 generatedRecords；
- 参会人和议题；
- 当前正式 DOCX。

保存为：

```text
tests/fixtures/2026-08-19-yuezhong/
```

只做读取和复制，不修改线上数据。

### 5. 执行 A1～A8 验收

- 5个核心事项全部覆盖；
- 不产生跨议题拼接的虚假决议；
- “引赛”等错字不进入正式件；
- 不出现“Whisper终审”作为正式说话人；
- 正式件0行完整原始转写；
- 决议可确认、修改、否决并重新生成文件；
- 落款包含模型、管线、切分和输入摘要；
- 纪要全文默认不出内网。

## P1：运行态测试

### 6. 启动模块化后端

- 使用临时测试密钥启动本地 `8002`；
- 不修改 `.env`；
- 检查健康接口；
- 检查路由表；
- 运行 `tests/test_core.py`；
- 修复实际接口失败；
- 验证前端 `3000` 到本地/44后端切换。

### 7. 文档视觉验证

- 用 Golden Case 生成正式 DOCX；
- 用 Golden Case 生成证据底稿 DOCX；
- 渲染为图片/PDF检查分页、表格、标题、引用和落款；
- 检查正式件是否混入原始转写；
- 检查证据底稿覆盖率是否100%。

## P1：部署准备与44上线

### 8. 部署前检查

- 对比本地与44代码版本；
- 记录44当前启动命令、Python环境和依赖；
- 备份当前代码、启动脚本和必要数据文件；
- 验证 v1 回滚开关；
- 准备模块化入口回滚方案；
- 不做数据库迁移。

### 9. 部署44

只有以下条件全部满足才部署：

- 本地模块测试全绿；
- 本地接口测试全绿；
- Golden Case A1～A8通过；
- 两份DOCX视觉检查通过；
- 模块化路由覆盖检查通过；
- 回滚包可用。

部署后：

- 先以 `RECORDS_PIPELINE=v1` 启动验证旧链路；
- 再切换 v2 做指定会议验证；
- 检查 `8002` 健康、日志和进程；
- 前端 `3000` 完成登录、建会、议题、录音、转写、纪要、签字和归档测试；
- 失败立即回滚。

---

## 八、下周一优先级

```text
后端路由拆分完成
→ backend.main 可独立启动
→ Golden Case 导出与 A1-A8
→ 本地接口和 DOCX 验证
→ 44 备份
→ 44 部署
→ 远端全流程验收
```

部署不是下周一的第一步。第一步是让模块化后端真正完整且可测试；任何本地验收未通过，都不进入44部署。
