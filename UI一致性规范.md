# UI 一致性规范

## 目标

当前系统的问题不是某一个页面不好看，而是每个功能都在使用不同的卡片、表单、间距、字重和颜色。后续 UI 调整必须先遵守统一规则，再做单页创新。

目标：

- 同一类信息使用同一类组件。
- 同一层级标题使用同一字号和字重。
- 同一容器使用同一边框、圆角和内边距。
- 表单、列表、AI 面板、证据面板的交互位置稳定。
- 政府人员进入页面后能用亲密、对齐、分类原则快速判断：现在在哪、要填什么、下一步点哪里。

## 设计原则

### 亲密

- 同一任务的信息必须靠近。
- 标题、说明、状态、主按钮属于同一组，放在同一头部区域。
- 表单字段和自动带入信息分组呈现，不混在同一视觉层。
- 审查结果、依据、风险、建议必须形成固定顺序。

### 对齐

- 页面统一使用 8px 网格。
- 页面外边距统一 `16px`。
- 卡片内边距统一 `16px`，密集工具面板可用 `12px`。
- 表单标签、输入框、按钮左边界必须对齐。
- 三栏布局中，左栏为发起/筛选，中栏为主工作区，右栏为 AI/审查/证据，不反复变化。

### 分类

- 页面内容固定分为：
  - `Page Header`：页面标题、状态、全局操作。
  - `Primary Workspace`：用户正在处理的主要对象。
  - `Assistant Panel`：AI 审查、建议、解释、证据。
  - `Evidence / Log`：留痕、材料、历史、导出。
- 不同业务可以换内容，但不能换结构逻辑。

### 易用

- 每屏只突出一个主操作。
- 主按钮最多一个，次按钮不超过两个。
- 弱化装饰性卡片，优先让状态、输入、结果可扫读。
- 不使用大段功能说明，页面文案只解释当前动作和风险。

## 色彩 Token

以 Arco Design token 为基础，Ant 页面也要映射到同一语义。

| 语义 | Token / 色值 | 用途 |
| --- | --- | --- |
| 主色 | `rgb(var(--primary-6))` / `#165DFF` | 主按钮、当前步骤、可操作链接 |
| 主色浅底 | `var(--color-primary-light-1)` | 当前项背景、聚焦态外发光 |
| 正文 | `var(--color-text-1)` / `#1D2129` | 标题、重要文本 |
| 次级 | `var(--color-text-2)` / `#4E5969` | 标签、辅助描述 |
| 弱化 | `var(--color-text-3)` / `#86909C` | 占位、时间、说明 |
| 禁用 | `var(--color-text-4)` / `#C9CDD4` | 禁用态 |
| 边框 | `var(--color-border-2)` / `#E5E6EB` | 卡片、输入框、列表边界 |
| 页面底 | `#F7F8FA` | 主内容背景 |
| 面板底 | `#FFFFFF` | 工作面板 |
| 轻填充 | `var(--color-fill-1)` | 信息条、轻量分组 |
| 成功 | `rgb(var(--green-6))` | 已完成、通过 |
| 警告 | `rgb(var(--orange-6))` | 待处理、提醒 |
| 危险 | `rgb(var(--red-6))` | 拦截、缺失、高风险 |

禁止：

- 同一页面内同时使用 `#1677ff`、`#1d5fd7`、`#165DFF` 三套主蓝。
- 大面积紫蓝渐变、重阴影、玻璃拟态和旧式灰底卡片混用。
- 红色只用于风险，不用于普通强调。

## 字体与字重

字体：

```css
font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif;
```

字号：

| 层级 | 字号 | 字重 | 用途 |
| --- | --- | --- | --- |
| 页面标题 | `20px` | `600` | 一级页面标题 |
| 模块标题 | `16px` | `600` | 卡片/面板标题 |
| 小节标题 | `14px` | `600` | 表单组、列表组 |
| 正文 | `13px` | `400` | 大部分业务文本 |
| 标签 | `12px` | `500` | 表单标签、状态说明 |
| 弱提示 | `12px` | `400` | 时间、备注、占位 |
| 数据大字 | `22px` | `600` | KPI 数字，不滥用 |

禁止：

- 常规文本使用 `800/900` 字重。
- 页面中同时出现 11px、12px、13px、14px、15px、16px、18px、20px 的无规则混用。
- 按视口宽度缩放字体。

## 间距

基于 8px 网格：

| Token | 值 | 用途 |
| --- | --- | --- |
| `space-xs` | `4px` | 图标和文字 |
| `space-sm` | `8px` | 紧密项目 |
| `space-md` | `12px` | 表单项、列表项 |
| `space-lg` | `16px` | 卡片内边距、模块间距 |
| `space-xl` | `24px` | 页面大分组 |

页面规则：

- 页面外边距：`16px`。
- 面板间距：`12px`。
- 面板内边距：`16px`。
- 密集工具条内边距：`12px`。
- 表单项间距：`12px`。

## 圆角与边框

| 组件 | 圆角 | 边框 |
| --- | --- | --- |
| 页面主容器 | `16px` | `1px solid color-border-2` |
| 普通面板 | `12px` | `1px solid color-border-2` |
| 输入框 | `8px` | `1px solid color-border-2` |
| 小状态标签 | `999px` | 无或浅边框 |
| 表格/列表项 | `8px` | `1px solid color-border-1` |

禁止：

- 卡片套卡片。
- 所有东西都用大圆角。
- 每个页面自定义一套阴影和边框。

## 页面结构规范

### Dashboard / 总览

- 顶部：一句话状态 + 2 到 4 个关键指标。
- 中部：风险队列 / 事项队列。
- 右侧：今日待处理 / AI 提醒。
- 不做营销式 hero。

### 表单 / OA / 会议创建

- 左侧：表单和发起动作。
- 中间：流程或主对象。
- 右侧：AI 审查、材料、证据。
- 表单顶部必须有任务状态和说明。
- 表单下方放自动带入信息，不能和输入框混排。

### AI 问答

- 左侧：知识源/会话列表。
- 中间：问答主区域。
- 右侧：引用依据/命中片段。
- 输入框固定底部。
- 回答内容用统一 Markdown 样式。

### 合同审查

- 左侧：文档预览。
- 右侧：AI 审查结果。
- 顶部：文件名、审查状态、导出。
- 风险项固定为：风险等级、原文定位、审查依据、修改建议、采纳动作。

### AI 会议

- 列表页：会议批次、阶段、创建人、创建时间、状态。
- 实施页：按阶段切换，不并列展示不存在的数据源。
- 会中页：录音、角色、实时转写、纠错。
- 归档页：纪要、签名、材料、证据链。

## 组件规范

### 表单

- 标签 `12px / 500 / color-text-2`。
- 输入内容 `13px / 400 / color-text-1`。
- 输入框高度 `36px`。
- 大段文本框最小高度 `88px`。
- 主按钮高度 `40px` 或 `42px`。

### 卡片 / 面板

- 普通卡片只用于重复项。
- 页面区块不能全都做浮卡。
- 面板头部只放标题、状态、必要操作。
- 面板内不要再套面板，使用分割线和轻填充区域分组。

### 状态

- 状态数量控制在 4 类：
  - 进行中：蓝色。
  - 已完成：绿色。
  - 待处理：橙色。
  - 风险/拦截：红色。
- 不再为每个页面自造状态颜色。

### AI 面板

- 固定结构：
  - AI 状态。
  - 结论。
  - 依据。
  - 建议。
  - 操作。
- 不把 AI 文本直接铺满整页。
- 风险等级和引用来源必须可扫读。

## 迁移顺序

第一批必须统一：

1. `App.jsx` 全局 token 和 shell。
2. `index.css` 全局 CSS 变量。
3. `OAWorkflow.jsx`。
4. `MeetingComplianceWorkflow.jsx`。
5. `OfficeEditor.jsx` / 合同审查。

第二批：

1. `ComplianceAudit.jsx`
2. `ComplianceAuditFocus.jsx`
3. `KnowledgeBase.jsx`
4. `KnowledgeLibrary.jsx`

第三批：

1. `DecisionArchive.jsx`
2. `RulesLibrary.jsx`
3. `UserManagement.jsx`
4. `MobileMeetingRecorder.jsx`

## 验收标准

- 1080p 下主流程不需要缩放截图。
- 同一页面主操作不超过一个。
- 所有面板标题字号一致。
- 所有表单输入高度一致。
- 所有页面使用同一主蓝和同一中性色。
- 业务状态颜色含义一致。
- 主要内容按亲密、对齐、分类原则分组。
- 政府人员第一次打开页面，3 秒内知道当前页面用途和下一步动作。
