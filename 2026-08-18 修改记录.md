# 2026-08-18 修改记录

## 一、AI 会议界面改造（参考 1920 设计稿）

参考文件：`/home/ai/文档/ai-compliance-meeting-1920.html`

### 1. 会议创建页三栏布局重构
**文件**：`frontend/src/pages/MeetingComplianceWorkflow.jsx`（L2799-L3156）

将原有 grid 三栏改为参考设计的 flex 布局 + 箭头连接器：

- **Column 01** "先收集碰到的问题"：Tab 栏、下载模板/上传台账/复制链接按钮行、蓝色信息提示框、问题列表、textarea + 提交按钮
- **箭头 01→02**
- **Column 02** "待生成开会待办"：居中空状态插图、AI 生成按钮、议题卡片列表、绿色已选统计框
- **箭头 02→03**
- **Column 03** "创建 AI 会议"：表单（名称/日期/性质 radio）、待上会议题列表、手动添加输入框、取消+创建会议按钮
- **底部横幅**：AI 功能说明

### 2. 参考设计 CSS 样式
**文件**：`frontend/src/pages/MeetingComplianceWorkflow.css`（末尾新增 ~400 行）

新增样式类：
- `.ref-create-layout` — 三栏 flex 容器
- `.ref-col` — 白色圆角 16px 卡片
- `.ref-col-head` / `.ref-num-badge` / `.ref-col-title` / `.ref-col-desc` — 列头
- `.ref-arrow` — SVG 箭头连接器
- `.ref-action-row` / `.ref-action-btn` — 操作按钮行
- `.ref-info-box` — 蓝底信息提示
- `.ref-tabs` / `.ref-tab` — Tab 栏
- `.ref-green-box` / `.ref-green-check` — 绿色状态框
- `.ref-radio-group` / `.ref-radio` — Radio 切换
- `.ref-field` / `.ref-field-label` / `.ref-input` — 表单字段
- `.ref-btn-primary` / `.ref-btn-white` / `.ref-btn-outline` — 按钮
- `.ref-footer-banner` — 底部信息横幅
- 响应式：1280px 以下自动纵向排列

### 3. 会议日期默认今天
**文件**：`frontend/src/pages/MeetingComplianceWorkflow.jsx`（L492）

```js
// 旧
const [meetingDate, setMeetingDate] = useState(initialSearchParams.get('date') || '');
// 新
const [meetingDate, setMeetingDate] = useState(initialSearchParams.get('date') || createLocalDate());
```

---

## 二、阶段流程精简（移除"议题确认"）

### STAGES 常量
**文件**：`frontend/src/pages/MeetingComplianceWorkflow.jsx`（L38）

```js
// 旧：4 阶段
[
  { key: 'collect', no: '01', title: '议题确认', desc: '收问题、成议题、绑项目' },
  { key: 'meeting', no: '02', title: '会中采集与校对', desc: '录音、认人、标事件' },
  { key: 'audit', no: '03', title: '会后终审', desc: '材料核验、合规拦截' },
  { key: 'archive', no: '04', title: '公文归档', desc: '纪要、签署、防伪归档' },
]

// 新：3 阶段
[
  { key: 'meeting', no: '01', title: '会中采集与校对', desc: '录音、认人、标事件' },
  { key: 'audit', no: '02', title: '会后终审', desc: '材料核验、合规拦截' },
  { key: 'archive', no: '03', title: '公文归档', desc: '纪要、签署、防伪归档' },
]
```

### getStageState() 适配
- `collect` 和 `meeting` 都映射到 `meeting` 为 active
- 移除 `collect` 相关的 complete/locked 判断

### currentStageCopy 编号更新
- meeting: 01、audit: 02、archive: 03

### CSS grid 列数
**文件**：`frontend/src/pages/MeetingComplianceWorkflow.css`（L557）
```css
/* 旧 */ grid-template-columns: repeat(4, minmax(0, 1fr));
/* 新 */ grid-template-columns: repeat(3, minmax(0, 1fr));
```

---

## 三、后端体验优化（4 项修复）

**文件**：`backend_full.py`

### 1. Chunk 索引冲突修复（L3950-L3974）
多用户同时上传录音 chunk 时，同序号文件互相覆盖。

```py
# 旧：chunk_{index:06d}.webm（全局）
chunk_name = f"chunk_{chunk_index:06d}.webm"

# 新：chunk_{username}_{index:06d}.webm（按用户隔离）
chunk_name = f"chunk_{username}_{chunk_index:06d}.webm"
```

合并逻辑同步更新（L3999-L4017）：
- 优先读取 `chunk_{username}_*.webm`
- 降级兼容旧格式 `chunk_*.webm`
- 日志增加 `user=` 字段

### 2. DashScope 重连优化（L5198-L5245）
- 断开后立即缓存音频，不再依赖连接状态
- 重连成功后自动回放缓冲区音频
- 修复 `dash_ws` 为 None 时的空指针问题

### 3. 转写保存日志（L4322）
```py
logger.info("转写保存: meeting=%s, speaker=%s, %d字, final=%s, text=%s",
            body.meeting_id, speaker_name, len(transcript),
            body.is_final, transcript[:80])
```

### 4. 去重逻辑放宽（L4254-L4273）
```py
# 旧：完全相同 + 子串包含都跳过（误杀短句跟进）
if curr_text in prev_text or prev_text in curr_text:
    is_duplicate = True

# 新：只跳过完全相同的文本
if prev_text == curr_text:
    is_duplicate = True
```

---

## 四、Whisper 固定 GPU 1

**文件**：`backend/whisper_transcribe.py`（L38-L59）

```py
# 旧
_model = whisper.load_model(model_path, device="cuda")

# 新：固定 GPU 1，可通过环境变量 WHISPER_GPU 覆盖
whisper_gpu = os.environ.get("WHISPER_GPU", "1")
device = f"cuda:{whisper_gpu}"
_model = whisper.load_model(model_path, device=device)
```

模型路径：`/home/ai/.cache/modelscope/hub/models/iic/Whisper-large-v3/large-v3.pt`（2.9GB）

---

## 当前 GPU 布局

| GPU | 用途 | 显存 |
|-----|------|------|
| 0 | Qwen3.8-27B-FP8 LLM | 42GB / 49GB |
| 1 | SenseVoice ASR (8.7GB) + Whisper (9.8GB) | 18.5GB / 49GB |

---

## 构建验证

- 前端 `npm run build` ✅
- 后端 Python 语法检查 ✅
