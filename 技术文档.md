# trihe_sglang_demo.py
# 三重一大合规审核 Demo（你的 SGLang 服务 + Qwen3.5 + LangChain Agent + Skill）
# 严格基于《中国核能电力股份有限公司 “三重一大”决策制度实施办法》（CG-AB-210 Rev.H）

from langchain_openai import ChatOpenAI
from langchain.agents import create_react_agent, AgentExecutor
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
from langchain_core.tools import tool
from sentence_transformers import SentenceTransformer, util
import json

# 你的 SGLang 服务配置（已确认端口和 model 名）
llm = ChatOpenAI(
    openai_api_base="http://192.168.66.44:8088/v1",  # ← 你的实际内网地址和端口
    openai_api_key="EMPTY",
    model="default",                                 # ← curl 中使用的 model 名
    temperature=0.1,                                 # 低温度确保准确、减少幻觉
    max_tokens=2048,
)

# 规则库（从你上传的制度文件提炼的核心内容，来自 6.2 事项范围 + 6.3 程序 + 5.0 责任 + 6.5 监督）
RULES_DB = {
    "重大决策": {
        "强制要求": "必须经党委前置研究讨论；集体决策；法律审查；会议纪要存档。",
        "禁止事项": "禁止个人或少数人决定；无会议纪要；未经法律审查。",
        "决策程序": ["提出书面建议书", "党支部审查列入", "承办部门拟方案", "征求意见", "院办公室报告", "院务会议集体讨论表决", "实施与监督"],
        "责任主体": "党委书记/董事长主持；主管领导论证；法律合规部审查；纪检监督部监督。"
    },
    "重大项目安排": {
        "强制要求": "必须可行性报告、风险评估、法律审查；党委前置。",
        "禁止事项": "禁止超预算、无审批。",
        "决策程序": ["项目审查", "专家论证", "征求意见", "会议决策", "公示", "正式审批"],
        "责任主体": "战略规划部论证；法律合规部审查。"
    },
    "大额度资金运作": {
        "强制要求": "必须资金使用计划；双人签字或集体审批。",
        "禁止事项": "禁止私下转账、无审计记录。",
        "决策程序": ["安排预算", "党组集体研究", "公开公示", "资金拨付"],
        "责任主体": "财务部门执行；审计部监督。"
    },
    "重要人事任免": {
        "强制要求": "坚持党管干部；事先征求纪检意见；集体决定；任前公示；试用期考核。",
        "禁止事项": "禁止个人决定。",
        "决策程序": ["民主推荐", "组织考察", "会议决定", "任前公示", "试用1年", "正式任免"],
        "责任主体": "人力资源部考察；纪检监督部意见。"
    }
}

# Skill 1: 规则提取
@tool
def extract_rules(matter_type: str) -> str:
    """从制度提取当前事项的强制要求、禁止事项、决策程序、责任主体"""
    data = RULES_DB.get(matter_type.strip(), {"error": "未匹配事项类型"})
    return json.dumps(data, ensure_ascii=False, indent=2)

# Skill 2: 材料文本校验
@tool
def validate_material(material_text: str, rules_text: str) -> str:
    """与规则交叉校验，返回每条规则状态 + 证据"""
    rules = json.loads(rules_text)
    embedder = SentenceTransformer('paraphrase-multilingual-mpnet-base-v2')
    report = []
    
    for key in ["强制要求", "禁止事项"]:
        if key in rules:
            rule_emb = embedder.encode(rules[key])
            sentences = material_text.split('。')
            sims = util.cos_sim(rule_emb, embedder.encode(sentences))[0]
            max_sim = sims.max().item()
            status = "合规" if max_sim > 0.72 else "⚠️ 不合规"
            evidence_idx = sims.argmax().item()
            evidence = sentences[evidence_idx] if max_sim > 0.5 else "无明显证据"
            report.append({"规则": rules[key], "状态": status, "相似度": round(max_sim, 2), "证据": evidence})
    
    return json.dumps(report, ensure_ascii=False, indent=2)

# Skill 3: 程序完整性检查
@tool
def check_procedure_completeness(material_text: str, rules_text: str) -> str:
    """检查决策程序是否完整覆盖"""
    rules = json.loads(rules_text)
    steps = rules.get("决策程序", [])
    report = []
    for step in steps:
        status = "已覆盖" if step in material_text else "缺失"
        report.append({"环节": step, "状态": status})
    return json.dumps(report, ensure_ascii=False, indent=2)

# Skill 4: 责任监督识别
@tool
def identify_responsibility(material_text: str, rules_text: str) -> str:
    """检查责任主体和监督是否落实"""
    rules = json.loads(rules_text)
    subject = rules.get("责任主体", "")
    if subject and subject in material_text:
        return "责任主体明确，监督机制提及。"
    else:
        return "责任主体或监督缺失，请补充。"

# Skill 5: 生成最终报告
@tool
def generate_compliance_report(results: str) -> str:
    """综合所有结果生成结构化报告"""
    prompt = f"""
    你是三重一大合规专家。
    基于以下工具结果：
    {results}
    
    输出结构化报告（用 Markdown 格式）：
    1. 整体合规结论（合规 / 部分不合规 / 不合规）
    2. 规则匹配情况
    3. 程序完整性结果
    4. 责任与监督落实情况
    5. 主要缺失或风险点
    6. 整改建议
    严格基于事实，不要添加外部内容。
    """
    return llm.invoke(prompt).content

tools = [
    extract_rules,
    validate_material,
    check_procedure_completeness,
    identify_responsibility,
    generate_compliance_report
]

# Agent Prompt（强制顺序执行）
prompt = ChatPromptTemplate.from_messages([
    ("system", """你是严格的三重一大合规审核 Agent。
必须按顺序执行：
1. extract_rules
2. validate_material
3. check_procedure_completeness
4. identify_responsibility
5. generate_compliance_report
不要添加任何外部司法/案件内容。"""),
    ("human", "{input}"),
    MessagesPlaceholder("agent_scratchpad"),
])

agent = create_react_agent(llm, tools, prompt)
agent_executor = AgentExecutor(
    agent=agent,
    tools=tools,
    verbose=True,                 # 显示思考链，便于你调试
    max_iterations=10,
    handle_parsing_errors=True
)

# 测试入口
if __name__ == "__main__":
    print("=== 三重一大合规审核 Demo（SGLang + Qwen3.5）===")
    print("事项类型示例：重大决策 / 重大项目安排 / 大额度资金运作 / 重要人事任免\n")
    
    matter = input("请输入事项类型：").strip()
    material = input("请输入材料文本（可粘贴文件内容）：").strip()
    
    if not matter or not material:
        print("输入不能为空！")
    else:
        query = f"审核事项：{matter}\n材料内容：{material[:3000]}"  # 防止超长
        print("\nAgent 正在审核...\n")
        result = agent_executor.invoke({"input": query})
        print("\n" + "═"*60)
        print("最终合规报告：")
        print(result["output"])