# AI 智能会议系统 — 功能完善度评估报告

> 评估日期：2026-07-10
> 评估方式：代码审查 + 数据库实际数据 + 端到端功能验证

---

## 一、系统现状数据

| 指标 | 数量 | 说明 |
|------|------|------|
| 总会议数 | 253 | 有真实使用痕迹 |
| 转写记录 | 1204 条 | 平均每场约 5 条，含测试数据 |
| 录音文件 | 93 段 | 有真实手机端录音 |
| 已归档 | 26 场 | 走完了全流程 |
| 会后终审 | 14 场 | 跑过 Whisper + AI 纪要生成 |
| 会中记录 | 28 场 | 正在进行中 |
| 议程已确认 | 44 场 | 完成议题确认阶段 |
| 问题收集中 | 136 场 | 停在议题收集阶段 |

---

## 二、逐项功能评估

### 2.1 议题确认（阶段 01）— 完成度 80%

| 能力 | 状态 | 说明 |
|------|------|------|
| 手动添加议题 | ✅ | 可用 |
| Excel 导入议题 | ✅ | 有模板下载、批量导入 |
| AI 提炼议题 | ✅ | DeepSeek 从导入数据中梳理议题 |
| 议题与会议绑定 | ✅ | agendaDrafts 字段关联 |

**不足**：议题没有分类优先级，没有"上次遗留议题"自动带入机制。

---

### 2.2 实时转写（阶段 02 核心）— 完成度 75%

| 能力 | 状态 | 说明 |
|------|------|------|
| DashScope 实时 ASR | ✅ | WebSocket 流式转写，延迟低 |
| 本地 Whisper/Paraformer | ✅ | 已下载模型，可作为兜底 |
| SSE 实时推送 | ✅ | 转写结果秒级推送到前端 |
| 手机端录音 | ✅ | 独立页面 `/mobile-recorder`，扫码加入 |
| 发言人识别 | ⚠️ | 能区分发言人，声纹精度未验证 |
| 热词优化 | ✅ | 支持自定义热词提升识别率 |
| 转写人工校正 | ✅ | 前端支持修改转写内容 |

**不足**：
- 实时 ASR 依赖 DashScope 云端，离线时只能用本地兜底
- 多人同时发言时的分离能力未知
- 噪声环境下的识别率未测试

---

### 2.3 AI 纪要生成（阶段 02→03 过渡）— 完成度 70%

| 能力 | 状态 | 说明 |
|------|------|------|
| DeepSeek 结构化纪要 | ✅ | 输出 summary/chronicle/minutes/decisions/todos |
| 智能采样 | ✅ | >100 条转写时自动采样，保证时间全覆盖 |
| Whisper 终审转写 | ✅ | 会后高精度转写，补充实时 ASR 不足 |
| 本地规则兜底 | ✅ | DeepSeek 不可用时用正则提取 |
| 反幻觉约束 | ✅ | prompt 中明确禁止编造未出现的人名、数字 |
| 纪要 Word 导出 | ✅ | 生成 .docx 文件可下载 |

**不足**：
- 本地兜底（local-rule）能力弱，只能做关键词匹配
- 纪要没有版本管理，修改后无法回溯
- 没有"纪要确认"流程——谁来定稿、谁签字

---

### 2.4 手机签名 — 完成度 40%

| 能力 | 状态 | 说明 |
|------|------|------|
| 手机端手写签名 UI | ✅ | 前端有 Canvas 签名组件 |
| 签名数据提交 | ✅ | POST /api/meeting/transcripts/{id}/correct |
| 签名数据落库 | ❌ | `meeting_transcripts` 表无 `signature_data` 列 |
| 签名导出到 Word | ⚠️ | 代码有引用 `correctionSigned`，但数据来源存疑 |
| 签名验证/防伪 | ❌ | 无 |

**关键问题**：

`meeting_transcripts` 表结构（实际数据库）：

```
id, meeting_id, meeting_title, agenda, speaker_name, speaker_role,
speaker_dept, seat, username, transcript, is_final, client_time,
server_time, confidence, source, payload_json
```

没有 `correction_signed`、`signature_data`、`correction_signed_at` 等字段。签名数据可能存储在 `payload_json` 或 `meeting_events` 中，但导出和展示时的读取逻辑需要验证。

---

### 2.5 待办跟踪 — 完成度 30%

| 能力 | 状态 | 说明 |
|------|------|------|
| AI 提取待办 | ✅ | DeepSeek 从转写中提取 todos（责任人、事项、截止时间） |
| 待办列表页面 | ❌ | 没有独立的待办看板页面 |
| 待办完成状态 | ❌ | 没有 done/undone 状态管理 |
| 到期提醒 | ❌ | 没有提醒机制 |
| 遗留待办带入下次会议 | ❌ | 没有跨会议待办关联 |
| 待办导出 | ❌ | 待办只在纪要中，无独立导出 |

**现状**：AI 提取了待办，但提取后无人管理，形成"提取了但没人用"的状态。

---

### 2.6 公文归档（阶段 04）— 完成度 50%

| 能力 | 状态 | 说明 |
|------|------|------|
| 会议状态改为"已归档" | ✅ | archive_meeting 接口修改 phase |
| Word 文档导出 | ✅ | 生成纪要 Word 文档 |
| Whisper 纪实导出 | ✅ | 高精度转写的纪实文档 |
| 红头文件格式 | ❌ | 不是标准党政机关公文格式 |
| 公文编号 | ❌ | 无发文字号、签发人 |
| 骑缝章/防伪码 | ❌ | 无 |
| 打印排版优化 | ❌ | 未针对 A4 打印优化 |
| 归档检索 | ⚠️ | 有会议列表，但无全文检索 |

---

### 2.7 离线能力 — 完成度 10%

| 组件 | 当前方案 | 离线状态 |
|------|---------|---------|
| ASR 实时转写 | DashScope 云端 WebSocket | ❌ 需外网 |
| ASR 兜底 | 本地 Whisper-large-v3 / Paraformer | ✅ 已下载 |
| AI 纪要/合规 | DeepSeek 云端 API | ❌ 需外网 |
| AI 兜底 | local-rule 正则提取 | ✅ 但能力弱 |
| 向量检索 | ChromaDB + text2vec-base-chinese | ✅ 本地 |
| 前端/后端 | FastAPI + React | ✅ 本地 |

**结论**：核心 AI 能力全部依赖云端，离线版本只能靠正则兜底，功能大打折扣。

---

## 三、三个硬伤

### 硬伤一：签名没落库

前端让用户手写了签名，但 `meeting_transcripts` 表里没有 `signature_data` 字段。签名数据的持久化、导出、验证链路不完整。

**影响**：政府/国企要求"全程留痕、签字确认"，签名不能落库就等于没有签名。

### 硬伤二：待办是半成品

AI 提取了待办，但：
- 没有待办列表页面
- 没有完成/未完成状态
- 没有到期提醒
- 没有跨会议遗留待办关联

**影响**：会议的价值在于"形成决策、推动执行"，待办不管就等于白开。

### 硬伤三：归档不是真归档

当前归档 = 改个 phase 字段 + 导出 Word。政府要的归档是：
- 红头文件格式（发文机关、发文字号、签发人）
- 骑缝章、防伪码
- 可打印、可上报、可存档

**影响**：导出的 Word 不能直接作为正式公文使用，还需要人工重新排版。

---

## 四、真正做好的部分

| 模块 | 评价 |
|------|------|
| **实时转写 + SSE 推送** | 技术方案扎实，WebSocket + SSE 双通道，延迟低 |
| **AI 纪要生成** | DeepSeek prompt 设计合理，有采样策略、反幻觉约束 |
| **Whisper 双引擎** | 实时 ASR + 会后 Whisper 高精度终审，思路正确 |
| **前端交互** | 5144 行代码，4 阶段流程 UI 完整，交互流畅 |
| **手机端录音** | 独立页面、扫码加入、设计合理 |
| **本地兜底机制** | DeepSeek 不可用时有 local-rule 兜底，不会完全瘫痪 |

---

## 五、综合评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整度 | 65/100 | 核心流程能跑，但签名/待办/归档是半成品 |
| 代码质量 | 75/100 | 后端 8000+ 行、前端 5000+ 行，有错误处理和兜底机制 |
| 实际可用性 | 60/100 | 能开会录音出纪要，但离"交付"差几个关键环节 |
| 离线能力 | 10/100 | 核心 AI 全走云端，离线几乎不可用 |
| 产品定位 | 50/100 | 叫"三重一大合规审核"但实际是"AI 会议管理"，定位错位 |

**综合：60/100 — 底子不错，但还不是一个能正式交付的产品。**

---

## 六、交付前必做清单

### P0（不做不能交付）

- [ ] **签名落库**：`meeting_transcripts` 表增加 `signature_data`、`correction_signed`、`correction_signed_at` 字段，签名数据持久化
- [ ] **离线 LLM**：本地部署 Qwen2.5-14B 或 DeepSeek-V2-Lite，替换 DeepSeek 云端 API
- [ ] **ASR 切本地**：实时转写从 DashScope 切到本地 Paraformer，去除外网依赖

### P1（不做不好用）

- [ ] **待办看板**：独立待办页面，支持完成状态、到期提醒、跨会议关联
- [ ] **纪要确认流程**：纪要生成后需要主持人/秘书确认定稿
- [ ] **归档格式优化**：按党政机关公文格式排版（发文机关、字号、签发人）

### P2（锦上添花）

- [ ] 纪要版本管理（修改历史、版本回溯）
- [ ] 全文检索（搜历史会议讨论内容）
- [ ] 遗留待办自动带入下次会议
- [ ] 会议统计报表

---

## 七、产品定位建议

| 当前 | 建议 |
|------|------|
| AI 三重一大合规审核系统 | **AI 智能会议管理系统（离线版）** |

**核心卖点**：
1. 数据不出内网，纯本地部署
2. 开会自动录音转写，会后即得纪要
3. 全程留痕，可追溯可归档

合规审核作为辅助功能存在，不作为主打卖点。

---

*评估人：AI Assistant*
*评估基于代码审查和数据库实际数据，未进行端到端功能实测*
