# 2026-07-01 AI 会议系统修改记录

## 一、线上模型环境梳理与问题定位（下午）

### 1.1 模型环境确认
| 组件 | 模型/路径 | GPU | 内存 | 状态 |
|------|----------|-----|------|------|
| ASR 流式 | Paraformer-Large (speech_seaco) | cuda:1 | ~6GB | ✅ |
| ASR 兜底 | Whisper-large-v3 | cuda:1 | ~3GB | ✅ |
| 说话人分离 | CAM++ | cuda:1 | 共用 | ✅ |
| 标点恢复 | CT-Punc | cuda:1 | 共用 | ✅ |
| 后端 | backend_full.py v5 | cuda:0 | ~1GB | ✅ |
| **总计 GPU1** | Paraformer + Whisper + CAM++ + CT-Punc | cuda:1 | **~9.2GB** | ✅ |

### 1.2 真凶定位：FSMN-VAD 全量误判
- **现象**：本地 Paraformer 所有 chunk 输出 `empty speech`，线上 DashScope 正常
- **根因**：FSMN-VAD 模型将 iPhone 低电平音频 **100% 判为静音**，与阈值无关（0.2/0.3/0.5/0.8 均无效）
- **验证**：Paraformer 裸模型（无 VAD）直接推理同一音频 → 完整识别
- **修复**：移除 `vad_model="fsmn-vad"`，改为前端 Energy Gate + ZCR 双因子联合拦截

### 1.3 iPhone 音频电平问题
- iPhone 麦克风原始电平极低（peak=756/32767=2.3%，99%样本接近零）
- 噪声门 2% 将所有语音误判为静音丢弃
- **修复**：噪声门 2%→0.5%，AGC 增益上限 2x→5x

### 1.4 asyncio.to_thread() CUDA 兼容性
- `asr_model.generate()` 移到线程池后推理全部返回空结果
- 疑似 PyTorch CUDA 上下文在线程池子线程不可用
- **回滚**：恢复同步 `generate()` 调用
- 全局锁 → per-session lock **保留**（不同 session 仍可并发）

### 1.5 AudioWorklet 输出电平异常
- AudioWorklet 在 iOS Safari 输出电平比 ScriptProcessor 低 ~200 倍
- **回滚**：直接使用 ScriptProcessor，AudioWorklet 代码保留备用

---

## 二、Paraformer 精度调优

### 2.1 模型参数优化
| 参数 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| beam_size | 1 | **10** | 搜索宽度最大化，精度显著提升 |
| chunk_size | [5,10,5] | **[10,10,5]** | 左上下文 300→600ms，减少开头音节误识 |
| punc_model | 移除 | **ct-punc** | 标点辅助解码器断句，提升连贯性 |
| VAD | 移除 | 移除 | FSMN-VAD 不可用，前端 Energy Gate 替代 |

### 2.2 音频预处理
| 参数 | 修改前 | 修改后 |
|------|--------|--------|
| 噪声门 RMS | <2% 丢弃 | **<0.5%** 丢弃 |
| AGC 目标RMS | 0.2，上限 2x | 0.2，上限 **5x** |
| Energy Gate | 无 | RMS + ZCR **联合判定**（双因子） |
| 动态阈值 | 无 | RMS [120,250] 自适应 |

---

## 三、状态机重构：同生共死定理

### 3.1 问题
- 标点切句时清空 `committed_text` 但 GPU cache 未重置
- LCP 拿 `""` 比对 `完整历史文本` → 前缀长度=0 → 全部当新字
- 导致历史文本海啸般倒灌进新气泡

### 3.2 修复：两套动作

**动作 A — 800ms 静音（同生共死硬切断）**：
```
finish(session_id) + start()     ← GPU 重置
committed_text = ""               ← Python 同步归零
last_full_text = ""
ui_bubble_start_idx = 0
```

**动作 B — 遇标点  。？！…（UI 换行，不碰 GPU）**：
```
发 type:"final"（从 ui_bubble_start_idx 到当前）
ui_bubble_start_idx = len(clean_text)  ← 只移游标
committed_text = clean_text             ← 更新锚点，不归零
→ GPU 继续跑，历史绝不倒灌
```

### 3.3 延迟启动 ASR
- 连接后不立即 `start()`，等 Energy Gate + VAD-lite 首次检测到语音时才创建 session
- 避免环境噪声被无 VAD 的 Paraformer 幻觉成"对对对/没有没有"

---

## 四、Whisper-large-v3 兜底转写

### 4.1 双模型架构
```
实时：iPhone → Paraformer 流式 → 即时上屏（快，可接受误差）
事后：录音停止 → 合并 webm → Whisper-large-v3 → 高精度转写 → 存入 events
```

### 4.2 新增端点
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/transcribe` | POST | Whisper 全量转写（body: PCM int16） |
| `/api/meetings/{id}/whisper-review` | GET | 查询 Whisper 终审结果 |

### 4.3 触发时机
- 录音停止（`session action=stop`）→ 合并该会议所有 webm → ffmpeg → PCM → Whisper → 存入 `meeting_events`（`action=whisper-review`）

### 4.4 DeepSeek docx 注入
- `_deepseek_generate_meeting_records()` 自动将 Whisper 文本注入 `all_clean` 数组
- DeepSeek 同时获取 Paraformer 实时转写 + Whisper 高精度转写
- Whisper 测试结果：556s 音频 → **4s 推理**（140x 实时），精度显著优于 Paraformer

---

## 五、其他修复

### 5.1 backend 标识更正
- WS 消息 `"backend": "sensevoice"` → `"backend": "paraformer"`
- 文件：`backend_full.py`（5 处）

### 5.2 滑动缓冲区（600ms 对齐）
- 前端发 250ms 碎片包，后端攒到 600ms 再发 ASR
- 对齐 Paraformer chunk 帧长，减少碎片化

### 5.3 send_chunk 重试 + 单例
- `qwen_asr_client.py`：5s 超时 + 1 次重试（仅瞬时错误）
- `_get_qwen_client()`：应用级单例，避免每个 WS 连接新建 httpx client

---

## 六、当前服务状态

### 6.1 运行中服务
| 服务 | 端口 | GPU | 模型 | 自启 | systemd |
|------|------|-----|------|------|---------|
| paraformer-asr | 8091 | GPU 1 | Paraformer + Whisper | ✅ | enabled |
| ai-compliance | 8002 | GPU 0 | ChromaDB + SentenceTransformer | ✅ | enabled |
| nginx | 80/443 | - | 静态 + 反代 | ✅ | enabled |

### 6.2 GPU 内存分配
```
GPU 0 (853MB):  ChromaDB + SentenceTransformer
GPU 1 (9.2GB): Paraformer-Large (6GB) + Whisper-large-v3 (3GB) + CAM++ + CT-Punc
```

### 6.3 开机自启
- `systemctl enable paraformer-asr` ✅
- `systemctl enable ai-compliance` ✅
- 两个 service 均配置 `Restart=on-failure`，崩溃自动恢复

---

## 七、关键命令

```bash
# 重启 ASR（Paraformer + Whisper 双模型）
sudo systemctl restart paraformer-asr
# 或手动（绕过 systemd 超时）
sudo kill $(pgrep -f funasr_asr_server)
nohup /home/ai/miniconda3/envs/funasr-sensevoice/bin/python /home/ai/桌面/funasr_asr_server.py --device cuda:1 > /tmp/asr.log 2>&1 &

# 重启后端
sudo systemctl restart ai-compliance
# 或
sudo /home/ai/文档/ai616/restart_safe.sh

# 构建前端
cd /home/ai/文档/ai616/frontend && npm run build

# 健康检查
curl http://127.0.0.1:8091/api/health   # ASR（含双模型）
curl http://127.0.0.1:8002/health       # 后端

# 查看日志
tail -f /tmp/asr.log                          # ASR 推理日志
tail -f /home/ai/文档/ai616/logs/backend.log  # 后端日志
journalctl -u paraformer-asr -f               # ASR systemd 日志
```

---

## 八、GLM-ASR-Nano-2512（已下载，保留备用）

- 模型路径：`/home/ai/.cache/modelscope/ZhipuAI/glm-asr-nano-2512`
- 大小：4.2GB
- 状态：已下载，非流式模型，API 兼容性问题未解决
- `funasr_asr_server.py` 中保留了 Paraformer-Large + GLM-ASR 配置注释，随时可切换
