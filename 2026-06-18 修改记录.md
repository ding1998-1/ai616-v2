# 2026-06-18 修改记录

## 一、知识文库修复（3 轮）

### 1. 用户身份硬编码修复
- **问题**：知识文库始终显示"张三"，与登录用户无关
- **根因**：`get_demo_assets()` 返回硬编码 `DEMO_USER = {name: "张三", ...}`，前端未调 `/api/auth/me`
- **修复**：
  - `KnowledgeLibrary.jsx` useEffect 先调 `/api/auth/me` 获取真实用户
  - 真实用户优先，demo 用户仅作 fallback
  - 导入 `authFetchJson` 用于认证 API 调用

### 2. 文件上传后切页面消失
- **问题**：上传成功，离开知识文库再回来，文件不见了
- **根因 1（后端）**：`PUT /api/knowledge_files/{id}` 使用 `KnowledgeFile` 模型（全字段必填），前端向量化后只发送 `{vectorized, linked, tags}` → 422 Validation Error → `.catch(() => {})` 静默吞掉
- **根因 2（前端）**：上传时没记录 `libraryCategory`，页面重载后 `categorizeFiles` 把无分类文件全归入"知识文库"tab，但 Tabs 默认显示"案例文库"tab（`defaultActiveKey="cases"`）
- **修复（后端）**：
  - 新增 `KnowledgeFileUpdate` 模型，所有字段 Optional
  - `PUT` 端点改用 `KnowledgeFileUpdate`，支持部分更新
  - `KnowledgeFile` 模型新增 `libraryCategory` 字段
- **修复（前端）**：
  - `DocTable` 新增 `tabKey` prop，上传时写入 `libraryCategory`
  - `Tabs` 从非受控改为受控模式（`activeKey` + `onChange`）
  - 新增 `activeTab` state，默认 `'knowledge'`
  - 所有 `fetch` 统一改为 `authFetch`
  - `.catch(() => {})` 改为 `.catch(err => console.warn(...))` 至少记录错误

### 3. 孤儿文件自动发现
- **问题**：之前通过其他路径上传到 docs 目录的文件，没有 knowledge_files 记录
- **修复**：新增 `_discover_orphaned_docs()` 函数
  - 扫描 `data/docs/` 目录下所有 .docx/.doc 文件
  - 自动跳过已有记录的文件、会议文件（meeting- 前缀）、审查版/留痕审查版导出文件
  - 为孤儿文件自动创建 knowledge_files 记录
  - 在 `GET /api/knowledge_files` 中自动调用
  - 本次运行发现并恢复了 26 个孤儿文件

---

## 二、合同审查修复

### 4. 合同上传后切页面无记录
- **问题**：合同审查页面上传合同后，切换到其他页面再回来，历史合同列表里没有
- **根因**：`handleUpload` 只调了 `/api/doc/upload` 保存文件，从未调 `POST /api/knowledge_files` 创建记录。页面重载时历史列表从 `/api/knowledge_files` 加载 → 找不到
- **修复**：
  - `CaseAnalysis.jsx` `handleUpload` 中，上传成功后追加 `POST /api/knowledge_files` 创建记录
  - 记录包含 `libraryCategory: 'cases'`、`savedName`、`tags: ['合同审查']`
  - `currentUser` 新增为组件 prop，用于记录上传人

---

## 三、AI 合同起草（新功能）

### 5. 后端新增端点

**`POST /api/contract/draft`** — AI 生成合同草案
- 输入：`{ contract_type, requirements }`
- LLM 按 12 节标准结构生成完整中文合同（Markdown 格式）
- 支持 10 种合同类型：采购/服务/劳动/租赁/技术开发/保密/战略合作/股权转让/委托/其他
- 返回：`{ success, markdown, title }`

**`POST /api/contract/draft/export`** — Markdown 转精美 Word
- 输入：`{ markdown, title }`
- 红头格式：方正小标宋标题（红色 22pt）+ 黑体章节 + 仿宋正文
- 自动识别 Markdown 标题层级、列表、表格
- 标准公文页边距（上3.7 下3.5 左2.8 右2.6）
- 页脚标注"AI 生成，请经法律审核后签署"
- 返回：`{ success, filename, saved_as, download_url }`

### 6. 前端合同起草 UI
- 合同审查首页"合同起草"按钮改为功能入口
- 弹窗包含：合同类型选择 → 需求描述 → AI 生成 → Markdown 预览 → 导出 Word
- 导出后自动写入 knowledge_files（标签 `AI起草`），可在历史列表中找到

---

## 四、部署研究

### 7. 私有化部署差距分析
- 新增 `docs/私有化部署差距分析.md`（完整评估报告）
- 评估维度：安全、Docker、配置、文档、数据库、监控
- 发现 P0 问题 3 个（密钥泄露、Docker 无法启动、多部署共享密钥）
- 发现 P1 问题 7 个（缺文档、模型下载、OnlyOffice 硬依赖等）
- 发现 P2 问题 7 个（HTTPS、限流、日志、镜像优化等）
- 工作量估计：P0 1-2 天 / P1 2-3 天 / P2 3-5 天，合计 8-10 工作日

---

## 五、文档

### 8. AI 会议使用说明
- 新增 `docs/AI会议使用说明.md`（完整用户手册）
- 覆盖：会议四阶段（议题确认→会中采集→会后终审→公文归档）
- 包含：桌面端操作、手机端操作、OA 审批联动、常见操作速查、角色权限、AI 能力说明

---

## 六、文件变更清单

| 文件 | 变更 |
|------|------|
| `backend_full.py` | +KnowledgeFileUpdate 模型, +libraryCategory 字段, +_discover_orphaned_docs, +ContractDraftRequest, +ContractDraftExportRequest, +POST /api/contract/draft, +POST /api/contract/draft/export, PUT 端点改用 KnowledgeFileUpdate |
| `frontend/src/pages/KnowledgeLibrary.jsx` | +auth/me 真实用户, +tabKey prop, +libraryCategory 写入, +activeTab 受控, +authFetch 统一, +错误日志 |
| `frontend/src/pages/CaseAnalysis.jsx` | +currentUser prop, +上传后写入 knowledge_files, +合同起草 Modal (合同类型选择/需求描述/AI生成/Markdown预览/导出Word) |
| `docs/私有化部署差距分析.md` | 新建：完整部署评估报告 |
| `docs/AI会议使用说明.md` | 新建：完整用户手册 |
| `2026-06-18 修改记录.md` | 本文件 |

---

## 七、服务重启记录

| 时间 | 操作 | 说明 |
|------|------|------|
| 09:14 | 重启 | 首次后端修复（KnowledgeFileUpdate） |
| 09:28 | 重启 | libraryCategory 模型变更 |
| 09:36-09:38 | 重启 ×2 | 孤儿发现函数 + 合同起草端点 |

---

## 八、当前系统状态

```
后端: 7,766 行 Python  │  103 API 端点  │  运行正常
前端: 15 页面          │  已构建到 dist │  nginx 正常
数据: SQLite 24MB + ChromaDB 43MB + 文件 502MB
部署: systemd + nginx  │  端口 8002 / 80
```
