# 2026-07-13 修改记录

## 一、声纹识别 — 会后校准功能补全

### 1.1 新增 `diarize()` 方法
- **文件**: `backend/voiceprint.py`
- 使用 `pyannote/speaker-diarization-3.1` 做 speaker diarization
- 懒加载 pipeline，GPU 优先 → CPU fallback
- 音频加载用 `soundfile`（避免 torchaudio 的 torchcodec 依赖问题）
- pyannote 4.0.7 API 适配：`result.speaker_diarization` 获取 annotation

### 1.2 新增 `match_diarization_to_enrolled()` 方法
- **文件**: `backend/voiceprint.py`
- 对每个 speaker 收集所有合格片段的 embedding，加权平均后匹配
- 阈值 0.40（适配注册样本少、录音条件差异大的场景）

### 1.3 完全切换到 pyannote
- **文件**: `backend/voiceprint.py`, `backend/routes/voiceprint.py`
- 去掉 resemblyzer，改用 `pyannote/wespeaker-voxceleb-resnet34-LM` 做 speaker embedding
- embedding 维度不变（256-dim），数据库 schema 无需改动
- 匹配区分度大幅提升：resemblyzer 0.44 vs pyannote 0.97（自己）/ 0.23（他人）

### 1.4 HuggingFace 模型缓存
- 需接受协议的模型：`pyannote/speaker-diarization-3.1`、`pyannote/speaker-diarization-community-1`、`pyannote/segmentation-3.0`
- 缓存目录：`~/.cache/huggingface/hub/`（共 1.5GB）
- 离线运行验证通过（`HF_HUB_OFFLINE=1`）

### 1.5 依赖更新
- **文件**: `requirements.txt`
- 新增：`torchaudio>=2.0.0`、`resemblyzer>=0.1.4`、`pyannote-audio>=4.0.0`

---

## 二、音频录制 — 合并逻辑修复（严重 Bug）

### 2.1 问题
- **根因**: `backend_full.py:3826-3828` 二进制拼接 chunk，导致 MP4/WebM 容器损坏
- 每个 3 秒 chunk 是独立容器，直接拼接后多个 EBML/ftyp 头出现在文件中间
- 175MB 音频文件损坏（只有 `moof` fragment，缺少 `ftyp` + `moov`）

### 2.2 修复：ffmpeg remux
- **文件**: `backend_full.py` (complete_meeting_recorder_audio 函数)
- `ffmpeg -f concat -c copy` 替代二进制拼接，重新封装为完整容器
- ffmpeg 失败时降级为二进制拼接（兼容无 ffmpeg 环境）

### 2.3 修复：原子写入
- chunk 上传：`write + fsync + rename`（防崩溃留半截文件）
- 合并文件：先写 `.tmp` 再 `rename`

### 2.4 修复：chunk 数量校验
- `total_chunks` 与实际数量不匹配时记录 warning 日志

### 2.5 修复：孤儿 chunk 清理
- 启动时 `_cleanup_orphaned_audio_chunks()` 清理超过 1 小时未合并的 chunk
- 当前有 1909 个孤儿 chunk 文件分布在 5 个目录

### 2.6 修复：事件记录异常保护
- `_append_meeting_activity_light` 包 try/except，不影响音频合并返回

### 2.7 测试验证
- 3 个 3 秒 chunk 合并：旧方式 3.008s（错误）→ 新方式 9.037s（正确）
- API 测试：`{"success": true}`, chunks 已清理

---

## 三、前端修复

### 3.1 修复 flushRetryQueue 竞态条件
- **文件**: `frontend/src/pages/MobileMeetingRecorder.jsx`
- `flushRetryQueue()` → `await flushRetryQueue()`
- 确保所有 chunk 上传完成后再调用 `completeAudioUpload`

### 3.2 音频合并进度提示
- **文件**: `frontend/src/pages/MobileMeetingRecorder.jsx`
- 新增 `isMerging` / `pendingChunks` 状态
- 合并中显示 "正在上传剩余 N 个音频片段…请勿关闭页面"
- 按钮变为 "正在上传…" 并禁用，防止重复点击

### 3.3 beforeunload 拦截
- 合并中注册 `beforeunload` 事件
- 浏览器弹出警告："音频正在上传中，离开将导致录音丢失！"
- 合成完成后自动移除

### 3.4 Alert 状态提示
- 合并中 Alert 类型变为 `warning`
- 描述显示 "音频正在上传和合并中，关闭或刷新页面将导致本次录音丢失！"

---

## 四、移动端声纹注册体验优化

### 4.1 实时音量电平
- **文件**: `frontend/src/pages/MobileMeetingRecorder.jsx` (MobileVoiceprintEnroll)
- 使用 `AudioContext + AnalyserNode` 实时显示麦克风音量
- 音量太小时提示"太小"，正常时显示 ✓

### 4.2 最短录制时长
- 至少 10 秒，不足时显示倒计时 "10s / 10s"
- 3 秒内禁用停止按钮

### 4.3 录制后回放
- 录制完成后显示 "录制完成（N秒），请回放确认声音清晰后上传"
- 三个按钮：回放 / 重新录制 / 确认上传

### 4.4 环境提示
- 未录制时显示提示：安静环境、手机距嘴 20-30cm

### 4.5 简化朗读脚本
- 旧：60 字长句
- 新："各位领导好，我是{姓名}，今天参加会议。"

---

## 五、待办清单更新

- **文件**: `待办清单.md`
- 声纹识别标记全部完成（含会后校准）
- 更新日期：2026-07-13

---

## 六、systemd 服务配置修复

### 6.1 去掉 HF_ENDPOINT
- **文件**: `/etc/systemd/system/ai-compliance.service`
- 移除 `Environment="HF_ENDPOINT=https://hf-mirror.com"`
- pyannote 模型已缓存到本地，加载不需要网络

### 6.2 重启策略改为 always
- **文件**: `/etc/systemd/system/ai-compliance.service`
- `Restart=on-failure` → `Restart=always`
- 原因：uvicorn 收到 SIGTERM 正常退出（exit code 0），`on-failure` 不会触发重启
- 改为 `always` 后无论正常退出还是崩溃都会自动重启

---

## 影响范围

| 模块 | 文件 | 改动类型 |
|---|---|---|
| 声纹引擎 | `backend/voiceprint.py` | 重写（pyannote 替代 resemblyzer） |
| 声纹路由 | `backend/routes/voiceprint.py` | 小改（engine 名称） |
| 后端主入口 | `backend_full.py` | 中改（音频合并逻辑 + 清理函数） |
| 前端录音 | `frontend/src/pages/MobileMeetingRecorder.jsx` | 中改（合并进度 + 声纹注册 UX） |
| 依赖 | `requirements.txt` | 新增 3 个包 |
| 待办 | `待办清单.md` | 更新 |
| systemd | `ai-compliance.service` | 去掉 HF_ENDPOINT，Restart=always |
