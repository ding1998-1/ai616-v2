# 2026-07-02 六项修复代码详解

## 修复 1：录音文件损坏根因 — 去掉 timeslice

### 问题

`MediaRecorder.start(3000)` 每 3 秒分段产生 fragmented MP4/WebM，部分片段丢失 moov atom，导致文件损坏（`file` 命令显示 `data` 而非媒体文件）。

### 改动

**`frontend/src/pages/MobileMeetingRecorder.jsx`**（行 875）

```js
// ❌ 之前：每 3 秒分段
recorder.start(3000);

// ✅ 之后：录音结束后一次性获取完整文件
recorder.start(); // 不使用 timeslice，避免 fragmented MP4 分段损坏
```

**`frontend/src/pages/MeetingComplianceWorkflow.jsx`**（行 488）

```js
// ❌ 之前
recorder.start(3000);

// ✅ 之后
recorder.start(); // 不使用 timeslice，避免 fragmented MP4 分段损坏
```

### 原理

不带 timeslice 时，`ondataavailable` 仅在 `recorder.stop()` 时触发一次，产出完整文件而非碎片拼接，从根本上避免 moov atom 丢失。

---

## 修复 2：强制 DashScope 云端 ASR 默认

### 问题

用户 localStorage 存了 `qwen`（本地 Paraformer），录音时切换按钮被禁用无法切回云端，导致转写质量极差（碎片化输出、人名全错）。

### 改动

**`frontend/src/pages/MobileMeetingRecorder.jsx`**（行 108-111）

```js
// ❌ 之前：从 localStorage 读取，可能读到旧值 'qwen'
const [asrBackend, setAsrBackend] = useState(() => {
  try { return localStorage.getItem('ai616_asr_backend') || 'dashscope'; } catch (_) { return 'dashscope'; }
});

// ✅ 之后：初始化时强制写入 'dashscope'，忽略旧值
const [asrBackend, setAsrBackend] = useState(() => {
  try { localStorage.setItem('ai616_asr_backend', 'dashscope'); } catch (_) {}
  return 'dashscope';
});
```

**同文件**（行 1229-1239）— 禁用"本地识别"按钮：

```js
// ❌ 之前：用户可点击切换到本地 ASR
<Button disabled={recording} ...>本地识别</Button>

// ✅ 之后：永久禁用，附带 tooltip
<Button
  disabled={true}
  title="本地 Paraformer 质量差，已禁用"
  ...
>
  本地识别
</Button>
```

### 效果

- localStorage 每次初始化时被覆盖为 `dashscope`
- "本地识别"按钮灰色不可点击
- 同时解决修复 6（DashScope ASR 未强制默认）

---

## 修复 3：DeepSeek API 间歇性 503 — 指数退避重试

### 问题

`DeepSeekThinkingLLM._generate` 单次 HTTP 调用，503 直接抛异常，纪要生成降级到本地规则（质量差）。

### 改动

**`backend/llm_client.py`**（行 264-271）

```python
# ❌ 之前：单次调用，失败直接抛异常
client = _get_httpx_sync()
resp = client.post(self.api_base, json=payload, headers=headers)
if resp.status_code >= 400:
    logger.error(...)
resp.raise_for_status()

# ✅ 之后：指数退避重试，最多 3 次
client = _get_httpx_sync()
_RETRYABLE_STATUS = {429, 502, 503, 504}
resp = None
for attempt in range(3):
    resp = client.post(self.api_base, json=payload, headers=headers)
    if resp.status_code in _RETRYABLE_STATUS and attempt < 2:
        delay = min(2 ** attempt, 8)  # 1s, 2s
        logger.warning(f"DeepSeek 返回 {resp.status_code}，{delay}s 后重试 ({attempt+1}/3)")
        time.sleep(delay)
        continue
    break
if resp.status_code >= 400:
    logger.error(...)
resp.raise_for_status()
```

**`backend_full.py`**（行 3435）— `_llm_refine_text` 异常日志：

```python
# ❌ 之前：静默吞掉所有异常
except Exception:
    return raw_text

# ✅ 之后：记录异常信息
except Exception as e:
    logger.warning("_llm_refine_text 失败: %s", e)
    return raw_text
```

### 重试策略

| 重试次数 | 延迟 | 说明 |
|----------|------|------|
| 第 1 次 | 0s | 首次请求 |
| 第 2 次 | 1s | 2^0 = 1 |
| 第 3 次 | 2s | 2^1 = 2 |

仅对瞬态错误（429 限流、502/503/504 服务端临时故障）重试，4xx 客户端错误不重试。

---

## 修复 4：Whisper 终审归档自动触发

### 问题

`_run_whisper_review` 是 fire-and-forget 的 `asyncio.create_task`，后端重启或用户快速归档会导致 Whisper 从未运行，归档文档缺少高精度转写。

### 改动

**`backend_full.py`**（行 3216）— 提取独立函数：

```python
# ✅ 新增：独立的 Whisper 终审函数，可从多处调用
async def _run_whisper_review_for_meeting(meeting_id: str):
    """Whisper 终审转写 —— 可从 session stop 和 archive 两处调用。"""
    try:
        import subprocess as _sp
        from backend.whisper_transcribe import transcribe_file
        safe_id = _safe_meeting_id(meeting_id)
        audio_dir = MEETING_FILES_DIR / "recordings" / safe_id
        if not audio_dir.exists():
            return
        files = sorted(audio_dir.glob("*.webm")) + sorted(audio_dir.glob("*.mp4")) + sorted(audio_dir.glob("*.m4a"))
        if not files:
            return
        # ... 过滤有效文件 → ffmpeg 合并 → Whisper 转写 → 保存事件 ...
    except Exception as e:
        logger.warning("Whisper终审异常 meeting=%s: %s", meeting_id, e)
```

**同文件**（行 3337）— session stop 调用简化：

```python
# ❌ 之前：内联 100 行闭包
if body.action == "stop":
    async def _run_whisper_review():
        # ... 100 行代码 ...
    asyncio.create_task(_run_whisper_review())

# ✅ 之后：一行调用
if body.action == "stop":
    asyncio.create_task(_run_whisper_review_for_meeting(body.meeting_id))
```

**同文件**（行 2540-2551）— 录音停止后自动触发：

```python
if stage in ("archive",):
    meeting["reviewDone"] = True
    meeting["archiveDone"] = True
    # ✅ 新增：检查 Whisper 是否已运行，未运行则触发
    has_whisper = any(
        e.get("type") == "transcript" and e.get("action") == "whisper-review"
        for e in meeting.get("events", [])
    )
    if not has_whisper:
        audio_dir = MEETING_FILES_DIR / "recordings" / safe_id
        if audio_dir.exists() and any(audio_dir.iterdir()):
            asyncio.create_task(_run_whisper_review_for_meeting(safe_id))
```

### 调用链

```
录音停止 → meeting_recorder_session(action="stop")
         → asyncio.create_task(_run_whisper_review_for_meeting(...))

归档     → update_meeting_stage(stage="archive")
         → 检查 events 是否有 whisper-review
         → 无 → asyncio.create_task(_run_whisper_review_for_meeting(...))
         → 有 → 跳过（已完成）
```

---

## 修复 5：转写时间戳按 clientTime 排序

### 问题

多台手机同时提交转写，服务端按 `server_time`（接收时间）排序，导致发言时间乱序（如 15:42:07 出现在 15:41:28 之前）。

### 改动

**`backend/db.py`**（行 694）

```sql
-- ❌ 之前：按服务端接收时间排序
SELECT * FROM meeting_transcripts WHERE meeting_id = ? ORDER BY server_time, id

-- ✅ 之后：优先按客户端发言时间排序，server_time 作为 fallback
SELECT * FROM meeting_transcripts WHERE meeting_id = ? ORDER BY COALESCE(client_time, server_time), id
```

**`frontend/src/pages/MeetingComplianceWorkflow.jsx`**（行 1307-1308）

```js
// ❌ 之前：按 serverTime 排序
merged.sort((a, b) => (a.serverTime || '').localeCompare(b.serverTime || ''));

// ✅ 之后：按 clientTime 排序，serverTime 作为 fallback
merged.sort((a, b) => (a.clientTime || a.serverTime || '').localeCompare(b.clientTime || b.serverTime || ''));
```

### 时间戳来源

| 字段 | 来源 | 用途 |
|------|------|------|
| `clientTime` | 浏览器 `new Date()` | 发言真实时间（多设备可能有小偏差） |
| `serverTime` | 服务端 `datetime.now()` | 接收时间（受网络延迟影响） |

`COALESCE(client_time, server_time)` 在 client_time 为 NULL 时自动降级到 server_time。

---

## 修改文件清单

| 文件 | 行号 | 改动 |
|------|------|------|
| `frontend/src/pages/MobileMeetingRecorder.jsx` | 875 | `start(3000)` → `start()` |
| `frontend/src/pages/MobileMeetingRecorder.jsx` | 108-111 | 强制 dashscope 初始化 |
| `frontend/src/pages/MobileMeetingRecorder.jsx` | 1229-1239 | 禁用本地 ASR 按钮 |
| `frontend/src/pages/MeetingComplianceWorkflow.jsx` | 488 | `start(3000)` → `start()` |
| `frontend/src/pages/MeetingComplianceWorkflow.jsx` | 1308 | 排序改用 clientTime |
| `backend/llm_client.py` | 29 | 添加 `import time` |
| `backend/llm_client.py` | 264-271 | _generate 添加重试循环 |
| `backend_full.py` | 2540-2551 | 录音停止后自动触发 Whisper |
| `backend_full.py` | 3216-3337 | 提取 `_run_whisper_review_for_meeting` |
| `backend_full.py` | 3435 | `_llm_refine_text` 添加异常日志 |
| `backend/db.py` | 694 | ORDER BY 改用 COALESCE |
