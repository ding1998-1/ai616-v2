# 2026-08-11 改进记录

## 一、修复 ffmpeg remux 录音合并失败

### 问题
浏览器 MediaRecorder 使用 AAC 编码 + WebM 容器（非标准组合），ffmpeg 用 `-c copy` 写入 `.webm` 时被拒绝：
```
Only VP8 or VP9 or AV1 video and Vorbis or Opus audio are supported for WebM.
```

### 修复
- 输出格式从 `.webm` 改为 `.mp4`（AAC 是 MP4 原生编码）
- 录音下载接口 `media_type` 改为按扩展名自动识别
- 历史 100 个 `.webm` 文件重命名为 `.mp4`，数据库同步更新

### 文件
- `backend_full.py` 第 3872、3874 行
- `backend_full.py` 第 4006 行（media_type）

---

## 二、LLM 双模型 Fallback（DeepSeek + 本地 Qwen）

### 架构
```
请求 → DeepSeek API
         ├─ 成功 → 返回结果
         └─ 失败（余额不足/超时/空内容）→ 自动切本地 Qwen3.6-35B → 返回结果
```

### 改动
- 新增 `QwenLocalLLM` 类：本地 Qwen3.6-35B 客户端，关闭思考模式
- 新增 `FallbackLLM` 类：DeepSeek 优先，失败自动降级
- 修复空 content 检测：DeepSeek thinking 模式可能只返回 reasoning_content

### 文件
- `backend/llm_client.py` 第 428-545 行

### 配置
```
DEEPSEEK_API_KEY=sk-xxx          # DeepSeek API
LLM_API_BASE=http://192.168.66.44:8088/v1  # 本地 Qwen
LLM_LOCAL_MODEL=/home/ai/.cache/modelscope/hub/models/Qwen/Qwen3.6-35B-A3B-FP8
```

---

## 三、修复 AI 会议纪要生成（核心问题）

### 问题
1. prompt 要求输出 chronicle（会议纪实），导致 JSON 被 max_tokens 截断
2. summary/decisions/todos 全部丢失，降级为本地规则引擎
3. 本地规则引擎质量差：待办是原始转写片段，决议没有提炼

### 修复
1. **去掉 chronicle 要求**：prompt 不再请求纪实（本地已生成，AI 的也会被覆盖）
2. **精简 prompt**：AI 只输出 summary/minutes/decisions/todos
3. **改进 JSON 提取**：`_extract_json_object` 支持 `required_keys` 参数
4. **调用清洗函数**：AI 输出经过 `_clean_ai_payload`、`_clean_raw_decisions`、`_clean_raw_todos` 过滤

### 文件
- `backend_full.py` 第 1826-1864 行（prompt）
- `backend_full.py` 第 1865-1877 行（JSON 提取）
- `backend_full.py` 第 1878-1892 行（清洗 + 合并）

---

## 四、改进本地规则引擎（兜底方案）

### 待办提取改进
- 更宽泛的关键词：增加"准备/整理/确认/记得/别忘了"等
- 正确提取责任人：从发言中找"由XX负责/让XX来做"，而非默认发言人
- 去重：同一发言人相似内容只保留一条

### 决议提取改进
- 上下文过滤：转折词（但是/不过/可是）后面的"同意"不算决议
- 长度限制：太短（<10字）或太长（>200字）不算决议

### 文件
- `backend_full.py` 第 1590-1700 行

---

## 五、红头文件待办表格改进

### 改动
待办表格从 3 列扩展为 5 列：

| 改进前 | 改进后 |
|--------|--------|
| 责任人 / 待办事项 / 来源 | 序号 / 待办事项 / 责任人 / 截止时间 / 优先级 |

Markdown 和 Word 两处同步更新。

### 文件
- `backend_full.py` 第 3146-3156 行（Markdown）
- `backend_full.py` 第 3353-3362 行（Word）

---

## 效果对比

| | 改进前 | 改进后 |
|---|---|---|
| AI Provider | local-rule | deepseek |
| 待办数量 | 8 条（原始转写） | 4 条（提炼） |
| 待办质量 | ❌ 全是转写片段 | ✅ 明确任务+责任人 |
| 决议质量 | ❌ 原始转写 | ✅ "会议确定…"格式 |
| 摘要质量 | ❌ 模板化 | ✅ 精炼概括 |
| 录音合并 | ❌ 二进制拼接降级 | ✅ ffmpeg remux 到 MP4 |
| LLM 稳定性 | ❌ DeepSeek 挂了就挂了 | ✅ 自动降级到本地 Qwen |
