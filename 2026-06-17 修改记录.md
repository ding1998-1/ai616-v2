# 2026-06-17 修改记录

## 一、AI 会议全链路体验优化

### 1. 前端去硬编码
- 删除 11 个 demo 常量（MINUTE_SUMMARY/CHAPTERS/TODOS、LIVE_TRANSCRIPT、CHAT_MESSAGES 等）
- 所有数据改为 API 驱动：会议记录/转写/议题/材料/合规提示
- `isDemoMeeting` 逻辑全部移除
- 空态用 `<Empty>`，加载用 `<Skeleton>`

### 2. 说话人校正
- 后端新增 `POST /api/meeting/transcripts/{meeting_id}/{transcript_id}/speaker`
- 桌面端转写条目旁加编辑按钮，可修改发言人姓名/角色
- 手机端加"当前发言人"选择器，支持多人共用一台手机时切换
- 转写 chunk 接口新增 `speaker_name/speaker_role/speaker_dept` 可选字段

### 3. 实时数据同步（轮询+SSE 双通道）
- 新增 `SSEManager`（`backend/config.py`）— 按 meeting_id 管理订阅队列
- 新增 `GET /api/meetings/{meeting_id}/transcripts/sse` — SSE 推送端点
- 转写写入和会话事件写入后自动推送到 SSE 订阅者
- 前端：轮询 1.5s 主通道 + SSE 加速并行，互相不阻塞
- **教训**：不能纯 SSE 替换轮询，必须双轨+降级

### 4. 会议记录缓存
- `GET /api/meetings/{id}/records` 和 DOCX 下载均优先读缓存
- 首次进入归档阶段调 AI 生成，结果缓存到 meeting 数据
- 后续访问秒出，不再重复调 AI

### 5. 议题计时器
- 后端新增 `POST /api/meetings/{meeting_id}/agenda-timer/{agenda_id}`
  - action: start/extend/advance/reset
  - extend_minutes 参数控制延长分钟数
- 前端：
  - 创建议题时可设每个议题时长（5/10/15/20/25/30/45/60 分钟）
  - 会中显示倒计时 `议题N · MM:SS / X分`
  - 剩余 ≤5 分黄色警告，≤2 分红色，到时显示 ⏰
  - "开始计时"→"+5分"→"下一议题"（自动开始）→"重置"
  - advance 自动激活下一个议题并开始计时

### 6. 实时比对优化
- 改为每 5 分钟批量送 DeepSeek（送最近 50 条转写，更多上下文）
- 本地降级改为：有实质发言（>30 字）即判定为正在讨论，不卡关键词
- DeepSeek prompt 改为宽松语义匹配：同领域话题算命中
- **移除 RAG 注入**：知识库检索不再干扰会议 AI（议题生成/实时检查/纪要生成只基于素材和转写）

### 7. 二维码优化
- URL 精简为只传 `meetingId`，手机端从 API 自动拉取会议信息
- 容错级别升级到 H（可遮挡 30%），尺寸 220px

### 8. 音频事件修复
- `_db_load_transcripts_for_meeting`（backend_full.py 覆盖版）补查 `meeting_events` 表
- 录音回放卡片改为 `MeetingAudioPlayer` 组件，先 fetch blob 再播放（带 token）
- 音频上传加重试：失败等 2 秒重试一次，每次构建新 FormData

### 9. 手机端改进
- Fun-ASR WebSocket 预检：启动录音前 5 秒测试连通性
- 首次失败 3 秒后自动重试一次
- 错误诊断：区分非 HTTPS/超时/未登录/浏览器不支持
- 降级策略：Fun-ASR → 浏览器语音识别 → 手动输入
- 音频上传加进度提示和失败重试

### 10. DOCX 导出重写
- 先生成标准 Markdown → 再转精美 Word
- 红头格式：方正小标宋标题 + 仿宋正文 + 黑体小标题
- 3.7cm 上边距（红头文件标准）+ 表格排版
- 八章结构：基本信息 → 议题 → 摘要 → 纪实 → 纪要 → 决议 → 待办 → 签署确认
- 同时保存 `.md` 和 `.docx` 两份

### 11. 会议议题数量限制
- AI 生成议题数 = 用户选中的素材数（min 1, max 6）
- 选 1 条就出 1 个议题，不再强制拆分

### 12. 仪表盘统计
- 新增 `GET /api/dashboard/stats` — 返回会议总数/活跃/已归档/转写数/最近会议
- 前端仪表盘未改动（保持原样）
- 新增 `GET /api/meetings/search?q=` — 跨会议搜索转写文本

### 13. 通知系统
- 新建 `notifications` 表（id/user_id/type/title/body/meeting_id/created_at/read）
- `GET /api/notifications`、`POST /api/notifications/{id}/read`
- 前端侧边栏 Badge 显示未读通知数

## 二、致命修复

### Nginx 路由冲突
- `location /api/meeting` 前缀匹配会把 `/api/meetings`（带 s）吃掉 → 301
- 改为 `location /api/meeting/`（加斜杠）精确限定
- 新增 `location = /api/meetings` 精确匹配
- 新增 `location /api/meetings/` SSE 流式配置
- `/api/kb_stream` 补 `proxy_buffering off`（不带 /api/ 前缀的版本已有）

### 引用残留
- 删 RAG 变量后 prompt 模板里 3 处 `{rag_context}` 未删 → NameError
- 彻底清理

### 服务重启
- 旧进程 PID 2051 未被杀，端口被占 → 手动 kill
- `restart.sh` 改用 conda python 避免系统 python3.13 兼容问题

## 三、架构认知

### 系统拓扑
```
📱 手机 → wss://aimeeting.xingsnb.cn/api/meeting/asr/ws
              ↓
    🌐 122.227.159.74 (SSL Nginx) ← 域名代理层
              ↓ HTTP
    🖥️ 5.34.216.210:80 (Nginx) → :8002 (FastAPI)
              ↓
    📦 SQLite + ChromaDB + DashScope Fun-ASR
```

### 关键教训
1. 纯 SSE 替换轮询 → 无降级，必须双轨
2. `body.speaker_name` 直接访问 → 用 `getattr` 防御
3. Nginx 前缀匹配 `location /api/meeting` 误吞 `/api/meetings`
4. `backend_full.py` 覆盖了 `db.py` 的同名函数，修 db.py 无效
5. 复杂 UI 改动前先读透 JSX 结构
6. 测试要过三层：本地 8002 → Nginx 80 → 域名代理

## 四、文件变更清单

| 文件 | 变更 |
|------|------|
| `backend_full.py` | +SSE manager, +speaker端点, +agenda-timer端点, +dashboard/stats, +search, +notifications, 修复events查询, 修复RAG残留, 议题限制, prompt优化, DOCX重写, 缓存 |
| `backend/config.py` | +SSEManager类 |
| `backend/db.py` | +timer字段迁移, +notifications表, +events查询 |
| `backend/models.py` | +speaker字段 |
| `backend/routes/sse.py` | 新建（未使用，功能在backend_full.py中） |
| `frontend/src/pages/MeetingComplianceWorkflow.jsx` | 大量：去硬编码, SSE+轮询, 议题计时器, 音频播放器, QR精简, 时长设置 |
| `frontend/src/pages/MobileMeetingRecorder.jsx` | 发言人选择器, WS预检, 上传重试, 错误诊断, 自动拉取会议信息 |
| `frontend/src/App.jsx` | +通知轮询和Badge |
| `nginx-fixed.conf` | +SSE location, 修复WebSocket前缀, +api/kb_stream流式 |
| `restart.sh` | 新建：自动重启+等待就绪+状态显示 |

## 五、晚间追加修复

### 14. 归档工作台 AI 生成交互优化
- "AI 生成会议纪要" 按钮 → 点击触发，显示转圈等待（30-90s）
- 完成后显示 ✅ AI 已完成 + 摘要预览 + 待办列表 + 下载按钮
- 空会议/无转写时明确提示，不再 404
- **DOCX 下载慢根因**：`_load_meetings()` 2s TTL 缓存 → records 端点生成后缓存未来得及刷新 → DOCX 端点读到旧数据又调一次 AI
- 修复：`_save_meetings()` 后立即 `_invalidate_meetings_cache()`

### 15. 议题时长设置
- 创建议题时每个议题可选时长：5/10/15/20/25/30/45/60 分钟
- 显示预计总时长
- 会中议题列表旁显示预设 X 分
- AI prompt 加 `durationMinutes: 15` 字段

### 16. 实时比对策略优化
- 改为每 5 分钟批量送 DeepSeek（50 条转写），非每次轮询
- DeepSeek prompt 加宽松语义匹配规则：同领域话题算命中
- 本地降级：有实质发言（>30 字）即判定为正在讨论

### 17. 发言命中议题优化
- 彻底移除 RAG 注入会议 AI（议题生成/实时检查/纪要生成）
- RAG 只保留在合规问答 `/kb_stream`

### 18. 议题数量严格限制
- `raw_items[: max(1, min(imported_count, 6))]` — 选 N 条最多 N 个议题
- Prompt 同步修改：只输出与素材数相同的待办

### 19. WebSocket 根因诊断
- 域名 `aimeeting.xingsnb.cn` → `122.227.159.74` (SSL 代理)
- 本机 → `5.34.216.210` (后端)
- WebSocket 需在 `122.227.159.74` 配 `/api/meeting/` 代理+Upgrade 头
- 手机端加 WS 预检：5 秒快速诊断，失败显式提示原因

### 20. 二维码精简单
- URL 只传 `meetingId`，从 300+ 字符减到 ~60 字符
- 手机端 useEffect 自动从 API 拉取会议信息
- 容错 H 级 + 220px 纯黑

### 21. 音频播放修复
- `MeetingAudioPlayer` 组件：fetch blob + URL.createObjectURL
- `<audio>` 标签不走 token → 401，改为 JS fetch 带 Authorization
- 音频上传加 FormData 重建 + 2 秒重试
