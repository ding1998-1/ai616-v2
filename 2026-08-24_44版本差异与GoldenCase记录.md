# 2026-08-24 44 版本差异与 Golden Case 记录

## 结论

已完成 `192.168.66.44:8002` 的只读取证，并定位到 8 月 19 日悦仲业务二部周例会：

| 项目 | 结果 |
|---|---|
| meetingId | `meeting-local-20260819131104` |
| 标题 | 悦仲业务二部周例会 |
| 日期 | 2026-08-19 |
| 远端健康检查 | HTTP 200；DB、ChromaDB、LLM、DashScope ASR、Qwen ASR 均正常 |
| 远端健康计数 | 272 场会议、2,183 条转写 |
| 会议转写 | 721 条、96 个事件 |
| 录音事件 | 8 段 |
| 旧版纪实 | 721 条 |

取证时间：2026-08-24 09:56（Asia/Shanghai）。

## 44 与当前仓库差异

本地当前 HEAD 为 `20d0984 fix(v2): 生产全链路联调修复`；工作区另有未提交的模块化后端和 Records Pipeline v2 修改。44 的实际接口仍返回旧版 records 结构，证据如下：

- `generated=true`，`aiProvider=deepseek`；
- `source` 为“DeepSeek 根据722条转写生成”，但 `transcriptCount=721`，存在计数口径不一致；
- 输出包含 `summary / minutes / decisions / todos / chronicle`，没有独立的 `risks / disclosures`；
- `minutes` 和 `decisions` 没有 `basis`（时间区间、逐字引句、segment ID）；
- 721 条 chronicle 中仍有说话人“Whisper 终审”；
- 旧待办包含责任人“土弟”，没有按参会人名册校验；
- 旧 records 没有生成参数快照、输入 SHA256、Map/Reduce 调用统计。

当前本地模块化实现具备 `records-v2` schema、全量分段覆盖、basis、风险/披露、责任人校验、生成快照和正式件/证据底稿拆分能力，但 Golden Case 尚未在本地真实 Qwen 上重放，故不能据此宣称 44 已升级。

## Golden Case 导出内容

本地目录：`tests/fixtures/2026-08-19-yuezhong/`

- `manifest.json`：可提交的非敏感清单、计数和 SHA256；
- `README.md`：fixture 使用说明；
- `meeting.json`：远端会议详情（本地忽略）；
- `transcripts.json`：721 条转写（本地忽略）；
- `records.json`：当前 44 旧版生成结果（本地忽略）；
- `whisper_review.json`：Whisper 复核公开接口响应（本地忽略）。

没有下载音频，没有保存密码、JWT 或 API Token。目录级 `.gitignore` 会忽略原始 JSON、音频和文档，避免业务内容进入版本库。

鉴权登录接口会更新远端用户的 `lastLoginAt`；除该认证副作用外，本次未调用远端写入、生成、更新、上传、删除或部署接口。

## 数据质量观察

### 转写来源

721 条转写的来源分布为：

- `whisper-post`：679 条；
- `mobile-recorder`：42 条。

公开的 `/api/meetings/{id}/whisper-review` 只返回 1 条摘要元数据，`sourceFiles=8`，不返回完整 segments；会议详情中的终审事件只保留 4 个分段预览。因此当前 Golden Case 的完整离线文本以 721 条转写和旧版 721 条 chronicle 保存，不能把 4 个公开 Whisper segments 误认为完整终审输入。

### 录音元数据

8 个音频事件的 `durationSeconds` 为：

`19, 40, 487, 6, 24, 1741, 3593, 3765`

合计 9,675 秒。文件存在明显重叠或重复采集，不能直接把时长合计当作 75 分钟会议时长；后续 Map 应按真实文件边界/segment 时间戳处理，必要时标注重复与重叠。

### 当前公开接口的数据缺口

721 条转写行没有 `audioFileId` / `fileId`。本地标准化器因此会把它们归到 `file-1`，但仍能保证 721/721 segment 覆盖并按 4,000 字上限切为 3 个离线 chunk。正式切换前应从 44 导出带录音文件边界的 Whisper segments，或在后端补充稳定的文件绑定字段；不能根据行序伪造文件归属。

## 测试

新增 `tests/test_records_golden_case.py`，覆盖：

- Golden Case 身份和清单脱敏；
- 721 条转写与 8 段录音计数；
- 旧版已知质量问题基线；
- 全量 segment 覆盖、不采样；
- Fake LLM 下 Map/Reduce 调用、basis 引用和 coverage 快照。

本地验证命令：

```bash
PYTHONPATH=/Library/Frameworks/Python.framework/Versions/3.13/lib/python3.13/site-packages \
  .venv/bin/pytest -q tests/test_records_golden_case.py
```

结果：**5 passed**。使用 Fake LLM，不调用外部模型，不改变远端数据。

## SSH / 部署凭据状态

- 44 的 TCP/22 可达；
- 本机 `~/.ssh` 没有可用私钥，`ai@192.168.66.44` 和 `ubuntu@192.168.66.44` 在 BatchMode 下均返回 `Permission denied (publickey,password)`；
- 文档显示远端项目目录为 `/home/ai/文档/ai616`，systemd 服务为 `ai-compliance`，后端端口为 `8002`；
- 当前没有可执行 SSH 备份、上传、切换或回滚的凭据，因此本记录不执行部署。

## 下一个部署前阻塞

1. 在本地用 Golden Case 真实重放 Records Pipeline v2，确认 A1～A8，而不是只跑 Fake LLM；
2. 取得带文件边界的完整 Whisper segments 或补充 44 侧导出接口；
3. 完成后端模块化入口的全量回归；
4. 获得 44 的 SSH 用户、密钥/临时凭据和确认的备份路径后，才进入备份与部署流程。
