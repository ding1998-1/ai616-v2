# 2026-07-06 修改记录

## 今日目标

修复 systemd 崩溃循环、Whisper 转写前端展示与自动注入 DeepSeek 提炼流程、录音时间戳对齐、Whisper 繁简转换、docx 纪实完整性、DeepSeek 幻觉问题。

---

## 已完成修复 ✅（共 13 项）

### 1. systemd 崩溃循环根因修复

- **问题**：`uvicorn.run("backend_full:app", ...)` 传字符串会 fork 子进程，systemd restart 时子进程变孤儿持有端口 8002，新进程反复 `address already in use`
- **佐证**：项目中有 `restart_safe.sh`、`start_backend_ubuntu.sh` 等多个绕过脚本用 `pgrep`/`fuser` 强杀残留进程，历史日志记录 247 次端口冲突
- **修复**：
  - `backend_full.py` 行 7854：`"backend_full:app"` → 直接传 `app` 对象，uvicorn 不再 fork 子进程
  - `ai-compliance.service`：添加 `KillMode=mixed` + `TimeoutStopSec=15`
- **验证**：`ss -tlnp | grep 8002` 仅 1 个进程

### 2. Whisper 转写完成后自动更新主 records

- **问题**：Whisper 完成后只生成独立 docx，不更新 `generatedRecords` 缓存。前端 `/api/meetings/{id}/records` 返回旧的低质量实时转写版本
- **修复**：`_generate_whisper_meeting_docx` 完成后，将 Whisper 增强的 records（纪实用 Whisper segments 替换）保存回 `generatedRecords`，设置 `whisperEnhanced=True`，并清除缓存
- **文件**：`backend_full.py`（`_generate_whisper_meeting_docx` 函数，行 2879-2900）

### 3. Whisper 纪实自动替换 + DeepSeek 重新提炼

- **问题**：`/api/meetings/{id}/records` 返回的纪实基于低质量实时转写，决议/待办也是从低质量文本提炼
- **修复**：
  - records API 中检测 Whisper 事件，设置 `whisperEnhanced: True` 标记
  - 用 Whisper segments 构建高精度纪实（带时间戳），替换原 chronicle
  - DeepSeek 从 Whisper 高精度文本重新提炼 summary/decisions/todos
- **文件**：`backend_full.py`（`generate_meeting_records` 函数，行 4032-4045）

### 4. records API 支持 force 重新生成

- **问题**：`/api/meetings/{id}/records` 有缓存（`cachedAt`），"重新生成纪要"按钮命中缓存不会真正重新生成
- **修复**：
  - 后端：添加 `force: bool = False` 查询参数，`force=true` 时跳过缓存并清除转写缓存（`_invalidate_transcripts_cache()`）
  - 前端：`loadGeneratedMeetingRecords(force)` 支持传参，"重新生成纪要"按钮传 `force=true`
- **文件**：`backend_full.py`（行 4015-4021）、`MeetingComplianceWorkflow.jsx`（行 1472-1485、4370）

### 5. 前端 Whisper 状态展示

- **新增状态**：`whisperStatus`（idle / running / done）
- **轮询**：audit 阶段每 10 秒检查 `/api/meetings/{id}/whisper-review`
- **状态栏**：普通会议和重大会议均显示第四列 "Whisper 终审：✓ 已完成 / ⏳ 转写中… / 未触发"
- **侧边栏**：Whisper 完成后标题变为 "Whisper 终审转写"，展示 Whisper segments（绿色标签，最多 100 行）
- **自动刷新**：Whisper 完成后自动 `loadGeneratedMeetingRecords(true)` 强制刷新
- **文件**：`MeetingComplianceWorkflow.jsx`（行 349、1493-1524、3997-4008、4131-4168、4309-4347）

### 6. 录音时间戳对齐会议时间轴

- **问题**：多个录音文件合并后，Whisper segments 的 `start` 是合并音频内的偏移，不等于会议真实时间
- **修复**：
  - 前端上传时带 `recording_start_time`（录音开始的 ISO 时间戳）
  - 后端 `/api/meeting/recorder/audio/complete` 存入事件的 `recordingStartTime` 字段
  - Whisper 合并前，从音频事件获取每个文件的 `recordingStartTime`，计算相对最早录音的时间偏移
  - 新增 `_build_whisper_chronicle()` 函数（行 3957-4002），根据 `fileOffsets` + `fileDurations` 将 segment 的 start 映射到会议真实时间轴
- **文件**：
  - `MeetingComplianceWorkflow.jsx`（行 451-462）— `completeMicAudioUpload` 传 `recording_start_time`
  - `MobileMeetingRecorder.jsx`（行 124、939、547-558）— 新增 `recordingStartTimeRef`，录音开始时记录时间
  - `backend_full.py`（行 3488-3543、3345-3400）— complete 端点、`_do_whisper_review` 偏移计算

### 7. audioCount 修复

- **问题**：纪要中 "系统已留存 0 段手机端录音原件"，实际有 4 个录音文件
- **根因**：`backend/db.py` 的 `_db_load_transcripts` 过滤事件时只允许 `session` 和 `transcript`，丢弃了 `type: "audio"` 事件
- **修复**：`db.py` 行 673：`("session", "transcript")` → `("session", "transcript", "audio")`
- **文件**：`backend/db.py`

### 8. Whisper 繁简转换

- **问题**：openai-whisper 中文模型默认输出繁体（"產品"、"會議"、"軟件"）
- **修复**：安装 `opencc-python-reimplemented 0.1.7`，在 `whisper_transcribe.py` 中添加繁简转换（t2s），Whisper 转写后自动将繁体转为简体
- **文件**：`backend/whisper_transcribe.py` — 新增 `_get_converter()` 懒加载 opencc，`_to_simplified()` 在转写结果上批量转换
- **验证**：转写输出全部简体（"产品"、"会议"、"软件"）

### 9. Whisper 触发时机提前到 audit 阶段

- **问题**：Whisper 在 archive 阶段才触发，用户审核纪要时看到的是低质量实时转写版本，需手动归档后才启动 Whisper
- **修复**：将 Whisper 触发从 `stage in ("archive",)` 移到 `stage in ("audit",)`，会议结束进入终审即自动启动 Whisper
- **文件**：`backend_full.py`（`update_meeting_stage` 函数，行 2577-2595）
- **效果**：用户进入终审页面时 Whisper 已在后台运行，~4 分钟后自动刷新为高精度版本

### 10. docx 纪实完整性修复

- **问题**：Whisper docx 纪实只写前 80 条（`records.get("chronicle", [])[:80]`），实际有 392 段
- **修复**：移除 `[:80]` 截断，docx 写入全部 chronicle 条目
- **附带修复**：DeepSeek 输入 Whisper 文本从 `[:5000]` 提升到 `[:15000]` 字
- **文件**：`backend_full.py`（`_generate_whisper_meeting_docx` 函数，行 2849-2851）
- **验证**：docx 纪实 392 段，时间跨度 00:00→20:49

### 11. DeepSeek 幻觉修复

- **问题**：DeepSeek 生成的纪要/决议中出现录音未提及的 "Coze"（AI 工具名），属于 LLM 幻觉
- **修复**：在 DeepSeek prompt 中增加反幻觉约束：`严禁编造：所有内容必须来源于转写原文，不得添加转写中未出现的人名、产品名、工具名、数字等。不确定的宁可不写`
- **文件**：`backend_full.py`（`_deepseek_generate_meeting_records` 函数，行 1732-1733）
- **验证**：force 重新生成后 summary/decisions 中无 "Coze"

### 12. force=true 保留 whisperDocx 元数据

- **问题**：force=true 重新生成 records 时，`m2["generatedRecords"] = records` 完全覆盖旧数据，丢失 `whisperDocx` 元数据，导致 whisper-docx 端点返回 500
- **修复**：保存 records 前，从旧 `generatedRecords` 中保留 `whisperDocx` 字段
- **文件**：`backend_full.py`（`generate_meeting_records` 函数，行 4046-4052）
- **代码**：
  ```python
  old_gr = m2.get("generatedRecords") or {}
  if old_gr.get("whisperDocx"):
      records["whisperDocx"] = old_gr["whisperDocx"]
  ```

### 13. CLAUDE.md 新增录音文件保护红线

- **内容**：在"自主边界"中新增：`绝对禁止删除录音文件：data/meeting_files/recordings/ 下的所有音频文件是会议的核心原始数据，任何情况下不得删除、覆盖或清理`
- **文件**：`CLAUDE.md`

---

## 修改文件清单

| 文件 | 改动项 |
|------|--------|
| `backend_full.py` | #1 uvicorn 传 app 对象、#2 Whisper records 更新、#3 whisperEnhanced 标记 + 纪实替换、#4 force 参数 + 缓存清除、#6 录音时间戳偏移 + `_build_whisper_chronicle`、#9 Whisper 触发提前、#10 docx 去截断 + DeepSeek 输入提升、#11 反幻觉 prompt、#12 保留 whisperDocx 元数据 |
| `backend/db.py` | #7 `_db_load_transcripts` 添加 `"audio"` 事件类型 |
| `backend/whisper_transcribe.py` | #8 opencc 繁简转换（t2s） |
| `ai-compliance.service` | #1 KillMode=mixed + TimeoutStopSec=15 |
| `frontend/src/pages/MeetingComplianceWorkflow.jsx` | #4 force 刷新、#5 whisperStatus + 轮询 + 状态栏 + 侧边栏、#6 `recording_start_time` 上传 |
| `frontend/src/pages/MobileMeetingRecorder.jsx` | #6 `recordingStartTimeRef` + `recording_start_time` 上传 |
| `CLAUDE.md` | #13 录音文件保护红线 |

---

## 验证结果

| 检查项 | 结果 |
|--------|------|
| 端口 8002 进程数 | 1（无崩溃循环） |
| 后端健康 | ok, 252 会议, 1203 转写 |
| Whisper API | 1 条结果, 3888 字 |
| Records (cached) | whisperEnhanced=True, 392 段 Whisper 纪实, 3 决议, 5 待办 |
| 纪实简体 | ✓ 全部简体中文 |
| audioCount | 4（修复前为 0） |
| docx 纪实完整性 | 392 段（修复前 80 段） |
| docx 幻觉检查 | ✓ 无 "Coze" |
| Whisper Docx 下载 | ✓ 200 OK |
| 前端资源 | 200 OK（新版本） |
| systemd 服务文件 | 已同步到 /etc/systemd/system/ |
| 磁盘占用 | ai616 仅占 2.1GB（总磁盘 82% 中的 0.1%） |

---

## 流程（修复后）

```
会议结束 → 进入 audit → 自动生成 records（实时转写）
    ↓ 同时自动触发 Whisper（后台运行 ~4 分钟）
Whisper 完成 → opencc 繁简转换 → 高精度纪实替换原始纪实（时间戳对齐会议时间轴）
    ↓ → DeepSeek 从 Whisper 文本提炼 summary/decisions/todos（反幻觉约束，输入上限 15000 字）
    ↓ → 保存回 generatedRecords + whisperEnhanced=True + whisperDocx 元数据
前端轮询检测（10 秒） → 自动刷新 → 侧边栏显示 Whisper 高精度纪实
用户点"确认纪要并归档" → 归档完成
```

---

## 关键依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| openai-whisper | 20250625 | Whisper 转写 |
| opencc-python-reimplemented | 0.1.7 | 繁简转换（t2s） |
| torch | 2.10.0+cu128 | CUDA 推理 |

---

## 系统稳定性评估

- **总体评分**：B+（良好）
- **自动恢复**：systemd Restart=on-failure，崩溃 5 秒自动重启
- **开机自启**：ai-compliance + paraformer-asr 均已 enable
- **已知风险**：磁盘 82%（非本系统造成，桌面目录 301GB）、restart_safe.sh 偶尔失效（有 systemd 兜底）
