# 192.168.66.44 生产部署与回滚执行单

## 1. 部署目标

- 将当前本地大版本部署到 `ai@192.168.66.44`。
- 生产入口继续使用 `80` 端口，不覆盖现有 Nginx 配置。
- 后端从旧单文件入口切换为模块化入口 `backend.main:app`。
- 完整保留旧数据库、会议文件、向量库与远端环境配置。
- GPU0 上的 vLLM 容器、GPU1 上的 ASR 服务均不停止、不重启、不改配置。
- 部署失败时可以在一次服务切换内恢复旧版本。

## 2. 已完成的只读审计

### 2.1 当前生产服务

- Nginx：监听 `80`，静态目录固定为 `/home/ai/文档/ai616/frontend/dist`。
- 旧业务服务：`ai-compliance.service`，监听 `8002`。
- 旧启动入口：`backend_full.py`。
- 项目目录：`/home/ai/文档/ai616`。
- 生产健康检查正常，LLM 与 ASR 均可用。

### 2.2 AI 服务隔离

- GPU0：Docker 内 vLLM，端口 `8088`，模型 `qwen27b-awq-4090`。
- GPU1：`paraformer-asr.service`，使用 `cuda:1`。
- 本次只操作 `ai-compliance.service`，禁止操作 Docker、vLLM、GPU0 与 ASR 服务。

### 2.3 数据与环境

- 主库：`data/app.db`，约 28 MB，`PRAGMA quick_check = ok`。
- 现有数据：272 场会议、2183 条转写、377 条议题草稿。
- 远端 `.env` 已配置 LLM、ASR、数据目录和鉴权参数；部署时原样保留，禁止由本地文件覆盖。
- 远端已有定时数据库备份；切换前仍需创建一份停机一致性备份和校验值。

## 3. 数据兼容验证结果

已将生产数据库只读副本下载到本地临时目录，使用新版初始化与旧议题物化逻辑完成演练：

| 项目 | 迁移前 | 迁移后 |
| --- | ---: | ---: |
| 会议 | 272 | 272 |
| 转写 | 2183 | 2183 |
| 旧议题草稿 | 377 | 377 |
| 正式议题 | 0 | 377 |
| 搜索文档 | 0 | 649 |

- 迁移后数据库 `quick_check` 为 `ok`。
- 271 场含旧议题的会议全部物化成功。
- 新增表为空属于正常状态，不影响旧数据：决议、版本、签字、角色和音频客户端将在新业务发生时写入。
- 正式议题与会议搜索索引均为幂等生成，可重复启动，不重复迁移。

## 4. 发布包范围

### 4.1 包含

- 前端源码与已构建的 `frontend/dist`。
- 模块化后端、测试、运行脚本和必要项目文档。
- Python 与前端依赖清单。

### 4.2 排除

- `.git`、本地 `.env`、本地数据库和本地向量库。
- `node_modules`、缓存、日志、截图与 `work/` 测试产物。
- 任何密钥、Token、密码和机器专属配置。

## 5. 分阶段执行

### A. 无停机预发布

1. 在远端建立带时间戳的预发布目录。
2. 上传发布包，并从旧目录复制远端 `.env`、数据和向量库。
3. 使用生产数据库的副本启动新版临时实例，监听 `18002`。
4. 验证健康检查、登录、会议列表、议题搜索、会议详情、纪要接口与静态资源。
5. 验证期间生产 `80/8002` 保持在线。

### B. 一致性备份

1. 仅停止 `ai-compliance.service`。
2. 不停止 Nginx、Docker、vLLM、ASR。
3. 对 `app.db` 执行一致性备份并记录 SHA-256。
4. 备份旧项目目录和 systemd 服务文件，写入版本、时间、数据库校验值和回滚说明。

### C. 原路径切换

1. 将旧目录改名为带时间戳的只读保留目录。
2. 将已验证的新目录切换到 `/home/ai/文档/ai616`。
3. 保持 Nginx 配置不变，因静态目录路径不变而自动使用新版前端。
4. 将 `ai-compliance.service` 启动入口改为：

   ```text
   /home/ai/miniconda3/envs/ai_compliance/bin/python -m uvicorn backend.main:app --host 0.0.0.0 --port 8002
   ```

5. 启动业务服务并验证 `8002` 与 `80`。

### D. 生产验收

- 后端健康检查正常，AI/ASR 子检查正常。
- 管理员可登录，原会议数量和原转写数量一致。
- 搜索议题可返回“议题属于哪场会议”。
- 新建会议、添加议题、打开会议室、生成纪要和导出链路可用。
- 1920×1080 下登录页、新建会议页、会议室无溢出和遮挡。
- GPU0 容器 PID/运行状态和 GPU 使用关系未被本次部署改变。

## 6. 回滚触发条件

出现以下任一情况立即回滚，不继续修补生产目录：

- 数据库校验失败或核心数据计数下降。
- 登录、会议列表或会议室主流程不可用。
- LLM/ASR 健康检查由正常变为异常。
- `80` 或 `8002` 无法在限定时间内恢复。
- GPU0/vLLM 状态出现非预期变化。

## 7. 回滚动作

1. 停止新版 `ai-compliance.service`。
2. 恢复旧 systemd 服务文件。
3. 将新版目录移出生产路径，将旧目录恢复为 `/home/ai/文档/ai616`。
4. 如新版已写入生产库，恢复切换前的一致性数据库备份。
5. 启动旧服务，验证 `8002`、`80`、LLM 与 ASR。

## 8. Git 大版本

- 仅提交源码、测试、部署文档和必要资源。
- 排除 `.env`、数据库、日志、缓存、截图和 `work/`。
- 在本地完成测试后创建一个明确的大版本提交。
- `git push` 作为独立生产动作执行，推送前再次核对暂存文件，确保无密钥与生产数据。

## 9. 执行门槛

- 预发布临时实例全部通过后，才允许停旧业务服务。
- 生产切换与 Git 推送均属于需即时授权的动作；执行前展示精确目标和影响范围。
