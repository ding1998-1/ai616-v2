# 4.29 启动与依赖关系说明

这份文档只做一件事：把当前项目的前后端关系、依赖关系、启动顺序和常见出错点写清楚，方便下次启动时少踩坑。

## 项目结构

- 根目录核心后端：
  - `backend_full.py`
  - `audit_persistence.py`
  - `demo_content.py`
  - `start_backend.sh`
- 前端目录：
  - `frontend/`
- 持久化与数据目录：
  - `data/`
  - `chroma_db/`
  - `onlyoffice_data/`
  - `rules/`
- Docker 相关：
  - `docker-compose.yml`
  - `Dockerfile`
  - `frontend/Dockerfile`

## 前后端关系

### 前端

- 技术栈：
  - `React 18`
  - `Vite 5`
  - `Ant Design`
  - `@ant-design/x`
  - `react-markdown`
  - `TipTap / reactjs-tiptap-editor`
- 入口是 `frontend/src/App.jsx`
- 前端开发服务端口是 `3001`
- 前端本身不直连线上地址，开发时通过 `vite.config.js` 反向代理到本地后端 `127.0.0.1:8000`
- `frontend/vite.config.js` 当前已确认：
  - `host: 0.0.0.0`
  - `port: 3001`

### 后端

- 技术栈：
  - `FastAPI`
  - `Uvicorn`
  - `LangChain`
  - `ChromaDB`
  - `sentence-transformers`
  - `torch`
  - `python-docx`
  - `pdfplumber`
  - `PyJWT`
- 核心服务文件是 `backend_full.py`
- 后端 API 端口是 `8000`
- 后端负责：
  - 登录与用户管理
  - 三重一大审查流
  - 合同审查
  - 知识库解析、向量化与问答
  - 自定义制度文件
  - `rules/` 图片画廊
  - OnlyOffice 编辑集成

## 关键运行关系

### 1. 前端依赖后端

前端大部分功能都依赖后端，尤其这些前端调用入口：

- `/api/auth/*`
- `/api/audit_stream`
- `/api/audit_history`
- `/api/custom_rules/*`
- `/api/rules_gallery`
- `/api/contract/*`
- `/parse_file`
- `/ingest_file`
- `/api/kb_stream`
- `/api/knowledge_files/*`
- `/api/doc/*`
- `/doc/*`（少数场景也会直接走）

结论：

- 前端可以单独跑起来
- 但后端没启动时，大多数页面功能不可用
- 首页在线状态检测也是通过 `/matter-types` 判断的

补充说明：

- 前端很多请求使用的是“前端代理路径”，不等于后端真实路由
- 例如：
  - 前端请求 `/api/doc/*`，会被 Vite 改写并转发到后端 `/doc/*`
  - 前端请求 `/api/contract/*`，会被 Vite 改写并转发到后端 `/contract/*`
  - 前端请求 `/api/audit_stream`，会被转发到后端 `/audit_stream`
- 所以排查问题时要区分：
  - 浏览器里看到的前端请求地址
  - `backend_full.py` 里真正注册的后端路由

### 2. 知识库依赖 ChromaDB

后端在 `backend_full.py` 里把向量库持久化目录固定到：

- `./chroma_db`

相关关系：

- 上传知识文件后，调用 `/parse_file` 提取文本
- 再调用 `/ingest_file` 或知识库相关接口写入 `ChromaDB`
- 问答页通过 `/api/kb_stream` 做检索增强问答

注意：

- `ChromaDB` 现在是懒加载
- 所以登录、基础页面加载不再被向量库拖慢
- 但第一次做知识问答或第一次入库，仍可能比普通接口慢

### 3. 合同审查依赖 OnlyOffice

合同审查不是纯前端编辑，它和 OnlyOffice 有关系：

- OnlyOffice Document Server 默认端口：`8081`
- 后端通过 `/doc/edit_url` 生成编辑配置
- JWT 密钥来自：
  - `ONLYOFFICE_JWT_SECRET`
- 后端内部访问地址由环境变量决定：
  - `BACKEND_INTERNAL_HOST`

开发脚本 `start_backend.sh` 里会设置：

- `BACKEND_INTERNAL_HOST=host.docker.internal`

这一步是为了让 Docker 内的 OnlyOffice 能回到宿主机上的后端。

### 4. 三重一大 / 合同审查 / 知识库之间的关系

- 三重一大审查：
  - 主要走 `/api/audit_stream`
  - 配置依赖 `custom_rules` 和 `rules_gallery`
- 合同审查：
  - 主要走 `/api/contract/analyze`
  - 文档上传依赖 `/api/doc/upload`
  - 编辑与定位前端常走 `/api/doc/*`
  - 代理后实际落到后端 `/doc/*`
- 知识库：
  - 主要走 `/parse_file`、`/ingest_file`、`/api/kb_stream`

三者共享后端，但页面和主流程相对独立。

## 前端代理关系

`frontend/vite.config.js` 已配置开发代理，核心映射如下：

- 前端 `3001` -> 后端 `8000`
- `/api/doc/*` -> `/doc/*`
- `/api/contract/*` -> `/contract/*`
- `/api/audit_stream` -> `/audit_stream`
- `/api/kb_stream` -> `/api/kb_stream`
- `/matter-types` -> `8000`
- `/api/auth/*` -> `8000` 原路径转发
- `/api/users/*` -> `8000` 原路径转发
- `/api/custom_rules/*` -> `8000` 原路径转发
- `/api/rules_gallery` -> `8000` 原路径转发
- `/api/rules_images/*` -> `8000` 原路径转发
- `/api/knowledge_files/*` -> `8000` 原路径转发
- `/parse_file` -> `8000` 原路径转发
- `/ingest_file` -> `8000` 原路径转发
- `/doc/*` -> `8000` 原路径转发
- `/contract/*` -> `8000` 原路径转发

这意味着：

- 开发时浏览器只访问前端地址 `http://127.0.0.1:3001`
- 具体 API 由 Vite 代理转发
- 如果后端没开，前端会出现接口报错或登录失败

## 启动方式

### 方式 A：本地开发，推荐

#### 1. 启动后端

在项目根目录执行：

```bash
./start_backend.sh
```

这个脚本会做这些事：

- 切到项目根目录
- 设置 `HF_ENDPOINT=https://hf-mirror.com`
- 清理占用 `8000` 端口的旧进程
- 使用固定 Python：
  - `/Users/macos/miniconda3/bin/python`
- 启动 `backend_full.py`

说明：

- 这个项目当前默认不是用系统 Python 跑
- 是绑定到 `miniconda3` 里的 Python
- 这样能保证 `torch`、`sentence-transformers`、LangChain 依赖一致

#### 2. 启动前端

进入前端目录：

```bash
cd frontend
npm install
npm run dev
```

默认启动：

- `http://127.0.0.1:3001`

#### 3. 打开系统

浏览器访问：

```bash
http://127.0.0.1:3001
```

### 方式 B：Docker 方式

如果要用容器方式整体跑：

```bash
docker compose up -d --build
```

容器关系：

- `backend`
  - 暴露 `8000`
- `frontend`
  - 暴露 `80`
- `onlyoffice`
  - 暴露 `8081`

注意：

- Docker 里的 backend 用的是 `Dockerfile`，启动命令是：
  - `uvicorn backend_full:app --host 0.0.0.0 --port 8000`
- 本地脚本方式和 Docker 方式不要混着长期跑
- 否则容易出现：
  - 端口占用
  - OnlyOffice 指向混乱
  - 数据目录读写来源不一致

## 当前关键依赖

### 后端 Python 依赖

来自 `requirements.txt`：

- `fastapi`
- `uvicorn`
- `python-multipart`
- `pydantic`
- `httpx`
- `langchain`
- `langchain-core`
- `langchain-community`
- `langchain-openai`
- `langchain-chroma`
- `langchain-huggingface`
- `chromadb`
- `sentence-transformers`
- `torch`
- `pdfplumber`
- `pypdf`
- `python-docx`
- `docx2txt`
- `python-dotenv`
- `loguru`
- `PyJWT`

### 前端 Node 依赖

来自 `frontend/package.json`：

- `react`
- `react-dom`
- `vite`
- `antd`
- `@ant-design/icons`
- `@ant-design/x`
- `axios`
- `dayjs`
- `react-markdown`
- `remark-gfm`
- `@tiptap/react`
- `@tiptap/starter-kit`
- `reactjs-tiptap-editor`

## 关键环境变量与配置

### 后端

重点环境变量：

- `HF_ENDPOINT`
  - 当前脚本里设置为 `https://hf-mirror.com`
- `BACKEND_INTERNAL_HOST`
  - 本地脚本里设为 `host.docker.internal`
- `ONLYOFFICE_JWT_SECRET`
  - 必须和 OnlyOffice 容器一致
- `DEEPSEEK_API_KEY`
  - Docker compose 里有配置
- `backend_full.py` 当前默认值已确认：
  - `BACKEND_INTERNAL_HOST` 默认回退为 `ia-audit-backend`
  - `ONLYOFFICE_JWT_SECRET` 默认回退为 `your-secret-key-change-in-production`

### OnlyOffice

Docker compose 中关键配置：

- `JWT_ENABLED=true`
- `JWT_SECRET=your-secret-key-change-in-production`
- `JWT_HEADER=Authorization`

后端也必须读取到同一套 JWT secret，否则合同编辑器会异常。

## 常见出错点

### 1. 登录报错 / 页面 HTTP 500

优先检查后端是否启动：

```bash
curl http://127.0.0.1:8000/matter-types
```

如果不通：

- 后端没启动
- 或被别的进程占了端口

### 2. 前端能打开，但接口都失败

优先检查：

- 前端是不是从 `3001` 启动的
- 后端是不是在 `8000`
- Vite 代理是否生效
- 浏览器请求路径是不是走了代理口径：
  - 前端调用常见是 `/api/doc/*`、`/api/contract/*`
  - 后端真实注册是 `/doc/*`、`/contract/*`

### 3. 合同审查编辑器打不开

优先检查：

- OnlyOffice 容器是否启动
- `8081` 是否可访问
- `ONLYOFFICE_JWT_SECRET` 是否与容器一致
- `BACKEND_INTERNAL_HOST` 是否设置正确

### 4. 知识问答第一次很慢

这是正常现象之一，原因通常是：

- Chroma 首次加载
- embedding 模型首次加载
- LLM 请求排队

### 5. 自定义制度 / 知识库上传后没有效果

优先检查：

- 文件是否真的解析成功
- 向量化是否成功
- `data/` 和 `chroma_db/` 是否可写

## 推荐启动顺序

下次开发时，建议严格按这个顺序：

1. 进入项目根目录
2. 启动后端

```bash
./start_backend.sh
```

3. 新开终端，启动前端

```bash
cd frontend
npm run dev
```

4. 如果需要合同在线编辑，再确认 OnlyOffice

```bash
docker ps
```

检查是否存在：

- `onlyoffice-ds`

## 推荐自检命令

### 后端存活

```bash
curl --max-time 10 -sS http://127.0.0.1:8000/matter-types
```

### 登录接口

```bash
curl --max-time 10 -sS -X POST http://127.0.0.1:8000/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"admin123"}'
```

### 归档接口

```bash
curl --max-time 10 -sS http://127.0.0.1:8000/api/audit_history
```

### 前端构建检查

```bash
cd frontend
./node_modules/.bin/vite build
```

### 后端语法检查

```bash
python3 -m py_compile backend_full.py demo_content.py audit_persistence.py
```

## 目前最稳的结论

- 开发态主链路是：
  - 前端 `3001`
  - 后端 `8000`
  - OnlyOffice `8081`
- 最稳的后端启动方式是：
  - `./start_backend.sh`
- 最稳的前端启动方式是：
  - `cd frontend && npm run dev`
- 不要同时长期混用：
  - 本地脚本后端
  - Docker backend

否则很容易出现“页面能打开但功能异常”的假启动状态。

## 当前口径修正

为了避免后续再被旧文档误导，当前以代码核对后的口径为准：

- 前端开发端口是 `3001`，不是 `3000`
- 前端接口很多带 `/api` 前缀，但后端真实路由里不一定带
- 合同相关前端常用 `/api/doc/*`、`/api/contract/*`
  - 实际后端主路由分别是 `/doc/*`、`/contract/*`
- 如果 `README.md` 或旧记录和这里冲突，以本文件和当前代码配置为准

## 文档更新时间

- 记录日期：`2026-04-29`
- 文件用途：下次启动和排障的第一参考文档
