# 2026-06-11 修改记录

## 部署文档更新

- 更新 `部署教程.md`，新增“当前演示版快速启动”章节。
- 明确当前桌面项目路径为 `/Users/macos/Documents/ai 合规 demo 4:28`。
- 修正前端开发端口：从旧文档的 `3000` 调整为当前实际端口 `3001`。
- 补充当前稳定启动命令：
  - 后端：`/Users/macos/miniconda3/bin/python backend_full.py`
  - 前端：`PATH=./node_modules/.bin:$PATH npm run dev -- --host 0.0.0.0 --port 3001`
- 补充后端健康检查命令，要求确认 API 标题为 `三重一大合规审核 API` 且 `/api/auth/login` 存在。
- 补充 AI 会议两个外部分享入口说明：
  - `/issue-collect?meetingId=...`：外部人员登录/登记后提交问题，写回会议问题池。
  - `/mobile-recorder?meetingId=...`：参会人员手机登录/登记后录音和转写。
- 补充 `localhost` 分享链接不可发给别人、手机录音建议使用 HTTPS 隧道或正式域名的说明。
- 修正 `.env` 示例中的本地路径，统一改为当前项目目录 `ai 合规 demo 4:28`。
- 更新 `Ubuntu部署教程.md` 的 Nginx 配置：
  - `/api/` 代理改为保留 `/api` 前缀，避免登录、会议、用户等接口被错误转发。
  - `/api/` 代理增加 WebSocket Upgrade 配置，用于支持会议 ASR WebSocket。
  - 非 `/api` 后端接口增加 `parse_file`、`matter-types`、`contract`、`doc` 等路径。
- 在 Ubuntu 文档中补充 `/issue-collect` 和 `/mobile-recorder` 为前端 SPA 路由，必须由 `try_files ... /index.html` 接管。
- 在 Ubuntu 文档中补充外部分享入口与 HTTPS 注意事项。
- 更新 Ubuntu 文档快速验证清单，新增分享入口和 `/api/auth/me` 验证。
- 更新 Ubuntu 文档最后更新时间为 `2026-06-11`。

## AI 会议会前页隐藏本地项目台账

- 隐藏会前确认工作区右侧“本地项目台账”面板，避免演示时把注意力从“开会”拉到“项目管理”。
- 会前确认工作区从三列改为两列：左侧“泛群聊素材池”，右侧“AI 议题生成看板”。
- AI 议题生成看板自动吃满剩余宽度，提升议题阅读和确认空间。
- 去掉顶部阶段栏里的“本地项目”显性标签。
- 将议题卡内的“本地项目”文案弱化为“会议事项”，并隐藏“一键绑定”按钮。
- 底层项目字段和绑定方法保留，不影响后续终审、归档或已有数据结构。
- 已执行前端构建：`npm run build` 通过。
- 已用 Playwright 验证会前确认页：`.collect-workspace` 为两列布局，列宽约 `340px + 1420px`，页面不再出现“本地项目台账”和“一键绑定”，无横向溢出。

## AI 会议普通会议 / 三重一大会议分流

- 创建 AI 会议时新增“会议性质”选择：
  - 普通会议：只做实时转写、会议纪实、会议纪要、会议决议和待办比对。
  - 三重一大会议：在普通会议能力基础上增加材料核验和合规拦截。
- 前端分享链接已携带 `mode=normal|major`，手机录音页和问题收集页可以识别会议性质。
- 后端会议请求模型新增 `meetingMode / meeting_mode` 兼容字段。
- 修复后端请求默认值覆盖风险：仅显式传入的 `meetingMode` 才覆盖已有会议性质，避免普通会议被默认值改回三重一大。
- 注意：真实数据库 `meetings` 表当前还缺少 `meeting_mode` 持久化列，完整保存会议性质需要执行一次受控数据库加列。

## 会中实时记录台改版

- 将会中红框区域从“大标题展示页 + AI 装饰面板”改为三栏工作台：
  - 左侧：手机端实时转写，显示谁、什么角色、什么时候说了什么。
  - 中间：对照会前待办，提示会后 AI 会判断是否讨论到、是否形成结论、是否跑题。
  - 右侧：结束后生成的会议纪实、会议纪要、会议决议；三重一大会议才显示材料缺口。
- 新增 `.meeting-runtime-*` 样式，三栏内部滚动，避免横向溢出。
- 修复新会议无真实发言时也显示样例转写的问题：只有 demo 会议才追加 `LIVE_TRANSCRIPT` 样例，新会议默认等待手机端真实回传。
- 普通会议结束按钮文案改为“结束会议，整理纪要”，三重一大会议保留“结束会议，进入终审”。
- 已执行后端语法校验：`python -m py_compile backend_full.py` 通过。
- 已执行前端构建：`npm run build` 通过。

## 外部问题收集页按会议性质分流

- `/issue-collect` 根据 URL `mode` 或会议详情识别会议性质。
- 普通会议填报页改为“问题 / 素材收集”：
  - 不再显示“事项类型”“涉及金额”“材料情况”。
  - 只保留问题描述。
  - 新增“上传图片 / 附件”入口，图片作为问题素材写回问题池，`docx/pdf/txt/md` 会调用现有 `/parse_file` 尝试提取文字后写回问题池。
- 三重一大会议填报页继续保留：
  - 事项类型。
  - 涉及金额。
  - 材料情况。
  - 图片 / 材料附件上传。
- 普通会议提交内容只写“问题描述 + 图片/附件素材”，不会拼接金额、材料缺口等三重一大字段。
- 已用浏览器验证：
  - `mode=normal` 页面无“事项类型 / 涉及金额 / 材料情况”，显示“上传图片 / 附件”。
  - `mode=major` 页面保留三重一大字段，显示“上传图片 / 材料附件”。
  - 两种页面均无横向溢出。

## 外部提交议题回显闭环与 OCR

- 修复外部问题收集页提交后，秘书端会前页面中间“AI 议题生成看板”仍显示 0 个待办的问题。
- 根因：
  - 外部提交已写入后端 `issueSources`，但主会前页没有主动刷新当前会议详情。
  - 部分新会议的 `agendaDrafts` 为空时，前端没有用 `issueSources` 兜底生成待确认议题。
  - 复制收集链接时若会议批次尚未保存，外部提交容易进入另一个自动创建的 `meeting-local-*`。
- 前端修复：
  - 会前创建页和问题收集阶段每 3 秒静默同步当前 `meetingId` 的会议详情。
  - `hydrateMeetingDetail` 增加兜底：只要 `issueSources` 有内容，即使后端 `agendaDrafts` 为空，也自动生成“待上会议题”。
  - 点击“复制收集链接”前会先保存当前会议批次，确保别人提交后回到同一个会议 ID。
- 后端修复：
  - `_derive_agenda_drafts` 在会议议题为“待确认议题/待梳理议题”时，会从最新外部提交的问题描述中提炼标题，不再一直显示默认标题。
  - 修复 `meeting_issue_sources` 保存时 issue id 反复套前缀的问题，避免数据 ID 越来越长。
- 新增图片 OCR 接口：
  - `/api/ocr/image`
  - 使用阿里云百炼 OpenAI 兼容接口，默认模型 `qwen-vl-ocr`。
  - API Key 读取现有 `DASHSCOPE_API_KEY / DASHSCOPE_BAILIAN_API_KEY / BAILIAN_API_KEY` 环境变量，不写入代码。
  - 问题收集页上传图片时会调用 OCR，识别文本写入提交素材。
- 已更新 `部署教程.md` 和 `Ubuntu部署教程.md`，补充 `DASHSCOPE_API_KEY`、`DASHSCOPE_OCR_MODEL`、`DASHSCOPE_FUN_ASR_MODEL` 配置说明。
- 已验证：
  - 接口模拟外部提交后，同一会议详情返回 `issues=1`、`drafts=1`。
  - 新生成的议题标题来自提交内容，例如“大厅叫号系统经常卡顿，群众等待时间过长，需要安排例会讨论优化方案”。
  - 浏览器打开同一 `meetingId` 的会前页，中间显示“AI 已整理出 1 个开会待办”，并自动选中该议题。
  - OCR 接口使用 `qwen-vl-ocr` 成功识别截图文字。

## 会议性质选择逻辑修正

- 明确产品逻辑：会议性质由秘书端创建会议时设置，外部填报人不能选择。
- 秘书端“会议性质”从下拉框改为两个决策卡片：
  - 普通会议：外部只填文字、图片或附件；会后生成纪实、纪要、决议。
  - 三重一大会议：外部补充事项类型、金额和材料；会后启用材料核验和合规拦截。
- 新建会议默认改为“普通会议”，避免未明确选择时把普通会议误判为三重一大。
- 外部问题收集页增加只读提示：
  - 当前为普通会议：本页只收文字、图片或附件，不需要填写金额和材料清单。
  - 当前为三重一大会议：会议性质由秘书端设置，本页需要补充事项类型、金额和材料情况。
- 外部问题收集页不再根据填报人输入自动判断会议性质，只读取秘书端分享链接中的 `mode=normal|major`；若未带 `mode`，默认按普通会议处理。
- 已用浏览器验证：
  - 秘书端右侧出现 2 个会议性质卡片，默认选中“普通会议”，旧下拉不再显示。
  - `mode=normal` 外部页无“事项类型 / 涉及金额 / 材料情况”。
  - `mode=major` 外部页保留“事项类型 / 涉及金额 / 材料情况”。
  - 三个页面均无横向溢出。

## OCR 404 修复

- 修复图片上传后提示“图片已接收，OCR 暂未完成：HTTP 404”的问题。
- 根因：前端 Vite 本地开发代理没有配置 `/api/ocr`，导致问题收集页请求 `/api/ocr/image` 时没有转发到后端。
- 已在 `frontend/vite.config.js` 中新增 `/api/ocr` 代理到 `http://127.0.0.1:8000`。
- 已重启前端和后端服务。
- 已验证：
  - 直连后端 `POST http://localhost:8000/api/ocr/image` 返回 200。
  - 经前端代理 `POST http://localhost:3001/api/ocr/image` 返回 200。
  - OCR 模型为 `qwen-vl-ocr`，可返回图片文字。

## AI 会议创建按钮与事项文案修复

- 右侧创建会议面板中的“本地项目名称”改为“会议事项名称”。
- “本地编号”改为“事项编号”。
- 顶部说明改为“确认会议事项名称”，不再强调创建本地项目。
- 创建步骤中的“提示秘书创建本地项目”改为“形成待上会议题”。
- 底部固定操作条文案优化：
  - 普通会议：将按普通会议收集素材并生成纪实、纪要、决议。
  - 三重一大会议：将按三重一大会议收集金额、材料并启用核验。
- 修复底部“确认议题”按钮颜色发灰的问题：
  - 可点击状态强制蓝底白字。
  - 无议题禁用状态才显示灰色。
- 已验证：
  - 页面显示“会议事项名称”和“事项编号”。
  - 页面不再出现“本地项目名称”和“本地编号”。
  - 按钮背景为 `rgb(22, 93, 255)`，文字为白色，可点击。
  - 无横向溢出。

## 问题收集素材包、原图预览与按人折叠

- 外部问题收集页上传图片时，前端会先读取图片 `dataURL`，页面立即显示图片缩略图，不再只显示文件名和 OCR 文本。
- 图片 OCR 逻辑保持不变：
  - 上传图片后调用 `/api/ocr/image`。
  - AI 议题生成仍然依赖 OCR 后写入 `content` 的识别文字。
  - 即使 OCR 失败，也会保留图片素材，提示“图片已接收，OCR 暂未完成”。
- Word/PDF/TXT/MD 附件继续走现有 `/parse_file`：
  - `.docx` 可解析为文字后进入“附件文字”。
  - 解析出的文字会拼入提交内容，供 AI 后续提炼议题。
  - 未改后端解析接口，未新增依赖。
- 提交时不改数据库结构：
  - 使用现有 `meta` 字段保存结构化 JSON。
  - `meta.label` 保存会议素材标签。
  - `meta.attachments` 保存图片、附件名、解析文本和图片预览数据。
- 秘书端“问题收集 / 泛群聊素材池”改为按提交人折叠：
  - 同一人多条提交合并成一张卡。
  - 卡片展示提交条数、原图数量、最新时间和素材标签。
  - 点击卡片可展开查看该人的全部原始提交。
- 秘书端支持原图追溯：
  - 展开卡片后可看到图片缩略图。
  - 点击缩略图打开“原图预览”弹窗。
  - 弹窗下方展示对应 OCR 文本，方便核对“原图证据”和“AI 识别文字”。
- 外部问题收集页“最近提交”也会显示已提交图片，方便填报人确认不是只传了文字。
- 已验证：
  - `npm run build` 通过。
  - 前端服务 `3001` 正常监听。
  - 后端服务 `8000` 正常监听。

### 按红框原型修正左侧素材池层级

- 修正秘书端左侧“问题收集”素材池展示方式：
  - 默认显示“参会单位/提交人 + 提交摘要 + 已提交 x 条”的折叠卡。
  - 不再默认铺开每条 OCR 文本和附件明细。
  - 卡片下方增加提交人身份条，包含圆形头像首字、姓名、单位和提交数量。
  - 点击卡片后才展开原始提交、图片缩略图、OCR 文本和附件解析文字。
- 目的：
  - 先让秘书一眼知道“谁提交了几条”。
  - 再进入明细核对“原图证据 / OCR 文字 / Word 解析文字”。
  - 符合用户红色原型示意中的信息层级。
- 已验证：
  - `npm run build` 通过。

## 会后录音回放与会议成果包

- 明确产品逻辑：会议结束后不应该让用户猜“录音和纪要在哪里”，会后阶段固定展示“会议成果包”。
- PC 端会后页新增会议成果包：
  - 录音回放：展示手机端上传的真实音频，可直接播放。
  - 转写底稿：按时间、发言人、角色展示会中实时转写。
  - 会议纪实：作为时间线证据材料入口。
  - 会议纪要：按议题整理讨论要点、结论、责任人和截止时间。
  - 会议决议：生成同意、暂缓、补材料后再议等可编辑决议草稿。
- 手机录音端结束录音时新增音频上传：
  - 保留本机即时回放。
  - 同时将音频 Blob 上传到后端，写入当前会议事件。
  - 上传成功后提示“音频和转写记录已回传”。
- 后端新增接口：
  - `POST /api/meeting/recorder/audio`：上传手机端录音文件。
  - `GET /api/meeting/recorder/audio/{meeting_id}/{audio_id}`：PC 端回放/下载录音。
- 存储策略：
  - 不修改数据库表结构。
  - 音频文件保存到 `data/meeting_files/recordings/{meeting_id}/`。
  - 录音元数据作为现有会议事件写入，PC 端通过原 `/api/meeting/transcripts/{meeting_id}` 获取。
- 已验证：
  - `python3 -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。
- 注意：旧录音只保存了转写和音频大小，没有真实音频文件，因此不能补回放；新版本之后手机端结束录音会上传真实音频，PC 会后成果包可播放。

## 普通会议与三重一大流程隔离修复

- 问题判断：
  - 普通会议页面仍出现“三重一大会议”“材料缺口”“合规判断”“三重一大材料拦截”等内容，属于严重流程污染。
  - 根因是会议模式默认值、议题生成、会中提示、会后归档文案没有完全按 `meetingMode` 分支。
  - 另一个问题是 AI 议题在问题收集阶段过早自动出现，用户还没等各部门提交完，就像系统已经替秘书定稿。
- 产品规则重新明确：
  - 普通会议只做：问题收集、待办议题、会中录音转写、会后纪实、会议纪要、会议决议、待办清单。
  - 普通会议不得出现：三重一大标签、材料核验、金额材料字段、合规拦截、可研/资金测算/法务意见等三重一大材料提示。
  - 三重一大会议才出现：金额/事项类型/材料字段、材料缺口、终审拦截、合规报告、红头纪要和防伪归档。
- 代码修复：
  - `normalizeMeetingRecord` 默认会议性质从 `major` 改为 `normal`，避免未声明会议被误判成三重一大。
  - 新增 `agendaGenerated` 状态：问题收集阶段不再自动把素材变成最终待办。
  - 新增“生成开会待办”按钮：秘书等所有人员提交完后，手动触发 AI 汇总。
  - 手动新增问题或外部继续提交问题后，会重置为“待生成”，防止漏掉新素材。
  - Excel 上传属于明确生成动作，上传后保持“已生成”状态。
  - 切换会议性质后，已生成议题会按新性质重新计算标签和下一步。
- UI 修复：
  - 中间议题池未生成前显示“等待生成开会待办”，不再显示伪完成结果。
  - 普通会议生成的议题标签改为“普通议题”，不再显示中风险/高风险。
  - 普通会议下一步改为“确认讨论范围，安排会议讨论”，不再显示“补材料、定责任人”。
  - 右侧创建会议面板删除“会议事项名称”输入框，会议由已选议题驱动，不再要求填一个无意义事项名。
  - “确认 x 个议题”按钮强制蓝底白字，修复按钮文字发白/发灰不清晰的问题。
  - 左侧提交人折叠卡的明细区增加独立滚动高度，超过 3 条也能滚动查看，不再被底部输入栏挡住。
- 会中页面修复：
  - 普通会议的 AI 输出改为“会议判断”，不再显示“合规判断”。
  - 普通会议右侧改为“会后产物”，不再显示“材料缺口”。
  - 普通会议会中提示改为议题回应、纪要素材、证据沉淀。
  - 三重一大会议仍保留材料缺口、合规提醒和终审提示。
- 会后/归档修复：
  - 普通会议会后说明改为“会后整理会议成果”，不再出现“不启用三重一大材料拦截”这种反向提示。
  - 普通会议归档包改为聊天碎片、议题变更、录音转写、会议纪实/纪要/决议、电子签和归档包。
  - 三重一大会议归档包才显示三重一大合规报告与催办闭环。
- 已验证：
  - `npm run build` 通过。
  - grep 检查普通会议直接可见文案，已清除“会议事项名称”“普通会议：不启用三重一大材料拦截”等错误表达。

## 手工/链接收集问题接入真实 AI 议题生成

- 问题判断：
  - 用户手工提交或外部链接提交了多条问题后，中间仍只生成 1 个“待确认议题”。
  - 根因不是 DeepSeek API 失效，而是前端“生成待办”按钮仍使用本地 `deriveIssueDraftsFromSources` 规则。
  - Excel 上传走的是后端 DeepSeek 提炼，手工/链接收集没有走后端 AI，因此多条问题被本地规则压缩成一个议题。
- 后端修复：
  - 新增 `POST /api/meetings/{meeting_id}/agenda/generate`。
  - 该接口读取当前会议真实 `issueSources`，调用 DeepSeek 进行聚类、拆分、生成开会待办，并写回同一个会议的 `agendaDrafts`。
  - 如果 DeepSeek 不可用，接口返回 `aiProvider=local-rule`，但仍保留可用的本地兜底结果，不阻断演示。
  - `MeetingUpsertRequest.meetingMode` 默认值改为 `normal`，新建会议默认普通会议，避免三重一大语境污染。
- AI 提示词修复：
  - 普通会议与三重一大会议分支处理。
  - 普通会议只输出“讨论待办 / 普通会议议题 / 普通风险”，禁止输出金额、材料缺口、合规拦截、三重一大类型。
  - 三重一大会议才输出重大事项、重要人事任免、重大项目、大额资金、材料补齐和风险等级。
  - 明确要求“同一事项多条消息合并，不同问题拆分”，最多 6 个，避免把不相关问题合成一个大议题。
- 前端修复：
  - “生成待办 / 重新生成”按钮改为优先调用后端 `/agenda/generate`。
  - 新增 `agendaGenerating` loading 状态，手工素材生成时也有明确反馈，不再只有 Excel 上传才有动画。
  - 生成成功后显示 DeepSeek 或本地兜底来源，并自动勾选生成出的待办议题。
  - 后端临时不可用时才使用前端兜底，并提示原因。
- 运行态修复：
  - 前端首次请求新接口出现 `Not Found`，原因是 8000 端口仍运行旧后端进程。
  - 已重启后端进程，接口探测返回 `401 未登录`，说明路由已加载，不再是 404。
- 已验证：
  - `/Users/macos/miniconda3/bin/python -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。
  - `curl -X POST /api/meetings/{id}/agenda/generate` 未登录返回 `401`，确认接口存在。

## 会中普通会议工作台去三重一大化与议题主线强化

- 问题判断：
  - 截图中的会议是普通专题会，但会中页顶部仍显示“三重一大会议”，右侧仍显示“材料缺口”。
  - 根因有两层：一是后端 `_public_meeting` 对缺省会议性质仍默认返回 `major`；二是会中工作台右侧仍按三重一大材料核验思路组织信息，普通开会场景看不到“正在讨论哪些议题”。
- 后端修复：
  - `_public_meeting` 的 `meetingMode` 默认值从 `major` 改为 `normal`。
  - 避免旧会议或缺少 `meetingMode` 的会议被接口包装成三重一大会议。
- 前端修复：
  - 新增会议性质推断：旧数据标记为 `major`，但标题/项目/议题/材料中没有预算、资金、合同、采购、任免、金额、材料缺口等三重一大信号时，按普通会议展示。
  - 顶部标题从系统宣传语改为当前会议工作台：优先显示会议名称，副标题显示当前议题/项目。
  - 普通会议顶部进度从“全链路闭环度”改为“会议推进度”，避免合规闭环语境污染普通会议。
  - 会中页顶部新增“本次会议正在讨论”议题主线条，直接展示当前议题和待讨论事项数。
  - 右侧“结束后生成”在普通会议下只展示会议纪实、会议纪要、会议决议、会后待办，不再渲染材料缺口清单。
  - 普通会议新增“会后生成物”小面板，强调纪实/纪要/决议/待办，不出现“三重一大”“材料补齐”等词。
  - 会中工具栏新增“普通/三重一大”会议性质切换，点击后写回后端，旧会议不用重建即可切换正确流程。
- 产品逻辑调整：
  - 普通会议的会中核心是：实时转写 -> 对照会前待办/议题 -> 标记决议/待办/争议 -> 会后生成纪实、纪要、决议和督办清单。
  - 三重一大会议才在会后进入材料核验和终审拦截。
- 已验证：
  - `/Users/macos/miniconda3/bin/python -m py_compile backend_full.py` 通过。
  - `npm run build` 通过。
  - 已重启 8000 后端服务，使新的默认会议性质生效。

## 按用户原型重排会中会议版式

- 问题判断：
  - 原会中页把参会人视频横条放在正文顶部，占用了会议工作区高度，导致实时转写、议题对照、会后生成三块都被挤压。
  - 用户给出的版式更合理：顶部左侧显示会议标题与状态，顶部右侧承载参会人横条；正文只处理会议本身。
- UI 调整：
  - 将参会人横条从 `meeting-call-shell` 正文中移到全局紧凑头部右侧。
  - 顶部右侧新增紧凑会议条：会议 ID、计时、录音状态、议题锁定状态、参会人席位。
  - 参会人卡片缩小为 74px 高的紧凑视频格，头像、姓名、说话中状态仍保留。
  - 正文区域删除原来的参会人横条和窗口状态栏，直接进入“正在讨论 + 三栏工作区”。
  - 会中正文保留：正在讨论通栏、实时转写、本次会议议题、结束后生成、底部控制栏。
- 产品效果：
  - 1080p 下正文释放出一整段高度，实时转写和议题区更接近真实会议软件工作台。
  - 会议主线更清晰：先看“本次会议正在讨论什么”，再看“谁说了什么”，再看“议题有没有讨论到”。
- 已验证：
  - `npm run build` 通过。
