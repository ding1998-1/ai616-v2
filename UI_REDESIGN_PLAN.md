# AI 会议系统 UI 重构约束

## 产品主线

- 会议是系统唯一主入口。
- 议题是会议内部最重要的数据与操作单元。
- 待办负责推动确认、签署和落实。
- 知识默认以历史议题为检索结果。
- 三重一大是重大会议的治理模式，不是一级产品入口。
- 合同、快速审查和 OA 属于扩展能力，降低导航权重。

## 用户判断原则

每个页面在设计与实现前回答五个问题：

1. 用户此刻最想完成什么？
2. 页面是否只有一个明确的主要操作？
3. 用户能否在三秒内判断自己所处的会议、议题和阶段？
4. 操作成功或失败后，页面状态是否与后端事实一致？
5. 次要信息是否可以延后出现，而不是同时占据注意力？

## 全局信息架构

```text
会议
待办
知识

管理
  用户与角色
  制度资料
  扩展工具
```

## 视觉方向

- 气质：温润、精确、安静、可信。
- 暖灰背景配合白色内容层，避免大面积深色侧栏。
- 单一蓝色用于主要操作；状态色只表达状态。
- 使用 12、16、20 三档圆角，不堆叠卡片边框。
- 依靠留白、字重和位置建立层级，不依赖大量标签。
- 动效只服务于进入、切换、保存、录音和错误回滚。

## 第一批改造范围

- 登录页从“合规系统”改为“AI 会议工作台”。
- 全局菜单收敛为会议、待办、知识、管理。
- 三重一大和业务工具移入会议内部或管理子菜单。
- App Shell 改为浅色、低噪声结构。
- 会议首页突出“继续当前工作”和“新建会议”。

## 不在第一批执行

- 不删除旧业务页面。
- 不修改数据库 Schema。
- 不迁移或删除现有会议、录音和转写。
- 不改变录音与 ASR 稳定性链路。

## 2026-08-20 实施进度

### 已完成

- 登录页和浏览器标题改为会议主导表达。
- 一级菜单收敛为会议、待办、知识、管理。
- 业务工具进入管理子菜单。
- App Shell 改为浅色、低噪声结构。
- 会议首页改为按“下一步行动”组织。
- 会议首页状态概览改为“正在推进 / 等待整理 / 已归档”三张轻量状态卡，并保留单一的“继续”主动作。
- 会议列表行支持键盘进入，编辑与归档动作增加明确悬浮提示，避免图标操作产生歧义。
- 创建会议页重排为“顶部 3 步 Stepper + 左侧事项/议题主工作区 + 右侧精简会议设置”。
- 创建会议只突出会议名称、日期/时间、待上会议题；会议名称按日期生成默认值，会议类型和文号收到更多设置，治理模式不在创建页展示。
- 事项导入收敛为“添加事项”主入口，模板、台账和收集链接统一放入更多导入方式；议题生成在同一个主工作区内完成。
- 创建页移除底部整条产品说明横幅，AI 提示只保留在生成议题动作附近。
- 创建页进一步移除显性的 01/02/03 步骤条，形成“今天要讨论什么 / 这场会议怎么开”两个工作面。
- 会议类型回到一级快捷选择，记住上次选择并自动带出预计时长；改变日期时，未手动修改的默认会议名称会同步更新。
- 创建期议题支持拖动排序、直接改名和删除；右侧只保留名称、类型、时间、参会人轻入口和预计时长。
- 登录页补充“记住账号”选项（仅保存账号名，不保存密码），并为减少动态效果的用户提供无障碍降级。
- 登录页补充“记住账号”选项（仅保存账号名，不保存密码），并为减少动态效果的用户提供无障碍降级。
- 会中议题列表、当前议题标题、AI 议题比对和标记改用正式议题。
- 正式议题切换增加加载锁、失败回滚和明确错误提示。
- 临时议题创建后刷新正式议题并尝试激活。
- 正式议题存在时，禁止在会中继续修改旧议题草稿。
- 文档相关入口改为 DOCX 预览、解析和下载，不再加载在线编辑器。
- 会中顶部改为“当前议题 + 录制状态 + 接入人数 + 讨论/议题计时”，治理模式不再占用高频操作位。
- 会中底部工具栏由九个同权动作收敛为录音、手机接入、参会人、会中备注、证据底稿和结束会议。
- 新增、临时新增、查看全部和编辑议题统一改为右侧抽屉；草稿支持拖动排序，新增前展示插入位置。
- 会后页面改为“会后整理 → 纪要确认 → 归档完成”三步进度；确认纪要只进入待归档状态，不再提前写成已归档。
- 归档页面改为“纪要与签字主内容 + 归档准备度”双栏，材料缺口显示完成比例和缺失数量。

### 下一步

- 对登录、创建会议和会中执行页完成 1920、1440、1366 与移动端视觉回归，消除遮挡、截断和嵌套滚动。
- 收敛创建页与会中页累计的样式覆盖，在不改变交互的前提下整理为单一生效版本。
- 将会后记录、决议与签署界面切换到正式议题子资源。
- 重做用户与角色管理页面。

## 会中到归档流程重构约束

### 线性主流程

```text
会中执行 → 会后整理 → 纪要确认 → 归档完成
```

- 会中结束只能进入会后整理，不能直接把会议写成“已归档”。
- 纪要确认后进入待归档状态；只有最终归档动作成功后才显示“已归档”。
- 每一步只保留一个主动作，并明确说明动作完成后会进入哪里。

### 会中执行

- 实时记录与议题进度同屏展示，当前议题必须是视觉主角。
- 会中主工具栏只保留录音、手机接入、参会人、备注和证据底稿等高频动作。
- 会议治理模式由会议类型和系统规则决定，不在会中工具栏提供即时切换。
- 录制状态、接入人数、讨论时长和当前议题计时必须持续可见。

### 议题管理

- 新增、临时新增、编辑和查看全部议题统一使用右侧抽屉，不再使用阻断式弹窗或浏览器输入框。
- 正式议题锁定后只允许新增临时议题；保存后立即切换并开始记录，保留来源与时间。
- 草稿议题支持在抽屉内直接编辑和排序，提交前展示插入位置预览。

### 会后整理与归档

- 会后页面固定展示“会后整理 / 纪要确认 / 归档完成”三步进度。
- AI 生成进度、会议记录、待办与决议在会后整理阶段完成；签字与材料完整性在归档确认阶段集中核对。
- 归档页面改为“主要内容 + 归档准备度”双栏，材料清单显示完成比例和缺口，不再使用三张同权重大卡片。
- 最终主动作统一为“确认归档并完成”。

## 创建会议：工作面而非表单

### 页面结构

```text
左侧 70%：今天要讨论什么？
  粘贴微信、表格、事项或文件
  AI 在同一工作区整理议题
  议题可排序、改名、删除和新增

右侧 30%：这场会议怎么开？
  名称、类型、时间、参会人
  弱化展示预计时长与更多设置
  创建会议
```

- 移除创建页显性的 01/02/03 步骤条，流程存在于系统状态中，不要求用户理解系统步骤。
- 会议名称默认按日期生成，用户直接修改，不显示空白必填框。
- 会议类型作为一级高频信息，使用单击快捷选择；默认记住当前用户上一次选择。
- 一级只展示普通会议、办公会、专题会和“更多”；项目会、评审会及治理类会议收进“更多类型”。
- 日期时间直接显示，默认取当前时间附近的整点。
- 参会人允许暂不指定；创建后仍可从用户库选择或发送手机邀请。
- 预计时长由会议类型提供默认值：普通会议 60 分钟、办公会 90 分钟、专题会 60 分钟、项目会 45 分钟。
- 会议文号、地点、归档分类、治理模式、纪要模板、录音规则、权限、自动生成开关和保密级别进入更多设置。
- 页面主动作统一为“创建会议”；创建成功后再依据会议状态进入对应页面。
