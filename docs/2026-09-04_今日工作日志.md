# 2026-09-04 AI 会议系统 2.0 工作日志

## 今日完成

### AI 提炼建议与人工支持

- 建立 `ai_suggested / human_supported / rejected` 三态审核模型。
- AI 新生成内容默认标记为“AI 提炼建议”，不再自动升级为正式会议成果。
- 增加单条“支持并采用、编辑后支持、不采用”接口。
- 编辑后支持同时保留 AI 原建议、人工修改文本、审核人、审核时间和原因。
- 人工支持内容设置锁定，AI 重新生成时不覆盖已审核结果。
- 后端增加核验权限检查，限制管理员、会议创建人、主持人、记录人、秘书及指定审核角色操作。
- 现有“一键确认纪要”兼容为批量支持有依据内容，并写入审核记录。

### Word 输出

- 增加内部审阅版与正式发布版两种模式。
- 内部审阅版允许 AI 建议和人工支持内容进入，供核验使用。
- 正式发布版只输出人工支持内容。
- 正式决议继续要求 `decision/principle` 类型与有效证据。
- 正式待办继续过滤“待确认、待定、未知”等占位值。
- 保留此前证据语义核验、风险去重、空待办表处理和依据排版修复。

### 前端

- 会后纪要页改为“AI 提炼建议与人工支持”审核区。
- 增加全部、待人工确认、人工支持、已否决状态筛选。
- 议题、决议和待办均可逐条审核。
- Word 模板弹窗增加“下载内部审阅版”和“下载正式发布版”。

## 验证结果

- 后端核心回归：48 项通过。
- 前端生产构建：通过，9887 个模块完成构建。
- 全量测试在本机依赖组合下存在 FastAPI/Starlette 版本不兼容的既有收集错误，与本次业务代码无关；已在 44 的正式 conda 环境完成应用导入和路由注册检查。
- 44 真实历史会议只读冒烟：成功读取 2026 年 9 月 1 日会议，兼容 19 条纪要、2 条决议、2 条风险和 5 条待办，并成功生成内部审阅版 Word。

## 44 部署

- 备份目录：`/home/ai/文档/ai616_backups/ai_review_20260904_093639`
- 项目目录：`/home/ai/文档/ai616`
- 仅重启 `ai-compliance`（8002），未操作 GPU0、8088、8091、8092。
- 服务状态：`active + enabled`。
- 健康检查：299 场会议、3251 条转写，数据库大小 43487232 字节，数据未减少。
- Web 首页返回 200；新前端资源已加载。
- GPU0 主模型进程及两路 ASR 进程保持原 PID 和显存占用。

## 后续真实验收

- 选择一场新会议，逐条完成“支持并采用 / 编辑后支持 / 不采用”。
- 下载内部审阅版核对 AI 建议和人工支持状态。
- 下载正式发布版确认未经人工支持内容完全不出现。
- 完成签字与归档全流程回归。
