# 2026-07-02 修改记录

## 今日目标

评估会议转写链路质量，修复 Whisper 终审、DeepSeek 客户端、录音文件损坏等关键问题。

---

## 已完成修复 ✅

### 1. Whisper 模型替换

- **问题**：faster-whisper (CTranslate2) 模型未下载到 `data/models/whisper/`，HuggingFace 网络不可达，Whisper 输出"喂喂喂"
- **修复**：改用 openai-whisper + ModelScope 已缓存的 `.pt` 模型（2.9GB）
- **文件**：`backend/whisper_transcribe.py` — 完全重写
- **效果**：同一段音频从"喂喂喂"→ 2070+ 字完整可读文本

### 2. DeepSeek 客户端并发 bug

- **问题**：`with _get_httpx_sync() as client:` 用 `with` 语句打开全局单例 HTTP 客户端，退出时关闭它，导致后续调用报 "Cannot open a client instance more than once"
- **修复**：改为 `client = _get_httpx_sync()` 直接使用，不关闭
- **文件**：`backend/llm_client.py` + `backend_full.py`（两处）

### 3. _llm_refine_text 函数修复

- **问题**：引用不存在的 `_get_deepseek_client` 函数，静默失败
- **修复**：改用全局 `llm` 单例的 `_agenerate` 方法
- **文件**：`backend_full.py`

### 4. Whisper 纪实 docx 使用高精度转写

- **问题**：docx 生成使用实时转写垃圾数据，不使用 Whisper 终审文本
- **修复**：`_generate_whisper_meeting_docx` 提取 Whisper 事件文本，替换纪实区内容
- **文件**：`backend_full.py`
- **效果**：纪实区显示 "Whisper 终审（AI 高精度转写）" 标注的高质量文本

### 5. Whisper 文本长度计算 bug

- **问题**：摘要显示 "247 段，3 字"，whisper_full_text 未从 segments 正确拼接
- **修复**：改为取最长的 Whisper 事件文本，不足 100 字时从 segments 拼接
- **文件**：`backend_full.py`

### 6. 录音文件损坏处理

- **问题**：多个会议录音文件损坏（3/4 或 2/4 损坏），ffmpeg 合并失败导致 Whisper 从未运行
- **修复**：Whisper 终审自动检测并跳过损坏文件，只处理有效文件
- **文件**：`backend_full.py`

### 7. Whisper 文件格式兼容

- **问题**：Whisper 只扫描 `*.webm`，但实际录音可能是 MP4 格式
- **修复**：同时扫描 `.webm`、`.mp4`、`.m4a` 格式
- **文件**：`backend_full.py`

### 8. PC 端新增 Whisper 触发按钮

- **问题**：Whisper 终审只能在录音停止时自动触发，无法手动触发
- **修复**：PC 端会后整理页面新增 "生成 Whisper 纪实" 按钮
- **文件**：`frontend/src/pages/MeetingComplianceWorkflow.jsx`

### 9. 录音停止逻辑优化

- **问题**：MediaRecorder 停止时最后 0-3 秒音频可能丢失，导致文件损坏
- **修复**：等待最后一次 `ondataavailable` 完成后再创建 Blob
- **文件**：`frontend/src/pages/MobileMeetingRecorder.jsx`

### 10. ASR 后端切换

- **问题**：手机端默认使用本地 Paraformer（质量差），DashScope 云端未被使用
- **修复**：默认值为 `dashscope`，用户需在手机端手动切换
- **状态**：DashScope 云端 ASR 已验证可用

---

## 已完成修复 ✅（补充）

### 11. 录音文件损坏根因修复

- **问题**：`MediaRecorder.start(3000)` 每 3 秒分段产生 fragmented MP4，部分片段丢失 moov atom
- **修复**：改为 `recorder.start()` 不带 timeslice，录音结束后一次性获取完整文件
- **文件**：`frontend/src/pages/MobileMeetingRecorder.jsx`、`frontend/src/pages/MeetingComplianceWorkflow.jsx`

### 12. DashScope ASR 强制默认

- **问题**：用户 localStorage 存了 `qwen`，无法切回云端 ASR
- **修复**：初始化时强制写入 `dashscope` 到 localStorage，"本地识别"按钮永久禁用
- **文件**：`frontend/src/pages/MobileMeetingRecorder.jsx`

### 13. DeepSeek API 重试逻辑

- **问题**：503 直接抛异常，纪要降级到本地规则
- **修复**：`_generate` 方法添加指数退避重试（503/429/502/504），最多 3 次；`_llm_refine_text` 添加异常日志
- **文件**：`backend/llm_client.py`、`backend_full.py`

### 14. Whisper 终审归档自动触发

- **问题**：`_run_whisper_review` 是 fire-and-forget，后端重启或快速归档导致 Whisper 未运行
- **修复**：提取 `_run_whisper_review_for_meeting` 为独立函数；归档时检查 Whisper 是否已完成，未完成则自动触发
- **文件**：`backend_full.py`

### 15. 转写时间戳按 clientTime 排序

- **问题**：多设备同时提交转写，按 server_time 排序导致时间乱序
- **修复**：DB 查询改用 `COALESCE(client_time, server_time)` 排序；前端排序同步修改
- **文件**：`backend/db.py`、`frontend/src/pages/MeetingComplianceWorkflow.jsx`

### 16. 本地 Paraformer ASR 质量问题

- **状态**：通过修复 12 解决 — 强制 DashScope 云端 ASR，本地 Paraformer 不再被使用

---

## 技术债务

| 项目 | 说明 |
|------|------|
| `backend_full.py` 行数 | 7900+ 行，单文件过大 |
| DeepSeekThinkingLLM 重复定义 | `backend/llm_client.py` 和 `backend_full.py` 各有一份 |
| `_is_garbled_asr` 未集成 | 乱码检测函数存在但未在纪要生成前过滤垃圾转写 |
| Whisper 模型内存占用 | openai-whisper 占 ~3GB GPU，与 ChromaDB/text2vec 共享 GPU0 |

---

## 当前系统状态

| 组件 | 状态 | 说明 |
|------|------|------|
| 后端服务 | ✅ 运行中 | PID 791749，端口 8002 |
| ASR 服务器 | ✅ 运行中 | Paraformer-Large，端口 8091，cuda:1 |
| DashScope ASR | ✅ 可用 | WebSocket 连接正常，task-started 正常 |
| Whisper 终审 | ✅ 可用 | openai-whisper + ModelScope 模型，GPU0 |
| DeepSeek 纪要 | ✅ 已添加重试 | 指数退避，最多 3 次（503/429/502/504） |
| 录音文件质量 | ✅ 已修复 | 去掉 timeslice，一次性获取完整文件 |
| 实时转写质量 | ✅ 已修复 | 强制 DashScope 云端 ASR |
