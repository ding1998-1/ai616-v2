# 2026-07-01 本地音频改进计划

## 背景

当前"本地识别"链路：
```
手机麦克风 → AudioContext 16kHz → ScriptProcessor(8192) → AGC
  → WebSocket → backend_full.py meeting_asr_qwen_websocket()
  → HTTP /api/chunk → FunASR Paraformer-Large (GPU 1, port 8091)
  → _fuzzy_lcp 增量提取 → type:"final" → 前端气泡
```

主要问题记录在今天（06-30）调试中已暴露，明天改进的具体方向如下。

---

## 一、要改的问题

### 1.1 ScriptProcessor 已废弃 → 换 AudioWorklet
**现状**：`frontend/src/pages/MobileMeetingRecorder.jsx:526`
```js
const processor = audioContext.createScriptProcessor(8192, 1, 1);
```
- `ScriptProcessor` 在主线程运行，iOS Safari / Chrome 移动版高负载时会丢帧
- Chrome 已标记废弃，Safari 17+ 随时可能停支持

**改法**：
1. 新建 `frontend/public/pcm-processor.js`（AudioWorklet 模块）
2. `audioContext.audioWorklet.addModule('/pcm-processor.js')`
3. 用 `AudioWorkletNode` 替换 `createScriptProcessor`
4. 帧大小从 8192 改为 4096（约 256ms），减少延迟

**文件**：`MobileMeetingRecorder.jsx` 行 510-555

---

### 1.2 AGC 逻辑过于激进
**现状**：`MobileMeetingRecorder.jsx:69-83`
- 目标 RMS 20%，最大 3 倍增益
- 静音段会被放大到噪声，Paraformer 容易把背景音误识别成字

**改法**：
- 加噪声门（noise gate）：RMS < 2% 时不增益、不发送
- 增益上限从 3x 降到 2x
- 连续 300ms 低于噪声门 → 发送静音标记（让后端触发 VAD flush）

**文件**：`MobileMeetingRecorder.jsx` 行 69-83，`_applyAGC` 函数

---

### 1.3 音频打包批次偏大，初始延迟明显
**现状**：`AUDIO_BATCH_BYTES = 16000`（约 500ms 才发一次）
- Paraformer 批量识别间隔本身就是 1s，再加 500ms 缓冲 → 感知延迟接近 1.5s

**改法**：
- `AUDIO_BATCH_BYTES` 改为 8000（约 250ms 发一次）
- 后端 `BATCH_INTERVAL` 保持 1s 不变（Paraformer 吃的是累积音频，发送频率不影响识别）

**文件**：`MobileMeetingRecorder.jsx` 行 527

---

### 1.4 Paraformer 批量识别重复文本（"鬼畜叠加"残余）
**现状**：`backend_full.py` `meeting_asr_qwen_websocket()`
- `_fuzzy_lcp` 用于增量提取，但偶尔 Paraformer 返回整段历史重播时 LCP 会提取出重复片段

**改法**：
- 在后端加"上次发送文本 hash"缓存，新文本与上次 diff < 3 字则跳过
- 或在 FunASR 服务端（`/home/ai/桌面/funasr_asr_server.py`）每次只传**增量音频**而非全量累积

**文件**：`backend_full.py` 行 3781 附近 `meeting_asr_qwen_websocket()`，`funasr_asr_server.py`

---

### 1.5 WS 断连后重连丢失当前句
**现状**：重连成功后 `_prev_text` 和 `_session` 被重置，重连前已识别但未发送的文字消失

**改法**：
- 断连前把 `_pending_text` 暂存到后端 session 内存
- 重连握手时带 `session_id=xxx&resume=1`，后端恢复 pending 文本继续输出

**文件**：`backend_full.py` `meeting_asr_qwen_websocket()` 的 disconnect 处理段，`MobileMeetingRecorder.jsx` 重连逻辑行 627-635

---

## 二、优先级

| 优先级 | 项目 | 预计耗时 |
|--------|------|---------|
| P0 | 1.2 噪声门（直接影响识别质量） | 30min |
| P0 | 1.3 发包间隔降到 250ms（降延迟） | 5min |
| P1 | 1.4 重复文本过滤 | 1h |
| P1 | 1.5 断连保留 pending 文本 | 1h |
| P2 | 1.1 AudioWorklet 替换 ScriptProcessor | 2h |

---

## 三、关键文件速查

| 文件 | 路径 | 关键行 |
|------|------|--------|
| 前端录音 | `frontend/src/pages/MobileMeetingRecorder.jsx` | 510-580（音频采集），69-83（AGC） |
| 后端 WS 处理 | `backend_full.py` | 3781-4022（`meeting_asr_qwen_websocket`） |
| FunASR 服务 | `/home/ai/桌面/funasr_asr_server.py` | 全文 217 行 |
| 后端 ASR 客户端 | `backend/qwen_asr_client.py` | 全文 129 行 |
| ASR 服务 systemd | `paraformer-asr.service` | ExecStart 行 |

---

## 四、验证方式

改完每项后：
```bash
# 重启后端
sudo systemctl restart ai-compliance

# 重启 ASR（如果改了 funasr_asr_server.py）
sudo systemctl restart paraformer-asr

# 看实时日志
journalctl -u ai-compliance -f | grep -E "final|ASR|error"
journalctl -u paraformer-asr -f

# 前端 build
cd /home/ai/文档/ai616/frontend && npm run build
```

用手机打开 `http://127.0.0.1/?page=mobile_recorder`，对着麦克风说话，
观察：① 延迟是否 <1s ② 是否有重复字 ③ 静音时是否无误识别输出。
