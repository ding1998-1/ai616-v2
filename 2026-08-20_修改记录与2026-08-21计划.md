# 2026-08-20 修改记录与 2026-08-21 计划

## 一、今日结论

- 前端主线已经收敛到“会议为主、议题为核心”：登录页、创建会议页和会中执行页均按最新参考图完成结构性改造。
- 创建会议不再表现为多步骤填表，而是“议题工作区 + 会议信息栏”；会议名称、类型、时间持续可见，参会人允许暂不指定，其余低频配置进入更多设置。
- 今日修改已通过前端生产构建和多分辨率浏览器检查；创建页此前出现的遮挡、嵌套滚动及标签变形问题已定位并修正。
- 后端模块拆分已有较多代码和测试记录，但生产入口尚未完成切换；今天没有执行数据库迁移，也没有向 `192.168.66.44` 部署。
- OnlyOffice 不再属于目标方案。前端文档链路按 DOCX 预览、解析和下载处理；今天未物理删除相关历史文件或依赖。

## 二、今日完成内容

### 1. 创建会议页

- 按参考布局重构为左右双栏：
  - 左侧上方：本次会议议题。
  - 左侧下方：讨论内容与事项导入。
  - 右侧：会议名称、会议类型、会议时间、参会人员、预计时长及创建动作。
- 会议名称继续支持自动生成和直接修改，不再要求从空输入框开始填写。
- 会议类型改为一级快捷选择，保留普通会议、办公会、专题会和更多类型。
- 参会人员改为可选项，允许“暂不指定”，后续仍可邀请或添加。
- 保留 AI 生成议题、手动添加议题、编辑、删除和排序能力。
- 保留微信/文本、表格、文件等事项导入能力，低频入口集中到“更多导入方式”。
- 修复事项来源区域和议题工作区相互遮挡、内部滚动截断、下方内容不可见的问题。
- 修复手动议题右侧“手动”标签被通用选择器压缩成圆形的问题，固定其文字、尺寸和胶囊形态。

### 2. 登录页

- 按参考图完成桌面端双栏布局：左侧品牌叙事与能力说明，右侧登录卡片。
- 登录按钮由参考图的黑色改为系统主题蓝色。
- 保留账号密码登录、显示密码、记住账号、忘记密码提示、其他登录方式和协议入口。
- 保留现有移动端注册/接入逻辑，桌面视觉改造未替换原有登录接口。
- 调整卡片尺寸、页面留白、标题比例和响应式布局，使 1440 与 1920 宽度下均无横向溢出。

### 3. 会中执行页

- 按参考图重构会议标题与状态区，集中展示会议名称、类型、时间、编号和议题状态。
- 参会人员单独形成横向接入区，突出主持人、签到状态、待接入席位和添加参会人入口。
- 当前议题、接入人数、已讨论时间、议题计时及开始计时放到同一状态栏。
- 主体改为左右工作面：左侧实时记录/纪要/待办，右侧议题管理。
- 补充“等待手机端实时转写”空状态插图和说明。
- 底部工具栏收敛为开始录音、手机接入、参会人、会议备注、证据底稿和结束会议。
- 右侧议题面板改为纵向弹性填充，减少大块无意义空白。

### 4. 设计与交互约束同步

- 会议继续作为最大容器，议题作为核心业务与检索单元。
- 三重一大、治理模式和业务工具不占据普通用户的一级操作视线。
- 修正旧 UI 计划中“将创建会议改成四步流程”的过时描述，明确继续采用当前双工作面方案。
- 创建页主动作统一为“创建会议”，创建成功后由系统依据会议状态进入对应页面。

## 三、今日验证记录

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 前端生产构建 | 通过 | `npm run build` 完成；仅保留 Vite CJS 弃用和 `vendor-antd` 体积过大的既有警告 |
| 创建页 1920×1080 | 通过 | 无横向溢出；左右工作面、议题列表与讨论内容可见 |
| 创建页 1440×900 | 通过 | 无横向溢出；未再出现来源卡片遮挡议题工作区 |
| 登录页 1920×1080 | 通过 | 双栏比例正常，页面无横向和纵向溢出 |
| 登录页 1440×900 | 通过 | 登录卡片完整可见，无横向溢出 |
| 手动标签计算样式 | 通过 | 文字为“手动”，约 44×24 像素，胶囊形态正常 |
| Git 补丁格式检查 | 通过 | 未发现空白符错误 |
| 后端完整测试 | 沿用既有记录 | 今日未重新执行后端全量测试，不将既有结果冒充本轮验证 |
| 远程后端部署 | 未执行 | `192.168.66.44` 仅作为后续联调和部署目标 |

## 四、当前遗留问题

### P0：明日优先处理

1. 会中执行页最后一次“右侧议题面板全高”调整已通过构建，但仍需在 1920、1440 和 1366 宽度重新截图核对最终比例。
2. 创建页经历多轮覆盖式改造，CSS 中存在历史版本规则叠加；功能已经可用，但继续追加覆盖会增加回归风险。
3. 需要使用真实后端数据覆盖空状态、单议题、多议题、长标题、多参会人和长转写内容，确认不会再次出现遮挡。
4. 需要串联“创建会议 → 进入会中 → 手机接入/转写 → 结束会议 → 会后整理”完整流程，确认页面状态与后端状态一致。

### P1：功能主线

1. 用户与角色管理页尚未按新的视觉体系重做；需覆盖一人多角色、组织范围、会议类型权限和账号状态。
2. 会后记录、决议、版本、签字与归档仍需完全落到正式议题子资源，避免继续把结果笼统挂在会议层。
3. 后端生产入口仍需完成剩余路由拆分和装配，当前不能直接替换远程生产入口。
4. OnlyOffice 相关旧代码、依赖和说明仍可能存在；任何物理删除需单独核对引用后再执行。

### P2：质量优化

1. 拆分体积过大的 `vendor-antd` 包，降低首屏加载成本。
2. 补齐键盘焦点、错误提示、加载状态、空状态和低分辨率适配。
3. 为登录、创建、会中和会后关键页面建立固定截图回归基线。

## 五、2026-08-21 执行计划

### 固定联调地址

- 前端会议室：`http://localhost:3000/`
- 远程后端：`http://192.168.66.44:8002`
- `3001/8000` 属于本机其他容器或旧服务，不作为本项目联调结果。

### 2026-08-21 上午执行进度

- [x] 将 Vite 开发端口从残留的 `3001` 统一为会议前端端口 `3000`。
- [x] 使用 `BACKEND_URL=http://192.168.66.44:8002` 启动前端并完成真实登录。
- [x] 远程 `/health` 返回正常：数据库、ChromaDB、LLM、DashScope ASR、Qwen ASR 均为 `ok`。
- [x] 完成会议首页、创建会议页和会中执行页的 1366×768 真实浏览器检查。
- [x] 修复会中议题“判断中 / 未命中”状态标签越出卡片的问题；验证标签完整位于议题卡内部。
- [x] 移除创建页议题卡自身的内部滚动，只保留左侧主工作区单一纵向滚动，消除嵌套滚动。
- [x] 前端生产构建通过，保留 `vendor-antd` 体积过大的既有警告。
- [x] 修复新建会议草稿的无意义请求：草稿尚未保存时不启动详情、转写、SSE 和 markers 轮询；真实浏览器复测错误列表为空。
- [ ] 远程后端尚未包含正式议题 `/api/meetings/{meeting_id}/agendas` 路由；前端当前使用兼容降级数据，部署前需完成后端入口切换。

### 第一阶段：视觉回归与样式收口

1. 对登录页、创建会议页、会中执行页分别检查 1920×1080、1440×900、1366×768。
2. 检查 125% 浏览器缩放、长文本、五个以上议题、十个以上参会人和长转写记录。
3. 合并创建页与会中页的历史 CSS 覆盖，保留单一生效规则，不改变现有业务行为。
4. 建立关键页面截图和尺寸记录，避免“构建通过但页面遮挡”的情况再次发生。

### 第二阶段：前后端完整联调

1. 配置前端开发代理访问 `192.168.66.44` 的测试后端，不在前端代码中写死远程地址。
2. 验证登录、用户列表、创建会议、正式议题、当前议题切换、转写、结束会议和会后整理接口。
3. 对接口失败、超时、断线重连、重复提交和空数据逐项检查 UI 反馈。
4. 记录远程环境与本地代码的接口差异，先修兼容问题，再准备部署清单。

### 第三阶段：用户与角色管理

1. 设计并实现用户列表、用户详情、组织归属、账号状态和角色分配。
2. 支持同一用户拥有股东、董事、高管等多个角色。
3. 支持按会议类型、组织范围和议题密级配置可见与操作权限。
4. 用户选择器与创建会议页的“添加参会人员”共用同一数据源和组件。

### 第四阶段：会后与后端收尾

1. 将会议记录、决议、版本和签字状态按 `agenda_id` 展示和提交。
2. 完成剩余后端路由装配、OpenAPI 对照和全量测试。
3. 输出远程部署前检查表：环境变量、依赖、数据库兼容、备份、回滚和健康检查。
4. 未经单独确认，不执行数据库迁移、远程生产部署或 OnlyOffice 文件物理删除。

## 六、明日验收标准

- 三个核心页面在 1920、1440、1366 宽度均无遮挡、截断、横向滚动和嵌套滚动死区。
- 创建会议首屏明确呈现议题与会议信息，不重新引入步骤条或低频治理字段。
- 会中页面右侧议题区与左侧内容区高度协调，底部工具始终可达。
- 使用真实后端完成一次从登录到会后整理的闭环，并记录每一步接口结果。
- 用户与角色管理至少完成可用的列表、创建/编辑、角色分配和权限范围展示。
- 前端生产构建与后端测试均通过；既有警告和未通过项必须单独列出，不能隐藏。

## 七、交接状态

| 对象 | 状态 | 备注 |
| --- | --- | --- |
| 前端代码 | 已修改并验证 | 登录、创建会议、会中执行为今日主改范围 |
| 本地运行界面 | 已验证 | 已做 1920 与 1440 核心检查 |
| 后端模块化代码 | 进行中 | 已有既有测试记录，生产入口仍未完成切换 |
| 远程环境 | 未变更 | 今日未部署、未迁移数据 |
| 项目文档 | 已更新 | 本文及 `UI_REDESIGN_PLAN.md` 已同步当前方案 |
| Git 工作区 | 未提交 | 保留现有用户与历史修改，未清理、未重置 |

## 八、相关文件

- `frontend/src/pages/LoginPage.jsx`
- `frontend/src/pages/MeetingComplianceWorkflow.jsx`
- `frontend/src/pages/MeetingComplianceWorkflow.css`
- `UI_REDESIGN_PLAN.md`
- `BACKEND_REFACTOR_SPEC.md`
- `AI会议系统_前后端重构与部署完整清单.md`
- `2026-08-20_代码审查记录.md`
