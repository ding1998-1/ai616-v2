# 2026-08-25 工作记录与部署前清单

## 今日目标

1. 收口会议/议题统一搜索。
2. 恢复可重复的项目测试环境。
3. 完成核心测试、真实接口和数据库迁移验证。
4. 形成 `192.168.66.44` 部署前清单。

## 已完成

- 确认项目前端端口 `3000`、模块化后端端口 `8002`。
- 定位本机全局 Python 环境错配：`FastAPI 0.115.0 + Starlette 1.6.0`。
- 切换到项目 `.venv`：`FastAPI 0.135.1 + Starlette 0.52.1`，与锁定文件一致。
- 在项目虚拟环境补齐 `uvicorn 0.42.0`、`pytest-asyncio 1.4.0` 和 `python-multipart 0.0.22`。
- 将 `pytest-asyncio` 补入依赖清单与锁定文件。
- 修正 5 项过时测试契约：
  - 受保护的事项类型、演示资源、制度规则、规则图库改为携带管理员鉴权。
  - 不存在的会议转写查询按真实契约断言 `404`。
- 核心测试集：`114 passed`。
- 统一搜索专项测试：`3 passed`。
- 新增旧库兼容测试：只有 `meeting_agenda_drafts` 的旧会议会自动物化为正式议题，随后进入搜索索引。
- 真实 HTTP 验证：`/api/meetings/search` 返回 `200`，议题结果的所属会议绑定全部有效。
- 前端生产构建完成：`9886` 个模块转换成功，产物已生成到 `frontend/dist`。
- 本地健康检查通过：后端数据库状态 `ok`，当前读取到 `60` 场会议、`59` 条转写；前端首页返回 `200`。
- 完成登录态 `1920×1080` 浏览器实测，会议列表与统一搜索无横向溢出和遮挡。
- 修复议题搜索跳转缺陷：
  - 草稿会议点击议题结果后，正式议题会回填到创建工作区并高亮目标议题。
  - 已创建会议点击议题结果后，只打开议题管理并定位目标，不会修改后端当前议题。
- 修复后再次完成前端生产构建，议题搜索专项测试保持 `3 passed`。
- 已启动本地服务：
  - 前端：`http://127.0.0.1:3000`
  - 后端：`http://127.0.0.1:8002`

## 已知非阻断项

- 两个根目录 Chroma 冒烟脚本需要整套 `langchain-chroma` / embedding 重依赖，不影响会议核心 API 和当前测试集。
- 前端 `vendor-antd` 产物约 `1.46 MB`，构建仅提示体积警告；后续应单独做按需拆包，不阻断本次功能验收。
- Pydantic 旧 `Config` 写法有弃用警告，不影响本次运行。
- 用户服务的一个单元测试使用 12 字节测试密钥，产品启动已强制需要环境密钥。

## 本地验收待完成

- [x] 登录后在 1920×1080 下搜索会议。
- [x] 搜索议题并确认“属于：XX会议”文案不溢出。
- [x] 点击议题结果，确认打开所属会议且只高亮定位，不自动切换当前议题。
- [x] 复测新建会议、会议列表、会中页面的纵向滚动。

## `192.168.66.44` 部署前清单

- [ ] 备份 44 的代码目录、`data/app.db` 和当前运行配置。
- [ ] 对比 44 的 Git/文件差异，禁止直接覆盖 44 独有数据和配置。
- [ ] 确认 44 使用项目虚拟环境和 `requirements_frozen.txt`。
- [ ] 由部署环境注入不少于 32 字节的 `APP_AUTH_SECRET`，不写入代码和日志。
- [ ] 将生产入口确认为 `backend.main:app`。
- [ ] 先在备份库副本执行启动迁移，核对会议数、议题数和搜索文档数。
- [ ] 运行核心测试和健康检查。
- [ ] 切换服务后验证登录、会议列表、议题搜索、会中录音、转写、纪要和归档。
- [ ] 保留可回滚的旧服务目录和数据库备份。
