# 2026-08-26 会议界面与纪要确认页 UI 重构计划

## 一、目标与边界

- 按参考图重构“会中工作台”和“纪要确认”页面，桌面端以 1920×1080 为基准基本 1:1 还原信息层级、布局比例、间距和操作位置。
- 保留现有会议、议题、录音、实时转写、临时议题、结论、待办、Whisper、签字确认和归档功能。
- 本轮不修改数据库 Schema，不改后端接口，不调整 ASR、Whisper、LLM 和归档业务规则。
- “会议是容器、议题是核心单元”保持不变；会中页面右栏继续以议题驱动记录、结论和待办。

## 二、实施结构

### 1. 会中工作台

- 顶栏：会议名称、会议 ID、会议状态、会议计时、通知、设置、全屏和结束会议。
- 参会人区：横向席位卡，明确主持人、在线、正在发言、待接入和添加参会人状态。
- 主内容左栏：实时会议记录、搜索、智能摘要入口、发言人、时间和实时发言状态。
- 主内容右栏：会议议题、当前议题进度、议题列表、临时议题、讨论结论和待办事项。
- 底部工具栏：录音状态、手机接入、参会人、会中备注、证据库和 AI 助手。
- 现有“会议纪实 / 会议纪要 / 待办事项”页签不再占据会中主视觉；对应能力映射到右栏和底部工具栏。

### 2. 纪要确认页

- 顶部会议摘要：会议名称、类型、时间、返回列表和记录完整度。
- 流程条：会后整理 → 纪要确认 → 归档完成。
- 左侧主栏：纪要概览、AI 会议纪要、会议摘要、会议决议、待办事项和讨论要点。
- 右侧固定栏：Whisper 音频播放器、时间轴转写和查看全部入口。
- 底部操作：返回、确认纪要、进入归档。
- 保留全员签字未完成时禁止归档的后端拦截和前端状态提示。

## 三、代码拆分

- 从 `MeetingComplianceWorkflow.jsx` 提取纯展示组件：
  - `MeetingLiveWorkspace.jsx`
  - `MeetingMinutesConfirmation.jsx`
  - `MeetingParticipantStrip.jsx`
  - `MeetingAgendaPanel.jsx`
  - `WhisperTranscriptPanel.jsx`
- 页面容器继续持有数据请求、业务状态和事件处理函数，通过 props 注入展示组件。
- 新增独立样式文件，统一布局变量、卡片边框、字号、状态色和 1920×1080 密度。

## 四、功能映射与防回归

| 参考图控件 | 现有功能 | 验证要求 |
|---|---|---|
| 结束会议 | 现有结束会议确认流程 | 二次确认、停止录音、进入会后阶段 |
| 添加临时议题 | 现有临时议题抽屉/弹窗 | 新议题写入当前会议并立即出现在列表 |
| 添加结论 | 现有决议编辑 | 必须绑定当前 `agenda_id` |
| 添加待办 | 现有待办编辑 | 责任人、期限和议题归属不丢失 |
| 手机接入 | 现有二维码接入 | 扫码、账号绑定、录音回传保持可用 |
| 实时会议记录 | 现有实时转写/SSE 数据 | 发言人、时间和议题归属正确 |
| 重新生成纪要 | 现有生成接口 | 保留加载态、失败提示和结果刷新 |
| Whisper 面板 | 现有终审音频与转写 | 播放、时间轴、滚动和状态一致 |
| 确认纪要 | 现有纪要确认流程 | 保存版本和确认状态 |
| 进入归档 | 现有归档入口 | 未签齐时必须阻断 |

## 五、响应式规则

- 1920×1080：完整双栏，参会人横排，底部工具栏固定可见。
- 1440×900：维持双栏，缩小卡片间距和字号，不出现横向溢出。
- 1280×720：右栏缩窄，次要文字省略，核心按钮不折行、不遮挡。
- 小于 1100px：会中议题栏切换为抽屉；纪要 Whisper 栏切换为标签页，不强行压缩。

## 六、执行顺序

1. 冻结现有事件处理函数和接口调用清单。
2. 提取会中工作台组件并完成参考图布局。
3. 提取纪要确认组件并完成参考图布局。
4. 接回所有现有功能和状态。
5. 完成前端构建、静态检查和关键函数回归。
6. 启动本地前后端，在 1920×1080 使用真实登录态进行人工视觉测试。
7. 逐项测试会议、议题、录音、临时议题、待办、Whisper、确认和归档链路。
8. 输出截图与问题清单；修完后再次完整回归。
9. 获得生产部署确认后备份并发布到 44。

## 七、完成标准

- 两页首屏布局与参考图结构、比例和视觉层级基本一致。
- 1920×1080 无遮挡、无双滚动条、无内容截断、底部主操作始终可达。
- 所有原有按钮都有真实功能，不出现仅展示的假控件。
- 议题、记录、决议和待办保持 `meeting_id + agenda_id` 绑定。
- 构建通过，关键流程人工操作通过，生产发布前有可回滚备份。
