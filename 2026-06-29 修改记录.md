# 2026-06-29 AI 会议系统改造记录

## 一、Qwen3-ASR 集成（上午）

将语音识别从云端 DashScope Fun-ASR 切换到本地 Qwen3-ASR-1.7B。

### 新增服务

- **文件**: `qwen3_asr_server.py` (v2 并发版)，位置 `/home/ai/桌面/`
- **端口**: 8091，GPU 1
- **模型**: Qwen3-ASR-1.7B，vLLM backend
- **并发**: 线程锁 + asyncio.Semaphore，最多 8 人同时识别
- **启动**: `./start_qwen_asr.sh`，Conda 环境 `qwen3-asr`

### 后端集成

- 新增 `backend/qwen_asr_client.py` — 异步 HTTP 客户端
- 新增 `WS /api/meeting/asr/qwen/ws` — Qwen3-ASR WebSocket
- 新增配置 `QWEN_ASR_URL`, `ASR_BACKEND`
- 原有 `WS /api/meeting/asr/ws` (Fun-ASR) 完整保留

### 前端改造

- MobileMeetingRecorder.jsx: WS URL 切换 + UI 文字更新
- MeetingComplianceWorkflow.jsx: 移除 react-window、修复 React #310、URL 同步
- App.jsx: ErrorBoundary 增强、防缓存 meta

---

## 二、Interim/Final 协议重构（下午）

### 背景

参考 Google Cloud Speech、Deepgram、Zoom、Teams 的架构，将转写管道从"全量入库+事后去重"改为 **Suffix Alignment 协议**。

### 核心改动

```
旧协议: {"type": "result", "text": "大家好，今天我们讨论", "isFinal": true}
        {"type": "result", "text": "大家好，今天我们讨论一下预算", "isFinal": true}
        → text 重叠，导致多条重复

新协议: {"type": "final", "newText": "大家好，今天我们讨论", "fullText": "..."}
        {"type": "final", "newText": "一下预算", "fullText": "..."}
        → newText 不重叠，直接追加
```

### 修改文件

| 文件 | 改动 |
|------|------|
| `backend_full.py` Qwen ASR WS | `_compute_delta()` suffix alignment，发送不重叠 `newText` |
| `backend_full.py` chunk handler | 连续发言合并（同说话人3秒内 → UPDATE 同一条） |
| `MobileMeetingRecorder.jsx` | 适配 `type: "final"` + `newText`，保留旧协议兼容 |
| `MeetingComplianceWorkflow.jsx` | 桌面麦克风适配新协议；SSE 推送完整文本；SSE 断连自动重连 |

---

## 三、全面 Bug 修复（下午/傍晚）

分三批共修复 20+ 个 bug，涉及后端、前端、数据层、安全。

### 第一批：致命级

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 1 | 删除标记清空全部转写 | `backend_full.py:3354` | `_save_meeting_transcripts` → `_save_meetings` |
| 2 | 路径遍历任意文件读取 | `backend_full.py:3933` | 文件名白名单 + resolve 边界检查 |
| 3 | 明文密码 + 自动注册 | `backend/routes/auth.py` | PBKDF2-SHA256 哈希，移除自动注册，明文自动升级 |
| 4 | 锁对象重复导致同步失效 | `backend_full.py:1111` | 删除本地覆盖，统一用 `backend.config` 锁 |
| 5 | 28 个端点无认证 | `backend_full.py` 多处 | 全部添加 `_get_request_user(required=True)` |

### 第二批：高风险

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 6 | `item.changes.find/map` 崩溃 | `MeetingComplianceWorkflow.jsx` | `(item.changes \|\| [])` 防御 |
| 7 | `materialRows` 过时闭包 | `MeetingComplianceWorkflow.jsx` | 添加 `isMajorMeeting` 到 deps |
| 8 | 文件上传 DoS（全量读入内存） | `backend_full.py:203` | 流式读取 `max_bytes+1` 提前拒绝 |
| 9 | SSE 静默失败 | `backend_full.py` 多处 | 移除 `get_event_loop()` try/except |
| 10 | 跨 worker 缓存不一致 | `backend_full.py:6798` | workers: 4 → 1（SQLite 不适合多进程） |

### 第三批：中风险

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 11 | JWT 30天过期无吊销 | `backend/deps.py` | 24h 过期 + jti + iat + 角色变更即失效 |
| 12 | MobileRecorder 卸载后 setState | `MobileMeetingRecorder.jsx` | `mountedRef` 检查 |
| 13 | 独立页面缺少 ErrorBoundary | `App.jsx` | mobile-recorder / issue-collect 包裹 |
| 14 | 删除会议不清理转写 | `backend/db.py:432` | 添加 `DELETE FROM meeting_transcripts` |
| 15 | 转写去重全量加载 | `backend_full.py:3084` | 30秒窗口 + LIMIT 50 SQL 查询 |
| 16 | 重复常量定义 | `backend_full.py` | 删除本地 MAX_*/锁覆盖 |
| 17 | 会议注册字段不匹配 | `auth.py` + `LoginPage.jsx` | snake_case/camelCase 双兼容 + 密码哈希 |
| 18 | 验证错误显示 [object Object] | `LoginPage.jsx` | Pydantic 数组错误格式化 |
| 19 | 转写事件不含文本 | `backend_full.py` chunk handler | 事件 payload 同时保存 transcript |
| 20 | 合并时 SSE 推送不完整文本 | `backend_full.py` SSE publish | 推送 `record["transcript"]`（完整合并文本） |
| 21 | SSE 断连不重连 | `MeetingComplianceWorkflow.jsx` | 3 秒自动重连 |

### 其他修复

- **录音 vs 转写数据对比**: 确认 9 场会议转写文本永久丢失（Bug 1 导致 `meeting_transcripts` 全表清空）。已加固：事件中同时存文本，表清空后可恢复。
- **前端健康检测修复**: `useBackendStatus` 改用 `/health`（公开端点），避免认证拦截导致"后端不可达"误报。
- **安全重启脚本**: 创建 `restart_safe.sh` 处理端口占用问题。

---

## 关键命令

```bash
# 安全重启后端
sudo /home/ai/文档/ai616/restart_safe.sh

# 启动 Qwen3-ASR 服务
cd /home/ai/桌面 && ./start_qwen_asr.sh

# 重建前端
cd /home/ai/文档/ai616/frontend && npm run build

# 查看服务状态
curl http://127.0.0.1:8002/health
```

---

## 四、流式文本质量优化（傍晚/夜间）

### 4.1 登录页硬编码修复

- **问题**: `LoginPage.jsx` 硬编码 `'高新区二期厂房消防改造专题会'` 作为默认标题
- **修复**: 去掉硬编码默认值，QR 链接同时传递 `meeting` 和 `project` 参数

### 4.2 创建会议 ID 复用修复

- **问题**: `createMeeting()` 中 `recordId = currentMeetingId || createLocalMeetingId()`，URL 带入旧 ID 时新建会议覆盖旧会议
- **修复**: 新建会议始终用 `createLocalMeetingId()` 生成全新 ID

### 4.3 流式分段方案演进

**阶段一 — Suffix Alignment**（下午）：
- `_compute_delta()` 用 `startswith` 提取增量
- 问题: Qwen3-ASR 修订文本时全量重发导致重复

**阶段二 — difflib 增量提取**（下午）：
- 用 `difflib.SequenceMatcher` 精确提取新增部分
- 10 秒窗口 + Jaccard 相似度兜底合并

**阶段三 — LCP + 双轨断句**（最终方案）：
- `_fuzzy_lcp()` 容错 1-2 字同音修正
- 双轨提交: interim 实时预览 + final 完整句入库
- 800ms 绝对时间戳稳定检测（替代帧计数）
- 8 字以上完整句才触发审计

### 4.4 前端高可用

- ErrorBoundary: chunk 加载失败 3 次自动重试，超限强制刷新 + 状态保留到 localStorage
- 创建会议后自动弹出二维码
- 二维码弹窗显示会议名称供确认
- SSE 断连 3 秒自动重连

### 4.5 录音转写数据一致性

- 转写事件 payload 同步保存 `transcript` 文本（防止 `meeting_transcripts` 表清空后数据全丢）
- 删除会议时清理 `meeting_transcripts` 孤儿数据
- 确认 9 场旧会议转写文本永久丢失（Bug 1 导致），新数据已加固

---

## 五、流式文本分段方案最终版（夜间）

### 5.1 LCP + 双轨 + 时间戳

**最终方案**：
- `_fuzzy_lcp()` 容错 1-2 字同音修正
- 缓冲攒够 6 字整段提交（不再逐句拆碎片）
- 800ms 绝对时间戳稳定检测（替代帧计数）
- interim 实时预览 + final 整段入库

### 5.2 ASR 文本清洗管线

- `_clean_asr_text()` 规则预处理：语气词去重、标点归一化
- `_llm_refine_text()` LLM 语义规整（DeepSeek，可选）

### 5.3 前端体验优化

- 登录页去掉硬编码默认标题
- 新建会议 ID 不复用旧 ID
- 进入会中自动弹二维码，弹窗显示会议名
- SSE 断连 3s 自动重连
- ErrorBoundary chunk 失败自动重试 + 状态保留

### 5.4 数据安全加固

- 转写事件 payload 同步保存 transcript 文本
- 删除会议清理 `meeting_transcripts` 孤儿数据
- 录音 vs 转写对比：确认 9 场旧会议数据丢失（Bug 1），新数据已加固

## 六、服务自启确认

| 服务 | 开机自启 | 说明 |
|------|---------|------|
| `ai-compliance` | enabled | 后端（端口 8002） |
| `qwen3-asr` | enabled | Qwen3-ASR（端口 8091，GPU 0） |
| `nginx` | enabled | 反向代理 + 静态文件（端口 80） |

重启系统后 3 个服务自动拉起，ai-compliance 等待 qwen3-asr 就绪后启动。

```bash
# 安全重启
sudo /home/ai/文档/ai616/restart_safe.sh

# 查看服务状态
curl http://127.0.0.1:8002/health
```
