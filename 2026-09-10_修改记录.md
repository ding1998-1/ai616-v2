# 2026-09-10 修改记录

## 今日处理的问题

### 1. 手机字幕没有及时同步到 PC

现象：手机端已经产生转写，PC 会议页面没有及时显示；页面从后台恢复或网络短暂中断后，也可能继续停留在旧内容。

改动：

- PC 只要打开具体会议页面，就持续拉取该会议的字幕，不再依赖本地会议列表先确认会议已持久化。
- 保留 SSE 实时推送和每 1.5 秒一次的接口轮询，任一通道可用都能更新字幕。
- PC 页面重新获得焦点、浏览器从后台恢复或网络重新连接时，立即补拉最新字幕。
- 会议进入其他阶段后不再主动关闭字幕同步，避免手机完成最后上传时 PC 漏掉内容。

### 2. 连续语音一次出现数千字

现象：`2026年9月10日会议` 的第 5 条转写一次显示 4809 字，时间范围约为 09:04:44 至 09:17:29。

根因：实时 ASR 每 15 秒已经执行技术切片，但二次审核链路把强制切片继续视为同一句话，等待自然静音后才统一提交。连续播放的声音长时间没有达到静音结束条件，最终把约 12 分 42 秒的内容合并成一条字幕。

改动：

- 15 秒强制切片现在直接作为一条实时字幕按顺序提交。
- 自然停顿仍可提前结束当前字幕。
- 不再为了拼成完整长句而延迟 PC 展示。
- 已经入库的 4809 字历史记录保持原样，新产生的字幕使用新规则。

### 3. 结束会议录音门禁核实

已确认并保留现有规则：只要手机录音分片尚未完整上传、录音尚未合并或服务端没有确认最终音频文件，PC 结束会议时会被服务端拒绝，并提示先在手机端结束录音或重试保存。本次没有放宽该门禁。

## 数据核实

- 最新会议 ID：`meeting-local-20260910090155`
- 会议名称：`2026年9月10日会议`
- 修复前已入库转写：5 条
- 前 4 条为正常短字幕；第 5 条为 4809 字连续音频转写
- 手机与 PC 使用的是同一会议 ID，转写数据没有丢失

## 验证结果

- 前端生产构建通过。
- ASR 双阶段转写及录音完成门禁相关测试：18 项通过。
- 44 服务器数据库 `PRAGMA quick_check`：`ok`。
- `ai-compliance`、`paraformer-asr`、`qwen3-offline-asr` 三项服务均为 `active`。
- 44 健康检查返回 `ok`，会议与转写数据可正常读取。
- 生产前端已确认包含 1.5 秒轮询、SSE 和页面恢复补拉逻辑。

## 发布记录

- 部署目标：`192.168.66.44`
- Git 分支：`master`
- 功能提交：`8cebb0a fix: stream mobile transcripts to desktop promptly`
- GitHub：已推送至 `origin/master`
