# 2026-08-21 会议纪要生成链路改造计划

## 一、改造结论

本次不再修补现有“抽样转写 + 单次大模型生成”的逻辑，直接将正式生成链路改造成：

```text
Whisper 完整分段
→ 领域词典与语义校对
→ Map 分段提炼
→ Reduce 合并去重
→ Schema 校验
→ 决议确认与签字闭环
→ 正式文件 + 独立证据底稿
```

会议时长约 75 分钟时，默认拆为 4～5 个 Map 分段，加 1 次 Reduce，总调用量控制在 5～6 次。每条正式结论、风险、待办都必须保留时间区间和原文依据。

本轮不做数据库 Schema 变更；新增结构继续保存在现有 `generatedRecords` JSON 中。暂不直接部署到 `192.168.66.44:8002`，本地完成验证后再提交部署清单并单独执行部署。

## 二、当前代码确认的问题

### 1. 长会议内容被抽样和截断

`backend_full.py` 当前存在两层信息损失：

- 转写超过 100 条时，只保留开头、均匀抽取的中间部分和结尾。
- Whisper 合并文本只取前 15000 个字符。

因此长会议的中段内容可能完全没有进入大模型，后续无论怎样优化 Prompt 都无法恢复。

### 2. 依据字段不可追溯

本地兜底生成的 `minutes.basis` 只是“命中 N 条相关转写”一类说明，没有：

- 开始与结束时间；
- 原文引句；
- 发言人；
- 来源分段标识。

当前 DOCX 也只能渲染概括性文字，不能从纪要反查证据。

### 3. 输出维度不完整

当前主要输出 `summary / minutes / decisions / todos`，缺少独立的：

- 合规风险；
- 披露事项；
- 质量异常；
- 校对记录；
- 生成参数快照。

### 4. 正式文件混入大段原文

当前会议 DOCX 和归档 DOCX 都会把完整会议纪实直接写入正文。正文与证据混合后，文件冗长且正式结论不突出。

### 5. 本地兜底存在确定性错误

- 决议循环的 `else` 位置错误，同一条决议可能被追加两次。
- 未明确责任人的待办会把发言人当作责任人，存在“系统替会议指定负责人”的失真风险。

### 6. 决议闭环不完整

现有服务已经具备版本、签字和内容哈希基础，但缺少严格的状态迁移约束、确认时间写入以及“内容变化后必须重新确认和签字”的完整联动。

### 7. 生产入口仍然分裂

正式运行入口仍为 `backend_full:app`。模块化入口 `backend.main` 尚未覆盖全部路由，生成、文档和声纹逻辑仍散落在大文件中，不能直接切换。

## 三、目标输出结构

### 1. Map 分段输出

每个分段必须返回稳定 JSON：

```json
{
  "chunkId": "chunk-01",
  "timeRange": {
    "startTime": "00:00:00",
    "endTime": "00:16:30"
  },
  "agendaCandidates": [],
  "minutes": [],
  "decisions": [],
  "risks": [],
  "disclosures": [],
  "todos": [],
  "qualityFlags": []
}
```

每个有效业务条目统一带 `basis`：

```json
{
  "basis": {
    "startTime": "00:12:18",
    "endTime": "00:13:05",
    "sourceSegmentIds": ["segment-108", "segment-109"],
    "quotes": [
      {
        "time": "00:12:36",
        "speaker": "张三",
        "text": "原文引句"
      }
    ]
  }
}
```

### 2. Reduce 最终输出

Reduce 负责跨分段合并、去重和冲突标注，不再读取被抽样的原始内容。最终结构为：

- `conclusions`：明确结论与决议；
- `risks`：合规风险、经营风险、程序风险；
- `disclosures`：需披露内容、对象和期限；
- `todos`：任务、负责人、期限和状态；
- `minutes`：必要的讨论过程；
- `qualityFlags`：断档、噪音、无法识别、发言人不确定；
- `generationSnapshot`：生成参数与来源快照；
- `summary`：保留旧前端兼容字段，但由上述结构生成，不再作为唯一主结构。

摘要和 DOCX 首页按“结论 / 风险与披露 / 待办”分栏，不再按议题流水堆叠。议题仍用于组织来源和检索。

## 四、分段与完整性策略

### 1. 分段规则

- 优先按 Whisper 时间戳分段，而不是按字符硬切。
- 默认目标长度 15～18 分钟。
- 在自然停顿、议题切换或说话人轮次边界处切分。
- 相邻分段保留少量上下文重叠，避免结论跨边界丢失。
- 75 分钟会议形成 4～5 个 Map 分段。

### 2. 完整性校验

生成前为所有有效 Whisper segment 分配稳定 ID。生成后检查：

- 每个有效 segment 必须归属某个 Map 分段；
- 不允许因字符数、条数或超时静默跳过；
- 噪音、空白和断档只能进入 `qualityFlags`，不能无记录丢弃；
- Map 失败仅重试失败分段，不重复整场会议；
- Reduce 失败保留全部 Map 结果，允许安全重试。

## 五、校对层

### 1. 领域词典

新增版本化词典，首批包含：

- 引债；
- 批复；
- 初设；
- 美林街道；
- 桃园；
- 后续从实际会议中补充的单位名、人名、项目名和专业术语。

词典只提供候选纠正，不允许直接覆盖原文。

### 2. LLM 校对

每个 Map 分段在提炼前执行一次受约束校对：

- 保留 `rawText`；
- 输出 `correctedText`；
- 记录修改词、原词、依据和置信度；
- 禁止改写数字、日期、金额、否定关系和责任主体；
- 无法确认的内容保留原文并标为待人工复核。

只有通过校验的内容可以进入正式文件。未通过时阻止生成“正式版”，但允许输出标有“待校对”的内部预览和证据底稿。

## 六、正式文件与证据底稿

### 1. 正式会议文件

正文只包含蒸馏结果：

- 会议基本信息；
- 结论与决议；
- 合规风险与披露事项；
- 待办事项；
- 必要的讨论纪要；
- 每项内容的简短依据；
- 生成参数快照和文件校验信息。

不再把数百行全文转写塞进红头正文。

### 2. 独立证据底稿

与正式文件同时生成独立附件，包含：

- 全量有效 Whisper 分段；
- 原始文本与校对文本；
- 时间戳与发言人；
- 断档、噪音和无法识别标记；
- Map 分段边界；
- 正式条目到来源 segment 的反向索引。

证据底稿覆盖率必须为 100%，被判定为噪音的片段也要留下剔除原因。

## 七、声纹会前检查

复用现有 `voiceprint_profiles`，不新增数据库表。

会议从准备阶段进入“会中”阶段前执行预检：

- 校验参会人员是否完成声纹录入；
- 缺失时返回 `409` 和具体人员清单；
- 前端引导录入后重新检查；
- 不允许静默跳过；
- 系统管理员的例外处理必须留下操作记录，不能作为默认路径。

## 八、决议确认闭环

建立明确状态链：

```text
draft → confirmed → signing → signed → archived
```

规则：

- 决议内容或标题变化时版本号递增；
- 内容变化后自动回到 `draft`；
- 旧版本签字立即失效；
- `confirmed` 时写入确认人和确认时间；
- 未确认的决议不能发起签字；
- 未完成必签人员签字不能归档；
- 归档只引用已签版本的内容哈希。

## 九、生成参数快照

每次生成记录：

- provider；
- model；
- temperature；
- promptVersion；
- schemaVersion；
- glossaryVersion；
- chunkPolicy；
- Map 与 Reduce 调用次数；
- Whisper 来源文件哈希；
- segment 数量与覆盖率；
- 生成时间；
- 校对状态。

快照保存到 `generatedRecords.generationSnapshot`，并以精简形式写入正式文件落款。

## 十、代码改造清单

### 新增

- `backend/services/meeting_record_generation_service.py`
  - 全量分段、Map、Reduce、Schema 校验、重试和覆盖率检查。
- `backend/services/meeting_proofread_service.py`
  - 词典候选、LLM 校对、修改日志和准入校验。
- `backend/services/meeting_document_service.py`
  - 正式文件和证据底稿双文件生成。
- `backend/services/voiceprint_preflight_service.py`
  - 会前声纹完整性检查。
- `backend/resources/meeting_terms.json`
  - 领域词典与版本。
- `backend/schemas/meeting_records.py`
  - Map、Reduce、basis、风险、披露和生成快照的数据模型。
- 对应的单元测试与集成测试文件。

### 修改

- `backend_full.py`
  - 删除抽样和 15000 字符截断；旧接口改为调用新服务；修复兜底重复决议与虚构负责人。
- `backend/services/outcome_service.py`
  - 接入新生成结果和兼容字段。
- `backend/routes/outcomes.py`
  - 统一生成、重试、状态和错误响应。
- `backend/services/agenda_service.py`
  - 完整决议状态迁移、版本和签字失效联动。
- `backend/services/meeting_service.py`
  - 进入会中前执行声纹预检。
- `backend/routes/meetings.py`
  - 返回声纹缺失清单和生成状态。
- `backend/routes/agendas.py`
  - 决议确认与签字状态接口约束。
- `backend/app_factory.py`
  - 补齐生成、文档和声纹路由。
- `backend/main.py`
  - 作为最终统一入口。

### 兼容策略

- 保留现有接口路径，避免前端同步大改。
- 保留旧 `summary / minutes / decisions / todos` 字段。
- `backend_full:app` 在切换前继续可运行，但核心实现只保留一份。
- 模块化入口通过完整回归后，才提交远端入口切换和部署方案。

## 十一、实施顺序（单次完成，不分批上线）

1. 建立 Schema、领域词典和完整分段器。
2. 实现校对、Map、Reduce 与覆盖率校验。
3. 替换现有抽样生成，修复本地兜底双问题。
4. 改造 DOCX 为正式文件与证据底稿双输出。
5. 补齐风险、披露、依据和生成快照渲染。
6. 完成决议确认、版本、签字和归档状态闭环。
7. 增加会前声纹预检。
8. 将散落逻辑收口到模块化服务并补齐统一入口。
9. 运行单元测试、接口回归、真实长会议样本测试和文件视觉检查。
10. 输出 `192.168.66.44:8002` 部署清单，获得部署授权后再执行。

## 十二、验收标准

### P0

- 75 分钟会议全量进入 4～5 次 Map 和 1 次 Reduce，总调用不超过 6 次。
- 不再存在条数抽样和前 15000 字符截断。
- 所有有效 segment 覆盖率为 100%。
- 每条结论、风险、披露和待办都有合法 `basis`。
- 摘要和 DOCX 按“结论 / 风险与披露 / 待办”组织。
- 合规风险和披露事项为独立结构，不混入普通摘要。

### P1

- 领域词典参与校对并保留修订日志。
- 未通过校对的数据不能生成正式版。
- 正式文件不包含整场原文流水。
- 证据底稿包含全部有效转写和异常标注。
- 缺失声纹时不能直接进入会中阶段。

### P2

- 本地兜底不再产生重复决议。
- 未明确责任人时输出“待确认”，不虚构负责人。
- 决议修改、确认、签字失效、重新签字、归档链路完整可测。
- 文件落款包含生成参数快照。
- `backend.main` 覆盖正式生产所需路由，旧入口只承担兼容委托。

### 回归

- 现有后端测试全部通过。
- 新增长会议、Map 失败重试、Reduce 重试、Schema 异常、声纹缺失和签字闭环测试。
- 使用至少一份真实长录音验证时间轴和原文引用。
- 渲染并人工检查正式 DOCX 与证据底稿，确认分页、表格和引用没有截断。

## 十三、本轮边界

- 不删除历史数据。
- 不修改 `.env`、密钥或模型凭据。
- 不做数据库 Schema 变更和迁移。
- 不安装全局依赖。
- 不直接切换或部署远端生产入口。
- 如实现中确认必须突破以上边界，立即停止对应部分并单独申请授权。
