# 三重一大 AI 智能合规审计系统 — 部署教程

> 本教程涵盖 **本地开发部署** 和 **Docker 生产部署** 两种方式，请根据实际场景选择。

---

## 📋 目录

1. [系统要求](#系统要求)
2. [依赖清单](#依赖清单)
3. [当前演示版快速启动](#当前演示版快速启动)
4. [方式一：本地开发部署](#方式一本地开发部署)
5. [方式二：Docker 生产部署](#方式二docker-生产部署)
6. [环境变量配置](#环境变量配置)
7. [初始化知识库](#初始化知识库)
8. [⚠️ 注意事项](#️-注意事项)
9. [常见问题排查](#常见问题排查)

---

## 系统要求

| 组件 | 要求 |
|------|------|
| 操作系统 | macOS / Linux（推荐 Ubuntu 22.04+） |
| Python | **3.10+**（必须，低版本不兼容 langchain-chroma） |
| Node.js | **18+** |
| npm | 8+ |
| 内存 | **≥ 8GB**（向量化模型 sentence-transformers 占用约 1~2GB） |
| 磁盘 | ≥ 10GB（PyTorch + 向量模型 + ChromaDB 数据） |
| 大模型服务 | 可访问的 OpenAI 兼容 API（内网 Qwen 或线上 DeepSeek） |

---

## 当前演示版快速启动

> 当前桌面演示项目路径：`/Users/macos/Documents/ai 合规 demo 4:28`  
> 当前真实服务端口：前端 `3001`，后端 `8000`。

### 1. 启动后端

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28"
/Users/macos/miniconda3/bin/python backend_full.py
```

后端健康检查：

```bash
curl -s http://localhost:8000/openapi.json | python3 -c 'import sys,json; data=json.load(sys.stdin); print(data.get("info")); print("/api/auth/login" in data.get("paths",{}))'
```

期望输出中包含：

```text
{'title': '三重一大合规审核 API', 'version': '0.1.0'}
True
```

### 2. 启动前端

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run dev -- --host 0.0.0.0 --port 3001
```

访问地址：

- 本机后台：`http://localhost:3001/`
- 接口文档：`http://localhost:8000/docs`
- 局域网访问：`http://本机局域网IP:3001/`

### 3. AI 会议外部分享入口

AI 会议现在有两个独立外部入口，不能再把后台管理页直接发给别人：

| 入口 | 路径 | 用途 |
|------|------|------|
| 问题收集 | `/issue-collect?meetingId=...` | 发给部门人员登录/登记后填报问题，提交后真实写入当前会议问题池 |
| 手机录音 | `/mobile-recorder?meetingId=...` | 发给参会人手机登录/登记后录音和回传转写 |

注意：

- 如果链接里是 `localhost:3001`，只有本机能打开，别人手机打不开。
- 需要手机访问时，必须用局域网 IP、cpolar、ngrok 或正式 HTTPS 域名打开后台后再复制链接。
- 手机录音需要浏览器允许麦克风。iOS、安卓、鸿蒙上的微信/钉钉内置浏览器对 `http://局域网IP` 可能限制麦克风，稳定演示建议使用 HTTPS 隧道或正式域名。

---

## 依赖清单

### 后端 Python 依赖（`requirements.txt`）

| 类别 | 包名 | 版本要求 | 用途 |
|------|------|----------|------|
| LLM / RAG | `langchain` | ≥ 0.1.0 | AI 链路框架 |
| | `langchain-openai` | ≥ 0.1.0 | OpenAI 兼容接口调用 |
| | `langchain-core` | ≥ 0.2.0 | LangChain 核心 |
| | `langchain-community` | ≥ 0.2.0 | 社区组件 |
| | `langchain-chroma` | ≥ 0.1.0 | ChromaDB 向量库适配 |
| | `langchain-huggingface` | ≥ 0.0.3 | HuggingFace 模型适配 |
| 向量数据库 | `chromadb` | ≥ 0.4.24 | 本地向量库 |
| 嵌入 / ML | `sentence-transformers` | ≥ 2.2.2 | 中文语义向量模型 |
| | `torch` | ≥ 2.0.0 | 深度学习框架（CPU 即可） |
| API 服务 | `fastapi` | ≥ 0.110.0 | 后端框架 |
| | `uvicorn` | ≥ 0.28.0 | ASGI 服务器 |
| | `python-multipart` | ≥ 0.0.9 | 文件上传支持 |
| | `pydantic` | ≥ 2.6.0 | 数据验证 |
| | `httpx` | ≥ 0.27.0 | 异步 HTTP 客户端 |
| 文档解析 | `pypdf` | ≥ 4.1.0 | PDF 解析 |
| | `pdfplumber` | ≥ 0.11.0 | PDF 高精度解析 |
| | `docx2txt` | ≥ 0.8 | Word 文本提取 |
| | `python-docx` | ≥ 1.1.0 | Word 文档处理 |
| 工具 | `python-dotenv` | ≥ 1.0.1 | 环境变量加载 |
| | `loguru` | ≥ 0.7.2 | 日志系统 |

### 前端 Node.js 依赖（`frontend/package.json`）

| 包名 | 版本 | 用途 |
|------|------|------|
| `react` | ^18.3.0 | UI 框架 |
| `react-dom` | ^18.3.0 | DOM 渲染 |
| `antd` | ^6.1.1 | Ant Design UI 组件 |
| `@ant-design/x` | ^2.3.0 | AI 对话组件扩展 |
| `@ant-design/icons` | ^5.2.6 | 图标库 |
| `axios` | ^1.6.0 | HTTP 请求 |
| `react-markdown` | ^9.0.0 | Markdown 渲染 |
| `remark-gfm` | ^4.0.0 | GitHub Flavored Markdown |
| `dayjs` | ^1.11.10 | 日期处理 |
| `vite` | ^5.0.0 | 前端构建工具 |

---

## 方式一：本地开发部署

适合开发调试，热更新方便。

### 第一步：克隆/准备项目

```bash
# 进入项目目录
cd "/Users/macos/Documents/ai 合规 demo 4:28"
```

### 第二步：配置环境变量

```bash
# 复制示例配置
cp .env.example .env

# 编辑 .env，填写实际的 API 地址和 Key
nano .env
```

> 关键配置项说明见 [环境变量配置](#环境变量配置)。

### 第三步：安装 Python 依赖

推荐使用 **Conda** 管理 Python 环境（项目固化环境）：

```bash
# 方式 A — 使用项目固化的 Conda 路径安装（推荐）
/Users/macos/miniconda3/bin/pip install -r requirements.txt

# 方式 B — 标准 pip 安装（确保 Python 3.10+）
pip install -r requirements.txt
```

> ⏳ **首次安装较慢**：PyTorch 约 800MB，sentence-transformers 约 400MB，请耐心等待或开启代理。

### 第四步：启动后端

```bash
# 使用一键启动脚本（推荐：自动清理旧端口、设置国内镜像源）
./start_backend.sh

# 或手动启动
/Users/macos/miniconda3/bin/python backend_full.py
```

后端启动成功后可访问：
- API 服务：`http://localhost:8000`
- 接口文档：`http://localhost:8000/docs`

### 第五步：安装前端依赖并启动

```bash
cd frontend
npm install   # 首次安装，约 200MB
PATH=./node_modules/.bin:$PATH npm run dev -- --host 0.0.0.0 --port 3001
```

前端地址：`http://localhost:3001`

### 第六步：初始化知识库（首次必须）

见 [初始化知识库](#初始化知识库) 章节。

---

## 方式二：Docker 生产部署

适合服务器正式上线，隔离性好。

### 前提条件

- 安装 Docker Desktop（Mac）或 Docker Engine + Docker Compose（Linux）
- Docker 版本 ≥ 20.10

### 第一步：配置环境变量

```bash
cp .env.example .env
# 修改 .env 中的 LLM_API_BASE、LLM_API_KEY 等
nano .env
```

### 第二步：一键启动所有服务

```bash
# 在项目根目录执行
docker-compose up -d --build
```

- 后端容器：`ia-audit-backend`，映射端口 `8000`
- 前端容器：`ia-audit-frontend`，映射端口 `80`

访问地址：`http://服务器IP`

### 第三步：查看运行状态

```bash
docker-compose ps       # 查看容器状态
docker-compose logs -f  # 实时查看日志
```

### 数据持久化

`docker-compose.yml` 已配置以下挂载卷，**重启不丢数据**：

| 挂载路径 | 说明 |
|----------|------|
| `./chroma_db:/app/chroma_db` | 向量数据库持久化 |
| `./data:/app/data` | 原始文档数据 |
| `./.env:/app/.env` | 环境变量 |

---

## 环境变量配置

编辑项目根目录下的 `.env` 文件：

```env
# ① 大模型 API（必须修改）
LLM_API_BASE=http://192.168.66.44:8088/v1   # 本地内网 Qwen，或改为 DeepSeek 线上地址
LLM_API_KEY=empty                            # 内网模型无需 Key；线上模型填入真实 Key

# ② ChromaDB 向量库路径（本地部署需改为实际路径）
PERSIST_DIR="/Users/macos/Documents/ai 合规 demo 4:28/chroma_db"

# ③ 向量化嵌入模型（无需修改，首次运行自动从 HuggingFace 下载）
EMBEDDING_MODEL="shibing624/text2vec-base-chinese"

# ④ 合规文档原始数据目录
DATA_DIR="/Users/macos/Documents/ai 合规 demo 4:28/城投合规资料"

# ⑤ 阿里云百炼能力（AI 会议实时语音和图片 OCR）
DASHSCOPE_API_KEY=your_dashscope_or_bailian_key
DASHSCOPE_OCR_MODEL=qwen-vl-ocr
DASHSCOPE_FUN_ASR_MODEL=paraformer-realtime-v2
```

> 若使用 **DeepSeek 线上 API**，将 `LLM_API_BASE` 改为 `https://api.deepseek.com/v1`，`LLM_API_KEY` 填入官网申请的 Key。
> 图片 OCR 使用百炼 OpenAI 兼容接口 `/compatible-mode/v1/chat/completions`，默认模型为 `qwen-vl-ocr`；实时语音识别使用 Fun-ASR WebSocket。

---

## 初始化知识库

**首次部署必须执行**，将合规文档向量化入库：

```bash
# 方式 A：批量入库（推荐）
/Users/macos/miniconda3/bin/python ingest_batch.py

# 方式 B：单份 PDF 入库
/Users/macos/miniconda3/bin/python ingest_pdf.py

# 方式 C：通过前端界面上传（支持 PDF / Word）
# 访问前端 → 知识库管理 → 上传文件
```

验证知识库状态：

```bash
curl http://localhost:8000/kb_stats
```

---

## ⚠️ 注意事项

### 1. 网络访问问题（国内重要）

`sentence-transformers` 模型默认从 HuggingFace 下载，**国内访问可能失败**。`start_backend.sh` 已自动设置镜像：

```bash
export HF_ENDPOINT=https://hf-mirror.com
```

若手动启动，请先执行：
```bash
export HF_ENDPOINT=https://hf-mirror.com
```

### 2. PyTorch 版本与 CUDA

- 本系统默认使用 **CPU 推理**，无需 GPU。
- 若服务器有 NVIDIA GPU，可安装 CUDA 版本的 torch 大幅加速向量化：
  ```bash
  pip install torch --index-url https://download.pytorch.org/whl/cu121
  ```

### 3. 端口冲突

- 后端端口：`8000`（若冲突，`start_backend.sh` 会自动 kill 旧进程）
- 前端开发端口：`3001`
- 如需手动清除：
  ```bash
  kill -9 $(lsof -t -i:8000)
  kill -9 $(lsof -t -i:3001)
  ```

### 4. ChromaDB 数据库迁移

- `chroma_db/` 目录包含所有向量数据，**切勿直接删除**（会丢失知识库）。
- 服务器迁移时，务必将整个 `chroma_db/` 和 `data/` 目录一起复制。

### 5. .env 文件安全

- `.env` 文件包含 API Key，**绝对不能提交到 Git 仓库**。
- 项目的 `.dockerignore` 已排除 `.env`；**但 Docker Compose 通过挂载卷方式注入**，安全可控。

### 6. PERSIST_DIR 路径问题（容器部署）

- 使用 Docker 部署时，`PERSIST_DIR` 必须改为容器内路径：
  ```env
  PERSIST_DIR=/app/chroma_db
  DATA_DIR=/app/data
  ```
  否则容器内找不到路径，知识库将无法正常工作。

### 7. 模型首次加载时间

- `text2vec-base-chinese` 模型约 400MB，**首次启动需下载**，大约需要 2~5 分钟。
- 后续启动会从本地缓存加载，速度正常（通常 <30 秒）。

---

## 常见问题排查

| 问题现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 后端启动报 `ModuleNotFoundError` | Python 环境依赖未安装 | `pip install -r requirements.txt` |
| 模型下载卡住 | HuggingFace 网络不通 | `export HF_ENDPOINT=https://hf-mirror.com` |
| `Address already in use 8000` | 端口被占用 | `./start_backend.sh` 或手动 kill |
| `Address already in use 3001` | 前端端口被占用 | `kill -9 $(lsof -t -i:3001)` 后重启前端 |
| 知识库为空，问答无结果 | 未初始化向量库 | 运行 `ingest_batch.py` 或前端上传 |
| 前端显示接口 404 | 后端未启动 | 先启动后端再启动前端 |
| 分享链接别人打不开 | 链接使用了 `localhost` | 用局域网 IP、cpolar/ngrok 或正式域名访问后台后再复制链接 |
| 手机录音无法启动麦克风 | 非 HTTPS 或内置浏览器限制 | 使用 HTTPS 隧道/正式域名，并在浏览器授权麦克风 |
| Docker 内模型路径报错 | `.env` 路径为本地绝对路径 | 将路径改为 `/app/chroma_db` |
| ChromaDB 报 `batch size` 错误 | 大批量入库超限 | 使用 `ingest_batch.py` 分批入库 |
| DeepSeek API 返回 400 | message 格式不正确 | 检查 `backend_full.py` 中 system message 配置 |

---

## 📞 快速验证清单

部署完成后，按顺序验证以下接口：

```bash
# 1. 后端健康检查
curl http://localhost:8000/

# 2. 知识库状态
curl http://localhost:8000/kb_stats

# 3. 接口文档（浏览器访问）
open http://localhost:8000/docs

# 4. 前端界面（浏览器访问）
open http://localhost:3001

# 5. AI 会议外部入口（浏览器访问）
open "http://localhost:3001/issue-collect?meetingId=demo"
open "http://localhost:3001/mobile-recorder?meetingId=demo"
```

---

> 📋 本文档由系统自动生成，最后更新：2026-03-18
