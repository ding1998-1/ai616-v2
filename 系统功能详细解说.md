# AI 三重一大合规审核系统 — 功能详细解说

> 昇晟科技 · 智能会议管理 · 全流程合规审核 · 数据驱动决策

---

## 目录

- [一、系统架构总览](#一系统架构总览)
- [二、登录与认证](#二登录与认证)
- [三、统计驾驶舱（工作台）](#三统计驾驶舱工作台)
- [四、三重一大合规审核（专家模式）](#四三重一大合规审核专家模式)
- [五、快速审核（简易模式）](#五快速审核简易模式)
- [六、AI 智能会议（核心功能）](#六ai-智能会议核心功能)
- [七、手机端会议录音](#七手机端会议录音)
- [八、议题征集页](#八议题征集页)
- [九、OA 审批联动](#九oa-审批联动)
- [十、决策归档中心](#十决策归档中心)
- [十一、合规问答（知识库 RAG）](#十一合规问答知识库-rag)
- [十二、合同审查](#十二合同审查)
- [十三、知识文库](#十三知识文库)
- [十四、规则 PDF 管理（管理员）](#十四规则-pdf-管理管理员)
- [十五、用户管理（管理员）](#十五用户管理管理员)
- [附录 A：后端 API 全景表](#附录-a后端-api-全景表)
- [附录 B：数据库表结构](#附录-b数据库表结构)
- [附录 C：AI 能力层详解](#附录-cai-能力层详解)

---

## 一、系统架构总览

### 1.1 技术栈

```
浏览器 → 宝塔(HTTPS终结+转发) → Nginx:80 → FastAPI:8002 → SQLite + ChromaDB + DeepSeek
```

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | React 18 + Vite 5 + Ant Design 6 | 单页应用，端口 3001(dev)/80(prod) |
| 后端 | Python FastAPI | 端口 8002，systemd 管理，单文件 8000+ 行 + 模块化 backend/ |
| 数据库 | SQLite (WAL 模式) | 24MB，252 场会议，1203 条转写 |
| 向量库 | ChromaDB | 43MB，用于知识库语义检索 |
| LLM | DeepSeek V3 (thinking 模式) | 合规审核、纪要生成、合同审查 |
| ASR | DashScope Fun-ASR + 本地 Qwen3-ASR | 双引擎实时语音转写 |
| Whisper | faster-whisper-large-v3 | 会后高精度转写终审 |

### 1.2 三种入口

系统支持三种独立入口，适配不同使用场景：

| 入口 | URL | 用途 |
|---|---|---|
| PC 桌面端 | `/` | 完整管理功能，侧边栏导航 |
| 手机录音端 | `/mobile-recorder` | 独立页面，参会人手机录音 |
| 议题征集页 | `/issue-collect` | 独立页面，外部人员提交议题 |

### 1.3 角色权限

| 角色 | 可见菜单 | 权限 |
|---|---|---|
| admin | 全部菜单（含规则 PDF 管理、用户管理） | 完整管理权限 |
| staff | 业务菜单（驾驶舱、会议、审核、问答等） | 业务操作权限 |

### 1.4 全局特性

- **健康检查**：每 30 秒轮询 `/health`，侧边栏显示绿/黄/红状态点
- **通知推送**：每 30 秒轮询 `/api/notifications`，侧边栏显示未读数
- **明暗主题**：一键切换，存储在 `document.documentElement.dataset.uiTheme`
- **实时钟表**：侧边栏底部显示当前时间
- **错误恢复**：页面懒加载失败自动重试 3 次（指数退避），失败后强制刷新

---

## 二、登录与认证

**前端文件**：`frontend/src/pages/LoginPage.jsx`
**后端路由**：`POST /api/auth/login`、`POST /api/auth/meeting-register`

### 2.1 功能说明

系统提供三种登录体验，根据入口路径自动适配：

**主登录（PC 桌面端）**：
- 两栏布局：左侧品牌展示（鼠标追踪渐变动画），右侧登录卡片
- 自动填充演示账号（admin / admin123）
- 提交按钮有折叠 + 旋转的加载动画

**手机录音端登录**：
- 分段控件切换"首次注册"和"已有账号"
- 注册时需填写：姓名、部门、会议角色、座位号
- 自动关联到指定会议

**议题征集页登录**：
- 与手机录音端类似，但文案针对议题提交场景

### 2.2 认证机制

- JWT Token 存储在 `localStorage`（key: `ai_compliance_token`）
- 所有 API 调用通过 `authFetch()` / `authFetchJson()` 自动注入 `Authorization: Bearer` 头
- Token 过期或无效时自动跳转登录页

### 2.3 快速注册（会议参会人）

`POST /api/auth/meeting-register` 用于会议场景下的快速注册：
- 如果用户不存在，自动创建
- 支持绑定会议角色（主持人、发言人、记录员等）和座位号
- 密码可选（简化手机端注册流程）

---

## 三、统计驾驶舱（工作台）

**前端文件**：`frontend/src/pages/StatisticalDashboard.jsx`
**页面标识**：`dashboard`
**后端路由**：`GET /api/demo_assets`

### 3.1 功能说明

登录后的默认首页，以风险优先视角展示"三重一大"各类事项的全局态势。

### 3.2 界面组成

**Hero 卡片**：
- 显示当前优先关注事项
- 快捷操作按钮：启动快速审核、合同审查、查看归档

**四类事项选择卡**：
- 重大决策、重大项目安排、重要人事任免、大额度资金运作
- 每张卡片显示：状态标签、步骤数、告警数
- 点击切换驾驶舱上下文

**决策路径流**：
- Ant Design Steps 组件展示审批流程节点
- 每个节点有风险弹窗（hover 显示详情）

**风险雷达面板**：
- 红色 = 严重风险，黄色 = 跟踪中
- 可点击跳转到具体审核详情

**异常快照面板**：
- 展示最近的高风险事项
- 底部统计栏汇总各项指标

---

## 四、三重一大合规审核（专家模式）

**前端文件**：`frontend/src/pages/ComplianceAudit.jsx`
**页面标识**：`compliance`
**后端路由**：`POST /api/audit_stream`（SSE 流式）

### 4.1 功能说明

面向合规专员和法务人员的完整审核工作台。支持 5 步 AI 审核流水线、文档上传解析、自定义规则 PDF 管理、以及内嵌文档编辑器进行问题定位。

### 4.2 审核流程

```
选择事项类型 → 上传/粘贴材料 → [可选] 附加定位文档 → 启动智能审核
    ↓
5 步流水线（SSE 流式）：
    ① 规则提取 → ② 材料验证 + ③ 程序检查 + ④ 责任识别（并行）→ ⑤ 报告生成
    ↓
结构化审核报告 → 风险雷达 → 证据链接 → 整改建议 → 模板生成
```

### 4.3 五步流水线详解

| 步骤 | 名称 | 说明 |
|---|---|---|
| ① | 规则提取 | 从规则库和上传的 PDF 中提取强制性规定、禁止事项、审批程序、责任主体 |
| ② | 材料验证 | 将材料文本与规则逐条交叉比对，用关键词匹配标注合规/违规 |
| ③ | 程序检查 | 检查决策程序覆盖率（是否缺少前置审批、是否超权限等） |
| ④ | 责任识别 | 核验责任主体和监督主体是否明确、是否到位 |
| ⑤ | 报告生成 | DeepSeek 流式生成完整审核报告（含风险雷达 XML + 9 节报告） |

步骤 ②③④ 并行执行，提高审核效率。

### 4.4 界面组成

**左侧配置区**：
- 事项类型选择器（4 类）
- 自定义规则 PDF 多选（支持上传/删除）
- 规则图库抽屉（流程图、风控图）
- 材料文件上传（PDF/DOCX/DOC/TXT，最大 80MB）
- 定位文档上传（.docx，用于审核后定位问题段落）

**中央对话区**：
- 聊天式界面（`@ant-design/x` Sender 组件）
- AI 脉冲动画（旋转光球 + 波形条）
- 5 步技能执行面板（实时显示每步状态和耗时）
- 流式文本展示（含可折叠的思考过程）

**右侧报告区**：
- 结构化审核报告（按预定义顺序排列的卡片）
- 风险雷达可视化（绿/黄/红点）
- 证据链接（点击跳转到文档段落）
- 整改链接（点击触发 AI 模板生成）
- 模板生成弹窗（流式输出、复制到剪贴板、下载 Word）
- 内嵌 OnlyOffice 文档编辑器（段落级问题高亮）

### 4.5 事项类型

| 类型 | 说明 |
|---|---|
| 重大决策 | 战略规划、改革方案、年度计划等 |
| 重大项目安排 | 工程建设、投资项目、采购项目等 |
| 重要人事任免 | 干部选拔、岗位调整、薪酬方案等 |
| 大额度资金运作 | 预算调整、大额支出、资产处置等 |

---

## 五、快速审核（简易模式）

**前端文件**：`frontend/src/pages/ComplianceAuditFocus.jsx`
**页面标识**：`compliance_focus`
**后端路由**：同专家模式

### 5.1 功能说明

面向普通员工的简化版审核。隐藏高级配置（规则 PDF、定位文档），聚焦核心流程：选类型 → 粘贴材料 → 开始审核 → 查看结论。

### 5.2 界面组成

- 4 步进度指示器：选择类型 → 添加材料 → 开始审核 → 获取结论
- 事项类型选择以可点击卡片形式展示（非下拉框）
- 大文本框供粘贴材料，支持文件导入
- 步骤芯片实时显示 5 步流水线进度和耗时
- 进度百分比条
- 摘要卡片显示完成进度和审核状态
- 风险雷达标签（绿/黄/红）
- 重点发现高亮卡片
- 完整 Markdown 报告渲染
- "高级选项"抽屉：规则 PDF 管理和流程图库

---

## 六、AI 智能会议（核心功能）

**前端文件**：`frontend/src/pages/MeetingComplianceWorkflow.jsx`（277KB，系统最大文件）
**页面标识**：`ai_meeting`

这是系统的核心模块，占整体功能的 70%，覆盖会议全生命周期。

### 6.1 四阶段生命周期

```
会前确认(collect) → 会中记录(meeting) → 会后终审(audit) → 已归档(archive)
```

### 6.2 会前确认阶段

#### 6.2.1 创建会议

**后端路由**：`POST /api/meetings`

- 填写会议基本信息：标题、项目名称、日期、会议类型、会议模式
- 会议模式自动检测：普通会议 vs 重大事项（三重一大）
- 创建后生成分享链接和二维码

#### 6.2.2 议题征集

**后端路由**：`POST /api/meetings/{id}/issues`

- 通过分享链接让外部人员提交议题
- 支持文本输入、图片上传（自动 OCR）、文档附件（自动解析）
- 重大事项模式下额外字段：类别（现场问题/资金疑点/材料缺失/合同变更/人员/其他）、金额、材料状态
- 秘书端实时查看按提交人分组的议题列表

#### 6.2.3 Excel 批量导入

**后端路由**：`POST /api/meetings/{id}/issues/import-excel`

- 下载 Excel 模板（`GET /api/meetings/issues/template`）
- 上传填好的 .xlsx 或 .csv 文件
- 4 步 AI 处理流水线：解析 → 提取 → 分类 → 生成议程草案

#### 6.2.4 AI 议程生成

**后端路由**：`POST /api/meetings/{id}/agenda/generate`

- DeepSeek 从征集到的议题中智能提取议程草案
- 支持手动添加/编辑/删除议程项
- 议程冻结后不可修改

### 6.3 会中记录阶段

#### 6.3.1 桌面端录音与实时转写

**后端路由**：WebSocket `/api/meeting/asr/qwen/ws`

- 点击录音按钮启动桌面麦克风
- 音频流通过 WebSocket 实时传输到后端
- Qwen3-ASR（本地 FunASR SenseVoiceSmall）进行实时转写
- 8 阶段工业级流水线：
  1. Silero VAD（神经网络语音活动检测）
  2. 音频缓冲
  3. Paraformer 推理
  4. LCP 文本差异比对
  5. 重复 token 融合
  6. 句边界检测
  7. 800ms 静音冲刷
  8. 过期检测
- 政府热词优化、同音字纠正、会话去重
- 断连恢复：5 分钟 TTL 的 pending text store

#### 6.3.2 手机端录音

- 生成二维码邀请参会人扫码加入
- 手机端通过 DashScope Fun-ASR（云端）实时转写
- 支持多人同时录音，自动声纹识别区分发言人

#### 6.3.3 实时转写显示

**后端路由**：`GET /api/meetings/{id}/transcripts/sse`（SSE 推送）

- 转写内容实时推送到桌面端
- 显示发言人姓名、角色、时间戳
- 支持虚拟滚动（VirtualList）处理大量转写
- 转写去重：同一发言人 10 秒窗口内自动合并（200 字上限）
- ASR 修订检测：SequenceMatcher > 0.85 时自动替换而非追加

#### 6.3.4 发言人管理

**后端路由**：`POST /api/meeting/transcripts/{id}/speaker`

- 支持事后修正发言人姓名、角色、部门
- 声纹识别辅助自动分配

#### 6.3.5 转写纠正与签名

**后端路由**：`POST /api/meeting/transcripts/{id}/correction`

- 手机端可对转写内容进行人工校正
- 纠正后需手写签名确认
- 存储原文 + 纠正文 + base64 签名图片

#### 6.3.6 议程计时器

**后端路由**：`POST /api/meetings/{id}/timer/{action}`、`POST /api/meetings/{id}/agenda-timer/{id}`

- 会议级计时器：开始/暂停/重置/设置时长
- 议题级计时器：每个议题独立计时
- 操作：开始/延时/推进（下一议题）/重置
- 超时自动提醒

#### 6.3.7 实时待办提取

**后端路由**：`POST /api/meetings/{id}/realtime-todos`

- DeepSeek 从最近的转写内容中实时提取待办事项
- 自动识别责任人、截止日期、任务描述

#### 6.3.8 会议标记

**后端路由**：`POST /api/meetings/{id}/markers`

- 在转写内容上打标记：决策/待办/争议/材料
- 关联到具体转写记录或议程项
- 用于后续纪要生成时的重点提取

#### 6.3.9 录音管理

**后端路由**：

| 接口 | 说明 |
|---|---|
| `POST /api/meeting/recorder/session` | 记录会话事件（加入/开始/停止） |
| `POST /api/meeting/recorder/audio/chunk` | 上传 3 秒音频片段（流式上传） |
| `POST /api/meeting/recorder/audio/complete` | 合并所有片段为完整录音文件 |
| `POST /api/meeting/recorder/audio` | 一次性上传完整音频 |
| `GET /api/meeting/recorder/audio/{id}/{id}` | 流式播放/下载录音 |

录音文件存储在 `data/meeting_files/recordings/{meeting_id}/`，**系统红线：录音文件绝对不可删除**。

### 6.4 会后终审阶段

#### 6.4.1 Whisper 高精度转写

**触发条件**：会议进入 `audit` 阶段时自动触发
**后端路由**：`GET /api/meetings/{id}/whisper-review`

- 后台合并所有录音文件
- 使用 faster-whisper-large-v3 进行高精度转写（GPU 加速）
- 时间戳对齐会议真实时间轴（基于 `recording_start_time` 计算偏移量）
- 繁简转换（opencc）
- 转写完成后自动替换低质量的实时转写纪实
- DeepSeek 从 Whisper 高精度文本重新提炼摘要/决议/待办

#### 6.4.2 AI 会议纪要生成

**后端路由**：`GET /api/meetings/{id}/records`

- DeepSeek 从全部转写内容生成结构化纪要：
  - **会议纪实**：按时间线的完整发言记录（Whisper 增强版）
  - **会议纪要**：议题讨论要点的精炼总结
  - **决策事项**：会议达成的决议
  - **待办任务**：责任人 + 任务 + 截止日期
- 支持 `force=true` 跳过缓存重新生成
- 支持人工修改覆盖（`POST /api/meetings/{id}/records/update`）
- 反幻觉约束：prompt 中明确要求只基于实际转写内容生成，不可编造

#### 6.4.3 Whisper 增强 DOCX

**后端路由**：`GET /api/meetings/{id}/whisper-docx`

- 生成包含 Whisper 高精度纪实的 Word 文档
- 章节顺序：纪要 → 决议 → 待办 → 纪实（纪实放最后）
- 红头公文格式

#### 6.4.4 音频回放与定位

- 转写列表中点击任意条目，音频自动跳转到对应时间点
- 支持拖拽进度条、倍速播放

### 6.5 归档阶段

#### 6.5.1 DOCX 归档文档

**后端路由**：`GET /api/meetings/{id}/archive/docx`

- 生成完整的归档 Word 文档（红头公文格式）
- 包含：会议基本信息、议程、转写纪实、决议、待办、附件清单

#### 6.5.2 会议状态流转

**后端路由**：`POST /api/meetings/{id}/stage`

```
collect → meeting → audit → archive
```

每个阶段推进时自动触发相应的后台任务（如 audit 阶段触发 Whisper 转写）。

---

## 七、手机端会议录音

**前端文件**：`frontend/src/pages/MobileMeetingRecorder.jsx`（80KB）
**URL 路径**：`/mobile-recorder`

### 7.1 功能说明

独立的移动端页面，供参会人使用手机录音。通过 DashScope Fun-ASR（云端）实时转写，支持声纹识别、转写纠正和数字签名。

### 7.2 工作流程

1. 扫码或点击链接进入 → 登录/注册（绑定会议角色和座位）
2. 查看已绑定的角色信息
3. 点击录音按钮 → 音频流通过 WebSocket 传输到云端 ASR
4. 实时显示转写结果（含时间戳）
5. 对转写内容进行人工校正 → 手写签名确认
6. 停止录音 → 音频保存用于后续 Whisper 后处理
7. 会议文档生成后可下载 DOCX

### 7.3 界面元素

- 角色绑定显示（姓名、角色、部门、座位）
- 4 步绑定流程指示器
- 大录音按钮 + 计时器
- 实时临时文本显示
- 最新识别文本 + 时间戳
- 转写历史列表（支持内联编辑和纠正签名）
- 手动文本输入（补充 ASR 识别）
- 录音统计摘要
- 设备信息显示（类型、ID、标签、传输方式、固件版本）

---

## 八、议题征集页

**前端文件**：`frontend/src/pages/IssueCollectPage.jsx`
**URL 路径**：`/issue-collect`

### 8.1 功能说明

独立页面，供外部参会人员提交会议议题/问题。支持文本输入、图片 OCR、文档自动解析。根据会议模式（普通/重大）动态调整表单字段。

### 8.2 界面元素

- 会议信息展示（标题、项目、日期、议程）
- 模式指示器（重大 vs 普通会议）
- 问题描述文本框
- 类别选择器（重大会议）：现场问题、资金疑点、材料缺失、合同变更、人员、其他
- 金额字段（重大会议）
- 材料状态字段（重大会议）
- 文件上传：支持图片、DOCX、PDF、TXT、MD
- 附件预览（图片缩略图）
- 最近提交列表（含图片预览）

### 8.3 OCR 与文档解析

- 图片上传后自动调用 `POST /api/ocr/image`（DashScope Qwen-VL-OCR 模型）提取文字
- 文档上传后自动调用 `POST /parse_file` 提取文本内容
- 提取的文本自动填入问题描述框

---

## 九、OA 审批联动

**前端文件**：`frontend/src/pages/OAWorkflow.jsx`
**页面标识**：`oa`
**后端路由**：`GET /api/demo_assets`、`POST /api/audit_stream`

### 9.1 功能说明

模拟企业 OA 审批流程与三重一大合规审核的集成。展示审批案卷、逐步流程推进、合规敏感节点的 AI 风险拦截、以及电子归档。

### 9.2 列表视图

- 审批案卷表格
- 统计卡片：总数、进行中、AI 已拦截、待归档
- 支持创建/编辑/删除

### 9.3 详情视图（三栏布局）

**左栏 — 受理面板**：
- 事项类型选择器
- 标题、部门、提案人信息
- 材料清单
- 生成/推进/重置按钮

**中栏 — 审批流程时间线**：
- 时间戳 + 处理人信息
- 操作标签
- 证据数量
- AI 风险拦截告警

**右栏 — AI 审核面板**：
- AI 脉冲动画
- 程序要求列表
- 当前差距
- 风险节点
- 真实 AI 审核报告生成（调用 `POST /api/audit_stream`）
- 电子归档包

### 9.4 风险拦截

- 合规敏感节点自动触发 AI 风险评估
- 红色告警弹窗 + 整改要求
- OA 凭证抽屉展示提取的审批数据要素

---

## 十、决策归档中心

**前端文件**：`frontend/src/pages/DecisionArchive.jsx`
**页面标识**：`archive`
**后端路由**：`GET /api/audit_history`

### 10.1 功能说明

集中查询所有已归档的审核记录、决策时间线、原始附件和迎检报告。

### 10.2 界面元素

**筛选栏**：
- 关键词搜索
- 事项类型选择器
- 日期范围选择器

**归档表格**：
- 列：决策 ID、事项类型、事项名称（含 OA 编号、部门、会议信息）、审核日期、风险等级、参会人、操作

**详情抽屉**：
- 基本信息卡片（ID、类型、日期、状态、风险等级、参会人、摘要）
- 决策时间线（带勾选节点）
- 原始证据文件库（预览/下载）
- 批量导出迎检报告

---

## 十一、合规问答（知识库 RAG）

**前端文件**：`frontend/src/pages/KnowledgeBase.jsx`
**页面标识**：`knowledge`
**后端路由**：`POST /api/kb_stream`（SSE 流式）

### 11.1 功能说明

基于 RAG（检索增强生成）的 AI 问答界面。用户用自然语言提问合规相关问题，系统从 ChromaDB 向量库中检索相关文档，结合 DeepSeek 生成有据可依的回答。

### 11.2 工作流程

1. 用户输入问题或点击快捷提示
2. 后端从 ChromaDB 语义检索相关文档片段
3. 将检索结果 + 用户问题发送给 DeepSeek
4. SSE 流式返回回答（含思考过程和引用来源）
5. 用户可点击引用来源查看原始文档片段

### 11.3 界面元素

- 欢迎横幅 + 系统描述
- 快捷提示卡片：重大决策流程、大额资金审批、人事任免、项目风险防范
- 聊天式对话（用户蓝色气泡 + AI 白色气泡）
- 思考过程展示（可折叠，等宽字体）
- 工具状态标签（显示当前活跃的 RAG 工具）
- 引用来源（点击打开模态框显示原文片段）
- 流式文本 + 打字光标动画
- 停止生成按钮
- 清除对话按钮（带确认）

### 11.4 知识库管理

知识库的内容通过"知识文库"模块（模块十三）管理，支持：
- 上传文档 → 解析 → 向量化 → 存入 ChromaDB
- 链接/取消链接到问答系统

---

## 十二、合同审查

**前端文件**：`frontend/src/pages/CaseAnalysis.jsx`（89KB）
**页面标识**：`cases`
**后端路由**：`POST /api/contract/analyze`、`POST /api/contract/draft`

### 12.1 功能说明

综合合同分析系统：解析上传的合同文档，跨 8 个类别识别风险点，匹配适用法规，生成修改建议，并提供内嵌文档编辑器进行在线修改。

### 12.2 审查流程

```
上传合同 → 解析文档结构 → AI 分类条款（8 类）→ RAG 检索法规 → 生成风险评估 + 修改建议
```

### 12.3 八大风险类别

| 类别 | 说明 |
|---|---|
| 合同主体 | 主体资质、签约权限、代理人授权 |
| 权利义务 | 权利义务对等性、履行标准、验收条件 |
| 违约责任 | 违约条款完整性、违约金合理性、救济途径 |
| 付款条款 | 付款条件、发票要求、质保金、预付款比例 |
| 终止条款 | 终止条件、提前解除、后果处理 |
| 知识产权 | IP 归属、保密条款、竞业限制 |
| 争议解决 | 管辖约定、仲裁条款、适用法律 |
| 其他重要条款 | 不可抗力、通知送达、合同附件 |

### 12.4 风险等级

| 等级 | 颜色 | 说明 |
|---|---|---|
| 高风险 | 红色 | 违反强制性法规、显失公平、重大遗漏 |
| 中风险 | 黄色 | 条款模糊、权责不清、违约责任不明确 |
| 低风险 | 绿色 | 格式瑕疵、表述优化建议 |

### 12.5 界面元素

- 合同文库（已有合同列表）
- 合同上传与解析
- 4 阶段分析进度（解析条款 → 匹配法规 → 生成建议 → 最终确认）
- 风险分类展示（8 个领域）
- 严重程度徽章（高/中/低）
- 问题导航（点击跳转到文档编辑器中的对应段落）
- 自定义问题输入（追加分析）
- 合同起草弹窗（AI 生成，支持采购/服务/劳动/租赁等类型）
- DOCX 预览弹窗
- 导出修改追踪

### 12.6 合同起草

**后端路由**：`POST /api/contract/draft`、`POST /api/contract/draft/export`

- AI 根据需求生成完整合同草稿（Markdown 格式）
- 支持类型：采购合同、服务合同、劳动合同、租赁合同等
- 导出为专业排版的 Word 文档（红头、中文字体、表格）

### 12.7 追加分析

**后端路由**：`POST /api/contract/re_analyze`

- 用户输入额外审查问题
- AI 基于已有分析结果追加审查
- 自动与上次结果对比（新增/已解决/已变更）

---

## 十三、知识文库

**前端文件**：`frontend/src/pages/KnowledgeLibrary.jsx`（43KB）
**页面标识**：`library`
**后端路由**：`/api/knowledge_files`、`/ingest_file`

### 13.1 功能说明

管理组织的文档仓库，分三个类别：案例、知识、共享。支持文档上传、解析、向量化（存入 ChromaDB）、链接/取消链接到问答系统、以及内联文档编辑。

### 13.2 三个类别

| 类别 | 说明 |
|---|---|
| 案例 | 法律案例、历史审核案例 |
| 知识 | 法规条文、行业规范、内部制度 |
| 共享 | 会议材料、审核报告、其他共享文档 |

### 13.3 界面元素

- 三标签页界面
- 文档表格：文件图标、名称、上传者信息（含管理员徽章）、标签、大小、日期、向量化状态、链接状态、操作
- 拖拽上传区域
- 向量化开关（链接/取消链接到合规问答）
- DOCX 预览弹窗
- 内联文档编辑器（OfficeEditor）
- 搜索/筛选

### 13.4 向量化流程

1. 上传文档 → 自动解析提取文本
2. 点击"向量化"→ 调用 `POST /ingest_file`
3. 文本分块 → SentenceTransformer 编码 → 存入 ChromaDB
4. 向量化完成后，合规问答模块可检索到该文档内容

---

## 十四、规则 PDF 管理（管理员）

**前端文件**：`frontend/src/pages/RulesLibrary.jsx`
**页面标识**：`rules`
**后端路由**：`/api/custom_rules`

### 14.1 功能说明

管理员专用页面，集中管理三重一大审核规则 PDF。提供上传、筛选、预览、删除功能。

### 14.2 界面元素

- 统计卡片：总数、筛选数、总字符数
- 事项类型筛选 + 关键词搜索
- 上传控件（指定事项类型）
- 规则卡片：PDF 图标、名称、事项类型标签、字符数、上传时间、摘要命中行（最多 4 行）
- 预览弹窗（完整摘要和解析文本）
- 删除确认（Popconfirm）

### 14.3 规则图库

`GET /api/rules_gallery` 返回演示规则图库（流程图、风控图），以图片形式展示在审核页面的抽屉中。

---

## 十五、用户管理（管理员）

**前端文件**：`frontend/src/pages/UserManagement.jsx`
**页面标识**：`users`
**后端路由**：`/api/users`、`/api/departments`

### 15.1 功能说明

管理员界面，管理用户账号、角色、状态和组织部门。

### 15.2 界面元素

- Hero 区域 + 操作按钮（管理部门、添加用户）
- 统计卡片：总账号数、启用数、管理员数、部门数
- 用户表格：姓名、用户名、角色标签、部门、状态标签、创建时间、编辑/删除操作
- 用户创建/编辑弹窗：姓名、用户名、密码、角色、部门、会议角色、会议座位、状态
- 部门管理弹窗：创建、重命名、删除部门

### 15.3 部门管理

- 创建部门时自动关联
- 重命名部门时同步更新所有关联用户的部门字段
- 删除部门前检查是否有用户关联，有则禁止删除

---

## 附录 A：后端 API 全景表

系统共注册 **82 个独立路由**（部分接口有双路径如 `/audit_stream` + `/api/audit_stream`）。

### 系统与健康

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/health` | 健康检查（DB、ChromaDB、LLM、ASR、数据统计） |
| GET | `/` | 根路径，返回 API 启动信息 |

### 认证与用户（7 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/login` | 用户登录，返回 JWT |
| POST | `/api/auth/meeting-register` | 会议参会人快速注册 |
| GET | `/api/auth/me` | 获取当前用户信息 |
| GET | `/api/users` | 用户列表（管理员） |
| POST | `/api/users` | 创建用户（管理员） |
| PUT | `/api/users/{id}` | 更新用户（管理员） |
| DELETE | `/api/users/{id}` | 删除用户（管理员） |

### 部门管理（4 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/departments` | 部门列表 |
| POST | `/api/departments` | 创建部门 |
| PUT | `/api/departments/{id}` | 重命名部门 |
| DELETE | `/api/departments/{id}` | 删除部门 |

### 会议管理（7 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/meetings` | 会议列表（分页、归档筛选） |
| POST | `/api/meetings` | 创建/更新会议 |
| GET | `/api/meetings/{id}` | 会议详情 |
| PATCH | `/api/meetings/{id}` | 部分更新会议 |
| DELETE | `/api/meetings/{id}` | 软归档会议 |
| GET | `/api/meetings/search` | 跨会议转写关键词搜索 |
| GET | `/api/dashboard/stats` | 仪表盘统计 |

### 议题管理（3 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/meetings/{id}/issues` | 添加议题 |
| GET | `/api/meetings/issues/template` | 下载 Excel 模板 |
| POST | `/api/meetings/{id}/issues/import-excel` | 批量导入议题 |

### 议程管理（2 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/meetings/{id}/agenda/generate` | AI 生成议程 |
| POST | `/api/meetings/{id}/agenda/realtime-check` | 实时议程匹配 |

### 材料管理（3 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/meetings/{id}/materials` | 材料列表 |
| POST | `/api/meetings/{id}/materials/upload` | 上传材料 |
| GET | `/api/meetings/{id}/materials/{id}/download` | 下载材料 |

### 转写与录音（10 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/meeting/transcripts/chunk` | 提交转写片段 |
| GET | `/api/meeting/transcripts/{id}` | 获取转写列表 |
| GET | `/api/meetings/{id}/transcripts/sse` | SSE 实时推送 |
| POST | `/api/meeting/recorder/session` | 记录会话事件 |
| POST | `/api/meeting/recorder/audio/chunk` | 上传音频片段 |
| POST | `/api/meeting/recorder/audio/complete` | 合并音频 |
| POST | `/api/meeting/recorder/audio` | 上传完整音频 |
| GET | `/api/meeting/recorder/audio/{id}/{id}` | 下载录音 |
| POST | `/api/meeting/transcripts/{id}/correction` | 转写纠正+签名 |
| POST | `/api/meeting/transcripts/{id}/speaker` | 修正发言人 |

### ASR WebSocket（2 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| WS | `/api/meeting/asr/ws` | DashScope Fun-ASR 云端 |
| WS | `/api/meeting/asr/qwen/ws` | 本地 Qwen3-ASR |

### 会议纪要与归档（6 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/meetings/{id}/records` | AI 生成纪要 |
| POST | `/api/meetings/{id}/records/update` | 人工覆盖纪要 |
| GET | `/api/meetings/{id}/whisper-review` | Whisper 终审结果 |
| GET | `/api/meetings/{id}/whisper-docx` | Whisper DOCX 下载 |
| GET | `/api/meetings/{id}/archive/docx` | 归档 DOCX 下载 |
| POST | `/api/meetings/{id}/realtime-todos` | 实时待办提取 |

### 会议标记（3 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/meetings/{id}/markers` | 添加标记 |
| GET | `/api/meetings/{id}/markers` | 获取标记列表 |
| DELETE | `/api/meetings/{id}/markers/{id}` | 删除标记 |

### 计时器（2 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/meetings/{id}/timer/{action}` | 会议计时器 |
| POST | `/api/meetings/{id}/agenda-timer/{id}` | 议题计时器 |

### 阶段流转（1 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/meetings/{id}/stage` | 推进会议阶段 |

### 通知（2 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/notifications` | 获取通知 |
| POST | `/api/notifications/{id}/read` | 标记已读 |

### 合规审核（3 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/matter-types` | 事项类型列表 |
| POST | `/audit_stream` | SSE 流式合规审核 |
| POST | `/api/audit_stream` | 同上（别名） |

### 法律案例（4 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/legal-case-types` | 案例类型 |
| POST | `/legal-compare` | 案例对比分析 |
| GET | `/ongoing-cases` | 进行中案件 |
| GET | `/upcoming-hearings` | 近 30 天开庭 |

### 知识库 RAG（6 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/kb_stream` | SSE 流式问答 |
| GET | `/kb_stats` | ChromaDB 统计 |
| POST | `/ingest_file` | 文档入库 |
| POST | `/parse_file` | 文档解析 |
| POST | `/api/ocr/image` | 图片 OCR |
| GET/POST/PUT/DELETE | `/api/knowledge_files[/{id}]` | 知识文件 CRUD |

### 自定义规则（5 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/custom_rules` | 规则列表 |
| POST | `/api/custom_rules/upload` | 上传规则 PDF |
| DELETE | `/api/custom_rules/{id}` | 删除规则 |
| GET | `/api/rules_gallery` | 规则图库 |
| GET | `/api/rules_images/{filename}` | 图库图片 |

### 模板生成（1 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/generate_template` | SSE 流式公文模板生成 |

### 文档编辑 — OnlyOffice（11 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/doc/upload` | 上传文档 |
| GET | `/doc/list` | 文档列表 |
| GET | `/doc/download/{name}` | 下载文档 |
| DELETE | `/doc/delete/{name}` | 删除文档 |
| GET | `/doc/extract_bookmarks/{name}` | 提取段落书签 |
| POST | `/doc/edit_url` | 生成 OnlyOffice 编辑器配置 |
| GET | `/doc/editor_page/{name}` | OnlyOffice 编辑器页面 |
| POST | `/doc/callback` | OnlyOffice 保存回调 |
| POST | `/doc/selection` | 获取段落文本 |
| POST | `/doc/submit_suggestion` | 提交修改建议 |
| POST | `/doc/export_reviewed` | 导出修订版 |

### 合同审查（8 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/contract/map_doc_structure` | 解析合同结构 |
| POST | `/contract/analyze` | AI 合同审查 |
| POST | `/contract/re_analyze` | 追加审查 |
| GET | `/contract/issues/{name}` | 获取审查问题 |
| DELETE | `/contract/issues/{name}` | 删除审查问题 |
| POST | `/contract/draft` | AI 合同起草 |
| POST | `/contract/draft/export` | 起草导出 Word |

### 其他（2 个）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/demo_assets` | 演示资源 |
| GET | `/api/audit_history` | 审核历史 |

---

## 附录 B：数据库表结构

SQLite 数据库（`data/app.db`），WAL 模式。

| 表名 | 说明 | 关键字段 |
|---|---|---|
| `app_metadata` | 应用元数据（KV 存储） | key, value |
| `meetings` | 会议主表 | id, title, project, agenda, meeting_date, type, meeting_mode, phase, creator, issue_sources_json, agenda_drafts_json, materials_json, events_json, archived, generated_records_json, whisper_docx_json |
| `meeting_issue_sources` | 议题征集条目 | meeting_id, content, submitter, category, amount |
| `meeting_agenda_drafts` | AI 生成的议程草案 | meeting_id, title, description, duration, order |
| `meeting_materials` | 上传的会议材料 | meeting_id, filename, filepath, size, uploaded_at |
| `meeting_events` | 时间线事件 | meeting_id, type, data_json, created_at |
| `meeting_transcripts` | 实时转写记录 | meeting_id, speaker_name, speaker_role, transcript, is_final, client_time, confidence, source |
| `notifications` | 用户通知 | user_id, type, title, body, meeting_id, read |

---

## 附录 C：AI 能力层详解

### C.1 DeepSeek V3（LLM）

- **用途**：合规审核、纪要生成、合同审查、议程生成、问答、模板生成
- **模式**：thinking（含思考过程），流式输出
- **并发控制**：全局信号量，默认 5 并发
- **客户端**：`backend/llm_client.py`（DeepSeekThinkingLLM）

### C.2 DashScope Fun-ASR（云端 ASR）

- **用途**：手机端实时语音转写
- **模型**：paraformer-realtime-v2
- **协议**：WebSocket 代理（客户端 → 后端 → DashScope）
- **特性**：自动重连（指数退避，最多 5 次）

### C.3 Qwen3-ASR / FunASR SenseVoiceSmall（本地 ASR）

- **用途**：桌面端实时语音转写
- **模型**：SenseVoiceSmall（本地 GPU 推理）
- **协议**：WebSocket（8 阶段工业级流水线）
- **特性**：
  - Silero VAD 神经网络语音检测
  - LCP 文本差异比对（避免重复）
  - 重复 token 融合
  - 句边界检测 + 800ms 静音冲刷
  - 政府热词优化
  - 同音字纠正
  - 断连恢复（5 分钟 TTL pending store）

### C.4 faster-whisper-large-v3（Whisper）

- **用途**：会后高精度转写终审
- **触发**：会议进入 audit 阶段自动触发
- **特性**：
  - GPU 加速推理
  - 时间戳对齐会议真实时间轴
  - 繁简转换（opencc）
  - 全局并发信号量 = 1（防 GPU OOM）
  - 孤儿结果恢复（启动时扫描 `_whisper_result.json`）

### C.5 ChromaDB（向量库）

- **用途**：知识库语义检索
- **嵌入模型**：shibing624/text2vec-base-chinese（SentenceTransformer）
- **数据量**：43MB
- **用途链路**：文档上传 → 解析 → 分块 → 向量化 → ChromaDB → RAG 检索 → DeepSeek 生成回答

### C.6 DashScope Qwen-VL-OCR（OCR）

- **用途**：图片文字提取（议题征集页）
- **模型**：Qwen-VL-OCR

---

*文档生成时间：2026-07-08 | 基于代码库实际状态编写*
