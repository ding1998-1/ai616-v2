# 2026-09-10 修改记录

## 今日处理的问题

### 1. 手机字幕没有及时同步到 PC

现象：手机端已经产生转写，PC 会议页面没有及时显示；页面从后台恢复或网络短暂中断后，也可能继续停留在旧内容。

改动：

- PC 只要打开具体会议页面，就持续拉取该会议的字幕，不再依赖本地会议列表先确认会议已持久化。
- 保留 SSE 实时推送和每 1.5 秒一次的接口轮询，任一通道可用都能更新字幕。
- PC 页面重新获得焦点、浏览器从后台恢复或网络重新连接时，立即补拉最新字幕。
- 会议进入其他阶段后不再主动关闭字幕同步，避免手机完成最后上传时 PC 漏掉内容。

### 2. 连续语音一次出现数千字

现象：`2026年9月10日会议` 的第 5 条转写一次显示 4809 字，时间范围约为 09:04:44 至 09:17:29。

根因：实时 ASR 每 15 秒已经执行技术切片，但二次审核链路把强制切片继续视为同一句话，等待自然静音后才统一提交。连续播放的声音长时间没有达到静音结束条件，最终把约 12 分 42 秒的内容合并成一条字幕。

改动：

- 15 秒强制切片现在直接作为一条实时字幕按顺序提交。
- 自然停顿仍可提前结束当前字幕。
- 不再为了拼成完整长句而延迟 PC 展示。
- 已经入库的 4809 字历史记录保持原样，新产生的字幕使用新规则。

### 3. 结束会议录音门禁核实

已确认并保留现有规则：只要手机录音分片尚未完整上传、录音尚未合并或服务端没有确认最终音频文件，PC 结束会议时会被服务端拒绝，并提示先在手机端结束录音或重试保存。本次没有放宽该门禁。

## 数据核实

- 最新会议 ID：`meeting-local-20260910090155`
- 会议名称：`2026年9月10日会议`
- 修复前已入库转写：5 条
- 前 4 条为正常短字幕；第 5 条为 4809 字连续音频转写
- 手机与 PC 使用的是同一会议 ID，转写数据没有丢失

## 验证结果

- 前端生产构建通过。
- ASR 双阶段转写及录音完成门禁相关测试：18 项通过。
- 44 服务器数据库 `PRAGMA quick_check`：`ok`。
- `ai-compliance`、`paraformer-asr`、`qwen3-offline-asr` 三项服务均为 `active`。
- 44 健康检查返回 `ok`，会议与转写数据可正常读取。
- 生产前端已确认包含 1.5 秒轮询、SSE 和页面恢复补拉逻辑。

## 发布记录

- 部署目标：`192.168.66.44`
- Git 分支：`master`
- 功能提交：`8cebb0a fix: stream mobile transcripts to desktop promptly`
- GitHub：已推送至 `origin/master`

### 4. 会议管理首页升级为会议工作台

目标：让用户进入会议首页后立即看清正在推进的会议、需要马上处理的会议，以及每场会议的下一步操作。本次为 Zero Logic Change，只调整首页信息架构、UI 和 UX，不修改后端会议流程。

改动：

- 首页头部改为工作状态问候和流程说明，移除偏展示型标题。
- 增加“正在推进、等待整理、已归档、本月会议”4 项工作指标；点击前三项可直接筛选对应会议。
- 会议列表由大卡片改为高密度表格式列表，一行展示日期、会议信息、状态、议题/参会人、更新时间和操作。
- 每场会议明确展示“下一步”，并根据会前确认、会中记录、会后终审、待归档和已归档阶段显示对应指引。
- 默认排序调整为优先展示需要用户处理的会议，其次为进行中、待召开和已归档会议；同类会议按最近更新时间排序。
- 主操作统一为“继续”；编辑会议、查看详情和归档会议收进更多菜单。
- 状态标签统一为短标签和固定颜色：会前确认绿色、会中记录蓝色、会后终审红色、待归档紫色、已归档灰绿色。
- 右侧增加快速操作、近期会议和轻量会议数据；所有快捷入口调用现有新建或筛选逻辑，没有放置无效入口。
- 搜索继续支持远程会议与议题命中，同时增加普通会议/重大事项会议类型筛选。
- 补齐会议标准化数据中的参会人数，避免首页人数显示缺失。
- 小屏下折叠右侧辅助栏，指标卡横向滚动，会议表格转为卡片布局，并保留标题、状态、下一步和继续按钮。

### 5. 首页 SVG 图标还原

- 首页指标、快速操作、近期会议、会议数据、议题、参会人及更多操作图标全部使用项目内联 SVG。
- 图标采用 24 × 24 统一视图和一致描边规则，高分屏缩放保持清晰。
- 按效果图固定使用蓝、橙、绿、紫四组语义颜色，并为图标配置对应浅色背景。
- 首页不依赖字体图标表现这些核心视觉元素。

## 本次首页验证与发布

- 前端生产构建通过，共转换 9890 个模块。
- `git diff --check` 通过。
- 44 部署前健康接口正常：302 场会议、3343 条转写。
- 已将生产构建部署至 `/home/ai/文档/ai616/frontend/dist`。
- 本地与 44 的 `MeetingComplianceWorkflow` 生产文件 SHA-256 一致：`3d394f55fa52808a82de294e564729931202c531b4058413aba38eb37267f62d`。
- 部署后 44 健康接口保持 `ok`。
- 浏览器新会话只能到达登录页，因此本轮完成了构建、文件一致性和服务健康核验，登录后页面仍需进行一次人工视觉验收。
- 首页功能提交：`68f97b3 feat: rebuild meeting management workbench`
- GitHub：功能提交已推送至 `origin/master`。
