# 2026-07-04 日志检查记录

## 检查时间
2026-07-04 约 08:50

## 系统状态
- 后端 `backend_full.py` 正常运行（PID 2848，ai_compliance 环境）
- ASR `funasr_asr_server.py` 在 cuda:1 正常运行
- Nginx 主进程 + 17 worker 正常

## 当前会议
- 会议 ID：`meeting-local-20260704084253`
- 录音 chunk 上传正常（双端 index 0~15）
- 音频合并完成：12 chunks → 570KB
- Whisper 终审启动，模型已加载，正在转写
- DeepSeek 实时议程检查正常（1.5s）

## 发现的问题

### 1. `/api/meetings/<id>/markers` 接口 404

- **现象**：前端持续轮询 `/api/meetings/<id>/markers`，所有请求均返回 404
- **影响范围**：所有会议均受影响，包括新会议和历史会议
- **影响程度**：低，不影响核心录音、转录、议程检查功能
- **原因推测**：markers 接口未实现，或前端轮询了尚未开发的接口
- **建议**：确认 markers 功能是否需要，如不需要可移除前端轮询减少无效请求

### 2. 旧会议 `meeting-local-20260615133627` 持续轮询

- **现象**：backend.log 中该旧会议的 transcripts 和 markers 请求持续出现
- **影响程度**：低，产生无效请求但不影响功能
- **建议**：确认是否有页面未关闭仍在轮询，或前端缺少会议结束后的轮询停止逻辑

## 待验证想法

### 小程序方案能否解决手机麦克风电压过低问题

- **背景**：手机浏览器访问 Web 页面录音时，麦克风采集音量偏小（电压/增益不足）
- **想法**：改为微信小程序（或其他小程序）是否能通过原生 API 获得更好的麦克风增益控制

### 调研结论

#### 1. 小程序 RecorderManager — ❌ 无原生增益参数
- `wx.getRecorderManager()` 不提供 `volumeGain` / `gain` 等音量调节参数
- 但支持 `onFrameRecorded` 回调获取 PCM 原始数据，可在 JS 层手动放大：
```javascript
recorderManager.onFrameRecorded((res) => {
  let buffer = new Int16Array(res.frameBuffer)
  let gain = 2.0
  for (let i = 0; i < buffer.length; i++) {
    buffer[i] = Math.max(-32768, Math.min(32767, buffer[i] * gain))
  }
})
```
- 结论：小程序本身不自动解决，但提供了 PCM 级别的处理能力

#### 2. Web 端 GainNode 方案 — ✅ 可行
- `AudioContext.createGain()` + `gainNode.gain.value = 3.0~5.0` 可在浏览器端放大音量
- 关键发现：关闭 `autoGainControl` 约束可显著提升输入音量：
```javascript
navigator.mediaDevices.getUserMedia({
  audio: {
    echoCancellation: true,
    noiseSuppression: false,
    autoGainControl: false  // 关闭自动增益，音量明显变大
  }
})
```
- ⚠️ iOS Safari 需在用户交互事件中调用 `audioContext.resume()`

#### 3. 后端处理 — ✅ 最稳妥
- FFmpeg：`ffmpeg -i input.aac -filter:a "volume=2.0" output.aac`
- pydub：`audio.apply_gain(6)` 或 `normalize()`
- 优点：不依赖客户端，ASR 转写前统一处理

### 结论与建议

| 方案 | 可行性 | 改动量 | 效果 |
|------|--------|--------|------|
| 小程序原生增益 | ❌ 无此 API | - | - |
| 小程序 PCM 手动放大 | ✅ | 中 | 好 |
| Web 端关闭 autoGainControl | ✅ | 小 | 中 |
| Web 端 GainNode 放大 | ✅ | 中 | 好 |
| 后端 FFmpeg/pydub 增益 | ✅ | 小 | 好 |

**推荐组合**：Web 端关闭 `autoGainControl`（零改动试效果） + 后端增益兜底（保证 ASR 输入质量）

### 当前后端现状

- ❌ **无增益处理**：后端 ffmpeg 仅用于 `whisper_transcribe.py` 中切段（`-ss`/`-to`），没有 volume filter
- 音频链路：录音 chunk → 合并 → ffmpeg 切段 → Whisper 转写，全程无音量放大
- 如需添加，可在 Whisper 转写前加一步：`ffmpeg -i input.wav -af "volume=2.0" -acodec pcm_s16le output.wav`
