# 4.30 合同审查与知识库链路修复记录

这份文档记录 2026-04-30 对项目做过的关键修改，重点覆盖：

- 合同审查页 `Word` 预览异常
- 知识库文档与合同审查的接通关系
- 大小屏预览表现不一致
- 启动顺序与下次避免“假启动 / 旧代码 / 端口混乱”的方法

---

## 一、今天改了什么

### 1. 合同审查页接入“从知识库继续审查”

修改文件：

- `frontend/src/pages/CaseAnalysis.jsx`

新增能力：

- 合同审查首页可以直接读取知识库里的 `docx`
- 支持：
  - `预览`
  - `继续审查`
- 不需要再把同一份合同重复上传一遍

当前入口逻辑：

- 前端先请求 `/api/knowledge_files`
- 只展示：
  - `type === 'docx'`
  - 且存在 `savedName` 的记录
- 点击“继续审查”后：
  - 调 `/api/contract/map_doc_structure`
  - 再调 `/api/contract/analyze`

目的：

- 把“知识文库”和“合同审查”从原来相互独立的两套入口，接成一条连续工作流

---

### 2. 修复知识库老记录没有 `savedName`，导致能看不能审

修改文件：

- `backend_full.py`
- `data/knowledge_files/files.json`

新增后端修复逻辑：

- 新增 `_resolve_editable_saved_name(record)`
- 新增 `_repair_knowledge_file_records(files, persist=True)`
- `/api/knowledge_files` 返回前会自动修复旧记录

解决的问题：

- 早期知识库记录里很多只有：
  - `name`
  - `parsedText`
  - `vectorized`
- 但没有 `savedName`
- 结果就是：
  - 知识库里看得到文件
  - 合同审查页拿不到真实可编辑文档实体
  - 无法进入左侧文档预览与审查

修复方式：

- 后端会去 `data/docs/` 里按文件名反查真实上传实体
- 找到后回填：
  - `savedName`
  - `可编辑` 标签
- 并写回：
  - `data/knowledge_files/files.json`

今天已确认修复的典型记录：

- `1-峙山场平施工合同.docx`
- 多条 `宁波梅山测试.docx`

---

### 3. 修复合同审查页左侧 `Word` 预览空白

核心修改文件：

- `frontend/src/components/OfficeEditor.jsx`

这部分今天一共做了 4 层修复。

#### 第一层：空白高保真渲染自动降级

如果 `docx-preview` 成功执行，但页面没有有效段落节点，就不再继续显示空白白板，而是自动切到结构化兼容预览。

新增：

- `escapeHtml`
- `renderStructuredFallback`
- `previewMode`

效果：

- 至少能看到正文
- 定位、高亮、段落替换仍然可用

#### 第二层：高保真渲染直接报错也降级

不是只处理“渲染成功但空白”。

现在即使：

- `renderAsync()` 直接抛错

也会自动切兼容预览，而不是整块左侧白屏。

#### 第三层：审查页嵌入态不再依赖高保真 `docx-preview`

这是今天最重要的稳定性调整。

现状确认：

- 小屏能显示
- 大屏在左右分栏布局下经常出现异常
- 根因不是文档没解析，而是嵌入态 `docx-preview` 在桌面分栏容器里不稳定

最终处理：

- 审查页左侧嵌入态 `OfficeEditor`：
  - 直接固定走结构化兼容预览
- 弹窗预览 `DocxPreviewModal`：
  - 保留高保真预览

结果：

- 合同审查工作台优先稳定
- 单独预览版式时仍可打开弹窗看高保真效果

#### 第四层：下载链路统一改成相对接口 + 文件名编码

之前审查页和弹窗页走的不是同一条下载链路。

问题点：

- 审查页曾优先使用后端返回的绝对 `directUrl`
- 弹窗预览直接走相对路径 `/api/doc/download/...`
- 带中文文件名时，前者更容易出错

最终调整：

- 新增 `buildDocDownloadUrl(savedName)`
- 审查页统一使用：

```js
/api/doc/download/${encodeURIComponent(savedName)}
```

这样：

- 审查页
- 弹窗预览

都走同一条下载链路，避免中英文文件名在不同路径下表现不一致。

---

### 4. 修复滚动时误伤审查页高度

修改文件：

- `frontend/src/App.jsx`
- `frontend/src/pages/CaseAnalysis.jsx`

今天中间做过一次“放开合同审查页外层滚动”的尝试，结果会破坏嵌入式编辑器的高度计算，导致左侧预览再次异常。

最终定稿：

- `App.jsx` 外壳恢复 `overflow: hidden`
- 只保留合同审查上传入口页自己的内部滚动
- 不再放开整个 `cases` 页外层滚动

结论：

- 审查工作台这种“双栏 + 嵌入编辑器”页面，外层高度必须稳定
- 不能用全局放开滚动的方式硬修

---

## 二、今天改动后的关系图

### 1. 知识库 -> 合同审查

链路如下：

1. 用户在知识文库上传 `docx`
2. 前端先调 `/api/doc/upload`
3. 后端把真实文档写入：
   - `data/docs/<savedName>`
4. 同时写知识库记录：
   - `data/knowledge_files/files.json`
5. 合同审查页读 `/api/knowledge_files`
6. 若记录存在 `savedName`，就允许：
   - `预览`
   - `继续审查`

也就是说：

- `savedName` 是“知识库文档”进入“合同审查”的桥

---

### 2. 合同审查页左侧预览依赖关系

当前左侧审查预览链路：

1. `CaseAnalysis.jsx`
2. 传 `savedName` 给 `OfficeEditor.jsx`
3. `OfficeEditor` 先调用：
   - `/api/doc/edit_url`
   - `/api/contract/map_doc_structure`
4. 再直接下载：
   - `/api/doc/download/${encodeURIComponent(savedName)}`
5. 如果是嵌入态：
   - 直接渲染结构化兼容预览
6. 如果是弹窗态：
   - 优先走高保真 `docx-preview`
   - 失败则回退兼容预览

结论：

- 审查页左侧预览现在不再依赖 OnlyOffice
- 它依赖的是：
  - 文档实体是否存在于 `data/docs/`
  - `map_doc_structure` 是否能拿到段落

---

### 3. 现在哪些地方还和 OnlyOffice 有关系

当前项目里：

- 合同审查工作台左侧预览：
  - 不再依赖 OnlyOffice
- 只有历史旧链路、编辑配置接口、文档服务集成层：
  - 还保留 OnlyOffice 相关代码和配置

所以要分清：

- `OfficeEditor.jsx` 这个名字虽然还在
- 但当前合同审查主流程，本质上已经是“前端兼容预览 + 段落定位”
- 不是“合同审查必须依赖 OnlyOffice 才能看文档”

---

## 三、下次启动如何不启动错误

这部分是最重要的实操规则。

### 规则 1：本地开发只跑一套，不要和 Docker 混跑

二选一：

- 本地开发模式
- Docker 模式

不要长期同时跑：

- 本地 `backend_full.py`
- 本地 `vite`
- Docker 里的 frontend/backend/OnlyOffice

否则最容易出现：

- 端口冲突
- 浏览器请求打到旧服务
- `doc/edit_url` 返回的地址与实际页面来源不一致
- 页面能开，但预览是旧代码

当前推荐：

- 日常开发统一用本地模式

---

### 规则 2：启动顺序固定

#### 第一步：启动后端

在项目根目录执行：

```bash
./start_backend.sh
```

作用：

- 切项目根目录
- 设置 `HF_ENDPOINT=https://hf-mirror.com`
- 清掉占用 `8000` 的旧进程
- 用固定 Python 启动 `backend_full.py`

#### 第二步：启动前端

```bash
cd frontend
npm run dev
```

默认访问：

```bash
http://127.0.0.1:3001
```

#### 第三步：浏览器只开 `3001`

不要直接拿 `8000` 当页面地址。

前端开发环境必须通过：

- `3001`

由 Vite 代理把 API 转发给：

- `8000`

---

### 规则 3：改完前端后必须强刷

尤其改过这些文件时：

- `frontend/src/components/OfficeEditor.jsx`
- `frontend/src/pages/CaseAnalysis.jsx`
- `frontend/src/App.jsx`

要做：

1. 让 `vite` 热更新完成
2. 浏览器强刷

否则很容易出现：

- 你以为代码改了
- 浏览器实际还在跑旧 bundle

表现通常就是：

- 小屏正常
- 大屏不正常
- 弹窗正常
- 嵌入页不正常

本质是缓存和旧 bundle 混着跑。

---

### 规则 4：先确认后端接口活着，再看页面

最小检查：

```bash
curl http://127.0.0.1:8000/matter-types
```

如果这个不通：

- 先别看前端
- 后端根本没起来

合同审查相关再检查：

- `/api/knowledge_files`
- `/api/doc/download/...`
- `/api/contract/map_doc_structure`

如果后端没起来，前端页面经常会呈现成一种“假可用”状态：

- 页面框架存在
- 实际数据和预览全部异常

---

### 规则 5：知识库文档要能审，必须满足两个条件

#### 条件 A：知识库记录里有 `savedName`

看：

- `data/knowledge_files/files.json`

#### 条件 B：真实文档存在于 `data/docs/`

例如：

- `data/docs/<uuid>_文件名.docx`

两者缺一不可。

如果只有知识库记录没有 `savedName`：

- 能在知识库里看到
- 但进不了合同审查

如果有 `savedName`，但 `data/docs/` 里实体文件没了：

- 会出现：
  - 预览失败
  - 下载失败
  - 映射失败

---

### 规则 6：合同审查主流程现在优先看兼容预览是否正常

下次排查时不要先盯 OnlyOffice。

先确认：

1. `savedName` 是否存在
2. `/api/doc/download/...` 是否可下载
3. `/api/contract/map_doc_structure` 是否返回段落
4. 左侧兼容预览是否能渲染

因为当前主链路已经是：

- 文档下载
- 段落映射
- 兼容预览
- 定位 / 高亮 / 替换

OnlyOffice 不是当前合同审查左侧预览是否成功的第一依赖。

---

## 四、今天改完后，当前最稳的启动方式

在项目根目录开 2 个终端。

### 终端 1

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28"
./start_backend.sh
```

### 终端 2

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
npm run dev
```

### 浏览器

打开：

```bash
http://127.0.0.1:3001
```

如果刚改过前端：

- 强刷一次

---

## 五、今天验证过什么

今天已完成的确认项：

- `OfficeEditor.jsx` 多次修改后，前端 `vite build` 可通过
- `1-峙山场平施工合同.docx` 的知识库旧记录已成功补回 `savedName`
- `files.json` 已持久化写回
- 合同审查页与知识库入口已经接通
- 审查页嵌入态预览已固定走兼容渲染，避免大小屏不一致

---

## 六、下次继续看哪里

如果下次还要继续合同审查这一块，优先看这几个文件：

- `frontend/src/pages/CaseAnalysis.jsx`
- `frontend/src/components/OfficeEditor.jsx`
- `frontend/src/components/DocxPreviewModal.jsx`
- `backend_full.py`
- `data/knowledge_files/files.json`
- `data/docs/`

如果只想先判断链路是否断：

先问 3 个问题：

1. 这条知识库记录有没有 `savedName`？
2. `data/docs/` 里有没有对应实体文件？
3. 审查页现在走的是嵌入态兼容预览，还是弹窗高保真预览？

---

## 七、一句话结论

今天的核心不是“再修一次样式”，而是把合同审查从“不稳定的嵌入式高保真 Word 渲染”改成了“知识库可回填、文档可追踪、审查页稳定兼容预览”的主链路。

下次只要按：

- `./start_backend.sh`
- `cd frontend && npm run dev`
- 浏览器强刷

这套顺序启动，出错概率会小很多。
