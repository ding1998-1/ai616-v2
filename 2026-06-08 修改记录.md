# 2026-06-08 修改记录

## AI 会议与“三重一大”合规流程

### 1. 真实业务状态机

- 将 AI 会议页面改为单线流程：创建会议 -> 会前议题收集 -> 会中录音与声纹校对 -> 会后终审拦截 -> 发文归档。
- 顶部保留总览时间线，但未来阶段只显示锁定状态，不允许提前跳转。
- 会前阶段只显示群聊素材池、AI 议题聚类、本地影子项目库。
- 会中阶段只显示录音、实时转写、声纹校对、AI 实时纪要。
- 会后阶段才显示“三重一大”终审拦截、材料核验、智能催办。
- 发文阶段才显示红头纪要、表决电子签、防伪归档包。

### 2. 桌面端会中录音分享

- 在会中阶段新增“手机录音分享页”。
- 桌面端秘书可复制独立手机录音链接。
- 参会人用手机打开链接后先登录账号，再开始录音。
- 录音流按账号身份绑定会议角色，用于后续声纹分轨和终审溯源。

### 3. 独立手机录音页

- 新增独立入口：`/mobile-recorder`。
- 手机录音页不进入后台管理框架，适合分享给参会人手机打开。
- 页面展示会议名称、议题、当前登录账号、部门、会议角色和席位。
- 支持“开始录音 / 结束上传”的演示状态。
- 展示账号登录、角色绑定、声纹采集、音频归档四步绑定链路。
- 回传状态展示声纹置信度和角色轨道写入说明。

### 4. 手机手写签名认定

- 在发文归档阶段新增“手机手写签名认定”模块。
- 展示手机签名采集框和笔迹认定结果。
- 表决签名记录使用“笔迹 / 账号 / 设备三因子”认定口径。
- 签名结果并入红头纪要、电子签、防伪归档证据链。

### 5. 移动端适配

- 创建会议页在窄屏下由双列改为单列。
- 各阶段工作台在手机宽度下自动单列展示。
- 验证 `390x844` 手机视口下无横向溢出。
- 验证 `1920x1080` 桌面视口下页面级高度不溢出。

## 涉及文件

- `frontend/src/App.jsx`
- `frontend/src/pages/MeetingComplianceWorkflow.jsx`
- `frontend/src/pages/MobileMeetingRecorder.jsx`
- `2026-06-08 修改记录.md`

## 验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
./node_modules/.bin/vite build
```

- 构建通过。
- Vite CJS API deprecated 和 antd chunk size 为构建提示，不影响功能。

## 追加说明

### 6. 已完成的 UI 升级

- 首页和工作台已经从“信息堆叠”调整为“主次明确”的业务式界面。
- 三重一大、OA 审批、AI 会议等页面统一向“政府/国企演示可读性”收敛，减少花哨营销感。
- 合规审查页、会议页、OA 页的主流程均改成更清晰的状态推进式布局。
- 重点页面已经按桌面 1080p 做过高度检查，避免内容被压缩到看不全。

### 7. 合同审查相关升级

- 合同审查页已从早期的混合式结果页，调整为更符合实际操作的“左侧文档预览、右侧 AI 分析、底部导出/审查动作”结构。
- 审查结果和高亮跳转逻辑已按更适合经办人员的方式整理，不再把 OnlyOffice 当成唯一主叙事。
- 合同审查页面的主目标改成“看得懂、找得到、能导出”，降低培训成本。
- 相关链路与知识库回填、审查建议、导出审查版的流程已经在项目里连通。

### 8. 具体实现细节

- `frontend/src/App.jsx`
  - 新增 `AI会议` 菜单入口。
  - 增加独立手机录音页路由 `/mobile-recorder`，不进入后台主框架。
  - 通过当前路径判断是否渲染手机录音页。

- `frontend/src/pages/MeetingComplianceWorkflow.jsx`
  - 把原来的“并排展示所有阶段”改成严格的状态机工作台。
  - 会前只显示议题收集与影子项目库。
  - 会中只显示录音、声纹校对、手机录音分享链接。
  - 会后只显示终审拦截和材料催办。
  - 发文阶段只显示红头纪要、表决电子签和归档包。
  - 新增桌面端复制手机录音链接的按钮。
  - 新增移动端角色确认展示，强调“账号 + 角色 + 声纹轨道”的绑定关系。

- `frontend/src/pages/MobileMeetingRecorder.jsx`
  - 新增独立手机录音页。
  - 页面展示会议名称、议题、当前账号、部门、角色、席位。
  - 支持开始录音、结束上传两个状态。
  - 录音时展示声纹置信度和回传说明。
  - 用轻量卡片结构保证手机端能直接演示，不依赖后台主界面。

- `frontend/src/pages/ComplianceAudit.jsx`
  - AI 审查结果改成更像政府办公场景的结构化呈现。
  - 风险等级、违规事项、程序完整性、责任主体、整改建议、归档信息分块展示。

- `frontend/src/pages/OAWorkflow.jsx`
  - 调整为更接近政府 OA 的审批演示链路。
  - 左侧表单、中间流转、右侧 AI 审查/归档的主次更清晰。

- `frontend/src/pages/CaseAnalysis.jsx`
  - 合同审查主流程保持“左侧文档、右侧 AI、底部审查动作”的使用习惯。
  - 导出和审查建议更偏向经办人员可直接理解的表达方式。

- 验证结果
  - 已完成 `vite build`。
  - 已验证桌面 `1920x1080` 不溢出。
  - 已验证手机 `390x844` 无横向溢出。
  - 已验证手机录音页可独立打开并开始录音。
