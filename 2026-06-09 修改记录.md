# 2026-06-09 修改记录

## AI 会议录音、角色绑定与发言转写

### 1. 后端会议转写接口

- 新增手机录音会话接口：`POST /api/meeting/recorder/session`。
- 新增发言转写回传接口：`POST /api/meeting/transcripts/chunk`。
- 新增桌面端轮询接口：`GET /api/meeting/transcripts/{meeting_id}`。
- 接口读取登录 token，将当前账号映射为会议角色、部门、席位。
- 转写记录保存到 `data/meetings/transcripts.json`，不改数据库结构。

### 2. 手机端录音页

- `frontend/src/pages/MobileMeetingRecorder.jsx` 接入 `navigator.mediaDevices.getUserMedia` 和 `MediaRecorder`。
- 浏览器支持 `SpeechRecognition` / `webkitSpeechRecognition` 时自动实时转写中文发言。
- 浏览器不支持自动转写时，保留手动补录发言入口。
- 录音开始时回传会话开始事件。
- 每条最终转写按当前登录账号角色回传后端。
- 录音结束时回传音频大小、录音时长等会话结束信息。

### 3. 桌面端 AI 会议页

- `frontend/src/pages/MeetingComplianceWorkflow.jsx` 会中阶段轮询会议转写接口。
- 手机端回传的发言会显示在实时转写列表中。
- 发言行带有“手机回传”标签。
- 分享链接增加 `meetingId` 参数，确保手机端和桌面端对同一场会议写入/读取。
- 手机录音分享卡展示已回传条数和更新时间。

### 4. 前端代理

- `frontend/vite.config.js` 新增 `/api/meeting` 代理到后端 `8000`。
- 修复桌面端无法通过 Vite 代理拉取会议转写的问题。

## 验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28"
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py

cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
./node_modules/.bin/vite build
```

- Python 语法检查通过。
- 前端生产构建通过。
- 后端接口登录、会话开始、发言转写、桌面拉取均验证通过。
- 桌面 AI 会议页验证到“已回传 1 条”“手机回传”和测试发言。
- 手机录音页 `390x844` 视口无横向溢出。

## 当前限制

- 手机端已经能调用浏览器麦克风和浏览器内置语音识别。
- 浏览器不支持语音识别时，需要用手动补录作为演示兜底。
- 当前还没有接入服务端声纹识别模型，也没有上传完整音频文件做离线转写。
- 下一步可接入后端音频分片上传、Whisper/FunASR、声纹向量匹配和角色置信度校验。

## AI 会议桌面端回传可见性修复

- 修复桌面端 AI 会议页只在“已创建会议 + 会中阶段”才轮询手机端数据的问题。
- `frontend/src/pages/MeetingComplianceWorkflow.jsx` 改为页面打开后常驻轮询当前会议的手机端事件和转写记录。
- 新增 `remoteEvents` 状态，桌面端同时读取录音开始、结束上传、转写文本三类数据。
- 实时转写列表改为手机端最新转写优先展示，避免新参会人发言排在长列表底部看不到。
- “移动端角色确认”从固定名单升级为“固定演示角色 + 真实手机回传角色”合并展示。
- 新登记账号 `cwq / 蔡万青` 不需要写死到前端名单，也会根据后端回传自动出现。
- 创建会议前的预览区新增“手机端实时回传”卡片，桌面未进入会中阶段时也能看到谁已经接入、谁上传了多少条。
- 手机录音分享卡新增上传事件列表，显示开始录音、结束上传、上传时长等状态。

## AI 会议项目与日期上下文补充

- AI 会议入口从单纯“创建会议”调整为“创建项目与 AI 会议”。
- 新增项目名称、项目编号、会议日期、首个审议议题字段。
- 创建时校验项目名称、项目编号、会议日期、会议名称和议题不能为空。
- 顶部流程区显示会议日期和项目编号，形成“项目台账 + 会议批次”的真实 OA 语境。
- 会前议题卡、影子项目推荐、本地影子项目库、会中当前议题、终审强绑定说明均使用当前项目上下文。
- 手机录音分享链接新增 `project`、`projectCode`、`date` 参数。
- 手机录音页顶部显示项目名称、项目编号、会议日期和议题，参会人打开链接后能知道自己给哪个项目录音。
- 手机录音专用登录页同步显示项目、日期、会议和议题信息，避免未登录参会人只看到孤立会议名称。

## AI 会议真实业务顺序修正

- 根据实际 OA 流程，将 AI 会议首页从“先创建会议”改为“先收集问题，再由 AI 生成议题，最后创建会议”。
- 首屏改为三栏结构：
  - 左侧 `01 问题收集`：展示业务部门在群聊中提交的文字、图片、单据线索，并支持继续追加问题。
  - 中间 `AI 议题池`：AI 将问题聚类成待办议题，展示风险等级、绑定项目、生成待办和变更记录。
  - 右侧 `02 创建会议`：秘书基于选中的 AI 议题创建会议批次，确认日期、会议类型、秘书和参会范围。
- 移除创建会议前首屏中的“实施流程预览”和“手机端实时回传”，避免业务上还没开会却显示录音完成态。
- 新增 `selectedIssueId`，支持在 AI 议题池中选择不同议题，并将议题、项目、项目编号、会议名称带入会议创建表单。
- 创建会议按钮文案改为“确认议题并创建会议批次”。
- 创建成功提示改为“会议批次已创建，进入会前议题确认”。
- 创建会议后的 01 阶段从“会前收集”改为“议题确认”，语义上承接首页已经完成的问题收集。
- 会前阶段说明改为“会议批次已创建，秘书只核对 AI 议题、项目绑定和议程范围”，避免再把收集放到创建会议之后。

## 本次补充验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 前端生产构建通过。
- 浏览器验证 AI 会议创建页已显示“项目名称 / 项目编号 / 会议日期”。
- 浏览器验证创建会议前可见“手机端实时回传”，蔡万青显示为“已转写 3 条”。
- 浏览器验证进入会中阶段后，蔡万青的三条手机端转写显示在实时转写列表顶部。
- 浏览器验证会中页显示上传事件、项目编号、会议日期和当前议题。
- 浏览器验证最新 AI 会议首页顺序为“01 问题收集 -> AI 议题池 -> 02 创建会议”。
- 浏览器验证首屏不再出现“实施流程预览”和“手机端实时回传”完成态内容。
- 浏览器验证点击“确认议题并创建会议批次”后仍可进入会前议题确认阶段。

## AI 会议本地项目创建简化

- 明确演示系统不对接外部政府项目系统，避免让用户误以为可以自动读取政府项目库。
- AI 会议创建表单取消“项目编号”人工输入，改为只要求秘书填写“本地项目名称”。
- 系统内部仍保留本地编号用于链接、归档和演示数据串联，但页面弱化为“本地编号：LOCAL-20260609-001”。
- 项目校验从“项目名称 + 项目编号必填”改为只校验“本地项目名称必填”。
- 首屏文案改为“手动创建，不读取外部政府项目库”。
- AI 议题池中“绑定项目”改为“本地项目”。
- 会前确认页中“影子项目推荐”改为“本地项目”。
- “本地影子项目库”改为“本地项目台账”。
- 终审阶段“项目强绑定”改为“本地项目绑定”。
- 手机录音页不再突出项目编号，改为显示“本地手动项目”。
- 手机录音登录页说明改为“当前链接绑定本地手动项目”，只展示项目名称、会议日期、会议和议题。

## 本地项目简化验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 前端生产构建通过。
- 浏览器验证 AI 会议首页存在“本地项目名称”和“手动创建，不读取外部政府项目库”。
- 浏览器验证 AI 会议首页不再出现“项目编号”输入。
- 浏览器验证 AI 会议首页不再出现“影子项目”和“本地影子项目库”文案。

## AI 会议问题来源导入入口补充

- 在 `01 问题收集` 区新增“导入 Excel 台账”按钮，用于演示政府部门已有问题台账的导入路径。
- 点击“导入 Excel 台账”后，会模拟追加两条 Excel 问题记录到问题池：
  - 资金来源测算表缺失。
  - 合同变更签证资料未齐。
- 在 `01 问题收集` 区新增“复制收集链接”按钮，用于演示将问题收集入口发到浙政钉或企业微信。
- 问题收集说明补充“浙政钉、企业微信里的问题不用对接接口：发收集链接给部门填写，或把现有 Excel 台账导入；零散聊天内容可直接粘贴到下方输入框。”
- 问题输入框 placeholder 改为“粘贴群聊、表格行或继续补充问题”，兼容从群聊、Excel、Word、OA 文本里复制内容。
- 主应用支持通过 `?page=ai_meeting&collect=1` 进入 AI 会议页，分享链接登录后可以直接落到问题收集界面。

## 问题导入入口验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 前端生产构建通过。
- 浏览器验证 `http://localhost:3001/?page=ai_meeting&collect=1` 可直接进入 AI 会议问题收集页。
- 浏览器验证页面显示“导入 Excel 台账”和“复制收集链接”。
- 浏览器验证点击“导入 Excel 台账”后，问题池追加两条 Excel 台账导入记录。
- 浏览器验证粘贴输入框 placeholder 为“粘贴群聊、表格行或继续补充问题”。

## AI 会议管理列表补充

- 在 AI 会议大流程页之前新增“会议列表”首页。
- 默认进入 AI 会议模块时先展示会议批次列表，而不是直接进入问题收集大页面。
- 会议列表展示字段：
  - 会议名称。
  - 本地项目名称。
  - 关联议题。
  - 会议日期。
  - 创建人。
  - 创建时间。
  - 当前阶段。
  - 议题数量。
- 会议列表新增“创建 AI 会议”按钮，点击后进入问题收集、AI 议题池、创建会议批次页面。
- 每条会议记录新增“编辑”“进入”“删除”按钮。
- “删除”仅在前端演示列表中移除记录，不删除文件、不改后端数据。
- 问题收集大页面新增“返回列表”按钮。
- 已创建会议后的流程页顶部新增“返回会议列表”按钮。
- `?page=ai_meeting&collect=1` 仍保持直接进入问题收集页，用于外部分享收集链接。

## 会议列表验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 前端生产构建通过。
- 浏览器验证 `?page=ai_meeting` 默认进入会议列表。
- 浏览器验证会议列表显示创建人、创建时间、会议日期、状态和议题数。
- 浏览器验证会议列表存在“创建 AI 会议”“编辑”“进入”“删除”按钮。
- 浏览器验证点击“创建 AI 会议”后进入问题收集大页面。
- 浏览器验证问题收集大页面存在“返回列表”按钮。

## AI 会议真实功能逻辑梳理

- 新增文档 `AI会议真实功能逻辑与查缺补漏.md`。
- 梳理 AI 会议模块从原型走向真实功能的对象边界：
  - 本地项目 `Project`。
  - 问题线索 `IssueSource`。
  - AI 议题草稿 `AgendaDraft`。
  - 会议 `Meeting`。
  - 录音与转写 `Transcript`。
- 明确当前最大风险：录音、转写、会议列表仍容易因为固定 `meetingId` 混到同一场会议里。
- 明确当前真实能力：手机登录、Fun-ASR 实时转写、转写回传和桌面轮询已有基础。
- 明确当前缺口：
  - 会议列表仍是前端状态。
  - 问题收集没有后端存储。
  - AI 议题池仍是固定数据。
  - 分享收集链接没有独立填报页。
  - 会议状态机还需要收紧。
- 给出推荐实现顺序：
  1. 先补会议后端存储和唯一 `meetingId`，防止录音串会。
  2. 再补真实问题收集、Excel 导入、分享填报。
  3. 再补 AI 议题聚类接口。
  4. 最后补会议结束后的纪要、待办、终审和归档。

## AI 会议会中页产品取舍调整

- 根据长会议场景重新定位实时转写：实时转写作为底层证据能力保留，不再作为会中主界面核心。
- 将会中页从“全文实时转写瀑布流 + 声纹校对 + 实时纪要”调整为三栏：
  - 左侧 `录音中控`：当前议题、会议时长、手机接入、当前说话人、录音健康、角色绑定、复制录音链接。
  - 中间 `关键事件流`：材料缺口、合规提醒、待办事件，以及“标记决议 / 标记待办 / 标记争议 / 标记材料缺口”按钮。
  - 右侧 `AI 会中助手`：阶段摘要、待办建议、合规提醒，以及会后将生成的材料清单。
- “完整实时转写”降级为 `最近转写片段`，默认只显示最近 3 条。
- 新增“展开底稿 / 收起底稿”按钮，需要核对证据时再展开更多转写。
- 顶部会中说明改为“录音中控、角色绑定、关键事件标记和会中提醒；全文转写只作为证据底稿沉淀。”
- 保留真实手机转写回传和角色绑定数据，仍然显示蔡万青、丁志强、刘强、张敏等手机端回传状态。

## 会中页调整验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 前端生产构建通过。
- 浏览器验证进入会中阶段后显示“会议时长 / 手机接入 / 当前说话人 / 录音健康”。
- 浏览器验证会中页显示“关键事件流”和四个标记按钮。
- 浏览器验证会中页显示“AI 会中助手”和“会后将生成”。
- 浏览器验证会中页不再显示旧的“实时转写与声纹分轨”和“AI 实时纪要”标题。
- 浏览器验证全文转写已降级为“最近转写片段 / 展开底稿”。

## 手机录音分享登录闭环补充

### 1. 参会人注册接口

- 新增 `POST /api/auth/meeting-register`。
- 参会人通过手机录音分享链接首次登记时，需要填写账号、密码、真实姓名、部门、会议角色和席位。
- 注册成功后立即签发登录 token，并返回脱敏后的用户信息。
- 注册用户写入 `data/auth/users.json`，角色为 `participant`，同时记录 `meetingRole`、`meetingSeat`、`meetingId`。
- 账号重复时返回 `409 Conflict`，提示参会人直接登录或更换账号。

### 2. 已安排账号登录

- 在 `data/auth/users.json` 补入会议演示账号：
  - `zhangmin / 123456`：会议秘书。
  - `liuqiang / 123456`：主要负责人。
  - `chenwei / 123456`：分管领导。
  - `wanglei / 123456`：纪检监察。
  - `wangming / 123456`：议题负责人。
- 秘书也可以继续通过用户管理创建账号，参会人拿到账号密码后从手机录音链接直接登录。

### 3. 手机录音页准入

- `frontend/src/App.jsx` 在 `/mobile-recorder` 未登录时，不再显示通用后台登录文案，而是进入手机录音专用登录页。
- `frontend/src/pages/LoginPage.jsx` 新增 `entry="mobile-recorder"` 模式。
- 手机录音专用登录页提供“首次登记 / 已有账号”两个入口。
- 会议名称、议题和 `meetingId` 从分享链接参数读取，确保登记账号与当前会议关联。
- 登录或登记成功后才进入 `MobileMeetingRecorder`，未登录用户无法直接开始录音。

### 4. 角色绑定修正

- `backend_full.py` 的会议角色解析优先读取账号中的 `meetingRole` 和 `meetingSeat`。
- `frontend/src/pages/MobileMeetingRecorder.jsx` 也优先展示后端返回的 `meetingRole` 和 `meetingSeat`。
- 自助登记参会人和预分配会议账号都会按实名账号写入转写轨道。

### 5. 本次补充验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28"
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py
python3 -m json.tool data/auth/users.json

cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 后端语法检查通过。
- 用户数据 JSON 校验通过。
- 前端生产构建通过。
- `POST /api/auth/login` 使用 `liuqiang / 123456` 验证通过。
- `POST /api/auth/meeting-register` 对重复账号返回 `409 Conflict`，验证注册路由已加载且不会覆盖已有账号。
- `/mobile-recorder?...` 分享路由返回 `200`，未登录时会进入专用登录/登记入口。

## 百炼兼容 OpenAI SDK 准备

- 确认项目后端 Python 环境 `/Users/macos/miniconda3/bin/python` 已安装 OpenAI Python SDK。
- 当前 Python 版本：`3.13.5`，满足百炼文档要求的 `Python >= 3.8`。
- 当前 OpenAI SDK 版本：`2.30.0`。
- 在 `requirements.txt` 显式加入 `openai>=2.30.0`，避免后续部署时只依赖 `langchain-openai` 间接安装。
- 本次没有修改 `.env`、密钥或百炼 API Key 配置。

## Fun-ASR 实时语音识别 WebSocket 接入

### 1. 后端安全代理

- 新增 `WebSocket /api/meeting/asr/ws`。
- 手机端不直接连接百炼，避免 API Key 暴露在浏览器。
- 连接链路改为：手机浏览器 -> 本地后端 WebSocket -> DashScope Fun-ASR WebSocket。
- 后端读取登录 token，未登录无法建立 ASR 代理。
- 后端读取环境变量 `DASHSCOPE_API_KEY` / `DASHSCOPE_BAILIAN_API_KEY` / `BAILIAN_API_KEY` 作为百炼鉴权令牌。
- 支持可选环境变量：
  - `DASHSCOPE_WORKSPACE` / `DASHSCOPE_WORKSPACE_ID`
  - `DASHSCOPE_FUN_ASR_WS_URL`
  - `DASHSCOPE_FUN_ASR_MODEL`
- 默认 DashScope WebSocket 地址：`wss://dashscope.aliyuncs.com/api-ws/v1/inference`。
- 默认模型：`paraformer-realtime-v2`。

### 2. Fun-ASR 协议流程

- 后端连接百炼后发送 `run-task`。
- 音频参数使用 `format: pcm`、`sample_rate: 16000`、中文语言提示。
- 手机端持续发送二进制 PCM 音频流。
- 后端收到百炼 `result-generated` 后提取 `payload.output.sentence.text`。
- `sentence_end` 为真时作为最终句子回传前端。
- 手机端点击结束后发送 `finish-task`，等待 `task-finished`。

### 3. 手机端实时推流

- `frontend/src/pages/MobileMeetingRecorder.jsx` 新增 Fun-ASR 优先识别路径。
- 手机麦克风音频通过 `AudioContext` 采集。
- 前端将音频降采样为 16k 单声道 PCM16。
- 通过 `/api/meeting/asr/ws` 发送二进制音频帧。
- Fun-ASR 最终识别文本继续走原有 `POST /api/meeting/transcripts/chunk`，按账号角色回传桌面端。
- 若未配置百炼 Key、连接失败或浏览器不支持实时音频处理，自动降级到浏览器内置语音识别或手动补录。

### 4. 前端代理与依赖

- `frontend/vite.config.js` 为 `/api/meeting` 开启 `ws: true`。
- `requirements.txt` 新增 `websockets>=15.0.0`。
- 当前后端环境已安装 `websockets 15.0.1`。

### 5. Fun-ASR 验证

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28"
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py

cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 后端语法检查通过。
- 前端生产构建通过。
- 本地 WebSocket 路由验证通过。
- 当前未配置 `DASHSCOPE_API_KEY`，接口按预期返回“未配置 DASHSCOPE_API_KEY”，前端会自动兜底。
- 本次没有修改 `.env`、API Key 或任何密钥文件。

### 6. 写入百炼 API Key 并完成握手验证

- 已按授权将 `DASHSCOPE_API_KEY` 写入项目 `.env`。
- `backend_full.py` 新增 `load_dotenv()`，后端启动时读取项目 `.env`。
- 当前网络环境访问百炼 WebSocket 需要 SOCKS 代理依赖，已安装并写入 `requirements.txt`：
  - `python-socks[asyncio]>=2.4.0`
- 重启后端后，`/api/meeting/asr/ws` 已能连接百炼并返回 `ready`。
- 测试阶段未发送真实音频，发送 `finish` 后百炼返回 `NO_VALID_AUDIO_ERROR`，符合“无有效音频输入”的预期。
- 修改记录中不记录明文 API Key。

## 手机录音登录页滚动修复

- 修复手机端打开 `/mobile-recorder` 时页面无法继续向下滑动的问题。
- 原因是登录页外层容器使用 `overflow: hidden`，在手机小屏和浏览器底部工具栏场景下会裁掉下方表单。
- `frontend/src/pages/LoginPage.jsx` 针对手机录音入口改为 `overflow-y: auto`，并启用 `-webkit-overflow-scrolling: touch`。
- 使用 `100dvh` 适配移动浏览器动态地址栏。
- 为底部增加 `env(safe-area-inset-bottom)` 安全距离，避免 Safari / 微信 / 钉钉底部工具栏遮挡按钮。
- 手机录音入口首屏标题和间距略收紧，让表单入口更早露出。
- 前端生产构建验证通过。

### 二次修复

- 继续修复手机仍无法滑到底的问题。
- 根因是全局 `frontend/src/index.css` 中 `body` 和 `#root` 固定了 `overflow: hidden`，并且 `#root` 使用 `height: 100vh`。
- `LoginPage.jsx` 在 `mobile-recorder` 模式下临时给 `html`、`body`、`#root` 增加 `mobile-recorder-scroll` 类。
- 该类只在手机录音入口生效，离开页面后自动移除，不影响后台主系统固定布局。
- 针对 `html/body/#root` 三层同时放开移动端纵向滚动。
- 进一步压缩手机录音登录页标题字号，减少首屏占用。
- 前端生产构建验证通过。

## 手机麦克风 HTTPS 限制处理

- 手机端通过 `http://192.168.67.58:3001` 可打开页面，但 iPhone Safari / 钉钉 / 微信内核会将该地址视为非安全上下文。
- 非安全上下文下浏览器会屏蔽 `navigator.mediaDevices.getUserMedia`，因此页面会提示无法启动麦克风。
- `frontend/src/pages/MobileMeetingRecorder.jsx` 已增加 `window.isSecureContext` 检测。
- 当页面不是 HTTPS 时，错误文案改为明确提示“请使用 HTTPS 链接打开录音页”。
- 开始录音按钮增加启动中状态，避免连续点击产生多条重复错误提示。
- `ngrok` 本机已安装但未配置 authtoken，无法启动。
- 已使用 `localtunnel` 临时开启 HTTPS 隧道，当前地址为 `https://witty-hats-repair.loca.lt`。
- 前端生产构建验证通过。

### 生产预览隧道修复

- 旧 localtunnel 指向 Vite 开发服务 `3001`，手机端可能因为 `/@vite/client`、开发态源码模块或 HMR 连接导致空白屏。
- 新增 `frontend/scripts/mobile-preview-server.mjs`。
- 新服务使用 Node 直接托管 `frontend/dist` 生产构建产物。
- 新服务将 `/api/*`、`/contract*`、`/doc*` 代理到后端 `8000`。
- 新服务支持 WebSocket upgrade，用于 `/api/meeting/asr/ws`。
- `frontend/package.json` 新增脚本：`npm run mobile-preview`。
- 生产预览服务端口：`3101`。
- 新 localtunnel 地址：`https://tough-stars-sink.loca.lt`。
- 已验证：
  - 手机录音页 HTML 返回 `200`。
  - 生产 JS/CSS 资源返回 `200`。
  - `/api/auth/login` 经 HTTPS 隧道返回成功。
  - `/api/meeting/asr/ws` 经 `wss://` 隧道返回 `ready`。

### 隧道稳定性调整

- `localtunnel` 地址出现 `Bad Gateway` / `408 Request Timeout`，免费隧道稳定性不足。
- 改用 `localhost.run` SSH 反向隧道。
- 当前稳定 HTTPS 地址：`https://7a709eee9708b7.lhr.life/mobile-recorder`。
- 已验证：
  - 页面 HTML 返回 `200`。
  - `/api/auth/login` 返回成功。
  - `/api/meeting/asr/ws` 返回 `ready`。
- 已停止旧 localtunnel 进程，避免误用失效地址。

## 手机录音识别显示体验优化

- 修复 Fun-ASR 最终识别结果很快消失、体感像“没识别到”的问题。
- 新增 `latestRecognizedText` 和 `latestRecognizedAt` 状态。
- 最终识别句子会常驻显示在“最新识别”区域。
- 临时识别中展示为蓝色状态；最终识别后展示为绿色常驻状态。
- 最近发言列表前移到手动补录输入框之前，用户先看到识别结果，再看到补录入口。
- 开始录音时清空上一轮最新识别，避免不同会话混在一起。
- 前端生产构建验证通过。

## AI 会议会中流程合并与界面瘦身

- 将原来的 `02 会议记录` 和 `03 会后校对` 合并为 `02 会中采集与校对`。
- 全链路阶段从 5 步压缩为 4 步：
  - `01 议题确认`
  - `02 会中采集与校对`
  - `03 会后终审`
  - `04 公文归档`
- 修正阶段状态判断逻辑，移除 `edit` 阶段在流程条中的激活/完成判断。
- 会中页面从三列密集说明改为两栏中控布局：
  - 左侧展示当前议题、录音状态、当前说话人、手机接入、底稿回传、参会人角色绑定。
  - 右侧展示秘书操作区、关键事件、AI 当前结论。
- 全文实时转写不再默认占据主视觉，只保留为“证据底稿”，点击后展开。
- 会中阶段不再强调“完整纪要已经完成”，只强调录音、认人、标记关键事件，完整纪要放到会后终审生成。
- 终审页标题编号同步从 `04 终审拦截` 调整为 `03 会后终审`。
- 阶段条 CSS 从 `repeat(5)` 调整为 `repeat(4)`，避免 1080p 下阶段卡片拥挤。
- 保留真实手机录音链接、登录绑定、转写回传、角色列表和后端轮询逻辑，没有改动真实录音接口。
- 前端生产构建验证通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## 语音 AI 动效 Icon 透明化接入

- 处理原始动画 `original-b29726f18880cb190bfdd044226b760d.gif`。
- 将深紫色背景逐帧抠除，并保留球体外圈光晕。
- 输出透明 WebP 动画：
  - `frontend/public/voice-ai-orb-transparent.webp`
- 输出静态透明预览图：
  - `frontend/public/voice-ai-orb-transparent-preview.png`
- 原始 GIF 约 `8.2MB`，透明 WebP 约 `1.8MB`，更适合前端页面使用。
- WebP 裁剪为 `300 x 300`，用于 AI 语音 icon 时不再带 800 x 600 的方形底图。
- 已验证透明预览图四角 alpha 为 `0`，球体中心 alpha 为 `255`。
- `MeetingAiPulse` 已改为使用 `/voice-ai-orb-transparent.webp`。
- 保留外圈 CSS 呼吸环，删除原来 CSS 绘制的圆盘和声波条，避免两套 AI icon 风格叠加。
- `MeetingAiPulse` 的核心尺寸改为组件内联固定，避免在会议创建页未注入主流程 CSS 时按原始 `300 x 300` 尺寸撑大页面。
- 浏览器验证 `/voice-ai-orb-transparent.webp` 加载完成，资源自然尺寸为 `300 x 300`，页面显示尺寸为 `86 x 86`。
- 前端生产构建验证通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## AI 会议第二步会议窗口化

- 根据飞阅会/视频会议共享材料的界面感觉，重构 `02 会中采集与校对`。
- 第二步不再使用后台面板式布局，改为会议现场布局：
  - 顶部会议条：会议 ID、会议时长、录音状态、议题锁定状态。
  - 参会人横条：显示参会人、当前说话人、说话中状态。
  - 共享工具条：显示“正在共享”的会议议题、手机录音链接、权限设置、停止共享。
  - 中央共享材料：用会议材料/审议稿的视觉方式承载当前议题。
  - 右侧 AI 浮层：只保留 AI 会中提醒、当前结论、关键事件和秘书标记按钮。
  - 底部会议控制栏：麦克风、手机录音、参会人、聊天、共享、录制、证据底稿、结束会议。
- 隐藏第二步原有的阶段条，进入会中后自动切换为专注会议模式。
- 隐藏第二步外层流程按钮栏，避免和会议窗口底部“结束会议”重复。
- 保留真实手机录音链接、参会人角色绑定、转写底稿和结束会议进入终审逻辑。
- 证据底稿仍可从底部按钮展开，不默认占据主视觉。
- 1080p 浏览器验证：
  - 会议窗口高度约 `790px`。
  - 共享材料区域高度约 `526px`。
  - 第二步不再显示阶段条。
  - 页面无额外纵向滚动。
- 前端生产构建验证通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## AI 会议新建会议防串会修复

- 修复新建 AI 会议进入第二步后，参会人条直接显示上一场会议历史人员的问题。
- 根因是前端一直使用固定 `meeting-gxq-fc-2026-02` 作为会议 ID，导致新建会议也读取旧会议的手机录音事件和转写数据。
- 新增本地动态会议 ID：
  - 新建会议使用 `meeting-local-YYYYMMDDHHMMSS`。
  - 打开历史演示会议时继续使用该记录自己的 `id`。
- 手机录音分享链接改为使用当前会议 ID，避免新会议手机录音写回旧会议数据。
- 切换会议 ID 时清空上一场的远端事件、远端转写和更新时间，再重新拉取当前会议数据。
- 演示旧会议仍可显示预设参会人和历史转写。
- 新建会议进入会中时不再默认塞满参会人：
  - 默认只显示 `张敏（我）` 主持人。
  - 其他席位显示 `待接入` 空位。
  - 只有参会人通过手机录音链接登录/上传后才进入参会人条。
- 新建会议时长不再沿用旧会 `00:42:18`，改为新会状态 `00:00:57`。
- 新建会议无人接入时，AI 会中提醒改为“等待参会人从手机录音链接接入，AI 将在发言后生成提醒。”
- 浏览器验证：
  - 新建会议会议 ID 显示为 `local-...`。
  - 新建会议不再出现 `蔡万青`、`丁志强` 等旧参会人。
  - 底部显示 `参会人 1`。
  - 参会人条显示 1 个主持人 + 5 个 `待接入` 空位。
  - 顶部不再显示旧会时长 `00:42:18`。
- 前端生产构建验证通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## 手机录音扫码邀请二维码

- 在 `02 会中采集与校对` 的共享工具条中新增 `扫码邀请录音`。
- 底部会议控制栏中的 `手机录音` 也可打开同一个邀请弹窗。
- 新增真实二维码弹窗：
  - 二维码内容为当前会议的 `/mobile-recorder` 分享链接。
  - 链接包含当前 `meetingId`、会议名称、议题、本地项目、项目编号和会议日期。
  - 参会人扫码后进入手机录音登录/登记页，录音和转写会绑定当前会议。
- 二维码使用 Ant Design `QRCode` 组件生成，不是静态图片或演示占位图。
- 弹窗显示当前会议 ID，方便秘书确认没有串会。
- 弹窗提供 `复制邀请链接` 按钮，复制的链接与二维码内容一致。
- 增加 `外部访问地址` 输入：
  - 当前页面是 `localhost` 时，会提示手机扫码无法打开。
  - 填入 cpolar / ngrok / 局域网 HTTPS 地址后，二维码和复制链接会自动替换为外部可访问地址。
- 浏览器验证：
  - 点击 `扫码邀请录音` 可打开二维码弹窗。
  - 弹窗中存在真实 QRCode canvas。
  - 默认链接为当前会议 `mobile-recorder` 地址，并包含 `meeting-local-...`。
  - `localhost` 状态下显示手机不可扫提醒。
  - 填入 `https://6b9ebde2.r40.cpolar.top` 后，二维码链接切换为外部地址，并显示可扫码提示。
- 前端生产构建验证通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## 会中界面按钮弹窗交互补齐

- 补齐 `02 会中采集与校对` 中所有会议按钮的点击反馈，避免出现“看起来能点但没有反应”的演示断点。
- 新增统一会议操作弹窗状态 `meetingActionType`。
- 顶部参会人右侧箭头：
  - 打开 `参会人接入情况` 弹窗。
  - 展示主持人、已接入参会人和待接入席位。
  - 弹窗内可继续打开扫码邀请。
- 顶部 `权限设置`：
  - 打开 `共享与录音权限` 弹窗。
  - 展示共享材料、手机录音、证据底稿、会后归档的权限说明。
- 顶部 `停止共享`：
  - 打开 `停止共享确认` 弹窗。
  - 二次确认后暂停共享/录音状态。
- AI 浮层四个标记按钮：
  - `标决议`：生成“拟形成决议”锚点说明。
  - `记待办`：生成会后督办清单说明。
  - `有争议`：生成争议段会后高亮说明。
  - `缺材料`：生成材料缺口终审拦截说明。
- 底部会议控制栏：
  - `麦克风`：打开麦克风状态弹窗，可暂停/开启主持人麦克风。
  - `手机录音`：打开扫码邀请录音二维码弹窗。
  - `参会人`：打开参会人接入情况弹窗。
  - `聊天`：打开会中聊天弹窗。
  - `共享`：打开共享材料弹窗。
  - `录制`：打开录制与转写状态弹窗。
  - `证据底稿`：打开最近转写底稿弹窗。
  - `结束会议`：打开结束会议确认弹窗，再进入 `03 会后终审`。
- 浏览器验证：
  - `权限设置` 弹出 `共享与录音权限`。
  - `标决议` 弹出 `标记决议`。
  - `参会人 1` 弹出 `参会人接入情况`。
  - `结束会议` 弹出 `结束会议并进入终审`。
  - `麦克风`、`录制`、`证据底稿`、`停止共享` 均有对应弹窗内容。
- 前端生产构建验证通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## AI 会议模块产品测试走查与修复

### 产品测试结论

- 主流程已经基本成立：
  - `会议列表 -> 创建 AI 会议 -> 问题收集 -> AI 议题池 -> 创建会议批次 -> 会前确认 -> 会中采集与校对 -> 会后终审 -> 公文归档`
- 当前最清晰的阶段是 `02 会中采集与校对`：
  - 会议窗口、共享材料、参会人、扫码录音、AI 提醒和结束会议的语义比较直观。
- 会中阶段已从“后台面板”改成“会议现场”，政府人员不需要理解复杂模块名也能知道自己在开会。
- 真实能力边界仍需注意：
  - 手机录音、登录、转写回传已接真实接口。
  - 会议列表、议题聚类、材料补齐和归档仍以原型状态为主，后续需要接后端持久化。

### 测试发现的问题

- `复制收集链接` 原来只带 `project` 和 `date`，别人打开后无法还原具体会议名称、会议 ID、议题和本地项目编号。
- 新建会议列表记录的 `createdAt` 原来固定为 `2026-06-09 09:12`，不符合真实创建行为。
- 会议列表中的 `删除` 原来点击即删除，没有二次确认，容易误操作。
- 自动化浏览器点击部分 Ant Design 弹窗按钮时，CDP 偶发超时；页面服务和构建均正常，后续测试改用更稳定的文本/表单值读取验证。

### 本次修复

- 新增 `createLocalTimestamp()`，新建会议批次时使用当前本地时间作为创建时间。
- `复制收集链接` 现在携带完整上下文：
  - `meetingId`
  - `meeting`
  - `agenda`
  - `project`
  - `projectCode`
  - `date`
- `collect=1` 入口现在会从 URL 参数恢复会议上下文：
  - 会议 ID。
  - 会议名称。
  - 待上会议题。
  - 本地项目名称。
  - 本地项目编号。
  - 会议日期。
- 会议列表删除改为 `Modal.confirm` 二次确认：
  - 明确提示只从当前演示列表移除。
  - 不删除录音、转写或归档文件。

### 回归验证

- 前端生产构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 浏览器验证带参数收集链接：
  - URL 示例：
    - `?page=ai_meeting&collect=1&meetingId=meeting-local-test-001&meeting=测试会议&agenda=测试议题&project=测试项目&projectCode=LOCAL-TEST-001&date=2026-06-10`
  - 页面成功恢复：
    - `测试议题`
    - `测试项目`
    - `LOCAL-TEST-001`
    - `测试会议`
    - `2026-06-10`

## AI 会议一日真实试用版

### 本次产品取舍

- 先保证别人能真实试用一天，不继续堆视觉细节。
- 优先补齐：
  - 会议列表真实持久化。
  - 新建会议不串旧会议数据。
  - 问题收集写入当前会议。
  - AI 议题池随问题素材重新生成。
  - 阶段推进写入当前会议。
  - 手机录音和转写继续按同一个 `meetingId` 回传。
- 暂不做：
  - Excel 文件真实解析。
  - 材料附件真实上传归档。
  - 红头纪要真实生成文件。
  - 声纹身份训练模型。

### 后端新增

- 新增会议持久化文件：
  - `data/meetings/meetings.json`
- 新增会议 API：
  - `GET /api/meetings`：读取未归档会议列表。
  - `POST /api/meetings`：创建或更新会议批次。
  - `GET /api/meetings/{meeting_id}`：读取会议详情。
  - `PATCH /api/meetings/{meeting_id}`：更新会议字段。
  - `POST /api/meetings/{meeting_id}/issues`：追加问题素材，并重新生成 AI 议题草案。
  - `POST /api/meetings/{meeting_id}/stage`：推进会议阶段。
  - `DELETE /api/meetings/{meeting_id}`：软归档会议，不删除录音、转写和文件。
- 手机录音接口联动会议案卷：
  - `POST /api/meeting/recorder/session` 仍写入 `transcripts.json`，同时更新会议事件和 `updatedAt`。
  - `POST /api/meeting/transcripts/chunk` 仍写入转写文件，同时记录到会议事件流。

### 前端改造

- AI 会议列表改为优先读取 `/api/meetings`。
- 列表增加数据状态：
  - `已连接真实数据`
  - `同步中`
  - `本地兜底`
- 新建 AI 会议改为空白草稿：
  - 不再默认塞入高新区历史项目。
  - 不再默认显示旧参会人或旧议题。
- 创建会议时写入后端：
  - `meetingId`
  - 会议名称
  - 本地项目名称
  - 本地项目编号
  - 议题
  - 会议日期
  - 会议类型
  - 问题素材
  - AI 议题草案
- 打开会议时读取详情：
  - 恢复问题素材池。
  - 恢复 AI 议题池。
  - 恢复本地项目和会议标题。
  - 恢复阶段状态。
- 问题收集输入框改为真实写入：
  - 手动粘贴的问题会写入 `/api/meetings/{meeting_id}/issues`。
  - Excel 台账导入仍是两条示例行，但会作为真实问题素材写入当前会议。
- 阶段推进改为真实写入：
  - 会前确认 -> 会中记录。
  - 会中记录 -> 会后终审。
  - 会后终审 -> 已归档。
- 一键绑定本地项目改为写入后端。
- 删除会议改为软归档：
  - 列表隐藏。
  - 不删除会议录音、转写和归档资料。

### 验证结果

- 后端语法检查通过：

```bash
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py
```

- 前端生产构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- FastAPI 接口烟测通过：
  - 登录。
  - 创建会议。
  - 追加问题。
  - 推进阶段。
  - 读取会议列表。
  - 软归档会议。
- 真实运行服务已重启：
  - 前端：`http://localhost:3001/`
  - 后端：`http://127.0.0.1:8000/`
- 代理链路验证通过：
  - `http://127.0.0.1:3001/api/meetings` 未登录返回 `401`。
  - 登录后通过 `3001` 代理读取会议列表返回 `200`。

## AI 会议真实数据库版

### 本次结论

- 已从 JSON 文件存储升级为真实 SQLite 数据库。
- 数据库文件：
  - `data/app.db`
- 旧 JSON 文件保留为备份和迁移来源：
  - `data/meetings/meetings.json`
  - `data/meetings/transcripts.json`
- 不修改 `.env`。
- 不删除旧数据文件。
- 不新增外部依赖，使用 Python 标准库 `sqlite3`。

### 新增数据表

- `app_metadata`
  - 保存迁移标记。
- `meetings`
  - 会议主表。
  - 保存会议名称、本地项目、议题、日期、类型、创建人、阶段、软归档状态。
- `meeting_issue_sources`
  - 问题收集素材表。
  - 保存粘贴的问题、Excel 台账示例行、图片/文字来源说明。
- `meeting_agenda_drafts`
  - AI 议题草案表。
  - 保存 AI 聚类后的议题、风险等级、触发类型和变更明细。
- `meeting_materials`
  - 材料清单表。
  - 预留材料状态、材料补齐、终审核验。
- `meeting_events`
  - 会议事件表。
  - 保存阶段推进、手机录音接入、转写事件等。
- `meeting_transcripts`
  - 会议转写表。
  - 保存手机端转写文本、发言人、角色、席位、时间、置信度。
- `meeting_participants`
  - 参会人表。
  - 预留账号、角色、席位、接入状态。

### 后端改造

- `_load_meetings()` 改为读取 SQLite。
- `_save_meetings()` 改为写入 SQLite。
- `_load_meeting_transcripts()` 改为读取 SQLite。
- `_save_meeting_transcripts()` 改为写入 SQLite。
- 首次读取时自动迁移旧 JSON：
  - 旧会议 JSON -> SQLite。
  - 旧转写 JSON -> SQLite。
  - 迁移成功后写入 `app_metadata.legacy_meetings_migrated=yes`。
- 修复迁移时的真实数据问题：
  - 不同会议的素材 `id=1` 会重复。
  - 数据库主键改为 `meeting_id + 原始素材 id + 序号`，避免冲突。
- 修复转写空字段问题：
  - `clientTime` 等可空字段写入数据库时统一落为空字符串，避免 SQLite `NOT NULL` 失败。

### 真实运行验证

- 后端语法检查通过：

```bash
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py
```

- FastAPI SQLite 烟测通过：
  - 登录。
  - 创建会议。
  - 追加问题。
  - 推进阶段。
  - 写入会议转写。
  - 读取会议转写。
  - 读取会议列表。

- 前端生产构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 已重启后端，加载 SQLite 版本：
  - `screen` 会话：`ai_compliance_backend`
  - 后端端口：`8000`
  - 前端端口：`3001`

- 通过 `3001` 代理做真实数据库写入烟测：
  - 登录返回 `200`。
  - 创建会议返回 `200`。
  - 写入问题返回 `200`。
  - 写入转写返回 `200`。
  - 软归档返回 `200`。
  - 直接查询 `data/app.db`：
    - 测试会议 `archived=1`。
    - 问题素材 `1` 条。
    - 转写记录 `1` 条。

## AI 会议材料补齐真实上传

### 问题

- 之前 `03 会后终审` 里的“补齐材料”是演示状态：
  - 材料清单是前端常量。
  - `已收集 / 待补充 / 缺失` 不是根据真实文件判断。
  - 点击“补齐材料并通过终审”不会校验真实附件。

### 本次修复

- 新增真实会议材料目录：
  - `data/meeting_files/{meetingId}/`
- 新增材料接口：
  - `GET /api/meetings/{meeting_id}/materials`
  - `POST /api/meetings/{meeting_id}/materials/upload`
  - `GET /api/meetings/{meeting_id}/materials/{material_id}/download`
- 上传文件会真实保存到本地文件夹。
- 文件元数据写入 SQLite：
  - `meeting_materials`
- 材料记录包含：
  - 材料 ID。
  - 材料名称。
  - 原始文件名。
  - 存储文件名。
  - 文件大小。
  - 上传人。
  - 上传时间。
  - 下载地址。
- 终审页面改为读取当前会议真实材料：
  - 默认必备材料均为 `待上传`。
  - 上传后变为 `已上传`。
  - 显示真实文件名、文件大小、上传时间。
  - 支持下载附件。
- `补齐材料并通过终审` 增加真实拦截：
  - 必备材料没上传齐时，不允许通过终审。
  - 页面提示剩余缺失数量。

### 验证结果

- 后端语法检查通过：

```bash
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py
```

- 前端生产构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 已重启后端：
  - 新后端 PID：`50904`
  - 端口：`8000`
- 通过 `3001` 代理做真实材料烟测：
  - 登录返回 `200`。
  - 创建会议返回 `200`。
  - 上传 `资金来源测算表.txt` 返回 `200`。
  - 查询材料列表返回 `200`，数量为 `1`。
  - 下载附件返回 `200`。
  - 直接查询 SQLite：
    - `meeting_materials` 中该会议材料数量为 `1`。
  - 测试会议已软归档。

## AI 会议 Excel 问题台账模板与真实导入

### 问题

- 之前 `导入 Excel 台账` 是模拟导入：
  - 没有模板。
  - 没有真实上传。
  - 没有解析 Excel 行。
  - 只是写入两条固定示例。

### 本次修复

- 新增 Excel 模板下载接口：
  - `GET /api/meetings/issues/template`
- 新增 Excel 台账上传解析接口：
  - `POST /api/meetings/{meeting_id}/issues/import-excel`
- 模板文件名：
  - `AI会议问题收集模板.xlsx`
- 模板包含两个 Sheet：
  - `问题台账`
  - `填写说明`
- `问题台账` 字段：
  - `问题描述`
  - `来源部门`
  - `提交人`
  - `发生时间`
  - `关联项目`
  - `涉及金额(万元)`
  - `材料缺口`
  - `备注`
- 上传解析后，每一行会写入当前会议的问题素材池：
  - `meeting_issue_sources`
- 写入后自动重新生成 AI 议题草案：
  - `meeting_agenda_drafts`
- 前端改造：
  - 原 `导入 Excel 台账` 按钮拆成：
    - `下载模板`
    - `上传台账`
    - `复制收集链接`
  - 上传成功后提示实际导入条数。
  - 空状态文案改为提示先下载模板再上传台账。

### 验证结果

- 后端语法检查通过：

```bash
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py
```

- 前端生产构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 通过 `3001` 代理做真实 Excel 烟测：
  - 登录返回 `200`。
  - 下载模板返回 `200`，文件大小 `8772` 字节。
  - 模板 Sheet：
    - `问题台账`
    - `填写说明`
  - 表头验证：
    - `问题描述`
    - `来源部门`
    - `提交人`
    - `发生时间`
    - `关联项目`
    - `涉及金额(万元)`
    - `材料缺口`
    - `备注`
  - 创建测试会议返回 `200`。
  - 上传模板返回 `200`。
  - 实际导入 `2` 条问题。
  - 直接查询 SQLite：
    - `meeting_issue_sources` 中该会议问题数量为 `2`。
  - 测试会议已软归档。

## AI 会议移动端转写纠错、手写签字与可编辑红头 DOCX

### 问题

- 参会人员手机端录音转写后，AI 识别可能存在错字、误判或口语化噪声。
- 原流程缺少“本人修正 + 手写签字确认”的证据动作，无法证明某条修正内容由本人确认。
- 归档阶段只有页面预览，不够真实；政府演示需要下载可编辑的 `.docx` 红头纪要。
- 正式纪要不能直接混入全部实时 ASR 底稿，否则会把未确认口误、误识别甚至不适合发文的内容写进正文。

### 本次修复

- 新增移动端本人修正与签字确认接口：
  - `POST /api/meeting/transcripts/{meeting_id}/{transcript_id}/correction`
- 接口行为：
  - 必须登录。
  - 普通参会人只能修正本人账号产生的转写。
  - 管理员可处理所有记录。
  - 要求提交修正后的发言文本。
  - 要求提交手机手写签名图片 `data:image/*`。
  - 保留 `originalTranscript`。
  - 写入 `correctedTranscript`、`correctionSigned`、`correctionSignedAt`、`correctionAuthor`、`signatureData`。
  - 写入会议事件 `transcript-correction`，并同步到会议活动日志。
- 移动端录音页新增：
  - 每条实时转写旁显示 `修正签字` / `已签字`。
  - 点击后打开“本人修正与签字确认”面板。
  - 支持修改本人发言文本。
  - 支持手机触摸手写签名。
  - 支持重签、取消、提交修正并签字。
  - 签字画布增加 `touch-action: none`，避免手机写字时被页面滚动打断。
- 新增红头 DOCX 下载接口：
  - `GET /api/meetings/{meeting_id}/archive/docx`
- DOCX 内容：
  - 红头标题：`XX 国有集团党委会会议纪要`。
  - 会议名称、日期、类型、本地项目、审议议题。
  - AI 议题草案。
  - 支撑材料清单。
  - 本人签字确认的发言修正记录。
  - 手机签名图片。
  - 决议与归档说明。
- 正式 DOCX 规则调整：
  - 系统统计全部转写底稿数量。
  - 只把已签字确认的发言写入正式纪要正文。
  - 未签字 ASR 底稿仅作为系统证据链留存，不进入正式纪要正文。
- 桌面端归档页新增：
  - `下载可编辑 DOCX` 按钮。
  - 使用当前登录 Token 真实下载 `.docx` 文件。
- DOCX 字体修复：
  - 生成时显式写入中文字体映射。
  - 使用 `Arial Unicode MS` 写入 `w:eastAsia`，保证 macOS/Word/QuickLook 打开中文正常。

### 验证结果

- 后端语法检查通过：

```bash
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py
```

- 前端生产构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 后端已重启并监听：
  - `0.0.0.0:8000`
- 前端仍监听：
  - `0.0.0.0:3001`
- 真实接口烟测通过：
  - 登录管理员返回 `200`。
  - 创建一条测试转写返回真实 `transcript_id`。
  - 提交修正文本 + 手机签名返回 `200`。
  - 返回记录中 `correctionSigned = true`。
  - 下载红头 DOCX 返回 `200`，文件大小约 `38 KB`。
- DOCX 渲染验证：
  - 使用 `render_docx.py` 成功渲染为 PDF/PNG。
  - `pdftotext` 抽取中文完整。
  - 使用 macOS QuickLook 预览中文、红头标题、正式正文正常。
  - 最终正式正文只保留已签字确认发言，未确认底稿没有混入正文。
  - 烟测产生的签字样例已清洁为正式会议话术，最终 DOCX 不含 `测试` 文案。

## OA 表单局部接入 Arco Design

### 问题

- 用户希望表单模块按 Arco Design 的快速上手方式接入。
- 其他 AI 问答、合规问答等页面仍需保持 Ant Design，不做全局替换。
- 直接引入 Arco 全量 CSS 会拖慢页面，不符合 Arco 文档里的最佳实践建议。

### 本次修复

- 安装项目依赖：
  - `@arco-design/web-react`
- 只在 OA 流程页局部使用 Arco：
  - `src/pages/OAWorkflow.jsx`
- 替换左侧 `1 填单并发起` 表单组件：
  - `ArcoForm`
  - `ArcoInput`
  - `ArcoSelect`
- 保留其他页面 Ant Design：
  - 合规问答未改。
  - 知识问答未改。
  - AI 审查页未改。
  - 用户管理等后台页未改。
- 样式接入使用组件级按需样式：
  - `@arco-design/web-react/es/Form/style/css.js`
  - `@arco-design/web-react/es/Input/style/css.js`
  - `@arco-design/web-react/es/Select/style/css.js`
- 未使用全量：
  - `@arco-design/web-react/dist/css/arco.css`
- 增加 `.oa-arco-quick-form` 局部样式，使 Arco 表单与当前政府 OA 页面视觉统一。
- 表单颜色和字重改为 Arco token：
  - 主色：`rgb(var(--primary-6))`
  - 正文：`var(--color-text-1)`
  - 标签：`var(--color-text-2)`
  - 占位：`var(--color-text-3)`
  - 边框：`var(--color-border-2)`
  - 禁用填充：`var(--color-fill-2)`
  - 字重：`var(--font-weight-400/500)`

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 构建确认：
  - 全量 Arco CSS 接入时 OA CSS chunk 约 `570 KB`。
  - 改为组件级按需样式后 OA CSS chunk 约 `248 KB`。
  - Ant Design 相关页面仍单独走原有 Ant chunk。
  - 调整 Arco token 颜色和字重后再次构建通过，CSS chunk 未回涨。

## OA 填单区现代化重构

### 问题

- 仅替换 Arco 输入框后，页面仍显老旧：
  - 外层仍是传统后台三栏小面板。
  - 表单区域缺少明确任务标题和状态层级。
  - 大量小字、重边框、嵌套卡片导致视觉像旧 OA 配置页。
  - 字重偏重，信息没有现代政务工作台的呼吸感。

### 本次修复

- 重构 OA 左侧 `1 填单并发起` 区域：
  - 顶部改为现代任务面板头部。
  - 增加 `STEP 01`、主标题、说明和草稿/已提交状态。
  - 增加当前事项摘要条。
  - 保留 Arco 表单作为核心输入区域。
  - 将发起人、紧急程度、预算口径、拟上会议改为轻量元信息网格。
  - 将材料清单改为更轻的列表，不再像旧式表格卡片。
  - 动作按钮区改为底部主操作结构。
- 视觉规范：
  - 主色继续使用 Arco `primary-6`。
  - 字重降低到 `400/500/600`。
  - 使用 Arco `color-text-*`、`color-border-*`、`color-fill-*` token。
  - 减少重边框和传统标题栏。

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 构建结果：
  - OAWorkflow JS chunk 正常生成。
  - OAWorkflow CSS chunk 仍保持按需 Arco 样式，没有回退到全量 Arco CSS。

## UI 一致性规范文档

### 问题

- 当前系统各功能模块 UI 风格不统一：
  - 卡片圆角、边框、阴影不一致。
  - 表单高度、字号、字重不一致。
  - 间距没有统一 8px 网格。
  - AI 面板、表单、列表、证据区的信息结构混乱。
  - 同一个业务状态在不同页面颜色含义不一致。

### 本次修复

- 新增文档：
  - `UI一致性规范.md`
- 文档明确：
  - 亲密、对齐、分类、易用四个原则。
  - Arco/Ant 统一色彩 token。
  - 字号和字重规则。
  - 间距、圆角、边框规范。
  - Dashboard、OA、AI 问答、合同审查、AI 会议页面结构规范。
  - 表单、卡片、状态、AI 面板组件规范。
  - 分批迁移顺序。
  - 验收标准。

## 第一批 UI 一致性迁移

### 本次范围

- `frontend/src/index.css`
- `frontend/src/App.jsx`
- `frontend/src/pages/OAWorkflow.jsx`
- `frontend/src/pages/MeetingComplianceWorkflow.jsx`
- `frontend/src/components/OfficeEditor.jsx`

### 本次修复

- 建立全局 CSS token：
  - `--ui-primary`
  - `--ui-text-1/2/3/4`
  - `--ui-border-1/2`
  - `--ui-fill-1/2`
  - `--ui-bg-page`
  - `--ui-bg-panel`
  - `--ui-radius-*`
  - `--ui-space-*`
  - `--ui-shadow-panel`
  - `--ui-font-*`
- 新增暗色模式 token：
  - 通过 `data-ui-theme="dark"` 切换。
- App 外壳统一：
  - 主色统一为 `#165DFF`。
  - 页面背景统一为 `var(--ui-bg-page)`。
  - 主内容容器统一为 `16px` 圆角、统一边框、统一阴影。
  - Header 字重降低到 `600`。
  - Ant ConfigProvider token 统一到项目 token。
- OA 页面统一：
  - 内部 palette 接入全局 token。
  - 面板阴影、标题字重统一。
  - 继续保留 Arco 表单，但与全局 token 对齐。
- AI 会议页面统一：
  - 基础 palette 接入全局 token。
  - 面板背景、边框、阴影、状态色与全局一致。
- 合同审查 OfficeEditor 基础统一：
  - 外壳背景接入 `--ui-bg-page`。
  - 文档纸张背景、边框、阴影接入统一 token。
  - 顶部工具条、提示层、底部栏使用统一色彩语义。
- 全局残留旧 Ant 蓝局部清理：
  - 将 `#1677ff` 系列替换为 `var(--ui-primary)` 或 `#165DFF`。
- 核心页面字重收敛：
  - `OAWorkflow.jsx`
  - `MeetingComplianceWorkflow.jsx`
  - `OfficeEditor.jsx`
  - 将主要 `800/900` 字重降到 `600`，避免页面显得老旧、厚重。

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 构建产物正常：
  - `App`
  - `OAWorkflow`
  - `MeetingComplianceWorkflow`
  - `OfficeEditor`
  - `MobileMeetingRecorder`
- 二次构建验证通过：
  - 清理旧蓝和重字重后再次执行 `npm run build`，构建正常。

## AI 会议状态标签黑底修复

### 问题

- AI 会议列表顶部 `6 个会议批次` 等状态标签出现黑底。
- 原因是 `StatusPill` 继续使用 Ant `Tag color="default"`，在当前统一 token / 暗色变量混合后渲染成黑色底。

### 本次修复

- 将 `MeetingComplianceWorkflow.jsx` 中的 `StatusPill` 从 Ant `Tag` 改为稳定的自定义 `span`。
- 新增统一色表：
  - `blue / processing`
  - `green`
  - `orange / gold`
  - `red`
  - `cyan`
  - `default`
- `default` 使用：
  - 背景：`var(--ui-fill-1)`
  - 文字：`var(--ui-text-2)`
  - 边框：`var(--ui-border-2)`
- 避免 Ant `Tag` 的默认色在不同主题下失控。

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```
- 仍有大 chunk 警告：
  - `vendor-antd`
  - 该问题为既有依赖体积问题，本次未做代码分割重构。

## 全局 Tag 与 OA 审批入口补齐

### 问题

- 之前只在 AI 会议页面局部修复了状态标签黑底问题，其他页面继续使用 Ant `Tag` 时仍可能出现黑底、黑字或暗色态失控。
- OA 审批页缺少总入口，一进入就是单个审批流程详情，用户无法先看到“有哪些案卷、谁创建、当前到哪一步、风险是什么”。

### 本次修复

- 在 `frontend/src/index.css` 增加全局 Ant `Tag` 样式兜底：
  - 基础 `.ant-tag`
  - `default`
  - `blue / processing`
  - `cyan`
  - `green / success`
  - `orange / gold / warning`
  - `red / error`
- 所有 Tag 统一为：
  - 999px 胶囊圆角
  - 12px 字号
  - 500 字重
  - 统一边框和柔和底色
- 暗色模式下补充 `default` 标签兜底，避免再次出现纯黑块。
- 在 `frontend/src/pages/OAWorkflow.jsx` 新增 OA 审批案卷总列表：
  - 顶部说明当前是“案卷总台账”。
  - 增加统计卡片：全部案卷、流转中、AI 拦截、待归档。
  - 增加案卷表格字段：审批案卷、事项类型、发起信息、当前阶段、状态、风险、归档、操作。
  - 操作按钮支持：
    - `进入`：进入单个审批流程详情。
    - `编辑`：弹窗说明并进入案卷详情页编辑。
    - `删除`：弹出权限/留痕说明，不执行真实删除，避免误删演示数据。
  - 详情页顶部新增 `返回案卷列表`。
  - 新建案卷未填写标题时，详情页标题显示为 `新建审批案卷`，避免继续露出旧演示标题。

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 端口检查：
  - `localhost:3001` 正在监听。
- 仍有既有大 chunk 警告：
  - `vendor-antd`
  - 本次未做依赖拆包。

## AI 会议 Excel 问题导入修复

### 问题

- 在 `AI 会议合规` 的问题收集页上传 Excel 台账时，页面提示：
  - `Excel 台账未写入后端：会议不存在`
- 原因是当前真实流程为“先收集问题，再基于问题创建会议”，但后端 Excel 导入接口之前要求 `meetingId` 必须已经存在。
- 当前页面会生成类似 `meeting-local-20260610094510` 的本地会议 ID，但此时还没有正式创建会议记录，因此后端返回 404。

### 本次修复

- 修改 `backend_full.py` 的 `POST /api/meetings/{meeting_id}/issues/import-excel`：
  - 如果会议不存在，不再返回 `会议不存在`。
  - 自动创建一个 `问题收集中` 状态的临时 AI 会议。
  - 再把 Excel 中的问题写入该会议的 `issueSources`。
  - 随后自动派生 `agendaDrafts`，用于前端生成待会议题。

### 验证结果

- 使用 `AI会议问题收集模板_已填满测试数据.xlsx` 直接请求当前页面会议：
  - `meeting-local-20260610094510`
- 接口返回成功：
  - `importedCount: 79`
  - `phase: 问题收集中`
  - 已生成会议问题池和议题草稿。
- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## AI 会议会中页二维码与管理员身份修复

### 问题

- `扫码邀请录音` 弹窗左侧二维码区域为空白，看不到可扫码二维码。
- 会中共享画面仍写死显示 `高新区二期厂房消防改造追加预算审议`，即使当前会议/导入议题已经变化。
- 当前登录用户是 `admin / 系统管理员`，但会中参会人卡片和移动端录音角色仍显示为 `张敏`。

### 本次修复

- `MeetingComplianceWorkflow.jsx`：
  - 组件接收 `currentUser`。
  - 会中主持人卡片改为当前登录用户：
    - `系统管理员（我）`
    - `会议管理员`
  - 临时创建会议的创建人改为当前登录用户部门与姓名。
  - 右侧会中提醒的角色文案改为当前用户会议角色，不再写死秘书/张敏。
  - 共享正文标题改为当前 `agendaTitle`，不再写死高新区标题。
  - 会中归档说明和待办提示同步使用当前议题与当前用户。
  - 邀请录音二维码改为显式 `svg` 渲染，并指定黑色前景、白色背景和固定尺寸，避免在弹窗中空白。
- `MobileMeetingRecorder.jsx`：
  - 移除前端移动端角色表里 `admin -> 张敏` 的错误映射。
- `backend_full.py`：
  - 移除后端 `admin -> 张敏` 的声纹/角色映射。
  - 管理员默认会议身份改为：
    - `displayName: 系统管理员`
    - `meetingRole: 会议管理员`
    - `seat: 主控席`
  - Excel 导入后，如果原会议仍是高新区 demo 占位议题/项目，会用新生成的议题草稿覆盖占位信息。

### 验证结果

- 后端语法检查通过：

```bash
/Users/macos/miniconda3/bin/python -m py_compile backend_full.py
```

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

- 后端录音会话接口验证：
  - `displayName: 系统管理员`
  - `meetingRole: 会议管理员`
  - `seat: 主控席`

## AI 会议会中页价值表达重构

### 问题

- 会中页原本像视频会议壳子，中心区域大面积展示“共享文档”，但用户看不出 AI 的价值。
- AI 提醒卡悬浮在右侧，信息太弱，用户需要自己猜：
  - 当前 AI 在做什么？
  - 已经抓到了什么？
  - 下一步应该处理什么？
  - 为什么要用这个系统而不是普通会议软件？

### 本次修复

- 保留原有会议风格，但将中间区域改为“AI 会中驾驶舱”：
  - 左侧：当前议题、会议名称、本地项目、当前说话人。
  - 中间：AI 会中驾驶舱，明确展示：
    - `听谁在说`
    - `抓到了什么`
    - `下一步做什么`
    - `阶段结论`
    - `合规判断`
    - `证据沉淀`
  - 右侧：现在该看什么，展示材料缺口、合规提醒、待办，并保留标决议/记待办/有争议/缺材料快捷按钮。
- 删除“用户自己猜功能”的表达方式，把价值改成一眼可读的业务语言：
  - 抓结论
  - 抓风险
  - 抓待办
  - 留证据
- 增加 1440px 与 1180px 响应式适配：
  - 中屏自动变两栏。
  - 小屏自动变单栏并允许滚动。

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## AI 会议会中页按截图压缩与滚动修正

### 问题

- 页面顶部信息区太高，完整标题和说明常驻，占用会中关键区域。
- 会中参会人条和正文区比例不合理，整体显得压迫。
- 中间驾驶舱、右侧“现在该看什么”等区域内容被底部控制条截断，看起来像不能滚动。

### 本次修复

- 顶部改成紧凑关键状态条：
  - 保留会议阶段、会议类型、日期、本地项目、返回会议列表。
  - 标题和项目/会议名合并为一行，长文本省略。
  - 全链路闭环度卡片缩小。
- 会中参会人条调整：
  - 高度从 `116px` 调整为 `128px`。
  - 卡片间距加大，头像区域更稳定。
- 会中驾驶舱区域修正：
  - `meeting-copilot-stage` 改为真实内部滚动。
  - 左侧议题卡、中间 AI 驾驶舱、右侧行动卡均设置 `min-height: 0`、`max-height: 100%`、`overflow: auto`。
  - 底部控制条高度收敛，避免遮住内容。
- 响应式仍保留：
  - 中屏两栏。
  - 小屏单栏滚动。

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```

## 手机扫码录音页刷新跳首页修复

### 问题

- 手机扫码进入 `/mobile-recorder` 后，页面刷新或隧道服务自动补末尾斜杠时，前端可能把路径识别成普通后台应用入口。
- 原判断为严格匹配：
  - `window.location.pathname === '/mobile-recorder'`
- 如果手机浏览器访问成 `/mobile-recorder/`，就会被判定为非录音页，登录后进入系统首页。
- 手机录音页里管理员身份兜底仍显示成 `会议秘书 / 移动端席位`，不符合 `系统管理员` 真实身份。

### 本次修复

- `App.jsx`：
  - 手机录音路径判断改为兼容末尾斜杠：
    - `window.location.pathname.replace(/\/+$/, '') === '/mobile-recorder'`
  - `/mobile-recorder` 和 `/mobile-recorder/` 都会保持在手机录音页。
- `MobileMeetingRecorder.jsx`：
  - 管理员兜底身份改为：
    - `会议管理员`
    - `主控席`
  - 不再显示为会议秘书或移动端席位。

### 验证结果

- 前端构建通过：

```bash
cd "/Users/macos/Documents/ai 合规 demo 4:28/frontend"
PATH=./node_modules/.bin:$PATH npm run build
```
