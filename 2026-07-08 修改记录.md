# 2026-07-08 修改记录

## 今日目标

系统稳定性加固：消除端口冲突 crash loop、修复中间件异常处理、保护后台任务错误传播、配置日志轮转。

---

## 已完成修复 ✅（共 4 项）

### 1. 端口冲突 crash loop 根因修复

- **问题**：systemd `Restart=on-failure` + `RestartSec=5s` 在进程崩溃后快速重启，但旧进程 TCP 端口处于 TIME_WAIT 状态（内核默认保留 60 秒），新进程绑定失败 `exit(1)` 触发再次重启，形成死循环。历史日志累计 **466 次** `address already in use` 错误
- **佐证**：每次 crash 循环都白跑一遍 ChromaDB + SentenceTransformer 预热（约 10 秒），浪费 GPU 显存和启动时间
- **修复**：
  - `backend_full.py` `_check_port_available()`：添加 `SO_REUSEADDR` 套接字选项，内核级别允许重用 TIME_WAIT 端口
  - 启动逻辑：端口被占用时等待最多 15 秒重试（每秒一次），而非直接退出。15 秒超时后才放弃并输出占用进程信息
  - uvicorn 参数：添加 `timeout_graceful_shutdown=10`，确保 SIGTERM 后有足够时间完成正在处理的请求
- **验证**：重启后 0 次端口冲突，服务正常启动

### 2. 中间件全局异常保护

- **问题**：`audit_request_middleware` 中 `call_next()` 无 try/except 包裹，未处理异常会导致请求挂起且审计日志丢失
- **修复**：`call_next()` 外层包裹 try/except，未处理异常返回标准 500 JSON 响应并记录完整 traceback
- **文件**：`backend_full.py`（`audit_request_middleware` 函数）

### 3. 后台任务错误保护

- **问题**：多个 `asyncio.create_task()` 为 fire-and-forget 模式，未捕获的异常被静默吞掉（仅在 task 垃圾回收时打印到 stderr），导致功能异常难以排查
- **修复**：新增 `_safe_create_task(coro, name)` 辅助函数，自动捕获异常并记录日志。4 个 fire-and-forget 任务已替换：
  - Whisper 终审触发 (`_run_whisper_review_for_meeting`)
  - Whisper DOCX 生成 (`_generate_whisper_meeting_docx`)
  - 会话 SSE 推送 (`sse_manager.publish` × 2)
- **保留原样**：3 个后续会被 await/cancel 的任务（lifespan ASR 清理、DashScope WebSocket、静音监控）
- **文件**：`backend_full.py`

### 4. systemd 输出日志轮转

- **问题**：`backend_server.log`（systemd stdout/stderr 重定向）无 rotation，持续增长至 16MB+。Python 内置的 `RotatingFileHandler` 只管 `logs/backend.log`，不管 systemd 输出
- **修复**：创建 `/etc/logrotate.d/ai-compliance` 配置，每天或超过 50MB 时 rotate，保留 7 份，压缩旧日志
- **文件**：`ai-compliance-logrotate` → `/etc/logrotate.d/ai-compliance`

---

## 涉及文件

| 文件 | 改动类型 |
|------|---------|
| `backend_full.py` | 修改：端口检查、中间件、后台任务 |
| `ai-compliance-logrotate` | 新建 |
| `/etc/logrotate.d/ai-compliance` | 新建（sudo cp） |
