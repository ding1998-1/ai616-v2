# 2026-08-25 今日完成与明日待办

## 今日完成

### 1. 声纹流程重定义

- 取消“会前必须录入声纹”的阻塞逻辑。
- 声纹改为会后完整录音生成后的身份匹配与分轨步骤。
- 声纹失败不影响现场录音、实时转写和会议推进。
- 恢复声纹路由注册，并改为可选模型依赖的延迟加载。

### 2. 手机扫码与身份绑定

- 手机录音页加入当前会议 Token 有效性校验。
- 旧会议或错误会议的登录状态不再被盲目复用。
- 扫码注册时校验会议真实存在。
- 参会人写入数据库后增加反查确认，避免“表面注册成功、实际未入会”。

### 3. Whisper 终审后台任务

- 实现 Whisper 终审的排队、运行、完成和失败状态。
- 录音合并后使用本地 `large-v3` 进行终审转写。
- 任务固定使用 GPU1，不占用 GPU0。
- 前端改为读取后端真实任务状态。

### 4. DOCX 生成与下载

- 红头 DOCX 改为先生成、再下载。
- 旧下载入口在文件不存在时会自动触发生成。
- 减少直接请求未生成文件导致的 HTTP 404。

### 5. 签字与归档拦截

- 统一会议归档服务的全员签字检查。
- 未完成必需签字时，阻止进入归档阶段。
- 阻断时返回明确的 HTTP 409 业务状态。

### 6. 手机多端录音回传重构

- 参考 MIT 开源项目 `Realmullens/tributary` 的可恢复录音状态机。
- 手机每 3 秒生成录音分片。
- 分片先写入 IndexedDB，再上传服务器。
- 只有收到服务器 ACK 后才删除手机本地分片。
- 页面刷新、断网或意外中断后，可查询服务器已收分片并按差集续传。
- 每次录音生成独立 `recording_session_id`，避免同一手机多次录音串片。
- 分片不完整时服务端禁止合并。
- 修复结束录音时未重传真实 pending ACK 队列的问题。
- 修复合并请求未携带 `client_id` 的问题。

### 7. 录音检查点与时间轴

- 参考 `/home/ai/文档/meeting-minutes` 的检查点与崩溃恢复设计。
- 每 10 个三秒分片聚合为一个 30 秒检查点状态。
- 检查点记录分片范围、字节数和相对时间范围，不重复保存音频。
- 服务端为每个录音会话生成独立 manifest。
- 最终合并后保存录音开始时间、分片数、输出文件和字节数。
- Tributary 的端侧续传与 meeting-minutes 的检查点被合并为同一条链路，没有双份音频或双重状态机。

### 8. 开源合规

- 新增 `THIRD_PARTY_NOTICES.md`。
- 保留 Tributary 的 MIT 许可声明和原始版权信息。

### 9. 验证结果

- 正式后端测试集：116 项全部通过。
- 录音服务与检查点定向测试：8 项通过。
- 前端 Vite 生产构建：通过。
- 仓库根目录两个 Chroma 手工诊断脚本因本地轻量环境未安装 `langchain_chroma` 无法收集；不属于正式 `tests/` 测试集。

### 10. 44 服务器部署

- 新版已部署至 `/home/ai/文档/ai616`。
- `ai-compliance.service` 正常运行并保持开机自启。
- Nginx 80 端口页面返回 HTTP 200。
- 后端 8002 端口返回 HTTP 200。
- 新录音状态接口 `/api/meeting/recorder/audio/status` 已注册。
- 生产数据库 `data/app.db` 保留，未覆盖。
- GPU0/vLLM 8088 与 ASR 8091 未停止、未修改。
- 部署前备份位于 `/home/ai/文档/deploy-backups/20260825-161955`。

## 明日待办

### P0：生产录音 E2E

1. 使用两台真实手机扫码加入同一会议。
2. 验证两个参会人获得不同身份、设备 ID 和录音会话 ID。
3. 并发录音 10–15 分钟，检查分片 ACK、转写回传和桌面端状态。
4. 录音中主动断网，恢复网络后检查差集续传。
5. 录音中刷新页面或杀掉浏览器，重新进入后检查 IndexedDB 恢复。
6. 人为删除一个分片，确认服务端返回 409 并禁止合并。
7. 检查每台手机生成独立完整音轨，不得串入其他参会人录音。
8. 核对 30 秒检查点 manifest 与实际分片完全一致。

### P0：转写与终审

1. 验证阿里在线 ASR 主链路。
2. 实现并验证“阿里在线优先，本地 ASR 自动降级”，不再依赖用户手动切换。
3. 在真实完整录音上触发 Whisper 终审。
4. 检查 Whisper 任务状态、GPU1 资源、终审文本和时间轴。
5. 将会后声纹匹配接到 Whisper 完整音轨，匹配失败时保持非阻塞。

### P0：完整会议闭环

1. 完成“建会 → 扫码加入 → 多端录音 → 转写 → 终审 → 纪要 → 签字 → 归档”生产 E2E。
2. 验证未签齐时所有归档入口均被拦截。
3. 验证 DOCX 首次下载自动生成，不再返回 404。
4. 检查会议记录 DOCX 的仿宋、黑体、表格、页码和落款。

### P1：仍未完成的产品要求

1. 按会议类型自动生成规范文号。
2. 用户管理改为“一人多角色＋组织范围＋会议范围”。
3. 议题保密标签、权限范围接入创建页和议题管理页。
4. 制度条款、历史决议与议题知识条目建立正式关联。
5. 完成上述数据库结构设计，在执行 Schema 迁移前单独确认具体表和字段。
6. 会议搜索结果以议题为最小单元，明确显示“该议题属于哪场会议”。

### P1：UI 与易用性

1. 用 1920×1080 登录态逐页检查创建会议、会中、会后整理和归档页。
2. 清理滚动容器嵌套、内容遮挡、右侧栏越界和底部操作不可见。
3. 重做会后整理页的信息层级，减少高密度文本和原生音频控件堆叠。
4. 待办事项在页面上明确表达“决议的执行任务”，包含责任人、期限、来源议题和完成状态。

## 交接说明

- 当前生产服务正常，但双手机并发、断网续传和刷新恢复尚未完成真机 E2E，不应标记为最终验收通过。
- 明日应先做生产录音 E2E，再继续产品功能与 UI 开发。
