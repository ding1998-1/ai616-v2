# 会议与议题统一检索改造

## 目标

- 会议是容器，议题是可独立检索的业务单元。
- 搜索议题时，必须同时告知用户“该议题属于哪场会议”。
- 点击议题结果后进入所属会议并定位议题，不因搜索行为擅自改变当前讨论议题。
- 搜索结果必须继承会议访问权限与议题 ACL，禁止泄露保密议题标题、说明和转写内容。

## 数据库设计

新增 `meeting_search_documents` 检索文档表，不替代会议主表和议题主表。

| 字段 | 用途 |
| --- | --- |
| `document_key` | `meeting:{id}` 或 `agenda:{id}`，全局唯一 |
| `entity_type` | `meeting` / `agenda` |
| `entity_id` | 会议 ID 或议题 ID |
| `meeting_id` | 所属会议 ID；会议结果等于自身 ID |
| `title` | 会议名称或议题名称 |
| `content` | 项目、会议旧议题文本或议题说明 |
| `status` | 会议阶段或议题状态 |
| `confidentiality_level` | 议题保密级别，会议默认 `normal` |
| `updated_at` | 搜索排序和同步检查 |

索引：

- `(entity_type, updated_at)`
- `(meeting_id, entity_type)`
- `(entity_id, entity_type)`

同步策略：

1. 应用启动时幂等创建表、索引和触发器。
2. 首次迁移从 `meetings` 和 `meeting_agendas` 全量回填。
3. 会议或议题新增、修改、删除时由 SQLite 触发器同步。
4. 会议删除时同时删除该会议的全部检索文档。

## API

`GET /api/meetings/search?q={keyword}&limit={limit}`

返回统一 `results` 数组，每项包含 `type`：

- `type=meeting`：会议结果。
- `type=agenda`：议题结果，必须包含 `agendaId`、`agendaTitle`、`meetingId` 和 `meetingTitle`。

搜索顺序：标题精确命中 > 标题包含 > 说明/项目包含 > 更新时间。

## 前端交互

- 无关键词时保持原会议列表。
- 输入关键词后调用远程搜索，并做防抖和请求取消。
- 结果按“会议”和“议题”分类展示。
- 议题行明确显示 `属于：{meetingTitle}`。
- 点击议题后打开会议、滚动到议题并短暂高亮；只有用户主动点击“进入该议题”才改变当前议题。

## 验收

- 搜索议题标题和说明均能命中。
- 议题结果正确显示所属会议。
- 无会议权限的用户不能通过搜索获知会议。
- 无议题权限的用户不能通过搜索获知保密议题。
- 旧数据库升级后无需人工处理即可搜索。
- 1920×1080 下搜索结果不遮挡、不溢出，键盘与鼠标均可操作。

## 2026-08-24 实施记录

- 已新增 `meeting_search_documents` 及 3 组索引。
- 已新增会议/议题增、改、删自动同步触发器。
- 已回填旧会议，并在搜索前只物化“有草稿但无正式议题”的会议。
- 已实现会议权限 + 议题 ACL 双重过滤。
- 已实现前端会议/议题分组结果、“属于哪场会议”文案和议题定位高亮。
- 新增 2 项专项测试，全部通过；前端生产构建通过。
- 全量测试在当前本机环境为 `98 passed / 7 failed / 4 collection errors`；失败集中在 FastAPI/Starlette 版本不匹配、未配置 `AUTH_SECRET`、旧接口鉴权预期和缺少 async pytest 插件，与本次搜索改造无关。
