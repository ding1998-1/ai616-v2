# 2026-08-10 修复记录

## 问答功能 network error 修复

**现象**：合规问答页面提问后显示"检索失败：network error"

**根因**：线上 nginx 配置缺少 `location = /api/kb_stream`，SSE 流式请求被兜底规则缓冲

**修复**：在 `/etc/nginx/sites-available/ai-compliance` 添加 SSE 专用 location：

```nginx
location = /api/kb_stream {
    proxy_pass http://127.0.0.1:8002/kb_stream;
    proxy_http_version 1.1;
    proxy_set_header Connection '';
    proxy_buffering off;
    proxy_cache off;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_read_timeout 600s;
}
```

**执行**：`sudo bash /home/ai/文档/ai616/fix_kb_stream.sh`

**教训**：nginx 配置修改后必须同步部署到线上，不能只改 `nginx-fixed.conf` 不推生产。
