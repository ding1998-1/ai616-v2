# AI 会议系统 — 源码架构与问题排查报告

> 日期：2026-07-01
> 范围：AI 会议实时语音转写系统（本地 FunASR SenseVoice + CAM++ 链路）

---

## 一、源码结构

### 1.1 核心文件

```
/home/ai/桌面/funasr_asr_server.py              # Paraformer ASR 推理服务（GPU 1, 端口 8091）
/home/ai/文档/ai616/backend_full.py              # 主后端（GPU 0, 端口 8002）
/home/ai/文档/ai616/backend/qwen_asr_client.py   # ASR HTTP 客户端
/home/ai/文档/ai616/frontend/src/pages/MobileMeetingRecorder.jsx  # 移动端录音页面
/home/ai/文档/ai616/frontend/public/pcm-processor.js              # AudioWorklet 处理器（备用）
```

### 1.2 数据流

```
iPhone Safari
  │  getUserMedia() → MediaStream
  │  AudioContext({sampleRate:16000}) → ScriptProcessor(8192)
  │  downsampleTo16k → floatToPcm16 → WebSocket binary
  ▼
nginx (aimeeting.xingsnb.cn:443)
  │  proxy_pass → 127.0.0.1:8002
  │  WebSocket: /api/meeting/asr/qwen/ws  (SenseVoice/Qwen 路径)
  │  WebSocket: /api/meeting/asr/ws        (DashScope 路径)
  ▼
backend_full.py (8002)
  │  meeting_asr_qwen_websocket() — 本地 ASR handler
  │  meeting_asr_websocket()       — DashScope 代理 handler
  │  QwenASRClient.send_chunk()    — HTTP POST PCM 到 ASR
  ▼
funasr_asr_server.py (8091, GPU 1)
  │  /api/start → Session
  │  /api/chunk → 攒 600ms → asr_model.generate()
  │  /api/finish → is_final=True
  ▼
Paraformer (speech_seaco_paraformer_large) + FSMN-VAD + CT-Punc + CAM++
```

### 1.3 辅助文件

| 文件 | 作用 |
|------|------|
| `backend/config.py` | 配置（QWEN_ASR_URL 等） |
| `backend/db.py` | SQLite 数据库操作 |
| `backend/llm_client.py` | LLM 客户端（DeepSeek） |
| `backend/models.py` | 数据模型 |
| `backend/deps.py` | FastAPI 依赖注入 |
| `frontend/src/App.jsx` | 前端路由入口 |
| `frontend/src/lib/auth.js` | 认证模块 |
| `ai-compliance.service` | 后端 systemd 服务 |
| `paraformer-asr.service` | ASR systemd 服务 |
| `nginx-8002.conf` | nginx 配置 |
| `restart_safe.sh` | 安全重启脚本 |

---

## 二、7 个架构修复（2026-07-01 下午）

### P0 — 直接影响长会议可用性

#### #2 每次停顿 >1.5s 重建 ASR session，上下文清零
- **根因**：`backend_full.py` 1.5s 超时 flush 时调用 `finish()` + `start()`，同时把 `last_full_text`、`committed_text`、`sent_tail` 全部清零。Paraformer 流式 cache 丢失。
- **修复**：拆分"缓冲区 flush"和"session 生命周期"。flush 只提交 `pending_buffer`，不重建 session。`last_full_text` 改为只追加不清零。删除 `committed_text`（死代码）。
- **文件**：`backend_full.py` `meeting_asr_qwen_websocket()`

#### #4 超时检测依赖"收到新帧才触发"，手机断网时永不触发
- **根因**：静默超时检查写在 `while True` 收包循环内，依赖 `await websocket.receive()` 返回才执行。断网后循环阻塞，永不超时。
- **修复**：`asyncio.create_task` 启动独立 `_silence_flush_monitor()` 协程，每 0.3s 检查 `last_change_time`，与主循环解耦。
- **文件**：`backend_full.py` `meeting_asr_qwen_websocket()`

### P1 — 影响延迟和稳定性

#### #1 全局单锁 + `generate()` 阻塞 event loop
- **根因**：`funasr_asr_server.py` 用一把全局 `SESSION_LOCK`，`asr_model.generate()` 同步 torch 推理阻塞 event loop。
- **尝试修复**：Session 各自持锁 + `asyncio.to_thread()` 线程池。
- **回滚原因**：`asyncio.to_thread()` 导致 Paraformer 推理全部返回空结果（疑似 CUDA 上下文在线程池子线程中不可用）。已回滚到 v4 原始同步调用。
- **文件**：`funasr_asr_server.py`（部分回滚，保留 per-session lock 设计思路待验证）

#### #3 ScriptProcessor 主线程运行，手机高负载丢帧
- **根因**：`MobileMeetingRecorder.jsx` 使用已废弃的 `createScriptProcessor(8192)`，在主线程同步执行音频回调。
- **尝试修复**：创建 `pcm-processor.js` AudioWorklet processor，音频处理移到独立 worklet 线程。
- **回滚原因**：AudioWorklet 在 iOS Safari 上输出电平异常低（peak=61 vs 正常=15977），原因待查。已回退到 ScriptProcessor 直接调用。
- **文件**：`MobileMeetingRecorder.jsx`（保留 AudioWorklet 备用）、`pcm-processor.js`（备用）

#### #6 `send_chunk` 无重试，超时直接丢帧
- **根因**：`qwen_asr_client.py` `send_chunk` 默认 10s 超时，单次 HTTP 超时直接返回空，音频帧丢失。
- **修复**：`send_chunk` 加 5s 超时 + 1 次重试（仅瞬时错误）。新增 `chunk_timeout` 参数。QwenASRClient 改为应用级单例（`_get_qwen_client()`）。检测到 ASR session 400 错误时自动重建 session。
- **文件**：`backend/qwen_asr_client.py`、`backend_full.py`

### P2 — 工程质量

#### #5 `_asr_pending_store` 无 TTL，进程重启失效
- **根因**：进程内全局 dict，无过期清理。进程重启后 store 清空。
- **修复**：值格式 `{"text": str, "timestamp": float, "version": int}`，TTL 5 分钟。新增 `_cleanup_asr_pending_store()` 后台协程，每 60s 清理。读取兼容旧格式。
- **文件**：`backend_full.py`

#### #7 GC 在每个 chunk 请求热路径上同步执行
- **根因**：`funasr_asr_server.py` `_get()` 每次调用 `_gc()` 遍历全部 session，O(N) per chunk。
- **修复**：GC 改为后台 `_gc_background()` 定时任务（30s 间隔），从热路径移除。SESSIONS 增删统一在锁内操作。
- **文件**：`funasr_asr_server.py`

---

## 三、2026-07-01 线上问题排查全记录

### 3.1 问题线

```
09:34  ✅ FunASR 正常识别（巅峰状态）
       PCM: peak=15977 rms=7836  raw='嘿嘿嘿嘿嘿...实时拉新非常的重要...'

上午   📝 7个架构修复实施
       backend_full.py → v5（session不销毁、silence monitor、TTL、单例client）
       funasr_asr_server.py → v5（per-session lock、to_thread、后台GC）
       qwen_asr_client.py → 重试+短超时
       MobileMeetingRecorder.jsx → AudioWorklet
       pcm-processor.js → 新建

12:14  ❌ 录音但走浏览器SR，FunASR全 empty speech
       后端重启后WS session 失效 → 400 错误 → 加自动重建

12:58  ❌ AudioWorklet 输出电平极低（peak=61-92 vs 正常=15977）
       → 回退到 ScriptProcessor → peak升至 2466~32767
       但 Paraformer 仍 empty speech

13:00  ❌ ASR 重启后 peak=32767（削波失真）
       → 去掉 AGC 增益 → peak 跌回 1623-3464
       但 Paraformer 仍 empty speech

13:17  ❌ 回滚 funasr_asr_server.py 到 v4（去掉 to_thread）
       仍 empty speech

13:21  ❌ 用真实录音文件直接测 ASR API → 仍返回空文本
       分析录音文件 → 99% 样本接近零（peak=756/32767=2.3%）

13:32  ✅ 定位真因：噪声门 RMS<2% 把 iPhone 低电平语音全过滤
       修复：噪声门 2%→0.5%，AGC 上限 2x→5x
```

### 3.2 最终定位：三个叠加因素

| 序号 | 问题 | 症状 | 修复 |
|------|------|------|------|
| 1 | **iPhone 麦克风电平极低** | 原始音频 peak 仅 2.3%，99% 样本接近零 | 无法修复（硬件限制） |
| 2 | **噪声门 2% 过滤所有语音** | iPhone 电平 0.8-1.7% < 2% 阈值，全部丢弃 | 降到 0.5% |
| 3 | **AGC 增益不足** | 2x 增益对 iPhone 低电平不够 | 提到 5x |

### 3.3 为什么浏览器SR能识别？

浏览器 `SpeechRecognition` API 自带硬件级 AGC/AEC 预处理，直接在系统音频层做增益和降噪，不受 JavaScript 噪声门影响。

### 3.4 `asyncio.to_thread()` 回滚说明

v5 计划将 `asr_model.generate()` 用 `asyncio.to_thread()` 移到线程池以避免阻塞 event loop。但实测发现移动到线程池后 Paraformer 推理全部返回空结果。怀疑 PyTorch CUDA 上下文在线程池子线程中不可用。该优化已回滚，待后续在隔离环境中验证后再启用。

### 3.5 AudioWorklet 回滚说明

v5 计划用 AudioWorklet 替代 ScriptProcessor。实测 iOS Safari 上 AudioWorklet 输出电平比 ScriptProcessor 低约 200 倍。原因待查（可能是 AudioContext 重采样或 worklet 线程音频路由问题）。已回退到 ScriptProcessor，AudioWorklet 代码保留备用（`pcm-processor.js`）。

---

## 四、当前服务状态

| 服务 | 端口 | GPU | 版本 | 状态 |
|------|------|-----|------|------|
| paraformer-asr | 8091 | GPU 1 | v4（原始同步推理） | ✅ |
| ai-compliance | 8002 | GPU 0 | v5（含 #2 #4 #5 #6 修复） | ✅ |
| nginx | 80/443 | - | - | ✅ |
| 前端 | - | - | ScriptProcessor + AGC 5x | ✅ |

---

## 五、关键命令

```bash
# 安全重启后端
sudo /home/ai/文档/ai616/restart_safe.sh

# 重启 ASR（systemctl 可能超时，直接用 nohup）
sudo kill $(pgrep -f funasr_asr_server)
nohup /home/ai/miniconda3/envs/funasr-sensevoice/bin/python /home/ai/桌面/funasr_asr_server.py --device cuda:1 > /tmp/asr.log 2>&1 &

# 查看后端日志
tail -f /home/ai/文档/ai616/logs/backend.log

# 查看 ASR 日志
tail -f /tmp/asr.log

# 前端 build
cd /home/ai/文档/ai616/frontend && npm run build

# 全栈重启脚本
sudo /home/ai/桌面/restart_all.sh
```

---

## 六、待办事项

- [ ] 在隔离环境验证 `asyncio.to_thread()` + CUDA 兼容性
- [ ] 排查 iOS Safari AudioWorklet 输出电平异常的根因
- [ ] `_asr_pending_store` 长期改用 Redis 持久化
- [ ] ASR session 长时间运行的内存监控
- [ ] 前端气泡显示 CAM++ 说话人标签
