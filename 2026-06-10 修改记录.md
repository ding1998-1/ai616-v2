# 2026-06-10 修改记录

## AI 会议移动端接入状态修复

- 修复手机扫码进入录音页后，桌面端不显示参会人已进入的问题。
- 移动端 `/mobile-recorder` 页面在登录并打开会议后，会立即向后端上报 `join` 会话事件，状态显示为“已进入会议，等待开始录音”。
- 桌面端 AI 会议驾驶舱识别 `join` 事件后，将参会人状态展示为“已接入，待录音”，不再误显示为“已上传”。
- 后端 `recorder/session` 链路补齐保底会议落库：当手机链接携带的 `meetingId` 尚未存在时，自动创建一条轻量会议记录，避免接入事件被丢弃。
- 手机录音页缺省会议日期改为当天日期，避免未携带 `date` 参数时仍显示旧日期。

## 验证

- 已执行 `frontend` 构建：`npm run build` 通过。
- 已执行后端语法校验：`python3 -m py_compile backend_full.py` 通过。
- 已重启后端 `http://0.0.0.0:8000`。
- 已通过真实接口验证：`POST /api/meeting/recorder/session` 上报 `join` 后，`GET /api/meeting/transcripts/{meetingId}` 可读取到 1 条 `join` 事件。

## AI 会议 Excel 台账 DeepSeek 真实提炼

- 将 `POST /api/meetings/{meeting_id}/issues/import-excel` 从本地关键词规则升级为真实 DeepSeek 提炼。
- Excel 上传后先落库为问题素材，再调用 DeepSeek 对台账行进行聚类、拆分和“三重一大”业务类型判断。
- DeepSeek 输出会转换为前端现有 `agendaDrafts` 结构，右侧“AI 议题池”直接展示真实模型提炼的议题、风险、本地项目和待办材料。
- 增加 JSON 解析兼容：支持模型返回 Markdown 包裹、前后说明、中文键名，以及单个议题对象被解析出来的情况。
- 保留本地规则兜底：DeepSeek 网络失败、额度异常或返回结构不可用时，上传仍能完成，不阻塞测试流程。
- 前端上传成功提示区分 `DeepSeek 已真实提炼议题` 与 `已用本地规则兜底提炼议题`。

## DeepSeek Excel 验证

- 已使用 `/Users/macos/Desktop/未命名文件夹 27/model/AI会议问题收集模板_已填满测试数据.xlsx` 上传 79 条测试问题。
- 接口返回 `aiProvider=deepseek`，确认走通真实 DeepSeek 调用。
- DeepSeek 生成 5 个聚类议题，包括高新区二期厂房消防改造、新能源科创园三期扩建、智慧停车平台升级、老旧小区雨污分流改造、政务服务大厅数字化改造等事项。
- 已执行后端语法校验：`/Users/macos/miniconda3/bin/python -m py_compile backend_full.py` 通过。

## Excel 上传 AI 生成动画反馈

- 修复上传台账后等待 DeepSeek 返回期间“页面像没反应”的体验问题。
- 上传按钮在处理中显示为 `AI 提炼中`，并进入 loading/disabled 状态，防止重复上传。
- 右侧 AI 议题池顶部新增生成中动效卡片，显示当前文件名、DeepSeek 处理状态和进度条。
- 处理步骤按真实业务展示：读取 Excel 台账、DeepSeek 聚类同类事项、判断三重一大类型与风险、生成待办议题并写入会议。
- DeepSeek 返回后动画自动消失，议题池替换为真实生成结果。
- 已执行前端构建：`npm run build` 通过。

## AI 议题池调整为开会待办导向

- 修正 DeepSeek 提炼目标：从“识别多个项目/项目清单”调整为“秘书下一步要创建的开会待办事项”。
- DeepSeek 提示词明确要求标题使用可执行待办表达，如“审议预算追加及资金来源补充”，禁止输出“某项目等 N 个项目问题”。
- 多个项目即使问题类型相似，也按真正要开会处理的事项拆成独立待办，不再为凑数量合并成项目组。
- 后端 `agendaDrafts` 增加 `todoText` 字段，用于展示会议前必须完成的动作。
- 前端议题卡将“生成待办”调整为“开会待办”，优先展示 DeepSeek 返回的具体动作，不再显示固定占位文案。
- 已使用 79 条 Excel 测试数据验证：输出变为“审议高新区二期厂房消防改造预算追加及资金来源补充”等会议待办标题，`aiProvider=deepseek`。

## 移动端录音结束结果反馈

- 修复手机端结束录音后缺少结果画面的问题。
- 结束录音时等待 `MediaRecorder` 写入最后一段音频，再生成本机播放地址。
- 新增“本次录音结果”卡片，展示录音时长、音频大小、转写条数和结束状态。
- 手机端结束后可直接播放本机录音，避免用户不知道是否真的录到声音。
- 如果没有自动转写文本，结果卡明确提示可能原因：未明显发言、说话时间太短、Fun-ASR 未接入或浏览器限制，并引导使用手动补录。
- 若已有转写，结果卡提示文本已按当前角色回传桌面端，可继续修正并手写签字。
- 已执行前端构建：`npm run build` 通过。

## 移动端登录与滚动适配

- 简化移动端录音入口登录页，去掉首屏大段说明和多余能力标签。
- 移动端登录页改为单列窄布局，仅保留会议名称、日期、项目、议题和登录/登记表单。
- 压缩移动端登录表单卡片间距、表单项间距和输入框高度，减少必须滑动才能看到登录/注册按钮的问题。
- 修复进入录音页后 `html/body/#root` 恢复为 `overflow:hidden` 导致手机无法继续下滑的问题。
- 录音页组件挂载时主动开启 `mobile-recorder-scroll` 滚动类，卸载时清理，保证登录页和录音页都能在手机浏览器里纵向滚动。
- 录音页增加底部安全区 padding，避免“本次录音结果”、补录框和回传状态被底部浏览器工具栏遮住。
- 已执行前端构建：`npm run build` 通过。

## 安卓 / 鸿蒙 / iOS 移动滚动兼容加强

- 将移动端录音页从依赖 `body` 滚动改为 `position: fixed; inset: 0; overflow-y: auto` 的独立滚动容器，绕开全局 `#root { overflow:hidden }`。
- 将移动端登录页同样改为 fixed 独立滚动层，避免安卓、鸿蒙、iOS WebView 对 `body` 滚动处理不一致。
- 录音页和登录页均保留 `-webkit-overflow-scrolling: touch`、`touch-action: pan-y`、`overscroll-behavior-y: contain`。
- 已用 Playwright 手机视口验证：
  - 登录页 `.login-minimal-shell`：`position=fixed`，`overflowY=auto`，可滚动到底。
  - 录音页 `.mobile-recorder-page`：`position=fixed`，`overflowY=auto`，可滚动到底。

## 归档中心页面样式修复

- 修复归档中心表格黑色表头过重、与整体政府 OA 浅色风格不一致的问题。
- 归档中心固定使用浅色业务列表样式，表头改为浅灰底、深灰文字、紧凑行高。
- 调整表格列宽：决策编号、事项类型、事项名称、审议日期、风险等级、参会人员、操作列重新分配宽度。
- 事项名称列拆分为主标题和摘要信息，自动从后端长文本中提取 `事项名称`、`OA编号`、`发起部门`、`拟提交会议`、`材料清单`，避免整段 OA 内容堆在一个单元格里。
- 操作按钮改为紧凑按钮组，`导出迎检报告` 简化为 `导出报告`，减少横向拥挤。
- 表格增加横向滚动和固定操作列，避免 1080p 下列内容互相挤压。
- 已执行前端构建：`npm run build` 通过。
- 已用 Playwright 桌面视口验证：表头背景为 `rgb(242, 243, 245)`，事项主标题可正确解析为“关于王建国同志拟任综合管理部副主任的审批”。

## AI 议题卡长文本越界修复

- 修复 DeepSeek 生成的长议题、长开会待办和长依据标签横向撑破右侧 AI 议题池的问题。
- AI 议题卡改为 class 样式控制，统一设置 `min-width: 0`、`max-width: 100%`、`overflow: hidden`。
- 议题标题和来源说明最多显示两行，超出自动截断。
- “开会待办”最多显示两行，并支持中文长句自动换行。
- 依据标签增加最大宽度和省略号，避免 Tag 撑出卡片边界。
- 已执行前端构建：`npm run build` 通过。
- 已用 Playwright 检查 `.meeting-issue-card`：`scrollWidth == clientWidth`，无横向溢出。

## AI 议题池视觉重构

- 针对“中间 AI 议题池不好看、像被压扁的表格行”的问题，重新调整待办列表视觉层级。
- 顶部 AI 区域压缩为横向状态条，减少大图标占用空间。
- 议题卡改为左对齐待办列表样式，增加左侧状态竖线，选中项使用蓝色竖线和浅蓝背景。
- 卡片内容重新分层：标题、来源/类型、本地项目、开会待办、依据标签分区展示。
- 依据标签最多展示 2 条，减少胶囊标签堆叠造成的视觉噪音。
- 已执行前端构建：`npm run build` 通过。
- 已用 Playwright 1920×1080 视口验证：卡片无横向溢出，卡片高度稳定约 94px。

## AI 会议创建页待办清单修复

- 修复“未创建会议”三列创建页没有加载 AI 议题池新样式的问题；此前 JSX 已调整，但该分支只有独立样式块，导致浏览器仍按默认按钮样式渲染。
- 中间列从“AI 议题池卡片”改为“AI 已整理出的开会待办清单”，顶部只保留状态、数量和下一步说明。
- 待办行统一满宽展示，左侧编号、标题、风险、项目、事项类型、素材来源、下一步动作按 OA 清单阅读路径排列。
- 去掉多余依据标签堆叠，避免政府人员第一眼误以为是在看功能说明或项目列表。
- 修复按钮默认样式导致的文字居中、宽度收缩和黑色默认边框问题，强制清单行 `display:grid`、`width:100%`、`text-align:left`。
- 选中态改为浅蓝底 + 右上确认图标，下方绿色提示只说明“已带入右侧会议表单”，减少解释文字。
- 已执行前端构建：`npm run build` 通过。
- 已用 Playwright 2048×1024 视口验证：`.meeting-issue-row` 样式规则已进入页面，行宽 575px 满列，`text-align=left`，无横向溢出。

## AI 会议待办议题改为多选

- 将会议创建页中间的 AI 待办事项从单选改为多选，符合“多个候选议题一起组成本次会议议程”的真实业务。
- 新增 `selectedIssueIds` 和 `selectedIssueCards`，保留当前主议题用于默认项目/会议名称带入，同时用多选集合生成正式会议议程。
- 点击待办行可勾选或取消，行内增加复选状态，顶部显示已选数量。
- 右侧“待上会议题”改为“已选议题清单 + 可编辑议题汇总”，秘书能直接看见本次会议包含哪些议题。
- 创建会议时只提交已勾选议题作为 `agendaDrafts`，未勾选的候选议题不会混入正式会议。
- 0 个议题时创建按钮显示 `确认 0 个议题并创建会议批次`，点击创建会拦截并提示“请至少勾选一个待上会议题”。
- 已执行前端构建：`npm run build` 通过。
- 已用 Playwright 验证多选交互：默认 2 个议题选中，取消第二条后变为 1 个，取消第一条后变为 0 个；右侧清单、底部提示和创建按钮数量均同步，且无横向溢出。

## 真实问题收集分享页

- 将“复制收集链接”从后台管理页 `/?page=ai_meeting&collect=1` 改为独立分享页 `/issue-collect`，避免外部人员进入复杂后台。
- 新增 `IssueCollectPage.jsx`：外部人员登录或登记后，只看到会议信息、项目、当前议题和极简问题填报表单。
- 收集页表单只保留必要字段：问题描述、事项类型、涉及金额、材料情况，提交按钮文案为“提交给秘书端 AI 议题池”。
- 提交后真实调用 `/api/meetings/{meeting_id}/issues`，写入会议 `issueSources`，来源标记为 `issue-collect-share`。
- 如果分享链接对应的会议草稿尚未落库，收集页提交前会先按链接参数创建轻量会议记录，再写入问题，避免变成无标题会议。
- 登录页增加 `issue-collect` 外部入口模式，登记时只填写真实姓名、部门/单位、账号、密码，隐藏录音入口才需要的会议角色和席位字段。
- `/issue-collect` 走独立页面，不显示左侧菜单和系统后台头部，适合发给浙政钉、企业微信里的部门人员填写。
- 已执行前端构建：`npm run build` 通过。
- 已用 Playwright 移动视口验证：清空 token 后打开分享链接，首次登记账号，进入问题填报页并提交测试问题；后端会议详情可读取到新写入的问题记录，页面无后台侧边栏。
