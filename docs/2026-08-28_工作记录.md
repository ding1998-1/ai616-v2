# 2026-08-28 AI 会议系统工作记录

> 本地项目：`/Users/macos/Documents/ai616`  
> 生产项目：`/home/ai/文档/ai616`  
> 生产服务器：`192.168.66.44`

## 一、Whisper 终审核验

- 生产 8002、8091、8092 均为 `active + enabled`。
- 会议进入 `audit` 阶段时，8002 自动排队执行 Whisper 终审。
- 已完成的会议不会因轮询或服务重启重复执行；人工 `force` 除外。
- 真实会议 `meeting-local-20260827161809` 于 2026-08-27 16:42:54 开始终审，16:49:49 完成。
- 使用 Whisper-large-v3，处理 1 个录音源，生成 685 个终审片段。
- 终审结果和状态已写入数据库，服务重启后仍可读取。

## 二、Records Pipeline v2 上线

- 修复前端把 `GET records?force=true` 当作普通刷新使用的问题。
- Whisper 完成后，前端先读取现有纪要；只有不存在产物时才调用一次 `POST /records/generate`。
- 用户主动重新生成仍使用明确的 POST，不再把读取接口混作生成接口。
- 前端生产构建通过并部署到 44。
- 生产 systemd 配置已加入 `RECORDS_PIPELINE=v2`。
- 仅重启 8002；8091、8092、GPU0 和 8088 未重启、未修改。
- 8002 重启后为 active，运行环境确认包含 `RECORDS_PIPELINE=v2`。

## 三、真实会议 v2 生成结果

对会议 `meeting-local-20260827161809` 使用 685 个 Whisper 终审片段执行 map-reduce：

| 指标 | 结果 |
|---|---:|
| 总耗时 | 114.36 秒 |
| Pipeline | `records-v2` |
| Pipeline 状态 | `ok` |
| 议题纪要 | 6 条 |
| 会议决议 | 6 条 |
| 风险/披露 | 5 条 |
| 待办 | 7 条 |
| 原文片段覆盖 | 685/685，100% |
| 落库大小 | 259,569 字节 |

当前质量边界：

- 纪要依据有效 3/6。
- 决议依据有效 3/6。
- 待办依据有效 3/7。
- 校对状态为 `needs_review`。
- 质量问题为 `basis_invalid`、`basis_recovery_unmatched`。

结论：v2 已能完整生成并落库，但部分原文引句与时间依据尚未匹配成功，不允许直接标记为正式定稿。

## 四、继续验收

1. 下一场 iPhone Safari 会议确认持续出现 `/audio/chunk` ACK。
2. 结束时确认 `/audio/complete` 合并成功，不依赖完整录音兜底。
3. 测试断网、切后台、锁屏后的缺片续传。
4. 修复 basis recovery，使纪要、决议和待办的引用依据达到可核验标准。
5. 从纪要确认页面核对 v2 展示、人工校对和文档生成。
# 2026-08-28 ASR 2pass 验收补充

## 已完成

1. 补齐 8 项 2pass 单元验收：VAD 前置回看、单次句尾、15 秒强制断句、Offline 超时降级、并发保序、`sentenceId` 持久幂等、CT-PUNC 降级和上下文限定纠错。
2. `sentenceId` 由 H5/PC 一并提交到后端，使用会议 ID 与句子 ID 生成稳定记录 ID；刷新、重连和重复回传不会重复入库，无需修改数据库 Schema。
3. 抽离 `OrderedFinalBuffer` 与 Offline fallback，避免并发终审乱序。
4. 增加 15 秒长句强制切分，切分后清空 online/VAD 缓存并自动承接后续语音。
5. CT-PUNC 异常改为返回原始 ASR 文本，不再中断句尾提交。
6. 领域同音纠错只对当前会议上下文中明确允许的规范词生效。
7. 发现历史真机记录存在 Offline 模型回显会议 context/热词清单，增加合理性门禁；异常复核自动退回 online final。

## 验证

- `.venv` 下 ASR、录音相关测试：20 项通过。
- 排除必须依赖本地 8002 常驻服务的 `test_core.py` 后，全量自动测试：88 项通过、1 项跳过。
- 前端生产构建通过。
- 2026-08-27 真机会议证据：约 21 分钟、140 条转写、35,208,224 字节录音完整回传。

## 仍待真机

1. 多人交替、远场和低音量。
2. 固定术语、人名、金额、日期、百分比逐字对照。
3. iPhone Safari 锁屏、切后台、断网续传及最终合并。

## 生产部署

- 2026-08-28 13:06 创建回滚备份：`/home/ai/文档/ai616_backups/20260828_asr2pass_acceptance_130626`。
- 服务器部署前测试 15 项通过；随后仅重启 8091 与 8002，8092、8088、GPU0 未重启。
- 重启后 8002、8091、8092 均为 `active + enabled`，数据库健康为会议 290、转写 2482。
- 公网首页与新前端资源返回 200。
- 完整 8002→8091→8092 WebSocket 实测返回 2 条有序 final，未出现 context/热词清单回显。
- GPU0 部署前后均为 43,727 MiB，未发生占用变化。
