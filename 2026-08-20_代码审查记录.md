# 2026-08-20 代码审查记录

## 一、结论

本轮改造已经建立“会议为容器、议题为核心”的后端基础，但尚未形成可验收的完整业务闭环。

当前状态可概括为：

- 前端产品表达已明显转向“会议主导”，三重一大已降为会议治理模式。
- 后端已新增正式议题、当前议题、议题记录、议题决议、签字、多人角色、议题 ACL、议题级检索等数据结构和接口。
- 录音稳定性相关改造已有较完整的实现基础，前端生产构建通过。
- 正式议题与旧 `agendaDrafts` 仍是两套并行事实来源，前端主流程没有真正切换到正式议题。
- 签字版本校验、全员签字闭环和保密议题隔离存在高风险缺口。
- 当前 `master` 的已提交版本存在 Python 语法错误；工作区未提交改动修复了该错误，因此“本地可运行”不等于“仓库版本可运行”。

综合判断：**架构方向正确，后端骨架完成度较高，但当前版本不能视为“议题中心闭环已完成”，不建议直接作为生产验收版本。**

## 二、审查范围

重点审查最近以下改造：

- `21b8150`：会议主导的前端产品层级
- `b30d783`：正式议题、多人身份、转写议题绑定
- `530a211`：议题记录、议题决议、版本签字
- `43737b6`：多角色与保密议题 ACL
- `d3e4d39`：议题级历史知识检索
- `a3bc51f`、`563f0b1`、`20d0984`：构建、可选依赖和生产联调修复

审查同时覆盖：

- `backend/db.py`
- `backend/routes/auth.py`
- `backend/services/agenda_service.py`
- `backend/services/signature_service.py`
- `backend/services/permission_service.py`
- `backend/services/knowledge_service.py`
- `backend_full.py`
- `frontend/src/pages/MeetingComplianceWorkflow.jsx`
- `frontend/src/pages/MobileMeetingRecorder.jsx`
- `frontend/src/pages/LoginPage.jsx`

## 三、阻断问题

### P0-1：正式议题没有接管前端主流程，当前议题切换可能只在页面内生效

证据：

- 前端已经请求 `/api/meetings/{meeting_id}/agendas`，并把结果保存到 `formalAgendas`。
- 但会中议题列表、计时器、当前议题查找仍以 `activeIssueCards` / `agendaDrafts` 为数据源。
- `formalAgendas` 当前没有进入主要渲染和业务计算。
- 点击会中议题时，将旧草稿的 `item.id` 请求到正式议题激活接口。
- 后端正式议题从旧草稿物化时会生成新的 `ag-*` ID，不保证与草稿 ID 相同。
- 激活请求失败后，前端保留乐观状态并静默吞掉错误，用户会看到“已经切换”，但后端可能没有切换。
- 新建临时正式议题后只刷新 `formalAgendas`，而页面列表仍渲染 `activeIssueCards`，因此临时议题可能创建成功却不出现在主列表。

影响：

- 手机端新转写可能继续挂到旧议题或空议题。
- 刷新、多端协作后当前议题状态不一致。
- “议题是最小业务单元”只完成了后端结构，没有落到真实用户流程。

建议：

- 明确 `meeting_agendas` 为唯一事实来源。
- 前端会中列表、计时、决议、记录、终审全部改为消费正式议题 ID。
- `agendaDrafts` 仅保留为会前草稿；确认议程时显式转换并返回 ID 映射。
- 激活失败必须回滚本地状态并提示，禁止静默保留假状态。

### P0-2：签字接口没有校验数据库中的真实目标、真实版本和真实内容

证据：

- `sign_target()` 直接使用客户端提交的 `targetId`、`version` 和 `content` 计算哈希并落库。
- 函数没有查询 `meeting_agenda_decisions` 或其他签字目标，无法确认目标存在、目标属于本会议/本议题、版本是否为当前版本、内容是否与当前定稿一致。
- `/signatures/hash` 同样只是对客户端输入计算哈希，不能构成可信校验。
- `invalidate_target_signatures()` 虽被导入，但更新决议时没有调用；决议版本增加后，旧有效签字不会自动失效。

影响：

- 客户端可对不存在的目标、旧版本或任意内容生成“有效签字”。
- 修改决议后旧签字仍可能被计入闭环。
- 当前签字不能作为正式认定证据链。

建议：

- 签字请求只提交目标 ID 和签名数据；版本、内容、哈希必须由后端读取当前目标后生成。
- 在同一事务中校验目标归属、当前版本、允许签署状态和签署人资格。
- 决议标题或内容变化时，同一事务内递增版本并作废该目标旧签字。
- 对签字目标建立统一的后端解析器，不允许客户端自证内容。

### P0-3：保密议题的记录、决议和签字接口绕过会议权限与议题 ACL

证据：

- 议题列表和议题详情调用了 `can_view_agenda()`。
- 议题记录、议题决议的查询、新增、生成、修改和删除接口只检查“已登录”，没有调用 `_check_meeting_access()`，也没有检查议题 ACL。
- 签字列表接口同样只检查“已登录”，可按会议 ID 读取签字数据。
- ACL 列表接口只检查“已登录”，没有确认调用者是否可管理该议题。

影响：

- 只要猜到或获得 meeting ID / agenda ID，普通登录用户可能读取保密议题的发言记录和最终决议。
- 普通登录用户可能创建、修改或删除不属于自己的会议决议。
- 前端隐藏无法弥补后端越权。

建议：

- 所有议题子资源统一经过“会议访问 + 议题可见 + 操作权限”三层校验。
- 查询要求 `view`，编辑要求 `edit`，签字要求 `sign`，授权管理要求 `admin`。
- `all` 查询必须逐议题过滤，不能先取整场再直接返回。

### P0-4：当前 `master` 提交版本无法通过 Python 语法编译

证据：

- `git show HEAD:case_similarity.py` 的编译结果为 `IndentationError`。
- 工作区存在未提交修改，将 `CaseSimilarityMatcher.__init__()` 缩进回类内部后，本地 `compileall` 才通过。

影响：

- 从当前 `master` 干净检出或部署时可能启动失败。
- 当前本地验证结果依赖未提交修改，不能代表仓库提交状态。

建议：

- 将现有缩进修复纳入一个独立提交。
- 提交前增加 `python -m compileall` 或等价语法检查。

## 四、高优先级问题

### P1-1：全员签字统计按“签过任意一个目标的人数”计算，不是按应签成果逐项闭环

`signed_signer_count()` 只要参会人在本会议存在任意一条有效签字，就计为已签。若会议有多个议题或多个决议，一个人只签其中一项也会被当作该会议已签。最终 `is_fully_signed()` 可能错误放行归档。

应改为先生成明确的签署任务矩阵：`应签人 × 应签目标 × 当前版本`，再逐项判断是否完成。

### P1-2：手机端把用户 ID 当作 participant row ID 提交

手机端发送 `participant_id: currentUser.id`，但后端字段定义要求的是 `meeting_participants.row_id`。后端只在客户端未传 participant ID 时才会查询真实 row ID，因此当前代码会把错误类型的 ID 原样写入转写记录。

应由后端始终根据认证用户和会议解析 participant row ID，客户端不应声明可信身份关系。

### P1-3：切换正式议题后未主动失效会议缓存

`activate_meeting_agenda()` 直接更新数据库，但没有调用会议缓存失效函数。转写接口随后通过 `_load_meetings()` 读取 `activeAgendaId`，在缓存 TTL 内可能继续拿到旧议题，造成切换边界附近的发言错挂。

应让当前议题读取直接查询数据库，或在激活事务完成后立即失效会议缓存。

### P1-4：议题记录/决议写入函数在事务提交前通过另一连接回读

`create_agenda_record()` 和 `create_agenda_decision()` 在写连接尚未退出、事务尚未提交时，又通过列表函数打开另一连接回读。另一连接看不到未提交数据：

- 新增记录响应可能不包含刚新增的记录。
- 新增决议可能返回空列表而非新决议对象。

应在同一连接内按新 ID 查询并返回，或先提交再回读。

### P1-5：正式议题及其子资源缺少引用完整性和存在性校验

当前可创建指向不存在会议/议题的记录、决议、ACL 或签字；部分表只有 meeting 外键，没有 agenda 外键。删除议题也没有处理其记录、决议、ACL、签字和知识索引。

这会形成孤儿数据。后续应补服务层存在性校验，并设计可恢复的级联/软删除策略。数据库结构调整属于迁移事项，执行前需单独确认。

### P1-6：正式议题版本历史尚未实现

决议只有当前行上的 `version` 数字，没有版本快照表；议题记录也没有初稿、修改稿、定稿及审批轨迹。版本号增加后无法恢复旧内容，也无法完整展示“某次签字对应的旧版本正文”。

当前只能称为“版本标记”，不能称为“多版本历史追溯”。

## 五、中优先级问题

### P2-1：正式议题创建权限不一致

临时议题限制主持人/秘书创建，但普通正式议题只要具有会议访问权即可创建。需要明确是否允许所有参会人新增正式议题；若不允许，应统一使用议题管理权限。

### P2-2：部分异常被静默吞掉

参会人同步、录音客户端绑定、正式议题加载和激活等路径存在宽泛 `except Exception: pass` 或静默降级。其结果是界面可继续运行，但身份链、议题链可能已经断裂。

涉及责任链的数据写入至少应记录结构化错误并向调用方返回可感知状态。

### P2-3：新增能力缺少自动化测试

现有 `tests/test_core.py` 主要覆盖旧健康检查、登录、会议 CRUD 和基础转写，没有覆盖：

- 多人同场扫码身份唯一性与密码恢复
- 草稿转正式议题及 ID 映射
- 当前议题切换与并发边界
- 转写自动绑定正式议题和真实参会人
- 保密议题读取/编辑/签字 ACL
- 决议版本变化与旧签字失效
- 多议题、多决议的全员签字矩阵
- 议题级知识检索的保密过滤

### P2-4：前端主包体积偏大

生产构建通过，但 `vendor-antd` 约 1.47 MB（压缩前），构建工具已发出超过 1 MB 的 chunk 警告。该项不阻断本轮业务闭环，但会影响移动端首屏和弱网体验。

## 六、与目标需求的对照

| 目标 | 当前状态 | 判断 |
|---|---|---|
| 前端突出会议、弱化三重一大 | 文案和入口层级已调整，三重一大作为增强治理模式出现 | 基本完成 |
| 会议 → 议题 → 记录/决议 | 后端实体已建立，前端仍主要依赖旧草稿 | 部分完成 |
| 当前议题后端持久化 | 已有字段和激活接口，但前端 ID 体系未接通且存在缓存窗口 | 未闭环 |
| 会中临时议题 | 后端可创建，前端主列表可能不显示 | 未闭环 |
| 多人扫码独立身份 | 已移除固定默认密码并生成独立用户序号 | 基础完成 |
| audio client → participant → user | 已有数据字段，但 participant ID 提交错误且后端信任客户端 | 未闭环 |
| 转写绑定 agenda ID | 已入库，依赖当前议题正确切换 | 部分完成 |
| 议题记录与决议拆分 | 后端表和接口已实现 | 基础完成 |
| 多版本与历史追溯 | 只有决议当前版本号，无历史快照 | 未完成 |
| 手机手签/电子签章 | 已有签名数据接口，但可信版本校验缺失 | 未完成 |
| 全员签字拦截 | 有会议级开关，但统计逻辑不能覆盖多目标 | 未完成 |
| 多角色权限 | 已有 `user_roles`，尚未完整进入所有业务授权判断 | 部分完成 |
| 保密议题后端隔离 | 仅议题列表/详情做过滤，子资源存在越权 | 未完成 |
| 议题级知识检索 | 已实现关键词检索并尝试过滤保密议题 | 基础完成 |

## 七、验证记录

本轮执行的只读/构建验证：

- 工作区 Python 语法编译：通过。
- 当前 `HEAD` 中的 `case_similarity.py` 单独编译：失败，`IndentationError`。
- 前端 `npm run build`：通过。
- 构建警告：Vite CJS API 已弃用；`vendor-antd` chunk 超过 1 MB。
- 未执行会写入真实会议数据库的接口测试，避免在未经授权的情况下修改数据库或触发 Schema 初始化迁移。

### 运行态补充验证

2026-08-20 在项目已启动状态下进行只读检查：

- 后端监听：`0.0.0.0:8002`，`GET /health` 返回 HTTP 200。
- 前端监听：`127.0.0.1:3000`，首页返回 HTTP 200，可正常渲染登录页。
- 数据库检查：`ok`。
- DashScope ASR：`ok`。
- ChromaDB：`not_loaded`。
- LLM：`no_api_key`。
- 本地 Qwen ASR：`unavailable`。
- 健康接口显示当前数据库包含 7 场会议、4 条转写。

因此，当前“服务已启动”只代表 Web 服务与数据库可访问，不代表知识库、LLM 润色或本地 Qwen ASR 已具备可用能力。

运行页面还暴露出一项产品层级偏差：

- 浏览器标题仍为“昇晟三重一大合规审核与法务对比系统”。
- 登录页主文案仍是“登录后进入‘三重一大’审查……”。
- 登录页能力标签把“三重一大”放在首位，没有突出会议管理。

这说明“会议主导、三重一大降为增强治理模式”的调整尚未覆盖登录入口和浏览器元信息，应补充修改页面标题、登录页说明及首屏功能标签。

## 八、建议修复顺序

1. 提交 `case_similarity.py` 语法修复，恢复干净检出的可运行性。
2. 让前端主流程完全切换到正式议题，并解决草稿到正式议题的 ID 映射。
3. 修复所有议题子资源的会议权限和 ACL 校验。
4. 重做签字可信校验、版本失效和应签任务矩阵。
5. 修复 participant row ID、当前议题缓存和创建后回读问题。
6. 增加上述新链路的服务级与接口级自动化测试。
7. 再补议题/决议版本快照与审批轨迹；涉及数据库变更时先单独评审迁移方案。

## 九、本轮文件变更

- 新增本审查记录。
- 未修改业务代码。
- 未修改数据库 Schema、数据库数据、环境变量或部署配置。
- 保留工作区原有 `case_similarity.py` 未提交修复，不做覆盖或回退。
